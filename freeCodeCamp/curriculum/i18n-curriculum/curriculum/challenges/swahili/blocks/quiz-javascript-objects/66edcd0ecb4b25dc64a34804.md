---
id: 66edcd0ecb4b25dc64a34804
title: Pokušao sam sa malim deo kodova za JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Ili je među sljedećim što ne opisuje objekt u JavaScript?

#### --distractors--

Kolekcija parova ključ-vrednost.

---

Kontejner sa karakteristikama i putevima.

---

Neprimitivni tip podataka (non-primitive).

#### --answer--

Osnovni tip podataka (primitivan).

### --question--

#### --text--

Koje od sledećih definicija/objašnjenja nije važeće?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

Kako se može dobiti svojstvo `job` za ovaj objekat od `person`?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

Da li možete dobiti karakteristiku `street` za predmet `person` koji je dole?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

Koje kriterijume među ovim se mogu pronaći samo korišćenjem notacije u uglastim zagradama?

#### --distractors--

Ključ atributa koji je poznat i nepromenljiv.

---

Ključ atributa koji je ugnježdjen kroz više nivoa objekta.

---

Ključ karakteristike koja možda ne postoji u nečemu.

#### --answer--

Dinamički ključ atributa.

### --question--

#### --text--

Koja od sljedećih koristi dekonstrukciju (destructuring) da bi tačno dobila svojstvo `name` objekta `person` prikazanog ispod?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

Koji od sljedećih koristi destrukturiranje (destructuring) kako bi ispravno dobio svojstvo ``flour`` objekta ``recipe`` ispod?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

Da li je moguće postaviti podrazumevanu vrednost za ključ svojstva kada koristite rešenje dizajna u JavaScript?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

Da li možete proveriti da li objekat sa `car` ima svojstvo od `year`?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

Koja je putanja za nešto?

#### --distractors--

Niz slova povezan sa nečim.

---

Red podataka povezan sa objektom.

---

Broj povezan sa predmetom.

#### --answer--

Funkcija povezana sa objektom.

### --question--

#### --text--

Kako mogu pozvati metodu `add` ovog objekta `calculator` da doda brojeve `10` i `7`?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

Koji stručnjak skida karakteristike sa nečega?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

Da li možete proveriti da li atribut `score` postoji u objektu `player`?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

Kako možete kreirati nešto potpuno novo u JavaScript?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

Koja sintaksa je tačna pri korišćenju sekvencijalnog povezivanja (optional chaining) u JavaScript?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

Zašto se serijska konekcija koristi u JavaScript?

#### --distractors--

Koristi se za definisanje parametara sa podrazumevanim vrednostima.

---

Koristi se za definisanje opcionih putanja unutar objekta koje možda mogu biti pozvane ili ne.

---

Koristi se za pisanje kratkog koda za spajanje više poziva metoda u jedan izraz.

#### --answer--

Koristi se za sigurno dobijanje svojstava objekta koji mogu biti `null` ili `undefined`.

### --question--

#### --text--

Koja se putanja koristi za konverziju stvari JavaScript u niz znakova JSON?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

Koja putanja menja niz slova JSON nazad u nešto kao JavaScript?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

Koji od ovih ima svojstvo `color` objekta `toy` korišćenjem uglastih zagrada?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

Kako da dobijem broj `home` ovog predmeta `learner` u JavaScript koristeći dot notaciju?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`