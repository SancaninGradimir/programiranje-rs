---
id: 69a98755ee5508d58e91dccc
title: Pokušaj instalacije paketa iz npm-a
challengeType: 8
dashedName: quiz-npm
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta npm pruža za programere?

#### --distractors--

Platforma koja je izabrana i koja samo postavlja dizajnerske resurse, kao što su fontovi i simboli.

---

Usluga za hostovanje veb stranica koja objavljuje vaš projekat direktno u oblak.

---

Rešenje baze podataka koje upravlja podacima o korisnicima za vaše aplikacije.

#### --answer--

Repozitorijum gotovih softverskih paketa koji rešavaju uobičajene probleme.

### --question--

#### --text--

Koji od ovih nije jedan od tri dela o kojima govori npm?

#### --distractors--

Sajtovi npmjs.com.

---

Registrar paketa gde se skladišti biblioteka.

---

Alat za komandnu liniju za instaliranje i upravljanje paketima.

#### --answer--

Okruženje za izvršavanje Node.js.

### --question--

#### --text--

Šta radi komanda za `npm install`?

#### --distractors--

Kreira potpuno novi projekat za Node.js od stakla.

---

Pokrenite svoju aplikaciju Node.js na lokalni razvojni server.

---

Briše pakete koji više nisu potrebni u tvom projektu.

#### --answer--

Dodajete već kreirane pakete u vaš projekat za rutinske zadatke.

### --question--

#### --text--

Koji fajl čuva metapodatke kao što su ime projekta, verzija, autor i zavisnosti za projekat Node.js?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Kako možete kreirati fajl za `package.json` putem interfejsa?

#### --distractors--

Završi `npm install` bez navođenja imena paketa.

---

Preuzmite paket sa naslovne stranice npm registra.

---

Kopiraj fajl iz drugog projekta i preimenuj ga.

#### --answer--

Izvršiti komandu `npm init`.

### --question--

#### --text--

Možete li kreirati fajl za `package.json` koji ima zadane vrednosti bez odgovaranja na pitanja?

#### --distractors--

Izvrši `npm init --interactive`.

---

Kreiraj prazan fajl nazvan `package.json` i ostavi ga praznog.

---

Koristi `npm init` i futu svaki put nakon otvaranja editora.

#### --answer--

Omogući `npm init -y` (ili `npm init --yes`) da prihvati sve podrazumevane opcije automatski.

### --question--

#### --text--

npm navodi samo potrebne pakete tokom razvoja, kao što su linteri ili test runneri, gde?

#### --distractors--

U delu `"dependencies"`.

---

Unutar sekcije `"scripts"` ispod standardnih komandi.

---

Unutar foldera `node_modules` samog.

#### --answer--

Unutar sekcije `"devDependencies"` iz `package.json`.

### --question--

#### --text--

Koja je to komanda koja instalira `nodemon` samo kao zavisnost za razvoj?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Da li se koji folder i koji fajl dodaju u koren vašeg projekta nakon instalacije zavisnosti?

#### --distractors--

Folder `public` na `index.html`.

---

Mapa za `dist` na `webpack.config.js`.

---

Mapa za `src` na `README.md`.

#### --answer--

Paпка za `node_modules` u datoteci `package-lock.json`.

### --question--

#### --text--

`package-lock.json` Pomaže li da se postavi usklađenost izgradnje?

#### --distractors--

Brisanje neiskorišćene zavisnosti pre svakog instaliranja.

---

Za skladištenje samo glavnih verzijskih brojeva paketa.

---

Za automatsko ažuriranje paketa na njihove nove verzije.

#### --answer--

Pri instaliranju tačne verzije svake instalirane zavisnosti.

### --question--

#### --text--

Zašto bi trebalo da unesete `package-lock.json` u vašu bazu podataka za Git?

#### --distractors--

Da bi se npm pokrenuo brže na mašini developera.

---

Da ažurirate zastarele zavisnosti.

---

Za smanjenje veličine baze podataka.

#### --answer--

Da ceo tim instalira identične verzije zavisnosti i konfiguracije.

### --question--

#### --text--

Da li možete instalirati specifičnu verziju paketa, kao što `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

Šta znak krezice (caret) (`^`) dozvoljava kada se navode verzije zavisnosti?

#### --distractors--

Zaključava zavisnosti za tačnu verziju patča.

---

Dozvoljava samo ažuriranje za glavnu verziju.

---

Pruža sve verzije navedene ispod.

#### --answer--

Dozvoljava manje verzije i palke, ali onemogućava glavne verzije.

### --question--

#### --text--

Šta znači tilde znak (`~`) u opsegu verzija kao što je `~1.2.3`?

#### --distractors--

Omogućava ažuriranje za glavne i manje verzije.

---

Instalira novu glavnu verziju bez obzira na raznolikost.

---

Sprečava npm da instalira bilo koje ažuriranje.

#### --answer--

Omogućava ažuriranja samo za patch verziju, zadržavajući manju verziju nepromenjenu.

### --question--

#### --text--

Koja promena uzrokuje povećanje glavne verzije prilikom izdavanja detaljne verzije?

#### --distractors--

Male stilističke izmene za komentare i strukturu.

---

Manji problemi sa greškama koje ne menjaju ponašanje.

---

Opcionalne karakteristike koje ne narušavaju postojeći kod.

#### --answer--

Značajne promene koje mogu uzrokovati da postojeći kod prestane sa radom.

### --question--

#### --text--

Koji je komanda za ažuriranje zavisnosti sa novim izdanjima?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM, i Bun se spominju kao alternative za npm. Koje su ovo vrste alata?

#### --distractors--

Sajtovi za registar gde objavljujete ili preuzimate npm pakete.

---

Usluge baze podataka koje čuvaju podatke korisnika za npm pakete.

---

Cloud platforme koje objavljuju npm projekte direktno.

#### --answer--

Administratori alternativnih paketa.

### --question--

#### --text--

Gde se u projektu objavljuju (ili deklarišu) standardni npm ambijenti/paketi?

#### --distractors--

U fajlu `.npmrc`.

---

Unutar sekcije `"dependencies"` od `package.json`.

---

U različitoj datoteci `scripts.json` u korenu.

#### --answer--

Unutar elementa `"scripts"` od `package.json`.

### --question--

#### --text--

Pokrećete standardnu npm komandu nazvanu `build`?

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

Šta radi simbol sa dve crte (`--`) prilikom izvršavanja `npm run start -- 8000`?

#### --distractors--

Stabilizuje rezultate komande.

---

Nateruje npm da pokrene komande istovremeno.

---

Tražim dozvolu od administratora pre izvršavanja komande.

#### --answer--

Rekao sam mu da prosledi sve nakon `--` kao argument za osnovnu komandu.