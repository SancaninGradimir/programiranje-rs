---
id: 6577254891048c8f2c19e961
title: Korak 8
challengeType: 20
dashedName: step-8
---

# --description--

Ponovo pozovi funkciju `merge_sort()`. Ne prosleđuj nikakve argumente trenutno.

# --hints--

Trebalo bi da pozovete `merge_sort()` ponovo nakon prethodnog poziva funkcije.

```js
({
  test: () => {
    const transformedCode = code.replace(/\r/g, "");
    const merge_sort = __helpers.python.getDef("\n" + transformedCode, "merge_sort");
    const { function_body } = merge_sort;

    assert.match(function_body, /merge_sort\s*\(\s*\)/);
  }
})
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
def merge_sort(array):
    middle_point = len(array) // 2
    left_part = array[:middle_point]
    right_part = array[middle_point:]
    
    merge_sort(left_part)

--fcc-editable-region--
```
