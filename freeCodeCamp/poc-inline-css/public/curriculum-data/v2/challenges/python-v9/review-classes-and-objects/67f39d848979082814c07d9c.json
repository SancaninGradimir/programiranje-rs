{
  "id": "67f39d848979082814c07d9c",
  "title": "Inspekcija učionica i stvari",
  "challengeType": 31,
  "dashedName": "review-classes-and-objects",
  "solutions": [],
  "assignments": [
    "<p>Proveri materijale i koncepte za predmete i šrafove.</p>"
  ],
  "tests": [],
  "description": "<section id=\"description\">\n<h2>Klase i predmete za Python</h2>\n<ul>\n<li><strong>Definicija klase objekata</strong>: Klasa je šema za kreiranje objekata. Definiše karakteristike koje će objekat imati putem svojih svojstava i metoda. Evo osnovnog primera definicije klase u Python:</li>\n</ul>\n<pre><code class=\"language-python\">class Dog:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def bark(self):\n        print(f'{self.name.upper()} says woof woof!')\n</code></pre>\n<ul>\n<li><strong>Kreiranje objekata</strong>: Objekti su instancije klase. Kreiraju se pozivanjem klase i potrebnih argumenata.</li>\n</ul>\n<pre><code class=\"language-python\">dog1 = Dog('Jack', 3)\ndog2 = Dog('Thatcher', 5)\n\ndog1.bark()  # JACK says woof woof!\ndog2.bark()  # THATCHER says woof woof!\n</code></pre>\n<ul>\n<li><strong>Pozivanje metoda na objektima</strong>: Možete pozvati metode na objektima kako biste izvršili akcije ili dobili informacije.</li>\n</ul>\n<pre><code class=\"language-python\">object_name1.method_name()\nobject_name2.method_name()\n</code></pre>\n<ul>\n<li><strong>Razlika između klase i objekta</strong>: Klasa je ponovno upotrebljiv šablon, dok je objekat specifična instanca te klase sa stvarnim podacima.</li>\n</ul>\n<h2>Atributi elementa</h2>\n<ul>\n<li><strong>Svojstva instance</strong>: Definisana su u <code>__init__()</code> koristeći <code>self</code>, i jedinstvene su za svaki objekat.</li>\n<li><strong>Svojstva klase</strong>: Definisana su direktno unutar klase i dele se sa svim objektima.</li>\n</ul>\n<pre><code class=\"language-python\">class Dog:\n    species = 'French Bulldog'  # Class attribute\n\n    def __init__(self, name):\n        self.name = name  # Instance attribute\n\nprint(Dog.species) # French Bulldog\n\njack = Dog('Jack')\nprint(jack.name)     # Jack\nprint(jack.species)  # French Bulldog\n</code></pre>\n<h2>Put</h2>\n<ul>\n<li><strong>Njia</strong>: Metode definisane unutar klase koje operišu na svojstvima atributa objekta.</li>\n</ul>\n<pre><code class=\"language-python\">class Car:\n    def __init__(self, color, model):\n        self.color = color\n        self.model = model\n\n    def describe(self):\n        return f'This car is a {self.color} {self.model}'\n\nmy_car_1 = Car('red', 'Tesla Model S')\nprint(my_car_1.describe())  # This car is a red Tesla Model S\n</code></pre>\n<ul>\n<li><strong>Pozivanje metode</strong>: Pozivate metode na objektima pomoću tačka-zarez (dot notation). Evo primera pozivanja metode <code>describe</code> na dva različita automobila:</li>\n</ul>\n<pre><code class=\"language-python\">class Car:\n    def __init__(self, color, model):\n        self.color = color  \n        self.model = model  \n\n    def describe(self):\n        return f'This car is a {self.color} {self.model}'\n\nmy_car_1 = Car('red', 'Tesla Model S')\nmy_car_2 = Car('green', 'Lamborghini Revuelto')\n\nprint(my_car_1.describe()) # Calling method using the dot notation\n\nprint(my_car_2.describe()) # Calling method using the dot notation\n</code></pre>\n<h2>Putevi za dunder (za magiju)</h2>\n<ul>\n<li><strong>Objašnjenje</strong>: Specijalne putanje koje počinju i završavaju sa dvostrukim donjim podvučnicama (npr., <code>__init__</code>, <code>__len__</code>, <code>__str__</code>, <code>__eq__</code>). Python ih koristi unutar za ugrađene operacije.</li>\n</ul>\n<pre><code class=\"language-python\">class Book:\n    def __init__(self, title, pages):\n        self.title = title\n        self.pages = pages\n\n    def __len__(self):\n        return self.pages\n\n    def __str__(self):\n        return f\"'{self.title}' has {self.pages} pages\"\n\n    def __eq__(self, other):\n        return self.pages == other.pages\n\nbook1 = Book('Built Wealth Like a Boss', 420)\nprint(len(book1))        # 420\nprint(str(book1))        # 'Built Wealth Like a Boss' has 420 pages\n</code></pre>\n<ul>\n<li>\n<p><strong>Pozivanje dunder metoda indirektno</strong>: Nije potrebno pozivati dunder metode direktno. Umesto toga, Python ih poziva automatski kada se izvrše određene akcije. Ove operacije uključuju:</p>\n</li>\n<li>\n<p><strong>Matematičke operacije kao što su sabiranje, oduzimanje, množenje, deljenje i slično</strong>. Pored toga, <code>__add__()</code> se zove, <code>__sub__()</code> za oduzimanje, <code>__mul__()</code> za množenje i <code>__truediv__()</code> za deljenje.</p>\n<ul>\n<li>\n<p><strong>Operacije sa nizovima karaktera kao što su konkatenacija, ponavljanje, kreiranje šablona i konverzija u tekst</strong>. <code>__add__()</code> se zove za spajanje, <code>__mul__()</code> za ponavljanje, <code>__format__()</code> za kreiranje šablona, <code>__str__()</code> i <code>__repr__()</code> za konverziju u tekst, itd.</p>\n</li>\n<li>\n<p><strong>Operacije poređenja kao što su jednako, manje od, veće od i slično</strong>. <code>__eq__()</code> se zove za proveru jednakosti, <code>__lt__()</code> za manje od, <code>__gt__()</code> za veće od, itd.</p>\n</li>\n<li>\n<p><strong>Operacije iteracije kao što je činjenje nečega ponovljivim i pomeranje kroz elemente</strong>. <code>__iter__()</code> se zove vraćanjem iteratora, a <code>__next__()</code> dobijanjem sledećeg elementa.</p>\n</li>\n</ul>\n</li>\n</ul>\n<h2>Stvarni svetski primer: Kupovni automobil</h2>\n<ul>\n<li><strong>Klasa vozila sa dunder metodama</strong>: Omogućava dodavanje, uklanjanje, iteriranje i proveru sadržaja sa ugrađenim ponašanjem.</li>\n</ul>\n<pre><code class=\"language-python\">class Cart:\n    def __init__(self):\n        self.items = []\n\n    def add(self, item):\n        self.items.append(item)\n\n    def remove(self, item):\n        if item in self.items:\n            self.items.remove(item)\n        else:\n            print(f'{item} is not in cart')\n\n    def list_items(self):\n        return self.items\n\n    def __len__(self):\n        return len(self.items)\n\n    def __getitem__(self, index):\n        return self.items[index]\n\n    def __contains__(self, item):\n        return item in self.items\n\n    def __iter__(self):\n        return iter(self.items)\n\ncart = Cart()\ncart.add('Laptop')\nprint(len(cart))        # 1\nprint('Laptop' in cart) # True\n</code></pre>\n</section>",
  "translationPending": false,
  "block": "review-classes-and-objects",
  "blockLabel": "review",
  "blockLayout": "link",
  "hasEditableBoundaries": false,
  "order": 38,
  "instructions": "",
  "questions": [],
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 0,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-classes-and-objects",
  "certification": "python-v9"
}