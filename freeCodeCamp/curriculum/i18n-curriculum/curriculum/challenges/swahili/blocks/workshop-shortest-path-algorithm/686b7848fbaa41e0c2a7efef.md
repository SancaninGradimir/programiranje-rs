---
id: 686b7848fbaa41e0c2a7efef
title: Korak 7
challengeType: 20
dashedName: step-7
---

# --description--

Sada, nakon inicijalizacije liste `distances`, moraš ispraviti rastojanje početnog čvora.

Pošto je rastojanje od početnog čvora do samog sebe uvek ni `0`, postavi vrednost na polje `start_node` u listi `distances` da bude `0`.

# --hints--

Trebalo bi da staviš `distances[start_node]` na mesto `0`.

```js
({
    test: () => {
      assert(
        runPython(
          `_Node(_code).find_function("shortest_path").has_stmt("distances[start_node] = 0")`
        )
      );
    }
});
```

# --seed--

## --seed-contents--

```py
INF = float('inf')
adj_matrix = [
    [0, 5, 3, INF, 11, INF],
    [5, 0, 1, INF, INF, 2],
    [3, 1, 0, 1, 5, INF],
    [INF, INF, 1, 0, 9, 3],
    [11, INF, 5, 9, 0, INF],
    [INF, 2, INF, 3, INF, 0],
]

--fcc-editable-region--
def shortest_path(matrix, start_node, target_node=None):
    n = len(matrix)
    distances = [INF] * n

--fcc-editable-region--
```
