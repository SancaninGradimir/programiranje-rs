---
id: 5900f3711000cf542c50fe84
title: 'Vežba 5: Najmanji podeljivi broj'
challengeType: 1
forumTopicId: 302160
dashedName: problem-5-smallest-multiple
---

# --description--

2520 je najmanji broj koji se može podeliti sa svakim brojem od 1 do 10 bez ostataka.

Najmanji pozitivan broj koji je deljiv sa svim brojevima od 1 do `n` jeste?

# --hints--

`smallestMult(5)` Trebalo bi da vrati broj.

```js
assert.isNumber(smallestMult(5));
```

`smallestMult(5)` Trebalo bi da vrati 60.

```js
assert.strictEqual(smallestMult(5), 60);
```

`smallestMult(7)` trebalo bi da vrati 420.

```js
assert.strictEqual(smallestMult(7), 420);
```

`smallestMult(10)` treba da vrati 2520.

```js
assert.strictEqual(smallestMult(10), 2520);
```

`smallestMult(13)` treba da vrati 360360.

```js
assert.strictEqual(smallestMult(13), 360360);
```

`smallestMult(20)` trebalo bi da vrati 232792560.

```js
assert.strictEqual(smallestMult(20), 232792560);
```

# --seed--

## --seed-contents--

```js
function smallestMult(n) {

  return true;
}

smallestMult(20);
```

# --solutions--

```js
function smallestMult(n){
  function gcd(a, b) {
    return b === 0 ? a : gcd(b, a%b); // Euclidean algorithm
  }

  function lcm(a, b) {
    return a * b / gcd(a, b);
  }
  var result = 1;
  for(var i = 2; i <= n; i++) {
    result = lcm(result, i);
  }
  return result;
}
```
