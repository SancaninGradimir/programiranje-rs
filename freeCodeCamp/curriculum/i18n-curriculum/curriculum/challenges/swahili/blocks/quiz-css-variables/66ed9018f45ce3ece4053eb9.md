---
id: 66ed9018f45ce3ece4053eb9
title: Pokušaj korišćenja CSS svojstava
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koja od sljedećih nije važeća karakteristika u zakonu `@property`?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

U sledećem kodu, koja vrednost `color` će imati ako `--main-color` nije deklarisano?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

Nekupotpunosti boje neće biti korišćena.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

Koji od sledećih načina je validan za oglašavanje specifičnih karakteristika u CSS?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

Koja je glavna svrha korišćenja specifične karakteristike u CSS?

#### --distractors--

Menja strukturu DOM.

---

Kreiranje glasača za promjene.

---

Poboljšanje dostupnosti

#### --answer--

Prikazivanje ponovo upotrebljivih stilova.

### --question--

#### --text--

Šta predstavlja `:root`?

#### --distractors--

Element prvog deteta od `body`.

---

Roditeljski element svih elemenata od `header`.

---

Prvi sin/ćerka brenda `html`.

#### --answer--

Najviši nivo u stablu DOM.

### --question--

#### --text--

Da li možete koristiti specifično svojstvo od `--foreground` kao `color` u CSS?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

Koja je svrha pravila `@property` u CSS?

#### --distractors--

Objavi medijski zahtjev/oglas za pretragu.

---

Upravljati crtežima za CSS u pravo vreme.

---

Sakupljanje karakteristika za CSS u jedan zakon.

#### --answer--

Objavljivanje kako specifične karakteristike funkcionišu.

### --question--

#### --text--

Kada deklariš specifično svojstvo `@property`, šta znači/koja je svrha `syntax` u njegovoj definiciji?

#### --distractors--

Opisuje osnovnu vrednost specifičnog atributa.

---

Određuje da li je karakteristika naslediva kod potomstva.

---

Objašnjava se kao atribut koji se primenjuje na sve elemente.

#### --answer--

Zahteva specifičan tip podataka ili format vrednosti za određeni atribut.

### --question--

#### --text--

Na šta trebate obratiti posebnu pažnju prilikom korišćenja specifičnih karakteristika/funkcija?

#### --distractors--

Specifično svojstvo se automatski konvertuje u jedinice za `px`.

---

Specifični atribut uvek poništava stilove unutar linije.

---

Specifična svojstva ne mogu se koristiti za postavljanje font vrednosti.

#### --answer--

Posebne funkcije možda neće biti podržane u starijim pregledačima.

### --question--

#### --text--

Koja je svrha pružanja vrednosti alternativnog puta u funkciji `var()` za specifične karakteristike CSS?

#### --distractors--

Smanite količinu koda za CSS.

---

Poboljšanje performansi u modernim pregledačima.

---

Poboljšati sporo vreme prikaza na mrežama.

#### --answer--

Osigurati da se koristi važeća vrednost ako nije deklarisan specifičan atribut.

### --question--

#### --text--

Kada navedete specifičan atribut `--foreground` unutar upita za pretragu medija, šta se dešava kada upit za pretragu ne uključuje trenutno vidljivi deo?

#### --distractors--

Specifična karakteristika je sačuvana i nastavlja se koristiti.

---

Specifična karakteristika se vraća na svoju početnu vrednost.

---

Specifična karakteristika se ponovo računa u zavisnosti od oblasti prikaza.

#### --answer--

Specifična funkcija više nije dostupna jer je onemogućena zbog upita za pretraživanjem medija.

### --question--

#### --text--

Kako bi trebalo da se objava izvrši u sledećem primeru?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

Koristiće `--main-color` čak i ako nije deklarisano, koristeći tu vrednost kao podrazumevanu.

---

Koristiće `--main-color` i `--fallback-color` istovremeno, proizvodeći mešavinu tih dve boje.

---

Uvek će koristiti `#000`, bez obzira na to da li su `--main-color` ili `--fallback-color` objavljeni.

#### --answer--

Koristi `--main-color` ako je deklarisano; inače, proverava `--fallback-color;` i ako nisu sve deklarisane, koristi `#000`.

### --question--

#### --text--

Koja mi je korist od korišćenja `@property` u CSS?

#### --distractors--

Poboljšava performanse predviđanjem vrednosti specifičnih atributa.

---

Dodaje posebne prefikse za bolju podršku u pregledaču.

---

Onemogućava korišćenje specijalnih atributa za specifične elemente.

#### --answer--

Omogućava crteže specifičnih karakteristika.

### --question--

#### --text--

Svojstvo `inherits` u deklaraciji specifičnog svojstva `@property`, šta kontroliše?

#### --distractors--

Da li će specifično svojstvo imati početnu vrednost?

---

Da li atribut može imati kratku verziju?

---

Da li karakteristika prihvata alternativne vrednosti?

#### --answer--

Da li se vrednost specifičnog atributa prenosi na elemente potomaka?

### --question--

#### --text--

U deklaraciji specifičnog atributa za `@property`, šta `initial-value` opisuje?

#### --distractors--

Prihvatljive vrednosti koje atribut može da primi.

---

Prioritet karakteristika u nizu zakona.

---

Tip vrednosti koji mora posedovati određenu osobinu.

#### --answer--

Vrednost alternativnog atributa za karakteristiku.

### --question--

#### --text--

Prema kodu HTML i CSS u nastavku, koja će biti vrednost svojstva `color` za element `.box`?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

Koje svojstvo bi trebalo da se koristi za mešavinu boja od CSS?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

Šta je svrha bočnih komponenti u mešavini boja CSS?

#### --distractors--

Izmerava stepen prozirnosti mešavine.

---

Pokazuje smjer mešavine.

---

Ponavljajte mešavinu za stabilnu konfiguraciju.

#### --answer--

Ističe određeni deo gde se boja menja u mešavini.

### --question--

#### --text--

Šta se dešava ako nema definisanog ugla ili pravca u mešavini boja linije CSS?

#### --distractors--

Mešavina koristi smer uzdizanja.

---

Mešavina koristi pravac od dna ka vrhu.

---

Mešavina koristi pravac od leve ka desnoj strani.

#### --answer--

Mešavina koristi smjer od vrha do dna.

### --question--

#### --text--

Koja je funkcija za mešanje boja CSS koja omogućava kreiranje blenda koje ide iz centra ka spolja?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

Koja je ispravna metoda za oglašavanje specifične karakteristike CSS?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

Koje vrste se obično koriste za oglašavanje specifičnih karakteristika CSS na međunarodnom nivou?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

Kada koristiš `var()`, zašto se savetuje da pružiš alternativnu vrednost?

#### --distractors--

Osigurava da parametar bude izvršen tačno.

---

Blokira učitavanje tabela sa eksternim stilovima.

---

Blokira promene strukture pretraživača.

#### --answer--

Osigurava važeću vrednost ako specifični atribut nije deklarisan.

### --question--

#### --text--

Koja je ispravna sintaksa za korišćenje specifičnih atributa kao što je pozadinska boja?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

Koji je zakon CSS koji dozvoljava programerima da oglašavaju specifične funkcije za veću kontrolu njihovog ponašanja?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

Šta deo od `inherits` u definiciji `@property` kontroliše?

#### --distractors--

Da li se atribut koristi u JavaScript?

---

Da li će karakteristika uzrokovati obnavljanje boje?

---

Da, karakteristika može imati funkcije unutar sebe.

#### --answer--

Da li se vrednost osobine prenosi na elemente/karakteristike dece?

### --question--

#### --text--

Objavljujete ugao kombinacije boja crtanje korišćenjem `@property`, koja sintaksa treba koristiti?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

Šta čini specifičnu karakteristiku CSS različitom od standardne karakteristike CSS u načinu predstavljanja?

#### --distractors--

Standardni atributi za CSS deklarisani su sa jednom crticom, dok specifični atributi koriste dve crte.

---

Svojstva podrazumevano za CSS koriste `var()` za definiciju, dok specifična svojstva ne koriste.

---

Podrazumevana svojstva za CSS su određena pravilom `@property`, kada specifična svojstva nisu korišćena.

#### --answer--

Specijalni atributi moraju imati dve crte na početku (`--`), dok standardni atributi za CSS nemaju.

### --question--

#### --text--

Koja je korist od korišćenja specifičnih atributa za CSS?

#### --distractors--

Smanjuje potrebu za komentarima CSS.

---

Poboljšava sliku automatski.

---

Uzrokuje da parametri JavaScript budu neželjeni.

#### --answer--

Omogućava korišćenje istih stilova, kao i centralno upravljanje.

### --question--

#### --text--

Koje svojstvo CSS se može promeniti korišćenjem specifičnih atributa na osnovu argumenata pretrage medija?

#### --distractors--

Ovo je `background-color` ovde.

---

Je `font-family` samo.

---

Ovo je `z-index` ovde.

#### --answer--

Bilo koja karakteristika koja prihvata vrednost.

### --question--

#### --text--

U kontekstu parametara CSS, uloga `initial-value` u zakonu `@property` je kakva?

#### --distractors--

Podižem nivo kvalifikacija.

---

Opisuje visok nivo za crteže.

---

Menja prioritet selektora.

#### --answer--

Pruža podrazumevanu vrednost za svojstvo ako mu nije data vrednost.

### --question--

#### --text--

Razmotri kod HTML i CSS koji sledi. Koja će biti pozadinska boja za element `.card`?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

U CSS sledećeg, šta se dešava ako korisnik pokuša da postavi nevažeću vrednost za `--padding` (na primer, boju umesto dužine)?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

Pretraživač prihvata vrednost, ali odbija tokom sortiranja.

---

Pretraživač izbacuje grešku vremena izvršavanja.

---

Pretraživač konvertuje vrednost direktno u validnu dužinu.

#### --answer--

Kivinjari koristi početnu vrednost atributa.

### --question--

#### --text--

Koja je svrha specifičnog atributa `--gradient-angle` u ovom primeru?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

Oписуje stopu promene mešavine.

---

Opiše veličinu mešavine.

---

Postavlja stanje za mešanje boja.

#### --answer--

Kontroliše smjer/okrenje mešavine.

### --question--

#### --text--

Kakva je situacija koja pokazuje prednost korišćenja specifičnih svojstava za okruženje?

#### --distractors--

Koristiti samo za veličinu fonta.

---

Samo za upotrebu na `:hover`.

---

Sprečavanje za jednu klasu od CSS.

#### --answer--

Menja vrednost kroz klasu.

### --question--

#### --text--

Zašto programeri bi mogli da preferiraju korišćenje ``:root`` za eksponovanje parametara `CSS`?

#### --distractors--

Zato što `:root` poboljšava brzinu učitavanja stranice.

---

Zato što `:root` uklanja prioritet.

---

Zato što `:root` je potrebno za JavaScript.

#### --answer--

Zbog što `:root` dozvoljava atributu globalni opseg.

### --question--

#### --text--

CSS šta će se desiti sledeće ako `--secondary-color` nije deklarisano?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

Koristiće `black`.

---

Odbacuje karakteristiku `color`.

---

Koristiće `--secondary-color`.

#### --answer--

Koristiće `orange`.

### --question--

#### --text--

Koja vrsta vrednosti `<color>` predstavlja u sintaksičkom delu `@property`?

#### --distractors--

Roze kao `90deg`.

---

Dužina kao `10px`.

---

Procenat kao `50%`.

#### --answer--

Vrednosti boja kao `#ff0000` ili `red`.

### --question--

#### --text--

Koja je glavna prednost korišćenja specifičnih atributa u kombinaciji sa `@property`?

#### --distractors--

Sprečava potrebu za alternativnim rešenjima.

---

Obezbeđuje stabilno postavljanje.

---

Smanjuje potrebu za časovima.

#### --answer--

Omogućava crteže karakteristika.

### --question--

#### --text--

`var(--undefined-property, fallback)` Šta radi kada nije deklarisana specifična karakteristika?

#### --distractors--

Prestani sa bacanjem grešaka i prestani da koristiš stilove.

---

Odbacuje sve zakone vezane za CSS.

---

Koristi ime parametra kako je.

#### --answer--

Koristi alternativnu vrednost umesto toga.