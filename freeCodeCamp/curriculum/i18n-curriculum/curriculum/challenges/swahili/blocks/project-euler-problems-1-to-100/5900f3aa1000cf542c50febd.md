---
id: 5900f3aa1000cf542c50febd
title: 'Vežba 62: Permutacije brojeva za lozinku'
challengeType: 1
forumTopicId: 302174
dashedName: problem-62-cubic-permutations
---

# --description--

Broj ujazja, 41063625 ($345^3$), može se promeniti redosled kako bi proizveo još dva broja ujazja: 56623104 ($384^3$) i 66430125 ($405^3$). Zapravo, 41063625 je najmanji broj ujazja koji ima samo aranžman od tri cifre koje su takođe brojevi ujazja.

Pronađi najmanji kubni broj čije `n` permutacije cifara takođe predstavljaju kubne brojeve.

# --hints--

`cubicPermutations(2)` Trebalo bi da vrati broj.

```js
assert(typeof cubicPermutations(2) === 'number');
```

`cubicPermutations(2)` trebalo bi da vrati `125`.

```js
assert.strictEqual(cubicPermutations(2), 125);
```

`cubicPermutations(3)` treba da vrati `41063625`.

```js
assert.strictEqual(cubicPermutations(3), 41063625);
```

`cubicPermutations(4)` trebalo bi da vrati `1006012008`.

```js
assert.strictEqual(cubicPermutations(4), 1006012008);
```

`cubicPermutations(5)` treba da vrati `127035954683`.

```js
assert.strictEqual(cubicPermutations(5), 127035954683);
```

# --seed--

## --seed-contents--

```js
function cubicPermutations(n) {

  return true;
}

cubicPermutations(2);
```

# --solutions--

```js
function cubicPermutations(n) {
  function getDigits(num) {
    const digits = [];
    while (num > 0) {
      digits.push(num % 10);
      num = Math.floor(num / 10);
    }
    return digits;
  }

  function getCube(num) {
    return num ** 3;
  }

  const digitsToCubeCounts = {};
  let curNum = 1;
  let digits;

  while (!digitsToCubeCounts[digits] || digitsToCubeCounts[digits].count < n) {
    const cube = getCube(curNum);
    digits = getDigits(cube).sort().join();
    if (!digitsToCubeCounts[digits]) {
      digitsToCubeCounts[digits] = {
        count: 1,
        smallestCube: cube
      };
    } else {
      digitsToCubeCounts[digits].count += 1;
    }

    curNum++;
  }
  return digitsToCubeCounts[digits].smallestCube;
}
```
