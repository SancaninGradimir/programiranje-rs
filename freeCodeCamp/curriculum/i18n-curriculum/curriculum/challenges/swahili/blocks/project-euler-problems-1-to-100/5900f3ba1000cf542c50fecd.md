---
id: 5900f3ba1000cf542c50fecd
title: 'Vežba 78: Raspodela valute'
challengeType: 1
forumTopicId: 302191
dashedName: problem-78-coin-partitions
---

# --description--

Acha ${p}(n)$ izračunati broj različitih načina na koje se valuta `n` može podeliti u grupe. Na primer, pet valuta može se podeliti u grupe na sedam potpuno različitih načina, pa ${p}(5) = 7$.

<div style='text-align: center;'>

| Grupe valuta |
| ----------------- |
| OOOOO             |
| OOOO<0xC2><0xA0> <0xC2><0xA0>O          |
| OOO<0xC2><0xA0> <0xC2><0xA0>OO          |
| OOO<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O       |
| OO<0xC2><0xA0> <0xC2><0xA0>OO<0xC2><0xA0> <0xC2><0xA0>O       |
| OO<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O    |
| O<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O<0xC2><0xA0> <0xC2><0xA0>O |

</div><br>

Pronađi najmanju vrednost `n` koja je ${p}(n)$ deljiva sa `divisor`.

# --hints--

`coinPartitions(7)` Trebalo bi da vrati broj.

```js
assert(typeof coinPartitions(7) === 'number');
```

`coinPartitions(7)` trebalo bi da vrati `5`.

```js
assert.strictEqual(coinPartitions(7), 5);
```

`coinPartitions(10000)` mora vratiti `599`.

```js
assert.strictEqual(coinPartitions(10000), 599);
```

`coinPartitions(100000)` treba da vrati `11224`.

```js
assert.strictEqual(coinPartitions(100000), 11224);
```

`coinPartitions(1000000)` treba da vrati `55374`.

```js
assert.strictEqual(coinPartitions(1000000), 55374);
```

# --seed--

## --seed-contents--

```js
function coinPartitions(divisor) {

  return true;
}

coinPartitions(7);
```

# --solutions--

```js
// compute pentagonal numbers per generating function
const pentagonalNumbers = Array(251)
  .fill(0)
  .flatMap((_, i) => i ? [i * (3 * i - 1) / 2, i * (3 * i - 1) / 2 + i] : []);

function coinPartitions(divisor) {
  // helper data
  const signs = [1, 1, -1, -1];

  // compute partition counts until we find a multiple of divisor
  const partitions = Array(divisor + 1).fill(0);
  partitions[0] = 1;
  for (let i = 1; partitions[i - 1] > 0; i++) {
    // compute next partition count
    for (let j = 0; pentagonalNumbers[j] <= i; j++) {
      partitions[i] += partitions[i - pentagonalNumbers[j]] * signs[j % 4];
    }
    
    partitions[i] = partitions[i] % divisor;
    if (partitions[i] < 0) partitions[i] += divisor; // positive mod
    // return when found
    if (partitions[i] === 0) return i;
  }
}
```
