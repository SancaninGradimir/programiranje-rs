---
id: 66edd4f31ff19bf5573bf64b
title: Pokušao sam da napišem program za performanse JavaScript-a
challengeType: 8
dashedName: quiz-javascript-functional-programming
---

# --description--

Da bi prošao kratki test, moraš tačno odgovoriti na najmanje 9 od 10 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je čisti funkcioner?

#### --distractors--

Funkcija koja menja globalni parametar i vraća ili `undefined` ili `null`.

---

Funkcija koja ispisuje rezultate na konzolu.

---

Funkcija koja menja svoje ponašanje u zavisnosti od spoljnih faktora i vraća `null`.

#### --answer--

Funkcija koja vraća isti rezultat uvek za isti ulaz i ne stvara sporedne efekte.

### --question--

#### --text--

Šta je bočni efekat u funkcionalnom programiranju?

#### --distractors--

`TypeError` nepredviđeno u vašem kodu.

---

Funkcija koja zahteva mnogo vremena za izvršavanje i izaziva pad programa.

---

Rekurzivni poziv funkcije koji uzrokuje beskonačnu petlju i dovodi do padanja programa.

#### --answer--

Promene stanja programa koje su vidljive izvan funkcije.

### --question--

#### --text--

Šta je koring u aplikaciji za funkcionisanje?

#### --distractors--

Metoda dizajniranja testova pomaže da se osigura da vaš kod nema nikakvih grešaka.

---

Najbolji način za pisanje ponavljajućih funkcija koje treba pokrenuti dvostruko brže od drugih funkcija.

---

Proces izgradnje, razvoja i testiranja vašeg koda kako bi se osiguralo da zadovoljava standarde definisane od strane ECMAScript-a.

#### --answer--

Proces konverzije funkcije koja ima više argumenata u niz funkcija, svaka sa samo jednim argumentom.

### --question--

#### --text--

Koji su rezultati za čistu funkciju prikazanu ispod?

```js
const add = (a, b) => a + b;
console.log(add(2, 5));
```

#### --distractors--

`3`

---

`4`

---

`undefined`

#### --answer--

`7`

### --question--

#### --text--

Koji od sledećih je primer za kuringa?

#### --distractors--

```js
const curriedAverage = (a, b, c) => a + b + c / 3
```

---

```js
const curriedAverage = (a, b, c) => a + b + c
```

---

```js
function curriedAverage(a) {
  return a 
}

function curried(c) {
  return c
}
```

#### --answer--

```js
function curriedAverage(a) {
  return function(b) {
    return function(c) {
      return (a + b + c) / 3;
    };
  };
}
```

### --question--

#### --text--

Koji je među sledećim glavni princip performansi programiranja?

#### --distractors--

Periodično menjanje globalnih parametara.

---

Korišćenje veoma promenljivih modela podataka.

---

Naglašavanje nasleđa stvari.

#### --answer--

Izbegavanje sporednih efekata korišćenjem nepromenljivih podataka.

### --question--

#### --text--

Šta je nečista funkcija?

#### --distractors--

Uređaj koji vraća nešto/predmet.

---

Funkcija koja nema nuspojave.

---

Funkcija koja vraća `null`

#### --answer--

Funkcija sa sporednim efektima.

### --question--

#### --text--

Koja od sledećih je ispravan način za pozivanje ugnježdenog funkcionera?

#### --distractors--

```js
curriedAverage(2 3 4);
```

---

```js
curriedAverage(2)==(3)==(4);
```

---

```js
curriedAverage(2)=>(3)=>(4);
```

#### --answer--

```js
curriedAverage(2)(3)(4);
```

### --question--

#### --text--

Koja od sljedećih je primjer nečiste funkcije?

#### --distractors--

```js
function example(num) {
  return num;
}
```

---

```js
function sum(num1, num2) {
 return num1 + num2
}
```

---

```js
function addToTotal(value) {
  let total = 0;
  total += value;
  return total;
}
```

#### --answer--

```js
let total = 0;
function addToTotal(value) {
  total += value;
  return total;
}
```

### --question--

#### --text--

Koji od sledećih NIJE primer nuspojave?

#### --distractors--

Pisanje u fajl.

---

Menjajte globalni parametar.

---

Izvršiti poziv API.

#### --answer--

Vrati zbir dve vrednosti.