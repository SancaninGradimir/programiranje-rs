---
id: 5900f37f1000cf542c50fe92
title: 'Vežba 19: Brojanje nedelja'
challengeType: 1
forumTopicId: 301827
dashedName: problem-19-counting-sundays
---

# --description--

Dobili ste sledeće informacije, ali takođe možete da izvedete sopstveno istraživanje.

<ul>
  <li>1. januar 1900. je bio ponedeljak.</li>
  <li>Trideset dana su septembar,<br>april, jun i novembar.<br>Sve ostale imaju trideset jedan,<br>osim samo februara,<br>koji ima dvadeset osam, kišu ili sunce.<br>A u godinama koje su skokne, dvadeset devet.</li>
  <li>Skokna godina se dešava u bilo kojoj godini koja je deljiva sa 4, ali ne u veku osim ako nije deljiva sa 400.</li>
</ul>

Koliko je nedelja pala na određeni datum u mesecu tokom dvadesetog veka (1 Jan 1901 do 31 Dec 2000)?

# --hints--

`countingSundays(1943, 1946)` Trebalo bi da vrati broj.

```js
assert.isNumber(countingSundays(1943, 1946));
```

`countingSundays(1943, 1946)` Trebalo bi da vrati 6.

```js
assert.strictEqual(countingSundays(1943, 1946), 6);
```

`countingSundays(1995, 2000)` trebalo bi da vrati 10.

```js
assert.strictEqual(countingSundays(1995, 2000), 10);
```

`countingSundays(1901, 2000)` Trebalo bi da vrati 171.

```js
assert.strictEqual(countingSundays(1901, 2000), 171);
```

# --seed--

## --seed-contents--

```js
function countingSundays(firstYear, lastYear) {

  return true;
}

countingSundays(1943, 1946);
```

# --solutions--

```js
function countingSundays(firstYear, lastYear) {
  let sundays = 0;

  for (let year = firstYear; year <= lastYear; year++) {
    for (let month = 0; month <= 11; month++) {
      const thisDate = new Date(year, month, 1);
      if (thisDate.getDay() === 0) {
        sundays++;
      }
    }
  }
  return sundays;
}
```
