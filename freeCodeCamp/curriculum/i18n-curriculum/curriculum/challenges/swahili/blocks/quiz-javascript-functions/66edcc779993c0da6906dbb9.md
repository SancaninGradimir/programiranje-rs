---
id: 66edcc779993c0da6906dbb9
title: Pokušao sam napisati funkciju za JavaScript
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji od sledećih je ispravan način za objavljivanje funkcije u JavaScript?

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

Šta se dešava kada pozovete (ili izvršite) funkciju?

#### --distractors--

Kod unutar funkcije će biti izvršen od strane `null` i uvek vraćen.

---

Neće se desiti ništa kada pozovete funkciju.

---

`TypeError` Program će baciti grešku i neće moći da radi.

#### --answer--

Kod unutar funkcije će biti izvršen pomoću vraćene vrednosti.

### --question--

#### --text--

Koji je među sledećih ispravan način za pozivanje funkcije?

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

Šta je funkcija glavne reči za `return`?

#### --distractors--

Ključna reč za `return` će pokrenuti funkciju.

---

Glavni deo `return` će nam dati tip kose.

---

Glavna funkcija od `return` nema nikakvu ulogu u operacijama.

#### --answer--

Primarni naziv za `return` vraća vrednost iz funkcije.

### --question--

#### --text--

Koja je među sljedećih validna funkcija?

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

Šta je funkcija (ili: Funkcioner) za kitendakazi?

#### --distractors--

Vrednosti koje predstavljaju nepostojanje vrednosti.

---

Specifične vrednosti koje funkcionišu kao anonimne funkcije.

---

Vrednosti promenljivih korišćene unutar funkcije.

#### --answer--

Argumenti koji se prosleđuju funkciji kada je pozovete.

### --question--

#### --text--

Šta su funkcionalne kriterijume/kriteriji?

#### --distractors--

Vrednosti decimalnih brojeva koje se koriste unutar funkcije.

---

Vrednosti koje se prosleđuju funkciji kada je pozovete.

---

Vrednosti koje predstavljaju namerno/svesno odsustvo vrednosti.

#### --answer--

Vrednosti navedene unutar definicije funkcije.

### --question--

#### --text--

Šta će biti prikazano na konzoli?

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

Koja je tačna sintaksa za strelicastu funkciju među sledećim?

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

Nema ništa sa kodom koje će se izvršiti normalno.

---

Niz karaktera `"Hello there!"` će biti prikazan na konzoli.

---

Niz znakova `"Hello!"` će biti prikazan u konzoli.

#### --answer--

Dobićete grešku referenciranja zato što `developer` nije međunarodno prepoznato.

### --question--

#### --text--

Kada možete da se oslobodite uobičajenih zagradi koje okružuju listu parametara za streličnu funkciju?

#### --distractors--

Uvek je potrebno izbegavati korišćenje standardnih zagrada oko liste parametara.

---

Nikada nije dozvoljeno napuštati standardne zagrade za streličnu funkciju.

---

Trebalo bi da koristite samo standardne zagrade kada radite sa više parametara.

#### --answer--

Trebalo bi da izbegavate standardne zagrade samo kada imate samo jedan kriterijum/uslov.

### --question--

#### --text--

Kako možete da uklonite zaokružene zagrade i glavne reči `return` za streličnu funkciju?

#### --distractors--

Treba ti uvek da izbegavaš okruglje zagrade i glavnu reč od `return`.

---

Nikada ne smijete da se odmaknete od krivuljastih zagrada i glavne riječi `return`.

---

Kada telo funkcije sadrži mnogo linija koda.

#### --answer--

Kada funkcionalno telo ima samo jedan matematički izraz.

### --question--

#### --text--

Šta će sledeća funkcija vratiti?

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

Koji od sledećih je ispravan način korišćenja osnovnih parametara?

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

Niz znakova `"Hello, developer"` će biti prikazan na konzoli.

---

Poruka o grešci će biti prikazana na konzoli.

---

Neće biti ništa prikazano u konzoli.

#### --answer--

Sekvenca karaktera `"Hello, Jessica"` će biti prikazana na konzoli.

### --question--

#### --text--

Šta se dešava ako pokušaš da ukloniš vitičaste zagrade iz obične funkcije?

#### --distractors--

Upozoravajuća kutija će biti prikazana na stranici sa greškom.

---

Program će prestati da radi i neće se ponovo pokrenuti.

---

Ništa se neće desiti.

#### --answer--

Dobići ćete sintaktičku grešku.

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

Poruka o grešci će biti prikazana na konzoli.

---

Broj `0` će biti prikazan na konzoli.

---

Nema ništa što se prikazuje u konzoli.

#### --answer--

Broj `10` će biti prikazan u konzoli.

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

Poruka o grešci koja kaže `num1` nije podržana.

---

Broj `0` će biti prikazan u konzoli.

---

`undefined` Biće prikazano na konzoli.

#### --answer--

Poruka o grešci koja kaže `num2` nije podržana.

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`