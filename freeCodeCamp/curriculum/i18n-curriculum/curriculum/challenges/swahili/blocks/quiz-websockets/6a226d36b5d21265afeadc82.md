---
id: 6a226d36b5d21265afeadc82
title: Pokušaj sa WebSockets-om
challengeType: 8
dashedName: quiz-websockets
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Zašto Node.js zahteva eksternu biblioteku za WebSocket servere?

#### --distractors--

Node.js ima ugrađeni WebSocket server, ali zahteva dodatnu konfiguraciju za omogućavanje.

---

Node.js podržava WebSockets samo prilikom korišćenja okvira Express.

---

Node.js Ne podržava WebSockets uopšte, bez obzira na eksterne pakete.

#### --answer--

Node.js nema ugrađenu WebSocket uslugu.

### --question--

#### --text--

Koja su tri glavna dela arhitekture Pub/Sub?

#### --distractors--

Pošiljaoci, foleni, i primatelji.

---

Kreatori, načini, i korisnici.

---

Klijenti, servis, i baza podataka.

#### --answer--

Izdavači, tema, i članovi.

### --question--

#### --text--

Koja vrsta komunikacije HTTP prati?

#### --distractors--

Primer dve trajne strane.

---

Primer obaveštenja.

---

Primer objave/podešavanja.

#### --answer--

Primer upita-odgovora.

### --question--

#### --text--

Koji je WebSocket događaj koji se dešava kada novi klijent pristupa serveru?

#### --distractors--

`connect`

---

`open`

---

`join`

#### --answer--

`connection`

### --question--

#### --text--

Koji je glavni nedostatak direktne komunikacije između dve aplikacije u poređenju sa Pub/Sub?

#### --distractors--

Direktna komunikacija je uvek sporija od Pub/Sub.

---

Direktna komunikacija sprečava aplikaciju da pošalje više od jedne poruke.

---

Direktna komunikacija uvek zahteva server za verifikaciju identiteta treće strane.

#### --answer--

Kreira neželjenu zavisnost između programa; promene ili greške u jednom programu mogu slomiti drugi.

### --question--

#### --text--

Koji je protokol sa kojim WebSocket konekcija počinje pre nadogradnje?

#### --distractors--

Povezivanje WebSocket-a počinje sa specifičnim WebSocket protokolom od samog početka.

---

Konekcija WebSocket počinje sa TCP-om, potpuno zaobilazeći HTTP.

---

Svaki WebSocket konekcija počinje sa FTP-om, a zatim se unapređuje u WebSocket.

#### --answer--

Svaki WebSocket konekcija počinje sa HTTP.

### --question--

#### --text--

Koji metod koristite za slanje podataka putem WebSocket konekcije klijentu?

#### --distractors--

`socket.emit()`

---

`socket.write()`

---

`socket.push()`

#### --answer--

`socket.send()`

### --question--

#### --text--

Koja je uloga teme u Pub/Sub sistemu?

#### --distractors--

Čuvam poruke zauvek radi kasnijeg pristupa.

---

Proverava izdavače pre slanja poruke.

---

Povezuje direktno izdavače i posebne članove.

#### --answer--

Služi kao posrednik između izdavača i članova.

### --question--

#### --text--

Kako se komunikacija HTTP razlikuje od komunikacije putem WebSocketa?

#### --distractors--

HTTP su oba kraja kada je WebSocket samo sa jedne strane.

---

HTTP uspostavlja trajno povezivanje, dok WebSocket zatvaranje nakon svake promene.

---

HTTP na WebSocket svi prate isti obrazac zahtev-odgovor.

#### --answer--

HTTP sledi `Client --> Request --> Server --> Response`, kada je WebSocket `Client <--> Server`.

### --question--

#### --text--

Zašto bi trebalo da proveriš `client.readyState === WebSocket.OPEN` pre obaveštavanja povezanih klijenata?

#### --distractors--

Skrij poruku pre slanja svakom korisniku.

---

Ograničiti broj poruka koje se šalje svakom korisniku u sekundi.

---

Osigurati da klijent ima dozvolu za primanje poruke.

#### --answer--

Izbegavajte greške prilikom slanja kupcima koji su odjavljeni ili se još uvek registruju.

### --question--

#### --text--

Koji je status kod za HTTP koji server šalje kada prihvati zahtev za nadogradnju WebSocket-a?

#### --distractors--

`200 OK`

---

`301 Moved Permanently`

---

`426 Upgrade Required`

#### --answer--

`101 Switching Protocols`

### --question--

#### --text--

Koji je pravi primer korišćenja Pub/Sub-a?

#### --distractors--

Web stranica učitava HTML sa servera.

---

Alat komandne linije koji obrađuje fajlove direktno.

---

Baza podataka odnosa koja čuva i upita strukturirane podatke.

#### --answer--

Sistem obaveštenja koji pruža upozorenje u realnom vremenu svim svojim članovima.

### --question--

#### --text--

Kako se postavlja WebSocket servis na port `3000` koristeći biblioteku `ws`?

#### --distractors--

```js
const server = new WebSocket({ port: 3000 });
```

---

```js
const server = WebSocket.createServer(3000);
```

---

```js
const server = ws.listen(3000);
```

#### --answer--

```js
const server = new WebSocket.Server({ port: 3000 });
```

### --question--

#### --text--

Koji je WebSocket događaj koji se dešava kada se veza zatvori?

#### --distractors--

`disconnect`

---

`end`

---

`terminate`

#### --answer--

`close`

### --question--

#### --text--

Šta je tok poruka Pub/Sub?

#### --distractors--

Pošiljalac šalje klijentu --> klijent šalje temu --> tema skladišti poruku.

---

Tema šalje poruku --> štampači je primaju --> štampači je isporučuju članovima.

---

Klijent se povezuje sa štampačem --> štampač šalje podatke --> klijent potvrđuje prijem.

#### --answer--

Korisnik se pretplaćuje na temu --> Objavljač šalje poruku toj temi --> Sistem za poruke stiže svim članovima te teme.

### --question--

#### --text--

Koji primer najbolje objašnjava razliku između HTTP i WebSockets?

#### --distractors--

HTTP je kao telefon, a WebSockets su kao razmena pisama.

---

HTTP je kao soba za razgovor, a WebSockets su kao novine.

---

HTTP je kao direktan tok, a WebSockets su kao snimljen video.

#### --answer--

HTTP je kao razmena pisama, a WebSockets su kao telefonski poziv.

### --question--

#### --text--

Koji je WebSocket događaj koji se dešava kada povezani klijent pošalje podatke serveru?

#### --distractors--

`data`

---

`receive`

---

`incoming`

#### --answer--

`message`

### --question--

#### --text--

Šta je tok WebSocket konekcije koji je pravilno objašnjen?

#### --distractors--

Klijent se povezuje preko TCP --> Server šalje podatke --> Klijent prima --> Koneksija se zatvara.

---

Klijent šalje podatke --> Usluga zahteva poboljšanje --> Povezivanje se otvara --> Klijent potvrđuje.

---

Server inicira konekciju --> Klijent prihvaća --> Oba kraja razmenjuju podatke --> Konekcija se zatvara.

#### --answer--

Klijent se povezuje preko HTTP --> server odgovara na `101 Switching Protocols` --> koneksija ostaje otvorena --> obe strane razmenjuju podatke u bilo kojem trenutku.

### --question--

#### --text--

WebSockets i Pub/Sub, kako se razlikuju u aplikaciji za stvarno vreme?

#### --distractors--

WebSockets obrađuju autentifikaciju kada Pub/Sub obrađuje skladištenje podataka.

---

WebSockets se koristi za serversku komunikaciju sa serverom, dok je Pub/Sub namenjen klijentskoj komunikaciji sa serverom.

---

To je ista tehnologija sa različitim nazivima.

#### --answer--

WebSockets pruža način komunikacije dok Pub/Sub šalje primer poruke.

### --question--

#### --text--

Koji je stvarni primer korišćenja WebSockets-a?

#### --distractors--

Neinspirativna blog stranica prikazuje članke.

---

Sistem za prikaz fajlova na strani servera.

---

Baza podataka koja čuva naloge korisnika.

#### --answer--

Program za razgovor.