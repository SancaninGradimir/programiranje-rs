{
  "id": "589fc830f9fc0f352b528e74",
  "title": "Podesi okruženje",
  "challengeType": 2,
  "forumTopicId": 301566,
  "dashedName": "set-up-the-environment",
  "solutions": [],
  "assignments": [],
  "tests": [
    {
      "text": "<p><code>socket.io</code> treba da bude zavisnost.</p>",
      "testString": "  const url = new URL(\"/_api/package.json\", code);\n  const res = await fetch(url);\n  const packJson = await res.json();\n  assert.property(\n    packJson.dependencies,\n    'socket.io',\n    'Your project should list \"socket.io\" as a dependency'\n  );"
    },
    {
      "text": "<p>Treba da zahtevaš i instanciraš <code>http</code> kao <code>http</code>.</p>",
      "testString": "  const url = new URL(\"/_api/server.js\", code);\n  const res = await fetch(url);\n  const data = await res.text();\n  assert.match(\n    data,\n    /http.*=.*require.*('|\")http\\1/s,\n    'Your project should list \"http\" as a dependency'\n  );"
    },
    {
      "text": "<p>Treba da zahtevaš i instanciraš <code>socket.io</code> kao <code>io</code>.</p>",
      "testString": "  const url = new URL(\"/_api/server.js\", code);\n  const res = await fetch(url);\n  const data = await res.text();\n  assert.match(\n    data,\n    /io.*=.*require.*('|\")socket.io\\1.*http/s,\n    'You should correctly require and instantiate socket.io as io.'\n  );"
    },
    {
      "text": "<p>Socket.IO treba da sluša konekcije.</p>",
      "testString": "  const url = new URL(\"/_api/server.js\", code);\n  const res = await fetch(url);\n  const data = await res.text();\n  assert.match(\n    data,\n    /io.on.*('|\")connection\\1.*socket/s,\n    'io should listen for \"connection\" and socket should be the 2nd arguments variable'\n  );"
    },
    {
      "text": "<p>Tvoj klijent treba da se konektuje na server.</p>",
      "testString": "  const url = new URL(\"/public/client.js\", code);\n  const res = await fetch(url);\n  const data = await res.text();\n  assert.match(\n    data,\n    /socket.*=.*io/s,\n    'Your client should be connection to server with the connection defined as socket'\n  );"
    }
  ],
  "description": "<section id=\"description\">\n<p>Sledeći izazovi će koristiti <code>chat.pug</code> fajl. Zato, u tvom <code>routes.js</code> fajlu, dodaj GET rutu koja cilja <code>/chat</code> koja koristi <code>ensureAuthenticated</code>, i prikazuje <code>chat.pug</code>, sa <code>{ user: req.user }</code> prosleđenim kao argument za odgovor. Sada, izmeni tvoju postojeću rutu <code>/auth/github/callback</code> da postavi <code>req.session.user_id = req.user.id</code>, i preusmeri na <code>/chat</code>.</p>\n<p><code>socket.io@~2.3.0</code> je već dodat kao zavisnost, pa ga uvezi/instanciraj u svom serveru kao što sledi za <code>http</code> (dolazi unutar Node.js):</p>\n<pre><code class=\"language-javascript\">const http = require('http').createServer(app);\nconst io = require('socket.io')(http);\n</code></pre>\n<p>Sada nakon što si http server povezao sa express app-om, treba da slušaš sa http servera. Izmeni liniju koja ima <code>app.listen</code> da bude <code>http.listen</code>.</p>\n<p>Prva stvar koja treba da se upravlja je slušanje novog konekcije od klijenta. Ključna reč <dfn>on</dfn> radi tačno to - sluša specifičan događaj. Zahteva 2 argumenta: niz karaktera koji opisuje ime događaja koji se emitira, i funkcija kroz koju se prosleđuju podaci. U slučaju našeg konekcionog slušača, koristi <code>socket</code> za definisanje podataka u drugom argumentu. Socket je jedan klijent koji je konektovan.</p>\n<p>Da bi slušao konekciju na tvom serveru, dodaj sledeće unutar tvoje baze podataka konekcije:</p>\n<pre><code class=\"language-javascript\">io.on('connection', socket => {\n  console.log('A user has connected');\n});\n</code></pre>\n<p>Sada da bi klijent konektovan, trebaš samo da dodaš sledeće u <code>client.js</code> koji se učitava na stranici nakon autentifikacije:</p>\n<pre><code class=\"language-js\">/*global io*/\nlet socket = io();\n</code></pre>\n<p>Komentari su tamo za sprečavanje grešaka koje obično vidite jer 'io' nije definisan u fajlu. Već si dodao pouzdan CDN za Socket.IO biblioteku na stranicu u <code>chat.pug</code>.</p>\n<p>Sada probaj da učitaš svoju app i autentifikuj se i trebalo bi da vidiš u konzoli servera <code>A user has connected</code>.</p>\n<p><strong>Napomena:</strong> <code>io()</code> radi samo kada si konektovan na socket na istom url/serveru. Za konektovanje na spoljašnji socket na drugom mestu, koristićeš <code>io.connect('URL');</code>.</p>\n<p>Pošalji svoju stranicu kada vidiš da si uradio dobro. Ako naiđeš na greške, možeš <a href=\"https://forum.freecodecamp.org/t/advanced-node-and-express/567135#set-up-the-environment-6\" target=\"_blank\" rel=\"noopener noreferrer nofollow\">pogledati projekat urađen do sada</a>.</p>\n</section>",
  "translationPending": false,
  "block": "advanced-node-and-express",
  "blockLayout": "legacy-challenge-list",
  "hasEditableBoundaries": false,
  "order": 1,
  "instructions": "",
  "questions": [],
  "superBlock": "quality-assurance",
  "superOrder": 21,
  "challengeOrder": 16,
  "isLastChallengeInBlock": false,
  "required": [],
  "helpCategory": "JavaScript",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "certification": "quality-assurance"
}