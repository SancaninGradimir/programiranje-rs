---
id: 67358be1c7903489c0a7db78
title: "Pokušao sam da napravim (ili: da napišem) deo sa JavaScript seta/biblioteke"
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je Set u JavaScript?

#### --distractors--

Struktura podataka koja dozvoljava ponavljanje vrednosti.

---

Struktura podataka koja skladišti samo sekvencu slova i brojeva.

---

Kolekcija koja ne dozvoljava brisanje svojih elemenata.

#### --answer--

Kolekcija čija je vrednost moguća samo jednom.

### --question--

#### --text--

Koji put među ovim je dostupan na Mapi, ali nije u Skupu?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Koji se metod koristi za dodavanje elemenata u Skup?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Možete li proveriti da li Set ima određenu vrednost?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Koje su koristi ovog seta među ovim?

#### --distractors--

Upravlja parovima ključ-vrednost u bazi podataka.

---

Kreirati strukturirane liste stavki.

---

Čuvanje sekvencijalnih struktura podataka u lokalnom skladištu pregledača.

#### --answer--

Uklanjanje duplikata iz niza podataka.

### --question--

#### --text--

WeakSet šta je u JavaScript?

#### --distractors--

Kolekcija slična Set-u koja dozvoljava samo osnovne tipove podataka.

---

Kolekcija koja je slična Skupu i koja omogućava ponavljanje vrednosti.

---

Kolekcija koja je slična Set-u i sprečava brisanje vrednosti.

#### --answer--

Kolekcija koja je slična skupu i čuva samo jedinstvene elemente.

### --question--

#### --text--

Koja je razlika između Set-a i WeakSet-a?

#### --distractors--

WeakSet može da skladišti bilo koji tip vrednosti, uključujući brojeve.

---

Seti ne dozvoljava ponavljanje vrednosti, ali WeakSet to dozvoljava.

---

Seti dozvoljava prikupljanje otpada, dok WeakSet ne dozvoljava.

#### --answer--

Set može da drži bilo koji tip vrednosti, dok WeakSet drži samo objekte.

### --question--

#### --text--

Ramani šta je u JavaScript?

#### --distractors--

Kolekcija koja dozvoljava samo jedinstvene vrednosti.

---

Sakupljanje koje koristi samo sekvencu slova kao ključeve.

---

Kolekcija koja ne dozvoljava brisanje svojih elemenata.

#### --answer--

Sakupljanje parova ključ-vrednost bilo kog tipa.

### --question--

#### --text--

Koji metod se koristi za unos parova ključ-vrednost u Mapu?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

Šta će sledeći kod prikazati?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

Kako se mapa razlikuje od stvari u JavaScript?

#### --distractors--

Ramani dozvoljava samo brojeve kao ključeve.

---

Vitu dozvoljava svaki tip ključa, dok Ramani dozvoljava samo sekvencu slova.

---

Mapa se ne spaja ručno da bi se ponovilo, kada to stvari urade.

#### --answer--

Ramani dozvoljava bilo kakve tipove ključeva, ali Vitu dozvoljava samo niz slova i simbola.

### --question--

#### --text--

Kako se može ukloniti par ključ-vrednost iz mape?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Šta karakteristika `size` za Ramani vraća?

#### --distractors--

Broj legende na mapi.

---

Broj jedinstvenih vrednosti na Mapi.

---

Ukupna memorija korišćena od strane mape.

#### --answer--

Broj parova ključ-vrednost na mapi.

### --question--

#### --text--

WeakMap šta je u JavaScript?

#### --distractors--

Kolekcija slična Mapi koja dozvoljava osnovne vrednosti kao ključeve.

---

Kolekcija slična Mapi koja sprečava brisanje stavki/predmeta.

---

Kolekcija slična Ramani koja automatski organizuje svoje ključeve.

#### --answer--

Kolekcija slična Mapi sa slabim referencama za ključeve.

### --question--

#### --text--

Koja je razlika između Map-a i WeakMap-a?

#### --distractors--

Mapa dopušta samo osnovne tipove kao ključeve, dok WeakMap dopušta samo objekte.

---

Map dozvoljava prikupljanje smeća, dok WeakMap ne dozvoljava.

---

WeakMap omogućava dupliranje ključeva, dok Map to ne dozvoljava.

#### --answer--

Maps se mogu iterirati, dok WeakMap ne može.

### --question--

#### --text--

Koji od ovih načina nema u WeakMap-u?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Šta će sledeći kod prikazati?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Kako možete ponoviti vrednost u Skupu?

#### --distractors--

Korišćenjem njia `.map()`.

---

Koristeći metod `.reduce()`.

---

Koristeći njia od `.filter()`.

#### --answer--

Korišćenjem načina `.forEach()`.

### --question--

#### --text--

Koje su dve metode za Set koje vraćaju `SetIterator` koji drži vrednosti određenog Set-a?

#### --distractors--

`entries()` na `values()`

---

`forEach()` i `values()`

---

`has()` na `add()`

#### --answer--

`keys()` na `values()`

### --question--

#### --text--

Šta će sledeći kod prikazati?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`