---
id: 66f1ad049d7a6ac0886cc2ba
title: Kratak test React forme, dostupnost podataka i metode za podatke
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji od sledećih je primer kontrolisanog unosa?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Da li je bilo koji od sledećih primer nekontrolisanog ulaza?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Nije ni jedna od sledećih puteva za dobijanje podataka u React?

#### --distractors--

SWR

---

Axios

---

Povući API

#### --answer--

Redux

### --question--

#### --text--

Koji od sledećih hook-ova se najčešće koristi za pristup podacima u React?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

Šta je memoizacija?

#### --distractors--

Algoritam koji se često koristi za pronalaženje najkraćeg puta između dva čvora na grafu.

---

Specijalna metoda koja se koristi za poboljšanje performansi svih ulaza, kako kontrolisanih tako i nekontrolisanih.

---

Algoritam podela i osvajanja koji se koristi za sortiranje malih nizova podataka, a zatim njihovo spajanje.

#### --answer--

Metoda za poboljšanje performansi gde su rezultati skupog poziva funkcije sačuvani (memorizovani) na osnovu specifičnih argumenata.

### --question--

#### --text--

Koji od sledećih hook-ova se koristi za skladištenje izračunatih vrednosti (memoize)?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Koji od sljedećih koristi ispravan hook za čuvanje referenci funkcija?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

Šta je zavisnost?

#### --distractors--

Zavisnost je biblioteka koja se široko koristi za testiranje u aplikacijama za React.

---

Zavisnost je vrsta komponente React koja se koristi samo na serveru.

---

Zavisnost je specijalni kolektor koda koji se koristi u programima za React konverziju koda iz React u JavaScript.

#### --answer--

Zavisnost je situacija kada jedan deo ili modul u programu zavisi od drugog kako bi pravilno funkcionisao.

### --question--

#### --text--

Šta je menadžer paketa?

#### --distractors--

Alat koji se koristi za ažuriranje paketa trećih strana instaliranih u programu.

---

Alat koji se koristi za upravljanje paketima kreiranim samo unutar projekta.

---

Alat koji se koristi za premeštanje zavisnosti iz foldera `node_modules` i ugradnju u specifičan konfiguracioni folder.

#### --answer--

Alat koji se koristi za instalaciju, ažuriranje i uklanjanje zavisnosti.

### --question--

#### --text--

Koji od sledećih je upravljač paketa koji se najčešće koristi u ekosistemu JavaScript?

#### --distractors--

pnpm

---

Nitak

---

npm

#### --answer--

Šala

### --question--

#### --text--

Koji od sledećih je važan konfiguracioni fajl sa metapodacima o vašem projektu?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Koji od sledećih je najčešće korišćeni radni sistem za React?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remiks

### --question--

#### --text--

Koja od sljedećih je ispravan način za dodavanje putanja podataka u vašu aplikaciju React koristeći React Router?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Koja od sledećih je ispravan način za kreiranje dinamičke rute podataka (dynamic route) na React Router?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Koji od sledećih hookova se koristi za dobijanje promenljivih parametara iz puta URL?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Koji od sljedećih načina je ispravan za kreiranje prilagođenog rukovaoca zahtjeva u Next.js?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

Šta je Prop drilling?

#### --distractors--

Proces kreiranja redukcionog funkcije (reducer) koja određuje kako bi se stanje trebalo promeniti korišćenjem specifičnih parametara.

---

Proces prenosa parametara sa strane potomka ka strani roditelja.

---

Proces kreiranja kontekst provajdera za aplikaciju React i korišćenjem ga za slanje parametara od roditeljskih do detetovskih komponenti.

#### --answer--

Proces prosleđivanja parametara od roditeljskog dela do veoma duboko ugnježđenih detilja, čak i kada neki detilji ne zahtevaju te parametre.

### --question--

#### --text--

Koja od sledećih nije standardno rešenje za upravljanje stanjem (state) za aplikacije React?

#### --distractors--

Kontekst API

---

Stanje

---

Redux

#### --answer--

Proleće

### --question--

#### --text--

React Developer Tools za šta se koriste?

#### --distractors--

Ovi alati se koriste samo za proveru problema sa pristupom u programima React.

---

Ova oprema se koristi za dodavanje dodatnih testova za komponente React.

---

Ovi alati se koriste za skladištenje svih delova React unutar programa.

#### --answer--

Ovi uređaji se koriste za proveru delova React i identifikovanje problema sa performansama.

### --question--

#### --text--

React Šta su Server Komponente?

#### --distractors--

Ovo su komponente koje upravljaju kontekstom na serveru.

---

Ovo su sačuvani (memoized) delovi koji se prikazuju samo na serveru.

---

Ovo su komponente koje jedinstveno upravljaju stanjem na serveru.

#### --answer--

Ovo su samo delovi koji se prikazuju na serveru.