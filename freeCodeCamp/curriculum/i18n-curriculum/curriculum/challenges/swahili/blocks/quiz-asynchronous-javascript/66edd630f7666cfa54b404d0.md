---
id: 66edd630f7666cfa54b404d0
title: Kratak jednokratni pokušaj JavaScripta
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je program koji nije vezan za jedan trenutak?

#### --distractors--

Način sekvencijalnog izvršavanja koda.

---

Metoda za rukovanje greškama u vašem kodu.

---

Putanja za pravljenje mrežnih zahteva, obično za dobijanje ili slanje podataka na server.

#### --answer--

Metoda za izvršavanje koda istovremeno bez blokiranja glavne niti.

### --question--

#### --text--

Šta su niti?

#### --distractors--

Funkcija koja se koristi za dohvaćanje podataka sa servera.

---

Program koji izvršava kod JavaScript u web pregledaču.

---

Specijalni tip funkcije koji se prosleđuje kao argument drugoj funkciji.

#### --answer--

Sekvenca instrukcija koje se mogu nezavisno izvršiti iz glavnog programa toka.

### --question--

#### --text--

Šta je svrha Fetch API u JavaScript?

#### --distractors--

Upravljanje zahtevom za vraćanje nejednokrotnog vremena.

---

Kreiranje i upravljanje kolačićima.

---

Prikazivanje sadržaja HTML koji se menja.

#### --answer--

Prijaviti se na serveru preko mreže.

### --question--

#### --text--

Koja je metoda za HTTP koja se koristi za slanje podataka na server radi obrade?

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

Ili je među ovim ispravnim načinima za dobijanje podataka iz API?

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

Šta je obaveza u JavaScript?

#### --distractors--

Način za sinhronizaciju više operacija prilikom korišćenja Fetch API.

---

Poseban tip reaktivnog sredstva koje je potrebno koristiti u svim visokonivo metodama.

---

Metoda koja se koristi za rukovanje greškama kada koristite Fetch API.

#### --answer--

Nešto što predstavlja završetak ishoda (ili neuspešnost) za asinhronu operaciju.

### --question--

#### --text--

Šta serijska veza obećanja omogućava?

#### --distractors--

Omogućava vam da izvršavate mnogo aktivnosti istovremeno zajedno.

---

Omogućava vam da efikasnije upravljate višestrukim zadacima istovremeno.

---

Omogućava vam da ponovo pokušate neuspešne aktivnosti odmah.

#### --answer--

Omogućava vam da izvršite niz aktivnosti jedna nakon druge.

### --question--

#### --text--

Koja metoda se koristi za uspešno ispunjenje obećanja?

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

Koji se način koristi za obradu grešaka u lancu obećanja?

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

Koja od ovih nije legitimni način za HTTP?

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

Koja je ispravna sintaksa za kreiranje funkcije async od ovoga?

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

Šta glavna reč za `await` radi u funkciji async?

#### --distractors--

On daje novo obećanje.

---

Izvrši kod jednom, bez čekanja, i vrati nešto.

---

Zabeležite rezultate obećanja i vratite `null`.

#### --answer--

Zaustavlja izvršavanje funkcije dok se obećanje ne ispuni.

### --question--

#### --text--

Ni API koji od ovih se koristi za omogućavanje web stranici da zatraži korisnički profil?

#### --distractors--

Lokator API

---

Geo API

---

Pronađi API

#### --answer--

Geolokacija API

### --question--

#### --text--

Koji od ovih atributa preuzima asinhronu skriptu, ali čeka da se dokument HTML pročita u potpunosti pre pokretanja skripte?

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

Šta će sledeći kod vratiti?

```js
fetch('https://api.example.com/data')
```

#### --distractors--

Greška koja kaže da su podaci oštećeni.

---

Greška koja kaže da podaci nisu dostupni.

---

Neće biti moguće vratiti ništa i JavaScript će nastaviti sa sledećom funkcijom u skripti.

#### --answer--

`Promise` koje rešava da je nešto od `Response`.

### --question--

#### --text--

Putanja za default od HTTP za Fetch API, šta je to?

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

Koji metod među ovim se koristi za brisanje resursa na serveru?

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

Koja je ispravna metoda za kreiranje obećanja od ovoga?

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

Koja je to karakteristika među ovim koja obaveštava pregledač da preuzme skript fajl u realnom vremenu dok nastavlja čitanje dokumenta HTML?

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Koja se metoda geolokacije API koristi za dobijanje trenutne lokacije uređaja?

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`