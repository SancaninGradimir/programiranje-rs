---
id: 66ed8fe7f45ce3ece4053eb2
title: Pokušaj sa CSS Flexbox-om
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je Flexbox?

#### --distractors--

Primer dve mere u rasporedu.

---

Primer tri merenja po redosledu.

---

Primer više dimenzija po rasporedu.

#### --answer--

Primer jedne mere po rasporedu.

### --question--

#### --text--

Koja je svojstvo CSS koje se postavlja da bi omogućilo flexbox raspoređivanje za element `div`?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Koje svojstvo CSS omogućava elementima da se prebace u novi red kada je trenutni red pun?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

Šta kontroliše svojstvo flexbox `justify-content`?

#### --distractors--

Kontroliše orijentaciju manjih komponenti fleksibilnog alata.

---

Upravlja tekstualnim stilom malih komponenti dinamičkog alata.

---

Upravlja poravnanjem manjih komponenti unutar dinamičnog kontejnera.

#### --answer--

Kontroliše distribuciju malih komponenti na glavnoj osi.

### --question--

#### --text--

Koje svojstvo upravlja smerom komponenti unutar mutabilnog kontejnera?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Koja svojstva za CSS mogu se koristiti da bi menjajući delovi nastavljali od leve ka desnoj strani, i da se novi menjajući deo pojavi iznad onoga što ga je prethodilo ako se pređe širina trenutnog reda?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Koje svojstvo kontroli raspored elemenata na koordinatnoj osi?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Da li je to vrednost `justify-content`?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Koja je karakteristika za CSS koju biste koristili da dohvatite elemente u alatu za horizontalno i vertikalno skaliranje?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Koje je `flex-direction` koje menja delove sa desna na levo?

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

Šta je vrednost atributa `flex-wrap`?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

Šta radi vrednost `stretch` u `align-items`?

#### --distractors--

Komponente su direktno pričvršćene blizu početne ivice kontejnera.

---

Komponente su raspoređene prema poravnanju baznih linija prilagodljivog alata.

---

Komponente su direktno upakovane blizu zadnjeg ruba kontejnera.

#### --answer--

Elementi imaju jednake automatske dimenzije za popunjavanje kontejnera.

### --question--

#### --text--

Koja vrednost za `justify-content` postavlja prvi element na početak, poslednji element na kraj i ravnomerno raspoređuje ostale elemente između njih na glavnoj osi?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Koja je vrednost `justify-content` koja postavlja jednak razmak između svih elemenata i ivica kontejnera?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Koja je vrednost `justify-content` koja postavlja jednak razmak između elemenata po glavnoj osi, ostavljajući pola tog prostora na ivicama kontejnera?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Koji od sledećih uzrokuje spajanje komponenti na početku poprečne osovine?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Koji od sledećih uzrokuje povezivanje komponenti na kraju križne osovine?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Ni vrednost za `flex-direction` koja će učiniti da se elementi poravnaju na glavnoj osi od leve ka desnoj?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Koja je vrednost `flex-direction` koja će učiniti da se elementi povežu na poprečnu osu od vrha do dna?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Koja upotreba među ovim nije pogodna za flexbox?

#### --distractors--

Postavljanje elementa u sredinu kontejnera.

---

Vertikalno poravnanje elemenata u nizu.

---

Intenzivno podešavanje distribucije i uniformnosti elemenata za različite zone gledanja.

#### --answer--

Fiksna veličina i razmak između elemenata, kao što je mreža.

## --quiz--

### --question--

#### --text--

Koja je svrha primera jednodimenzionalnog rasporeda, kao što je Flexbox?

#### --distractors--

Obrađivanje rasporeda u dve dimenzije, redove i kolone istovremeno.

---

Kontrolisanje podešavanja za postavljanje specifične pozicije korišćenjem `z-index`.

---

Kreiranje 3D transformacija i složenih crteža na elementima stranice.

#### --answer--

Organizovanje elemenata na jednoj liniji ili osi, bilo kao red ili kolonu.

### --question--

#### --text--

Koji atribut od CSS će uporediti sve dinamičke delove na desnoj strani elementa `.container`?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Koje su dve ose koje određuju smjer rasporeda elemenata u Flex modelu?

#### --distractors--

Horizontalna osa i vertikalna osa.

---

Osa x i osa y.

---

Glavna osa i druga osa.

#### --answer--

Glavna osa i osa krsta.

### --question--

#### --text--

Koja pravila za CSS će rasporediti elemente u vertikalnu kolonu i takođe ih centrirati ravnomerno unutar njihovog kontejnera?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Glavna svrha atributa `flex-wrap` je šta?

#### --distractors--

Postavlja orijentaciju gde su fleksibilni delovi smešteni unutar fleksibilnog kontejnera.

---

Poravnava zamjenjive dijelove na glavnoj osovini kontejnera.

---

Poravnava zamjenjive dijelove na poprečnoj osovini kontejnera.

#### --answer--

Odlučuje da li varijabilni delovi treba da se prebace na nove linije kako bi odgovarali postojećem prostoru.

### --question--

#### --text--

Šta je podrazumevano ponašanje deklaracije `flex-wrap: nowrap;`?

#### --distractors--

Primorava delove da se kreću po novoj liniji ako je preširoko za kontejner.

---

Okreće se komadići na novoj liniji, ali u suprotnom redosledu.

---

On sprečava da se element poveća više nego njegov roditeljski element.

#### --answer--

Ono prisiljava delove da ostanu na jednoj liniji, čak i ako premaše kontejner.

### --question--

#### --text--

Koje je svojstvo CSS prečica za postavljanje svojstava za `flex-direction` i `flex-wrap` zajedno?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Koja je posledica postavljanja `flex-direction: column-reverse;` u varijablu?

#### --distractors--

Poravnava elemente ravnomjerno od desna ka lijevo.

---

Preuređuje raspored elemenata u nizu, ali oni ostaju u jednoj liniji.

---

Raspoređuje elemente vertikalno, od vrha do dna.

#### --answer--

Raspoređuje vertikalne elemente u obrnutom redosledu, od dna ka vrhu.

### --question--

#### --text--

Koji deo od CSS raspoređuje fleksibilne delove u vertikalni red i omogućava prebacivanje na novi red ako nadmaše visinu kontejnera?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Koja je vrednost `justify-content` koja komparira sve varijabilne segmente na kraju glavne ose kontejnera?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Šta je uticaj ovog pravila CSS na promenljive delove?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Delovi su poređani u red i nastaviće se na novi red.

---

Komadi su poređani u redove i nastaviće se na novoj liniji.

---

Elementi su poređani u redoslijed i nastaviće se ukoliko je potrebno.

#### --answer--

Komadi su poređani u red i će preći kapacitet kontejnera ako ne stanu.

### --question--

#### --text--

`justify-content: space-evenly;` Kako postiže poravnanje promenljivih delova?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Komadi su poređani ravnomerno, bez razmaka ni na početku ni na kraju.

---

Komadi su se okupili/skupili u sredini kontejnera.

---

Komadi su postavljeni na desnoj strani kontejnera, sa jednakim razmakom između njih.

#### --answer--

Komadi imaju jednak prostor između sebe i sa obe strane kontejnera.

### --question--

#### --text--

Koju funkcionalnost biste koristili da postavite komade vertikalno u sredinu kada `flex-direction: row;` je postavljeno?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

Šta znači vrednost `flex-start` za atribut `justify-content`?

#### --distractors--

Poravnajte delove na početku ose krsta.

---

Širi delove kako bi popunili glavnu osovinu kontejnera.

---

Svi delovi se spajaju/okupljaju u centru glavne ose.

#### --answer--

Poravnava delove na početku glavne osovine kontejnera.

### --question--

#### --text--

Kako biste započeli fleksibilni uređaj koji raspoređuje svoje delove od desna ka levo na jednoj liniji, i istovremeno ih stavlja na kontejner?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Koji je važeći dokument po osnovu `flex-flow`?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Koje su propisi za CSS koji će dovesti interchangable delove da se povežu ravnomerno u sredini i prošire vertikalno kako bi popunili dužinu kontejnera?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

`align-items: stretch;` koji uticaj ima kada `flex-direction` je postavljen na `column`?

#### --distractors--

Proširuje vertikalne delove kako bi popunio dužinu kontejnera.

---

Nema efekta kada je smer `column`.

---

Širi vertikalne delove tako da liče na duži komad.

#### --answer--

Proširuje delove ravnomerno kako bi popunio širinu kontejnera.

### --question--

#### --text--

Ukoliko želiš da promeniš redosled elemenata u jednakom nizu, koju izjavu bi koristio?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Koja je vrednost `align-items` koja će postaviti delove na kontejner sa `flex-direction` da bude `row`?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`