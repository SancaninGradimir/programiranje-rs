---
id: 5900f3b21000cf542c50fec5
title: 'Vežba 70: Permutacija totijenta'
challengeType: 1
forumTopicId: 302183
dashedName: problem-70-totient-permutation
---

# --description--

Ojlerova Totijent funkcija, ${\phi}(n)$ (ponekad nazvana phi funkcija), koristi se za određivanje broja pozitivnih brojeva manjih ili jednakih `n` koji su uzajamno prosti sa `n`. Na primer, pošto su 1, 2, 4, 5, 7 i 8 svi manji od devet i uzajamno prosti sa devet, ${\phi}(9) = 6$. Broj 1 se smatra uzajamno prostim sa svakim pozitivnim brojem, pa je ${\phi}(1) = 1$.

Zanimljivo je da je ${\phi}(87109) = 79180$, i da je 79180 permutacija broja 87109.

Pronađi vrednost `n`, 1 &lt; `n` &lt; `limit`, koja ${\phi}(n)$ je permutacija `n` i odnos $\displaystyle\frac{n}{{\phi}(n)}$ postižu najniži nivo.

# --hints--

`totientPermutation(10000)` Trebalo bi da vrati broj.

```js
assert(typeof totientPermutation(10000) === 'number');
```

`totientPermutation(10000)` treba da vrati `4435`.

```js
assert.strictEqual(totientPermutation(10000), 4435);
```

`totientPermutation(100000)` treba da vrati `75841`.

```js
assert.strictEqual(totientPermutation(100000), 75841);
```

`totientPermutation(500000)` trebalo bi da vrati `474883`.

```js
assert.strictEqual(totientPermutation(500000), 474883);
```

`totientPermutation(10000000)` trebalo bi da vrati `8319823`.

```js
assert.strictEqual(totientPermutation(10000000), 8319823);
```

# --seed--

## --seed-contents--

```js
function totientPermutation(limit) {

  return true;
}

totientPermutation(10000);
```

# --solutions--

```js
function totientPermutation(limit) {
  function getSievePrimes(max) {
    const primes = [];
    const primesMap = new Array(max).fill(true);
    primesMap[0] = false;
    primesMap[1] = false;

    for (let i = 2; i < max; i += 2) {
      if (primesMap[i]) {
        primes.push(i);
        for (let j = i * i; j < max; j += i) {
          primesMap[j] = false;
        }
      }
      if (i === 2) {
        i = 1;
      }
    }
    return primes;
  }

  function sortDigits(number) {
    return number.toString().split('').sort().join('');
  }

  function isPermutation(numberA, numberB) {
    return sortDigits(numberA) === sortDigits(numberB);
  }

  const MAX_PRIME = 4000;
  const primes = getSievePrimes(MAX_PRIME);

  let nValue = 1;
  let minRatio = Infinity;

  for (let i = 1; i < primes.length; i++) {
    for (let j = i + 1; j < primes.length; j++) {
      const num = primes[i] * primes[j];
      if (num > limit) {
        break;
      }

      const phi = (primes[i] - 1) * (primes[j] - 1);
      const ratio = num / phi;

      if (minRatio > ratio && isPermutation(num, phi)) {
        nValue = num;
        minRatio = ratio;
      }
    }
  }
  return nValue;
}
```
