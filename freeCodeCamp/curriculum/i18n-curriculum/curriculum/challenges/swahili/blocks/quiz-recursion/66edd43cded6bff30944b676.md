---
id: 66edd43cded6bff30944b676
title: Pokušao sam sa rekursijom
challengeType: 8
dashedName: quiz-recursion
---

# --description--

Da bi prošao kratki test, moraš tačno odgovoriti na najmanje 9 od 10 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je rekurzija u programiranju?

#### --distractors--

Način sortiranja redova podataka.

---

Beskonačni ciklus.

---

Funkcija koja възстановља `undefined`.

#### --answer--

Proces u kojem funkcija poziva samu sebe.

### --question--

#### --text--

Da li je nešto od navedenog primerak rekurzije?

#### --distractors--

```js
function factorial(n) {
  let result = 1;

  while (n > 0) {
    result = result * n;
    n--;
  }

  return result;
}
```

---

```js
function factorial(n) {
  const arr = Array(n).fill().map((_, i) => i + 1);
  return arr.reduce((acc, curr) => acc * curr, 1);
}
```

---

```js
function factorial(n) {
  let result = 1;

  for (let i = n; i > 0; i--) {
    result *= i;
  }

  return result;
}
```

#### --answer--

```js
function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  }
  return n * factorial(n - 1);
}
```

### --question--

#### --text--

Šta će sledeća funkcija vratiti?

```js
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);
}
sum(3);
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

Funkcija `mystery` Koliko će puta biti korišćena?

```javascript
function mystery(n) {
  if (n <= 1) return 1;
  return mystery(n - 2);
}
mystery(5);
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

Koje od sljedećih tvrdnji su tačne u vezi sa rekursijom?

#### --distractors--

Nikada ne bi trebalo da ima osnovni slučaj.

---

Trebalo bi da se koristi samo za Fibonacci niz.

---

Trebalo bi se koristiti samo za fabriku.

#### --answer--

Trebalo bi da ima osnovni slučaj uvek.

### --question--

#### --text--

Koja od sledećih opcija bi bila osnovni slučaj koji odgovara datoj ilustraciji/primeru?

```js
function countDownToZero(number) {
  // Base case goes here.

  console.log(number);
  countDownToZero(number - 1);
}
```

#### --distractors--

```js
if (number > 0) {
  return;
}
```

---

```js
if (number !== 0) {
  return;
}
```

---

```js
if (number === 0) {
  return;
}
```

#### --answer--

```js
if (number < 0) {
  return;
}
```

### --question--

#### --text--

Zašto rekurzija mora imati bazni slučaj?

#### --distractors--

Da bi se osiguralo da funkcija uvek ima vrednost return.

---

Da smanji broj poziva funkcija.

---

Kako bi se osiguralo da su svi rekurzivni pozivi izvršeni u ispravnom redosledu i da `RecursionError` nije izgubljen.

#### --answer--

Da bi se funkciji omogućilo da prekine svoje rekurzivne pozive i spreči beskonačne petlje.

### --question--

#### --text--

Šta će ova rekurzivna funkcija da uradi?

```js
function repeatString(str) {
  return str + repeatString(str);
}
```

#### --distractors--

Ponovi niz slova dva puta.

---

Vrati `undefined`.

---

Napravi niz praznih karaktera.

#### --answer--

Uzrokovati preliv steka.

### --question--

#### --text--

Šta je stakl (call stack)?

#### --distractors--

Lista izvršenih poziva funkcija.

---

Lista vrednosti koje je vratila funkcija.

---

Specijalna funkcija koja se koristi za pozivanje funkcije samo tri puta.

#### --answer--

Struktura podataka koja prati pozive funkcija i njen red izvršavanja.

### --question--

#### --text--

Šta radi ova rekurzivna funkcija?

```js
function changeString(str) {
  if (str === "") return "";
  return changeString(str.slice(1)) + str[0];
}
```

#### --distractors--

Kopiraj niz slova.

---

Ukloni vokale.

---

Ukloni prostor.

#### --answer--

Obrati redosled slova nazad.