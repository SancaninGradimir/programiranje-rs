---
id: 685cdfd1932d8ac6ad986813
title: Korak 28
challengeType: 20
dashedName: step-28
---

# --description--

Unutar puta `__str__`,će prikazati da li je e-mail pročitan ili ne.

Matematička funkcija uslova omogućava vam da postavite vrednost na osnovu uslova u jednoj liniji.

Evo primera:

```py
x = 10
y = 'Even' if x % 2 == 0 else 'Odd' # y will be Even
```

Unutar procesa, prethodno, koristite matematički izraz uslova za postavljanje sekvence slova `Read` u parametar `status` ako je e-mail pročitan i `Unread` ako nije pročitan.

# --hints--

Trebalo bi da imaš kriterijum `status` unutar puta `__str__`.

```js
({
    test: () => {
        assert(runPython(`_Node(_code).find_class("Email").find_function("__str__").has_variable("status")`));
    }
})
```

Treba da unesete matematički izraz uslova u parametar `status`.

```js
({
    test: () => {
        runPython(`
        import ast
        assert isinstance(_Node(_code).find_class("Email").find_function("__str__").find_variable("status").tree.value, ast.IfExp)`);
    }
})
```

Vaš izračunati status treba da se evaluira kao niz slova `Read` ako je e-mail pročitan i `Unread` ako nije pročitan.

```js
({
    test: () => {
        runPython(`
        if_exps = [
          "'Read' if self.read else 'Unread'",
          "'Read' if self.read == True else 'Unread'",
          "'Unread' if not self.read else 'Read'",
          "'Unread' if self.read == False else 'Read'"
        ]
        foo = _Node(_code).find_class("Email").find_function("__str__")
        assert any(foo.has_stmt(f"status = {if_exp}") for if_exp in if_exps)
        `);
    }
})
```

# --seed--

## --seed-contents--

```py
class Email:
    def __init__(self, sender, receiver, subject, body):
        self.sender = sender
        self.receiver = receiver
        self.subject = subject
        self.body = body
        self.read = False

    def mark_as_read(self):
        self.read = True

    def display_full_email(self):
        self.mark_as_read()
        print('\n--- Email ---')
        print(f'From: {self.sender.name}')
        print(f'To: {self.receiver.name}')
        print(f'Subject: {self.subject}')
        print(f'Body: {self.body}')
        print('------------\n')

--fcc-editable-region--
    def __str__(self):
        pass
--fcc-editable-region--

class User:
    def __init__(self, name):
        self.name = name
        self.inbox = Inbox()

    def send_email(self, receiver, subject, body):
        email = Email(sender=self, receiver=receiver, subject=subject, body=body)
        receiver.inbox.receive_email(email)

class Inbox:
    def __init__(self):
        self.emails = []
    
    def receive_email(self, email):
        self.emails.append(email)

```
