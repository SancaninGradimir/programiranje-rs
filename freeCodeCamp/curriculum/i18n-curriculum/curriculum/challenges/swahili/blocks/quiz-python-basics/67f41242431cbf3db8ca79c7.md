---
id: 67f41242431cbf3db8ca79c7
title: Kratak test osnova Pythona
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji funkcija od sledećih se koristi za proveru da li parametar odgovara određenom tipu podataka?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Zar nije među ovim način za spajanje sekvence slova?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Koji funkcija se koristi iz navedenih za vraćanje broja znakova u nizu karaktera?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

`result` Šta će biti u ovom primeru?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

Šta se dešava kada dodate decimalni broj i ceo broj?

#### --distractors--

Rezultat će biti poruka o grešci.

---

Rezultat će biti `None`.

---

Rezultat će biti ceo broj.

#### --answer--

Rezultat će biti decimalni broj.

### --question--

#### --text--

Da li je neki od ovih ispravan način za definisanje funkcije?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

Šta će biti ispisano na konzolu?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Koja od ovih informacija nije tačna u vezi sa navođenjem parametara?

#### --distractors--

Ime parametra može početi samo slovom ili donjom crtom (\_), a ne brojem.

---

Ime parametara može sadržati slova i brojeve (a-z, A-Z, 0-9) i samo donje crte (_).

---

Imena parametara ne mogu biti rezervne ključne riječi za Python kao što su `if`, `class`, ili `def`.

#### --answer--

Ime parametra mora imati tačno dužinu od 10 karaktera, inače Python će vratiti grešku.

### --question--

#### --text--

Koja od ovih nije podržani tip podataka u Python?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Koji od ovih će vratiti novu sekvencu slova sa svim velikim slovima?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Koja funkcija od sledećih se koristi za dobijanje unosa od korisnika?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Šta će biti rezultat ovog koda?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

Šta radi put `split()`?

#### --distractors--

Ova metoda se koristi za razdvajanje tuple-a u listu manjih elemenata.

---

Ovaj metod se koristi za razdvajanje decimalnog broja u listu manjih delova.

---

Ova metoda se koristi za razdvajanje rečnika u listu manjih segmenata.

#### --answer--

Ovaj metod se koristi za razdvajanje niza znakova u listu manjih segmenata.

### --question--

#### --text--

Šta se ispisuje na konzoli?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Koja funkcija među sljedećih se koristi za kreiranje tabele mape znakova 1 na 1 za prevođenje?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Šta će biti rezultat ovog koda?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

Šta će biti vraćeno metodom `find()` ako nema pojavljivanja poddelova u nizu karaktera?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Koja funkcija iz sledećih se koristi za brojanje koliko puta podnizak se pojavljuje u nizu karaktera?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Šta radi floor division u Python?

#### --distractors--

Ova funkcija se koristi za povećanje dva broja i za zaokruživanje rezultata na sledeći ceo broj.

---

Ova funkcija se koristi za konverziju decimalnog broja u ceo broj.

---

Ova funkcija se koristi za podizanje broja na neku drugu potęgu.

#### --answer--

Ova funkcija se koristi za deljenje dva broja i za zaokruživanje rezultata nadole na najbliži ceo broj.

### --question--

#### --text--

Koja funkcija među sledećim se koristi za zaokruživanje broja na najbliži ceo broj?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`