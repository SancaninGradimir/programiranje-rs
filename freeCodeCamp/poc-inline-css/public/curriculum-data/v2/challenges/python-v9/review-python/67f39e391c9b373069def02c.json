{
  "id": "67f39e391c9b373069def02c",
  "title": "Python pregled",
  "challengeType": 31,
  "dashedName": "review-python",
  "solutions": [],
  "assignments": [
    "<p>Proverite teme i koncepte za Python.</p>"
  ],
  "tests": [],
  "description": "<section id=\"description\">\n<h2>Python Šta je to?</h2>\n<ul>\n<li><strong>Uvod</strong>: Python je programski jezik opšte namene poznat po svojoj jednostavnosti i fleksibilnosti. Python se koristi u mnogim oblastima kao što su nauka o podacima i mašinsko učenje, razvoj veb aplikacija, skriptovanje i automatizacija, ugrađeni sistemi i IoT, i još mnogo toga.</li>\n<li><strong>Uobičajena primena</strong>: Python se koristi u nauci o podacima, mašinskom učenju, razvoju veb aplikacija, sajber bezbednosti, automatizaciji i mikroračunarima kao što su Raspberry Pi i ploče kompatibilne sa MicroPython.</li>\n</ul>\n<h2>Python u Vašem Unutrašnjem Okruženju</h2>\n<ul>\n<li><strong>Instalacija</strong>: Najbolji način za instaliranje Python na Windows, Mac i Linux je preuzimanje instalacionog paketa sa zvaničnog sajta Python (<code>https://www.python.org/</code>).</li>\n<li><strong>Terminal</strong>: Interfejs komandne linije koji vam omogućava da komunicirate sa svojim računarom upisivanjem komandi. Svaki operativni sistem dolazi sa osnovnim terminal programom. Na macOS, možete koristiti aplikaciju Terminal. Na Windows-u, možete koristiti Command Prompt ili PowerShell. Na Linux-u, svako okruženje radne površine ima svoj osnovni terminal kao što su GNOME Terminal ili Konsole.</li>\n<li><strong>IDE</strong>: IDE je skraćenica za Integrated Development Environment (Integrisovano razvojno okruženje) i ima funkcije poput editora koda, alata za debagovanje, terminala i više.</li>\n<li><strong>Popularni editori koda i IDE-ovi za Python</strong>: VS Code, PyCharm i Spyder</li>\n<li><strong>Pokretanje koda na vašem računaru</strong>: Da biste pokrenuli kod Python, imate nekoliko opcija. Prva opcija je kliknuti na dugme za pokretanje u VS Code koje izgleda kao dugme za reprodukciju (play) u gornjem desnom uglu. To će otvoriti terminal i izvršiti vašu skriptu Python tamo.</li>\n</ul>\n<p>Druga opcija je otvoriti terminal i pokrenuti program ručno. Evo primera pokretanja programa pod nazivom <code>main.py</code>:</p>\n<pre><code class=\"language-bash\">python main.py\n</code></pre>\n<p>Morate pokrenuti ovu komandu iz foldera gde je <code>main.py</code> sačuvan. Na primer, ako se <code>main.py</code> nalazi u folderu pod nazivom <code>python-projects</code>, prvo koristite sledeće u terminalu:</p>\n<pre><code class=\"language-bash\">cd python-projects\n</code></pre>\n<p>Zatim trči:</p>\n<pre><code class=\"language-bash\">python main.py\n</code></pre>\n<h2>Upotreba Python Interaktivnog Shell-a</h2>\n<ul>\n<li><strong>Opis</strong>: Interaktivna shell je program koji vam omogućava da ukucate komandu direktno i vidite rezultate. Otvorite terminalnu aplikaciju i ukucajte <code>python</code>, a zatim pritisnite <code>Enter</code>. Ovo će pokrenuti interaktivnu shell za Python. Znak <code>>>></code> pokazuje da Python čeka da ukucate komandu. Pokušajte da izbacite (ili: pokrenete) <code>\"Hello, world!\"</code> u terminalu:</li>\n</ul>\n<pre><code class=\"language-bash\">print(\"Hello, world!\")\n</code></pre>\n<p>Trebalo bi da vidite tekst koji izgleda ovako:</p>\n<pre><code class=\"language-bash\">>>> print(\"Hello, world!\")\nHello, world!\n</code></pre>\n<p>Nakon što se tekst objavi, Python vraća se na sledeće:</p>\n<pre><code class=\"language-bash\">>>>\n</code></pre>\n<p>Ovo znači da možete napisati još jednu komandu.</p>\n<ul>\n<li><strong>Ciklus čitanja, evaluacije, štampanja, ponavljanja (REPL)</strong>: Svaki put kada napišete komandu, interpreter će pročitati unos, izvršiti ga, ispisati rezultate i ponoviti prikaz <code>>>></code> ili nastavite sa pisanjem više komandi.</li>\n</ul>\n<h2>Kriterijumi</h2>\n<ul>\n<li><strong>Deklarisanje parametara</strong>: Da biste deklarisali parametar, počnite sa imenom parametra praćenim operatorom dodeljivanja (<code>=</code>) a zatim vrednošću. Ovo može biti broj, niz karaktera, vrednost tačno ili netačno, itd. Evo primera:</li>\n</ul>\n<pre><code class=\"language-py\">name = 'John Doe'\nage = 25\n</code></pre>\n<ul>\n<li>\n<p><strong>Protokoli za pozivanje parametara</strong>: Ovo su protokoli koje treba da koristite za parametre:</p>\n</li>\n<li>\n<p>Imena parametara mogu početi samo slovom ili donjom crtom (_), a ne brojem.</p>\n<ul>\n<li>Imena parametara mogu sadržati slova i brojeve (a-z, A-Z, 0-9) i donje crte (_).</li>\n<li>Imena parametara su osetljiva na veličinu slova — <code>age</code>, <code>Age</code> i <code>AGE</code> se smatraju različitim.</li>\n<li>Imena parametara ne mogu biti rezervne ključne reči za Python kao što su <code>if</code>, <code>class</code> ili <code>def</code>.</li>\n<li>Imena parametara sa više reči razdvajaju se donjim crtama. Primer: <code>snake_case</code>.</li>\n</ul>\n</li>\n</ul>\n<h2>Komentari</h2>\n<ul>\n<li><strong>Jedelinijski komentari</strong>: Ove vrste komentara treba da se koriste za kratke opise koje želite ostaviti u svom kodu.</li>\n</ul>\n<pre><code class=\"language-py\"># This is a single line comment\n</code></pre>\n<ul>\n<li><strong>Višelinijski niz znakova</strong>: Ovi tipovi niza znakova mogu se koristiti za ostavljanje velikih opisa ili komentisanje delova koda.</li>\n</ul>\n<pre><code class=\"language-py\">\"\"\"\nThis is a multi-line string.\nHere is some code commented out.\n\nname = 'John Doe'\nage = 25\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>print()</code></strong>: Da biste ispisali podatke na konzolu, možete koristiti funkciju <code>print()</code> kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">print('Hello world!') # Hello world!\n</code></pre>\n<h2>Tipovi standardnih podataka u Python</h2>\n<ul>\n<li><strong>Uvod</strong>: Python je jezik koji dinamički menja tipove podataka tokom izvršavanja, kao što je JavaScript, pa vam nije potrebno deklarisati tipove podataka za parametre. Jezik zna tip parametra na osnovu onoga što unesete.</li>\n<li><strong>Celobrojni broj</strong>: Broj bez decimalnih delova:</li>\n</ul>\n<pre><code class=\"language-py\">my_integer_var = 10\nprint('Integer:', my_integer_var) # Integer: 10\n</code></pre>\n<ul>\n<li><strong>Decimalni broj (Float)</strong>: Broj sa decimalnim delovima:</li>\n</ul>\n<pre><code class=\"language-py\">my_float_var = 4.50\nprint('Float:', my_float_var) # Float: 4.5\n</code></pre>\n<ul>\n<li><strong>Niz slova/karaktera (String)</strong>: Niz slova postavljen unutar navodnika:</li>\n</ul>\n<pre><code class=\"language-py\">my_string_var = 'hello'\nprint('String:', my_string_var) # String: hello\n</code></pre>\n<ul>\n<li><strong>Logička vrednost (Boolean)</strong>: Vrednost koja predstavlja <code>True</code> ili <code>False</code>:</li>\n</ul>\n<pre><code class=\"language-py\">my_boolean_var = True\nprint('Boolean:', my_boolean_var) # Boolean: True\n</code></pre>\n<ul>\n<li><strong>Skup</strong>: Neporedjena kolekcija jedinstvenih elemenata:</li>\n</ul>\n<pre><code class=\"language-py\">my_set_var = {7, 5, 8}\nprint('Set:', my_set_var) # Set: {7, 5, 8}\n</code></pre>\n<ul>\n<li><strong>Kamusi (Dictionary)</strong>: Kolekcija parova ključ-vrednost, unutar okruglih zagrada:</li>\n</ul>\n<pre><code class=\"language-py\">my_dictionary_var = {\"name\": \"Alice\", \"age\": 25}\nprint('Dictionary:', my_dictionary_var) # Dictionary: {'name': 'Alice', 'age': 25}\n</code></pre>\n<ul>\n<li><strong>Tuple</strong>: Kolekcija uređene i nepromenljive vrednosti, postavljena unutar standardnih zagrada:</li>\n</ul>\n<pre><code class=\"language-py\">my_tuple_var = (7, 5, 8)\nprint('Tuple:', my_tuple_var) # Tuple: (7, 5, 8)\n</code></pre>\n<ul>\n<li><strong>Range</strong>: Niz brojeva, često se koristi u petljama:</li>\n</ul>\n<pre><code class=\"language-py\">my_range_var = range(5)\nprint(my_range_var) # range(0, 5)\n</code></pre>\n<ul>\n<li><strong>Orodha (List)</strong>: Organizovana kolekcija elemenata koja podržava različite vrste podataka:</li>\n</ul>\n<pre><code class=\"language-py\">my_list = [22, 'Hello world', 3.14, True]\nprint(my_list) # [22, 'Hello world', 3.14, True]\n</code></pre>\n<ul>\n<li><strong>None</strong>: Specifična vrednost koja predstavlja odsustvo vrednosti:</li>\n</ul>\n<pre><code class=\"language-py\">my_none_var = None\nprint('None:', my_none_var) # None: None\n</code></pre>\n<h2>Nepromenljive i promenljive vrste</h2>\n<ul>\n<li><strong>Nepromenljive vrste</strong>: Ove vrste ne mogu se promeniti nakon deklarisanja, iako možete postaviti njene parametre da indikuju nešto novo, što se naziva ponovno definisanje (ili re-deklaracija). One uključuju integer, decimalni broj, kompleksan broj, boolean vrednost (istina ili laž), niz znakova, tuple, range, i <code>None</code>.</li>\n<li><strong>Promenljive vrste</strong>: Ove vrste mogu se promeniti nakon deklarisanja. Možete dodati, ukloniti ili modifikovati njihove elemente. One uključuju kolekivne tipove kao što su lista, set i rečnik.</li>\n<li><strong>Funkcija <code>type()</code></strong>: Da biste videli tip parametra, možete koristiti funkciju <code>type()</code> na sledeći način:</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'Hello there!'\nage = 21\n\nprint(type(greeting)) # &#x3C;class 'str'>\nprint(type(age)) # &#x3C;class 'int'>\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>isinstance()</code></strong>: Koristi se za proveru da li parametar odgovara određenom tipu podataka:</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'Hello world'\nname = 'John Doe'\n\nprint(isinstance(greeting, str)) # True\nprint(isinstance(name, int)) # False\n</code></pre>\n<h2>Rad sa sekvencama slova</h2>\n<ul>\n<li><strong>Opis</strong>: Kao što ste se setili iz JavaScript, niz slova je nepromenljiv, što znači da ga ne možete promeniti nakon kreiranja. U Python, možete koristiti pojedinačne ili dvostruke navodnike. Savetuje se da izaberete jedno pravilo i da ga sledite:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\ncity = \"Los Angeles\"\n</code></pre>\n<ul>\n<li><strong>Dobijanje karaktera iz sekvence karaktera</strong>: Karakteri se mogu dohvatiti iz niza pomoću notacije uglastih zagrada, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">my_str = 'Hello world'\n\nprint(my_str[0])  # H\nprint(my_str[6])  # w\n\nprint(my_str[-1])  # d\nprint(my_str[-2]) # l\n</code></pre>\n<ul>\n<li><strong>Escape znakovi u sekvenci</strong>: Možete koristiti povratnik (backslash) (<code>\\</code>) ako vaš niz sadrži navodnike kao što je sledeće:</li>\n</ul>\n<pre><code class=\"language-py\">msg = 'It\\'s a sunny day'\nquote = \"She said, \\\"Hello!\\\"\"\n</code></pre>\n<ul>\n<li><strong>Konkatenacija niza karaktera</strong>: Da biste spojili sekvencu karaktera, možete koristiti operator <code>+</code>, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\nprint('My name is ' + developer + '.') # My name is Jessica.\n</code></pre>\n<p>Drug način za povezivanje sekvence je korišćenje operatora <code>+=</code>. Ovo se koristi za spajanje i distribuciju u jednom koraku, kao što sledi:</p>\n<pre><code class=\"language-py\">greeting = 'My name is '\ndeveloper = 'Jessica.'\n\ngreeting += developer\nprint(greeting) # My name is Jessica.\n</code></pre>\n<ul>\n<li><strong><code>f-strings</code></strong>: Ovo je prečica za formatirane string literal-e. Omogućava vam da izvršite umetanje vrednosti i takođe spajanjem sa kratkom i čitljivom sintaksom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\ngreeting = f'My name is {developer}.'\nprint(greeting) # My name is Jessica.\n</code></pre>\n<ul>\n<li><strong>Ekstrakcija dela niza znakova (String slicing)</strong>: Ovo je kada želite da izvučete deo niza. Ovde je osnovna sintaksa:</li>\n</ul>\n<pre><code class=\"language-py\">str[start:stop:step]\n</code></pre>\n<p>Početna pozicija ukazuje model od kojeg se uzima deo. Krajnja pozicija je mesto gde deo treba da se završi, a ta pozicija nije inkluzna. Korak pokazuje rastanak za preuzimanje delova. Evo primera:</p>\n<pre><code class=\"language-py\">message = 'Python is fun!'\n\nprint(message[0:6])  # Python\nprint(message[7:])  # is fun!\nprint(message[::2])  # Pto sfn\n</code></pre>\n<ul>\n<li><strong>Dobijanje dužine niza znakova</strong>: Funkcija <code>len()</code> se koristi za vraćanje broja znakova u nizu:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(len(developer)) # 7\n</code></pre>\n<h2>Rad s operacijom od <code>in</code></h2>\n<ul>\n<li><strong>Funkcija za <code>in</code></strong>: Ovo vraća vrednost tačno/netačno koja pokazuje da li se slova ili karakteri nalaze unutar niza ili ne:</li>\n</ul>\n<pre><code class=\"language-py\">my_str = 'Hello world'\n\nprint('Hello' in my_str)  # True\nprint('hey' in my_str)    # False\nprint('hi' in my_str)    # False\nprint('e' in my_str)  # True\nprint('f' in my_str)  # False\n</code></pre>\n<h2>Standardni načini za nizove karaktera</h2>\n<ul>\n<li><strong><code>upper()</code></strong>: Ova sekvenca metoda vraća novu sekvencu sa svim velikim slovima:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(developer.upper()) # JESSICA\n</code></pre>\n<ul>\n<li><strong><code>lower()</code></strong>: Ovaj metod sekvence vraća novu sekvencu sa svim slovima u malim slovima:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(developer.lower()) # jessica\n</code></pre>\n<ul>\n<li><strong><code>strip()</code></strong>: Ova metoda vraća kopiju niza i uklanja određene početne i krajnje karaktere (ako nema argumenata, uklanja razmakove sa početka i kraja).</li>\n</ul>\n<pre><code class=\"language-py\">greeting = '  hello world  '\n\ntrimmed_my_str = greeting.strip()\nprint(trimmed_my_str)  # 'hello world'\n</code></pre>\n<ul>\n<li><strong><code>replace()</code></strong>: Ovo vraća novu sekvencu gde su svi događaji starog redosleda zamenjeni novim redosledom.</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'hello world'\n\nreplaced_my_str = greeting.replace('hello', 'hi')\nprint(replaced_my_str)  # 'hi world'\n</code></pre>\n<ul>\n<li><strong><code>split()</code></strong>: Koristi se za podelu sekvence u listu pomoću delimintera. Delimiter je niz koji opisuje mesto na kojem treba da se izvrši podela.</li>\n</ul>\n<pre><code class=\"language-py\">dashed_name = 'example-dashed-name'\n\nsplit_words = dashed_name.split('-')\nprint(split_words)  # ['example', 'dashed', 'name']\n</code></pre>\n<ul>\n<li><strong><code>join()</code></strong>: Koristi se za spajanje iterativnih elemenata u niz znakova pomoću separatora. Iterable je kolekcija elemenata po kojima se može iterirati, kao što su lista, niz znakova ili tupel.</li>\n</ul>\n<pre><code class=\"language-py\">example_list = ['example', 'dashed', 'name']\n\njoined_str = ' '.join(example_list)\nprint(joined_str)  # example dashed name\n</code></pre>\n<ul>\n<li><strong><code>startswith(prefix)</code></strong>: Ova funkcija niza vraća booleanski vrednost (tačno ili netačno) ako niz počinje sa definisanim prefiksom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.startswith('N')\nprint(result)  # True\n</code></pre>\n<ul>\n<li><strong><code>endswith(suffix)</code></strong>: Ova funkcija sekvence vraća vrednost tačno ili netačno ako sekvenca završava sa navedenim zadnjim karakterom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.endswith('N')\nprint(result)  # False\n</code></pre>\n<ul>\n<li><strong><code>find()</code></strong>: Ova putanja sekvence vraća instancu prvog događaja fragmenta sekvence. Ako nije pronađeno, vraća <code>-1</code>:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.find('N')\nprint(result)  # 0\n\ncity = 'Los Angeles'\nprint(city.find('New')) # -1\n</code></pre>\n<ul>\n<li><strong><code>count(substring)</code></strong>: Ova sekvencijalna metoda broji koliko puta se podsekvenca pojavljuje:</li>\n</ul>\n<pre><code class=\"language-py\">city = 'Los Angeles'\nprint(city.count('e')) # 2\n</code></pre>\n<ul>\n<li><strong><code>capitalize()</code></strong>: Ova metoda niza vraća novi niz sa velikim početnim slovom i ostalim malim:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.capitalize()) # Chocolate cake\n</code></pre>\n<ul>\n<li><strong><code>isupper()</code></strong>: Ova sekvenca metoda vraća <code>True</code> ako su svi znakovi velika slova i <code>False</code> inače:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.isupper()) # False\n</code></pre>\n<ul>\n<li><strong><code>islower()</code></strong>: Ova sekvenca putanja vraća <code>True</code> ako su svi znakovi mala slova i <code>False</code> inače:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.islower()) # True\n</code></pre>\n<ul>\n<li><strong><code>title()</code></strong>: Ova funkcija sekvence vraća novu sekvencu sa velikim početnim slovom svake reči:</li>\n</ul>\n<pre><code class=\"language-py\">city = 'los angeles'\nprint(city.title()) # Los Angeles\n</code></pre>\n<ul>\n<li><strong><code>maketrans()</code></strong>: Ovaj sekvencijalni metod se koristi za kreiranje tabele mape karaktera jedan na jedan radi prevoda. Često se koristi zajedno sa <code>translate()</code>, koja koristi tu tabelu na sekvenci i vraća prevedene rezultate.</li>\n</ul>\n<pre><code class=\"language-py\">trans_table = str.maketrans('abc', '123')\nprint(trans_table) # {97: 49, 98: 50, 99: 51}\n\nresult = 'abcabc'.translate(trans_table)\nprint(result)  # 123123\n</code></pre>\n<h2>Standardne operacije koje se koriste sa celim i decimalnim brojevima</h2>\n<ul>\n<li><strong>Osnovne matematičke operacije</strong>: U Python, možete izvesti osnovne matematičke operacije sa celim brojevima i decimalnim brojevima, uključujući sabiranje, oduzimanje, množenje i deljenje:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\nfloat_1 = 5.4\nfloat_2 = 12.0\n\n# Addition\n\nprint('Integer Addition:', int_1 + int_2) # Integer Addition: 68\nprint('Float Addition:', float_1 + float_2) # Float Addition: 17.4\n\n# Subtraction\n\nprint('Int Subtraction:', int_1 - int_2) # Int Subtraction: 44\nprint('Float Subtraction:',  float_2 - float_1) # Float Subtraction: 6.6\n\n# Multiplication\n\nprint('Int Multiplication:', int_1 * int_2) # Int Multiplication: 672\nprint('Float Multiplication:', float_2 * float_1) # Float Multiplication: 64.80000000000001\n\n# Division\n\nprint('Division:', int_1 / int_2) # Division: 4.666666666666667\nprint('Float Division:', float_2 / float_1) # Float Division: 2.222222222222222\n</code></pre>\n<p>Kada dodate decimalni broj i ceo broj, rezultat će biti konvertovan u decimalni broj, kao što sledi:</p>\n<pre><code class=\"language-py\">int_1 = 56\nfloat_1 = 5.4\n\nprint(int_1 + float_1) # 61.4\n</code></pre>\n<ul>\n<li><strong>Modulo operator (<code>%</code>)</strong>: Ovo vraća ostatak kada se jedan broj deli sa drugim brojem:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\n\nprint(int_1 % int_2) # 8\n</code></pre>\n<ul>\n<li><strong>Celobrojno deljenje (<code>//</code>)</strong>: Ovaj operator se koristi za deljenje dva broja i zaokruživanje rezultata na najbliži ceo broj.</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\n\nprint(int_1 // int_2) # 4\n</code></pre>\n<ul>\n<li><strong>Eksponent snage (<code>**</code>)</strong>: Ovaj eksponent se koristi za podizanje broja na drugu potenciju:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 4\nint_2 = 2\n\nprint(int_1 ** int_2) # 16\n</code></pre>\n<ul>\n<li><strong>Funkcija za <code>float()</code></strong>: Možete koristiti ovu funkciju da biste konvertovali ceo broj u decimalni broj.</li>\n</ul>\n<pre><code class=\"language-py\">num = 4\n\nprint(float(num)) # 4.0\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>int()</code></strong>: Možete koristiti ovu funkciju za konverziju decimalnog broja u ceo broj.</li>\n</ul>\n<pre><code class=\"language-py\">num = 4.0\n\nprint(int(num)) # 4\n</code></pre>\n<ul>\n<li><strong>Funkcija za <code>round()</code></strong>: Koristi se za zaokruživanje brojeva na najbliži ceo broj:</li>\n</ul>\n<pre><code class=\"language-py\">num_1 = 3.4\nnum_2 = 7.7\n\nprint(round(num_1)) # 3\nprint(round(num_2)) # 8\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>abs()</code></strong>: Koristi se za vraćanje pune vrednosti broja:</li>\n</ul>\n<pre><code class=\"language-py\">num = -13\n\nprint(abs(num)) # 13\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>pow()</code></strong>: Koristi se za podizanje broja na drugu stepenicu:</li>\n</ul>\n<pre><code class=\"language-py\">result = pow(2, 3) \nprint(result)  # 8\n</code></pre>\n<h2>Optimizovana distribucija</h2>\n<ul>\n<li><strong>Opis</strong>: Napredno dodeljivanje spaja binarnu operaciju i dodeljivanje u jednom koraku. Uzima varijablu, izvodi operaciju sa drugom vrednošću, i čuva rezultat nazad u tu varijablu.</li>\n</ul>\n<pre><code class=\"language-py\"># Addition assignment \nmy_var = 10\nmy_var += 5\n\nprint(my_var) # 15\n\n# Subtraction assignment\ncount = 14\ncount -= 3\n\nprint(count) # 11\n\n# Multiplication assignment \nproduct = 65\nproduct *= 7\n\nprint(product) # 455\n\n# Division assignment \nprice = 100\nprice /= 4\n\nprint(price) # 25.0\n\n# Floor Division assignment \ntotal_pages = 23\ntotal_pages //= 5\n\nprint(total_pages) # 4\n\n# Modulo assignment \nbits = 35\nbits %= 2\n\nprint(bits) # 1\n\n# Exponentiation assignment \npower = 2\npower **= 3\n\nprint(power) # 8\n</code></pre>\n<p>Postoje takođe i druge operacije sa poboljšanom alokacijom, poput onih za bitwise operacije. Obuhvataju <code>&#x26;=</code>, <code>^=</code>, <code>>>=</code>, i <code>&#x3C;&#x3C;=</code>.</p>\n<h2>Raditi sa uređajem</h2>\n<ul>\n<li><strong>Opis</strong>: Funkcija je deo koda koji se može ponovo koristiti, koji prima ulaz (argument) i vraća rezultate. Da biste pozvali funkciju, morate navesti ime funkcije praćeno standardnim zagradama:</li>\n</ul>\n<pre><code class=\"language-py\"># Defining a function\n\ndef get_sum(num_1, num_2):\n    return num_1 + num_2\n\nresult = get_sum(3, 4) # function call\nprint(result) # 7\n</code></pre>\n<p>Ako funkcija ne vraća vrednosti eksplicitno, vrednost za default od return je <code>None</code>:</p>\n<pre><code class=\"language-py\">def greet():\n    print('hello') \n\nresult = greet() # hello\nprint(result) # None\n</code></pre>\n<p>Takođe možete da pružite vrednosti za default za parametre kao što sledi:</p>\n<pre><code class=\"language-py\">def get_sum(num_1, num_2=2):\n    return num_1 + num_2\n\nresult = get_sum(3) \nprint(result) # 5\n</code></pre>\n<p>Ako pozovete funkciju bez ispravnog broja argumenata, dobićete <code>TypeError</code>:</p>\n<pre><code class=\"language-py\">def calculate_sum(a, b):\n    print(a + b)\n\ncalculate_sum()\n\n# TypeError: calculate_sum() missing 2 required positional arguments: 'a' and 'b'\n</code></pre>\n<h2>Poznate unutrašnje funkcije</h2>\n<ul>\n<li><strong>Funkcija za <code>input()</code></strong>: Ovo se koristi da zatraži od korisnika unos podataka:</li>\n</ul>\n<pre><code class=\"language-py\">name = input('What is your name?') # User types 'Kolade' and presses Enter  \nprint('Hello', name) # Hello Kolade\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>int()</code></strong>: Koristi se za konverziju brojeva, vrednosti tačno/netačno (ili boolean vrednost), ili sekvence brojeva u ceo broj:</li>\n</ul>\n<pre><code class=\"language-py\">print(int(3.14)) # 3\nprint(int('42')) # 42\nprint(int(True)) # 1\nprint(int(False)) # 0 \n</code></pre>\n<h2>Opseg u Python</h2>\n<ul>\n<li><strong>Lokalni opseg</strong>: Ovo je mesto gde se varijabla deklarše unutar funkcije ili klase i koja može biti dostupna samo unutar te specifične funkcije ili klase.</li>\n</ul>\n<pre><code class=\"language-py\">def my_func():\n    num = 10\n    print(num)\n</code></pre>\n<ul>\n<li><strong>Opseg (Scope)</strong>: Ovo je situacija u kojoj funkcija ugnježdena unutar druge funkcije može pristupiti parametrima spoljašnje/roditeljske funkcije.</li>\n</ul>\n<pre><code class=\"language-py\">def outer_func():\n    msg = 'Hello there!'\n    \n    def inner_func():\n        print(msg)\n    inner_func()\n\nprint(outer_func()) # Hello there!\n</code></pre>\n<ul>\n<li><strong>Globalni opseg</strong>: Ovo se odnosi na parametre deklarisane van funkcija ili klasa koji mogu biti dostupni iz bilo kog dela programa.</li>\n</ul>\n<pre><code class=\"language-py\">tax = 0.70 \n\ndef get_total(subtotal):\n    total = subtotal + (subtotal * tax)\n    return total\n\nprint(get_total(100))  # 170.0\n</code></pre>\n<ul>\n<li><strong>Ugrađeni interni opseg</strong>: Imena sačuvana u Python za prethodno deklarisane funkcije, module, ključne reči i entitete.</li>\n</ul>\n<pre><code class=\"language-py\">print(str(45)) # '45'\nprint(type(3.14)) # &#x3C;class 'float'>\nprint(isinstance(3, str)) # False\n</code></pre>\n<h2>Opereta za poređenje</h2>\n<ul>\n<li><strong>Sawa (<code>==</code>)</strong>: Ovo proverava da li su dve vrednosti jednake:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 == 4) # False\n</code></pre>\n<ul>\n<li><strong>Sio sawa (<code>!=</code>)</strong>: Ovo proverava da li su dve vrednosti različite:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 != 4) # True\n</code></pre>\n<ul>\n<li><strong>Najveći od svih (<code>></code>)</strong>: Ovo proverava da li je jedna vrednost veća od druge:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 > 4) # False\n</code></pre>\n<ul>\n<li><strong>Najmanja vrednost (<code>&#x3C;</code>)</strong>: Proverava da li je jedna vrednost manja od druge:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 &#x3C; 4) # True\n</code></pre>\n<ul>\n<li><strong>Veće ili jednako (<code>>=</code>)</strong>: Ovo proverava da li je jedna vrednost veća ili jednaka drugoj:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 >= 4) # False\n</code></pre>\n<ul>\n<li><strong>Manje ili jednako (<code>&#x3C;=</code>)</strong>: Ovo proverava da li je jedna vrednost manje ili jednaka drugoj:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 &#x3C;= 4) # True\n</code></pre>\n<h2>Rad sa <code>if</code>, <code>elif</code> i <code>else</code> Statements</h2>\n<ul>\n<li><strong><code>if</code> Statements</strong>: Ovo su uslovi koji se koriste za određivanje da li je nešto tačno ili netačno. Ako je uslov potvrđen kao <code>True</code>, onda će se taj deo koda izvršiti.</li>\n</ul>\n<pre><code class=\"language-py\">age = 18\n\nif age >= 18:\n    print('You are an adult') # You are an adult\n</code></pre>\n<ul>\n<li><strong><code>elif</code> Klauzula</strong>: Ovo su uslovi koji prate informaciju o <code>if</code>. Ako je uslov <code>elif</code> potvrđen da je <code>True</code>, onda će taj deo koda biti izvršen.</li>\n</ul>\n<pre><code class=\"language-py\">age = 16\n\nif age >= 18:\n    print('You are an adult')\nelif age >= 13:\n    print('You are a teenager')  # You are a teenager\n</code></pre>\n<ul>\n<li><strong><code>else</code> Klauzula</strong>: Ovo će biti izvršeno ako druge uslove ne potvrđuju da <code>True</code>.</li>\n</ul>\n<pre><code class=\"language-py\">age = 12\n\nif age >= 18:\n    print('You are an adult')\nelif age >= 13:\n    print('You are a teenager')\nelse:\n    print('You are a child')  # You are a child\n</code></pre>\n<p>Takođe možete koristiti informacije iz <code>if</code> koje su uključene unutra, kao što sledi:</p>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nif is_citizen:\n    if age >= 18:\n        print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<h2>Vrednosti istine i laži</h2>\n<ul>\n<li><strong>Opis</strong>: U Python, svaka vrednost ima inherentnu istinitost ili lažnost, ili unutrašnje osećanje da treba da se tretira kao <code>True</code> ili <code>False</code> u kontekstu logike. Većina vrednosti smatra se istinitim, tj., dokazano je da su <code>True</code> u kontekstu logike. Ostale su lažne, što je dokazano kao <code>False</code>. Evo primera lažnih vrednosti:</li>\n</ul>\n<pre><code class=\"language-md\">None\nFalse\nInteger 0\nFloat 0.0\nEmpty strings ''\n</code></pre>\n<p>Ostale vrednosti, kao što su brojevi različiti od nule i ne-prazan niz znakova, su tačne.</p>\n<h2>Rad sa funkcijom <code>bool()</code></h2>\n<ul>\n<li><strong>Opis</strong>: Ako želite proveriti da li je vrednost istinita ili lažna, možete koristiti ugrađenu funkciju <code>bool()</code>. Ona konvertuje vrednost na ekvivalent istinite ili lažne vrednosti i vraća <code>True</code> za istinite vrednosti i <code>False</code> za lažne vrednosti. Evo nekoliko primera:</li>\n</ul>\n<pre><code class=\"language-py\">print(bool(False)) # False\nprint(bool(0))  # False\nprint(bool('')) # False\n\nprint(bool(True)) # True\nprint(bool(1)) # True\nprint(bool('Hello')) # True\n</code></pre>\n<h2>Algoritam za vrednosti tačno i netačno i smanjenje ciklusa</h2>\n<ul>\n<li><strong>Opis</strong>: Ovo su specijalni operatori koji vam omogućavaju da spojite više izraza i kreirate složeniju logiku odlučivanja u vašem kodu. Postoji tri operatera za vrednosti istinito i lažno u Python: <code>and</code>, <code>or</code>, i <code>not</code>.</li>\n<li><strong>Operater <code>and</code></strong>: Ovaj operater uzima dva operanda i vraća prvi operand ako je lažan, inače vraća drugi operand. Oba operanda moraju biti istinita da bi izraz proizveo vrednost tačno.</li>\n</ul>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nprint(is_citizen and age) # 25\n</code></pre>\n<p>Možete takođe koristiti operaciju <code>and</code> na sledećim uslovima:</p>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nif is_citizen and age >= 18:\n    print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<ul>\n<li><strong>Operater za <code>or</code></strong>: Ovaj operator vraća prvi operand ako je tačan, inače vraća drugi operand. Izraz <code>or</code> ima vrednost istinu (true) ako je barem jedan operand tačan. Evo primera:</li>\n</ul>\n<pre><code class=\"language-py\">age = 19\nis_employed = False\n\nprint(age or is_employed) # 19\n</code></pre>\n<p>Kao što je slučaj sa operetom <code>and</code>, možete koristiti operetu <code>or</code> na uslovima kao što su sledeći:</p>\n<pre><code class=\"language-py\">age = 19\nis_student = True\n\nif age &#x3C; 18 or is_student:\n    print('You are eligible for a student discount') # You are eligible for a student discount\nelse:\n    print('You are not eligible for a student discount')\n</code></pre>\n<ul>\n<li><strong>Skraćivanje ciklusa</strong>: Operacije za <code>and</code> i <code>or</code> poznate su kao operacije skraćivanja ciklusa. Skraćivanje ciklusa znači da Python proverava vrednosti od levi ka desni i zaustavlja se čim detektuje konačan rezultat.</li>\n<li><strong>Operacija <code>not</code></strong>: Ova operacija uzima jedan operand i konvertuje njegovu tačnu ili netačnu vrednost. Konvertuje tačne vrednosti u <code>False</code>, a netačne vrednosti u <code>True</code>. Za razliku od prethodnih operacija, <code>not</code> uvek vraća <code>True</code> ili <code>False</code>. Evo primera:</li>\n</ul>\n<pre><code class=\"language-py\">print(not '') # True, because empty string is falsy\nprint(not 'Hello') # False, because non-empty string is truthy\nprint(not 0) # True, because 0 is falsy\nprint(not 1) # False, because 1 is truthy\nprint(not False) # True, because False is falsy\nprint(not True) # False, because True is truthy\n</code></pre>\n<p>Ovde je primer operete <code>not</code> u uslovu:</p>\n<pre><code class=\"language-py\">is_admin = False\n\nif not is_admin:\n    print('Access denied for non-administrators.') # Access denied for non-administrators.\nelse:\n    print('Welcome, Administrator!')\n</code></pre>\n<h2>Alati za Python</h2>\n<ul>\n<li><strong>Uvod</strong>: U Python, tip podataka liste je organizovan red elemenata koji mogu biti niz slova, brojeva ili čak drugih lista. Lista je dinamična i ima indeksiranje koje počinje od nule.</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\n</code></pre>\n<ul>\n<li><strong>Pristup elementima iz liste</strong>: Da biste dobili element iz liste <code>cities</code>, možete se osvrnuti na indeks broj u sekvenci:</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\ncities[0] # Los Angeles\n</code></pre>\n<ul>\n<li><strong>Dohvatanje elemenata korišćenjem negativnog selektora</strong>: Da biste dobili poslednji element bilo koje liste, možete koristiti <code>-1</code> kao identifikator selektora:</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\ncities[-1] # Tokyo\n</code></pre>\n<ul>\n<li>\n<p>Negativni selektor se koristi za dobijanje elemenata počevši od kraja liste umesto od početka, za selektor <code>0</code>.</p>\n</li>\n<li>\n<p><strong>Kreiranje liste korišćenjem komponente <code>list()</code></strong>: Liste se takođe mogu kreirati korišćenjem komponente <code>list()</code>. Komponenta <code>list()</code> koristi se za konverziju iterabilnog objekta u listu:</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">developer = 'Jessica'\n\nprint(list(developer)) \n# Result: ['J', 'e', 's', 's', 'i', 'c', 'a']\n</code></pre>\n<ul>\n<li><strong>Dobijanje dužine liste</strong>: Možete koristiti funkciju <code>len()</code> da dobijete dužinu liste:</li>\n</ul>\n<pre><code class=\"language-python\">numbers = [1, 2, 3, 4, 5]\nlen(numbers) # 5\n</code></pre>\n<ul>\n<li><strong>Modifikacija liste</strong>: Lista je dinamična, što znači da možete ispraviti bilo koji element unutar liste samo unosivanjem validnog identifikatora. Da biste modifikovali listu za određeni identifikator, možete postaviti novu vrednost za taj identifikator:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\nprogramming_languages[0] = 'JavaScript'\nprint(programming_languages) # ['JavaScript', 'Java', 'C++', 'Rust']\n</code></pre>\n<ul>\n<li><strong>Greška parametra izvan opsega</strong>: Ako postavite parametar (sa pozitivnom ili negativnom vrednošću) koji je van granica liste, onda ćete dobiti <code>IndexError</code>:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\nprogramming_languages[10] = 'JavaScript'\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nIndexError: list assignment index out of range\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Uklanjanje stavki iz liste</strong>: Stavke se mogu ukloniti sa liste korišćenjem ključne reči <code>del</code>:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Jane Doe', 23, 'Python Developer']\ndel developer[1]\nprint(developer) # ['Jane Doe', 'Python Developer']\n</code></pre>\n<ul>\n<li><strong>Proveriti da li je element u spisku</strong>: Glavna reč <code>in</code> se može koristiti za proveru da li je element u spisku:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\n\n'Rust' in programming_languages # True\n'JavaScript' in programming_languages # False\n</code></pre>\n<ul>\n<li><strong>Ugnježđivanje lista</strong>: Liste se mogu organizovati unutar druge liste:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\n</code></pre>\n<ul>\n<li>Da biste dobili interni spisak, moraćete da ga dobijete koristeći <code>2</code> jer liste imaju indeks koji počinje od nule.</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\ndeveloper[2] # ['Python', 'Rust', 'C++']\n</code></pre>\n<ul>\n<li>Da biste dobili drugi jezik iz ugrađene liste, moraćete da ga dobijete koristeći šablon <code>1</code>:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\ndeveloper[2][1] # Rust\n</code></pre>\n<ul>\n<li><strong>Izdvajanje vrednosti iz niza (Unpacking)</strong>: Izdvajanje vrednosti iz niza je metoda koja se koristi za postavljanje vrednosti iz niza u nove parametre. Evo primera izdvajanja niza <code>developer</code> u nove parametre nazvane <code>name</code>, <code>age</code> i <code>job</code>, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ['Alice', 34, 'Rust Developer']\nname, age, job = developer\n</code></pre>\n<ul>\n<li>\n<p>Ako broj parametara na levoj strani operatera dodeljivanja ne odgovara broju elemenata u listi, dobićete <code>ValueError</code>.</p>\n</li>\n<li>\n<p><strong>Sakupljanje preostalih stavki sa liste</strong>: Da biste sakupli sve preostale stavke sa liste, možete koristiti operater zvezdice (<code>*</code>) kao što sledi:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">developer = ['Alice', 34, 'Rust Developer']\nname, *rest = developer\n</code></pre>\n<ul>\n<li><strong>Izrezivanje dela liste (Slicing)</strong>: Slicanje je koncept dobijanja dela liste korišćenjem operatera za slajsanje <code>:</code>. Da biste izrezali deo liste počevši od elementa <code>1</code> i završavajući pre elementa <code>3</code>, možete koristiti sledeću sintaksu:</li>\n</ul>\n<pre><code class=\"language-python\">desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']\ndesserts[1:3] # ['Cookies', 'Ice Cream']\n</code></pre>\n<ul>\n<li><strong>Korakni interval</strong>: Takođe je moguće definisati korakni interval koji određuje koliko se povećava između parametara. Evo primera ako želite da generišete listu brojeva čak i koristeći preuzimanje delova:</li>\n</ul>\n<pre><code class=\"language-python\">numbers = [1, 2, 3, 4, 5, 6]\nnumbers[1::2] # [2, 4, 6]\n</code></pre>\n<h2>Načini za listu</h2>\n<ul>\n<li><strong>append()</strong>: Koristi se za dodavanje elementa na kraj liste. Evo primera korišćenja metode <code>append()</code> za dodavanje broja <code>6</code> u listu <code>numbers</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<ul>\n<li><strong>Dodavanje liste</strong>: Metoda <code>append()</code> se takođe može koristiti za dodavanje jedne liste na kraj druge:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.append(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, [6, 8, 10]]\n</code></pre>\n<ul>\n<li><strong>extend()</strong>: Koristi se za dodavanje više stavki na kraj liste. Evo primera dodavanja brojeva <code>6</code>, <code>8</code>, i <code>10</code> na kraj liste <code>numbers</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.extend(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, 6, 8, 10]\n</code></pre>\n<ul>\n<li><strong>insert()</strong>: Koristi se za umetanje elementa u specifičan model liste. Evo primera korišćenja metode <code>insert()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insert(2, 2.5)\n\nprint(numbers) # [1, 2, 2.5, 3, 4, 5]\n</code></pre>\n<ul>\n<li><strong>remove()</strong>: Koristi se za uklanjanje elementa iz liste. Metoda <code>remove()</code> će ukloniti samo prvi primerak elementa unutar liste:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5, 5, 5]\nnumbers.remove(5)\n\nprint(numbers) # [1, 2, 3, 4, 5, 5]\n\n</code></pre>\n<ul>\n<li><strong>pop()</strong>: Koristi se za uklanjanje specifičnog elementa iz liste i vraćanje tog elementa:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop(1) # The number 2 is returned\n</code></pre>\n<ul>\n<li>Ako ne opišete funkciju pomoću <code>pop</code>, onda će biti uklonjena poslednja funkcija.</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop() # The number 5 is returned\n</code></pre>\n<ul>\n<li><strong>clear()</strong>: Koristi se za uklanjanje svih elemenata sa liste:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.clear()\n\nprint(numbers) # []\n</code></pre>\n<ul>\n<li><strong>sort()</strong>: Metoda za <code>sort()</code> se koristi za sortiranje elemenata na mestu. Evo primera sortiranja nasumične liste <code>numbers</code> na mestu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nnumbers.sort()\n\nprint(numbers) # [1, 2, 19, 35, 41, 67]\n</code></pre>\n<ul>\n<li><strong>sorted()</strong>: Ugrađena funkcija koja vraća novu sortiranu listu umesto da modifikuje originalnu listu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nsorted_numbers = sorted(numbers)\n\nprint(sorted_numbers) # [1, 2, 19, 35, 41, 67]\nprint(numbers) # [19, 2, 35, 1, 67, 41]\n</code></pre>\n<ul>\n<li><strong>reverse()</strong>: Koristi se za menjanje redosleda elemenata u nizu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [6, 5, 4, 3, 2, 1]\nnumbers.reverse()\n\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<ul>\n<li><strong>index()</strong>: Koristi se za dobijanje indeksa prvog elementa u nizu:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\nprogramming_languages.index('Java') # 1\n</code></pre>\n<ul>\n<li>Ako element ne može biti dostupan putem <code>index()</code>, onda će rezultat biti <code>ValueError</code>.</li>\n</ul>\n<h2>Tuple u Python</h2>\n<ul>\n<li><strong>Opis</strong>: Tuple je tip podataka Python koji se koristi za kreiranje usrednice uređena vrednosti. Tuple mogu imati mešoviti tip podataka:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\n</code></pre>\n<ul>\n<li>Tupel je nemenjiv, što znači da se elementi unutar tupela ne mogu promeniti nakon kreiranja. Ako pokušate da modifikujete element unutar tupela, dobićete <code>TypeError</code>:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Python', 'Java', 'C++', 'Rust')\nprogramming_languages[0] = 'JavaScript'\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nTypeError: \"tuple\" object does not support item assignment\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Dobijanje elemenata iz tupaola</strong>: Da biste dobili element iz tupaola, koristite notaciju zagrada i indeks:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\ndeveloper[1] # 34\n</code></pre>\n<ul>\n<li>Negativni indeks se može koristiti za dobijanje elemenata počevši od kraja tupela:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (1, 2, 3, 4, 5)\nnumbers[-2] # 4\n</code></pre>\n<ul>\n<li>Ako pokušate da postavite vrednost koja je veća ili jednaka dužini tupa, dobićete <code>IndexError</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (1, 2, 3, 4, 5)\nnumbers[7]\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nIndexError: tuple index out of range\n\"\"\"\n</code></pre>\n<ul>\n<li>Tuple se takođe može kreirati korišćenjem komponente <code>tuple()</code>. Unutar komponente, možete postaviti različite iterabilne objekte kao što su niz karaktera, lista i čak drugi tuple.</li>\n</ul>\n<pre><code class=\"language-python\">developer = 'Jessica'\n\nprint(tuple(developer)) \n# Result: ('J', 'e', 's', 's', 'i', 'c', 'a')\n</code></pre>\n<ul>\n<li><strong>Provera elemenata u tuplu</strong>: Da biste proverili da li je element u tuplu, možete koristiti ključnu reč <code>in</code> na sledeći način:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Python', 'Java', 'C++', 'Rust')\n\n'Rust' in programming_languages # True\n'JavaScript' in programming_languages # False\n</code></pre>\n<ul>\n<li><strong>Izvlačenje elemenata iz tupola</strong>: Elementi se mogu izvlačiti iz tupola na sledeći način:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\nname, age, job = developer\n</code></pre>\n<ul>\n<li>Ako trebate sakupiti preostale elemente iz tupaola, možete koristiti zvezdasti operator (<code>*</code>) kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\nname, *rest = developer\n</code></pre>\n<ul>\n<li><strong>Izvlačenje dela tupa</strong>: Izvlačenje se može koristiti za izvlačenje dela tupa. Na primer, stvari <code>pie</code> i <code>cookies</code> mogu se smatrati različitim tupelom:</li>\n</ul>\n<pre><code class=\"language-py\">desserts = ('cake', 'pie', 'cookies', 'ice cream')\ndesserts[1:3] # ('pie', 'cookies')\n</code></pre>\n<ul>\n<li><strong>Brisanje elemenata iz tupa</strong>: Brisanje elementa iz tupa će dodati <code>TypeError</code> jer su tupi nepromenljivi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Jane Doe', 23, 'Python Developer')\ndel developer[1]\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nTypeError: \"tuple\" object doesn't support item deletion\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Kada koristiti tuple umesto liste?</strong>: Ako vam je potreban kolekcija promenljivih elemenata gde možete dodati, ukloniti i izmeniti elemente, onda koristite listu. Ako znate da radite sa fiksnom i nepromenljivom kolekcijom podataka, onda koristite tuple.</li>\n</ul>\n<h2>Uobičajene metode za tuple</h2>\n<ul>\n<li><strong><code>count()</code></strong>: Koristi se za određivanje koliko puta je element prisutan u tuplu. Na primer, možete proveriti koliko puta je jezik <code>'Rust'</code> prisutan u tuplu:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.count('Rust') # 2\n</code></pre>\n<ul>\n<li>Ako element pomenut u putanji <code>count()</code> potpuno nije prisutan u tuple, onda vrednost return će biti <code>0</code>:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.count('JavaScript') # 0\n</code></pre>\n<ul>\n<li>\n<p>Ako nisu prosleđeni argumenti putem <code>count()</code>, onda Python će vratiti <code>TypeError</code>.</p>\n</li>\n<li>\n<p><strong>index()</strong>: Koristi se za pronalaženje indeksa gde se određeni element nalazi u tuplu. Evo primera korišćenja metode <code>index()</code> za dobijanje indeksa jezika <code>'Java'</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.index('Java') # 1\n</code></pre>\n<ul>\n<li>\n<p>Ako navedena funkcija nije dostupna, onda Python će vratiti grešku od <code>ValueError</code>.</p>\n</li>\n<li>\n<p>Takođe možete proslediti početni obrazac putem <code>index()</code> da biste specificirali gde početi pretraživati element u tuplu:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\nprogramming_languages.index('Python', 3) # 5\n</code></pre>\n<ul>\n<li>Takođe možete proslediti poslednji model putem <code>index()</code> kako biste specificirali gde prestati pretraživati element u tuplu:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python', 'JavaScript', 'Python')\nprogramming_languages.index('Python', 2, 5) # 2\n</code></pre>\n<ul>\n<li><strong><code>sorted()</code></strong>: Koristi se za sortiranje elemenata u bilo kom iterabilnom objektu i vraća novu, sortiranu listu. Evo primera kreiranja nove liste brojeva korišćenjem funkcije <code>sorted()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (13, 2, 78, 3, 45, 67, 18, 7)\nsorted(numbers) # [2, 3, 7, 13, 18, 45, 67, 78]\n</code></pre>\n<ul>\n<li><strong>Promena ponašanja sortiranja</strong>: Možete prilagoditi ponašanje sortiranja za <em>iterable</em> koristeći opcione argumente <code>key</code> i <code>reverse</code>. Evo primera korišćenja argumenta <code>key</code> za sortiranje elemenata u <em>tuple</em>-u prema dužini:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\nsorted(programming_languages, key=len)\n\n# Result\n# ['C++', 'Rust', 'Java', 'Rust', 'Python', 'Python']\n</code></pre>\n<ul>\n<li>Takođe možete kreirati novu listu vrednosti u obrnutom redosledu, koristeći argument <code>reverse</code> kao što je ovo:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\n\nprint(sorted(programming_languages, reverse=True))\n\n# Result\n# ['Rust', 'Rust', 'Python', 'Python', 'Java', 'C++']\n</code></pre>\n<h2>Krugovi u Python</h2>\n<ul>\n<li>\n<p><strong>Definicija</strong>: Petlje se koriste za ponavljanje dela koda određeni broj puta.</p>\n</li>\n<li>\n<p><strong><code>for</code> loop</strong>: Koristi se za iteriranje preko sekvence (kao što su lista, tuple ili niz znakova) i izvršavanje dela koda za svaki element u toj sekvenci. Evo primera korišćenja <code>for</code> loop-a da bi se iterirao preko liste i ispisao svaki jezik na konzolu:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\n\nfor language in programming_languages:\n    print(language)\n\n\"\"\"\nResult \n\nRust\nJava\nPython\nC++\n\"\"\"\n</code></pre>\n<ul>\n<li>Ovo je primer korišćenja <code>for</code> loop za ponavljanje sekvence karaktera <code>code</code> i štampanje svakog karaktera:</li>\n</ul>\n<pre><code class=\"language-py\">for char in 'code':\n    print(char)\n\n\"\"\"\nResult \n\nc\no\nd\ne\n\"\"\"\n</code></pre>\n<ul>\n<li>Petlje za <code>for</code> mogu se postaviti unutar drugih petlji. Evo primera korišćenja <code>for</code> petlje ugnježdene unutra:</li>\n</ul>\n<pre><code class=\"language-py\">categories = ['Fruit', 'Vegetable']\nfoods = ['Apple', 'Carrot', 'Banana']\n\nfor category in categories:\n    for food in foods:\n        print(category, food)\n\n\"\"\"\nResult\n\nFruit Apple\nFruit Carrot\nFruit Banana\nVegetable Apple\nVegetable Carrot\nVegetable Banana\n\"\"\"\n\n</code></pre>\n<ul>\n<li><strong><code>while</code> petlja</strong>: Ponavlja deo koda dok ne bude zadovoljen određeni uslov <code>False</code>. Evo primera korišćenja <code>while</code> petlje za igru na sreću:</li>\n</ul>\n<pre><code class=\"language-py\">secret_number = 3\nguess = 0\n\nwhile guess != secret_number:\n    guess = int(input('Guess the number (1-5): '))\n    if guess != secret_number:\n        print('Wrong! Try again.')\n\nprint('You got it!')\n\n\"\"\"\nResult\n\nGuess the number (1-5): 2\nWrong! Try again.\nGuess the number (1-5): 1\nWrong! Try again.\nGuess the number (1-5): 3\nYou got it!\n\"\"\"\n\n</code></pre>\n<ul>\n<li>\n<p><strong>Instrukcije za <code>break</code> na <code>continue</code></strong>: Koriste se u kola za promenu izvršavanja kola.</p>\n</li>\n<li>\n<p>Deklaracija <code>break</code> izvršava se jednom po ciklusu kada je uslov zadovoljen. Evo primera korišćenja deklaracije <code>break</code> za listu <code>developer_names</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">developer_names = ['Jess', 'Naomi', 'Tom']\n\nfor developer in developer_names:\n    if developer == 'Naomi':\n        break\n    print(developer)\n</code></pre>\n<ul>\n<li>Izjava <code>continue</code> se koristi za preskakanje trenutne ponavljanja i nastavak sa sledećom ponavljanjem u ciklusu. Evo primera korišćenja izjave <code>continue</code> umesto izjave <code>break</code>:</li>\n</ul>\n<pre><code class=\"language-py\">developer_names = ['Jess', 'Naomi', 'Tom']\n\nfor developer in developer_names:\n    if developer == 'Naomi':\n        continue\n    print(developer)\n</code></pre>\n<ul>\n<li>Ciklusi <code>for</code> i <code>while</code> mogu biti povezani sa klauzulom <code>else</code>, koja se izvršava samo kada ciklus ne završi sa izjavom <code>break</code>:</li>\n</ul>\n<pre><code class=\"language-python\">words = ['sky', 'apple', 'rhythm', 'fly', 'orange']\n\nfor word in words:\n    for letter in word:\n        if letter.lower() in 'aeiou':\n            print(f\"'{word}' contains the vowel '{letter}'\")\n            break\n    else:\n        print(f\"'{word}' has no vowels\")\n</code></pre>\n<h2>Upotreba raspona/opsega u krugovima</h2>\n<ul>\n<li><strong>Функција за <code>range()</code></strong>: Koristi se za kreiranje sekvence punih brojeva.</li>\n</ul>\n<pre><code class=\"language-py\">range(start, stop, step)\n</code></pre>\n<ul>\n<li>Važan parametar za <code>stop</code> je kompletan broj (nije uključen) koji predstavlja kraj generisanog brojačkog rednaka. Evo primera korišćenja funkcije <code>range()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(3):\n    print(num)\n</code></pre>\n<ul>\n<li>Ako argument <code>start</code> nije dostupan, podrazumevana opcija je <code>0</code>. Za serijski niz punih brojeva, povećanje će biti dodato sa <code>1</code>. Možete koristiti opcioni argument <code>step</code> da promenite vrednost inkrementa. Evo primera kreiranja serije punih brojeva čak i od 2 do 10 (bez uključivanja 11):</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(2, 11, 2):\n    print(num)\n</code></pre>\n<ul>\n<li>\n<p>Ako pružite bilo koji argument funkciji za <code>range()</code>, onda ćete dobiti <code>TypeError</code>.</p>\n</li>\n<li>\n<p>Funkcija <code>range()</code> prihvata samo cele brojeve za argument, a ne decimalne brojeve. Korišćenje decimalnih brojeva će izazvati grešku tipa <code>TypeError</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">ERROR!\nTraceback (most recent call last):\n  File \"&#x3C;main.py>\", line 1, in &#x3C;module>\nTypeError: 'float' object cannot be interpreted as an integer\n</code></pre>\n<ul>\n<li>Možete koristiti negativan ceo broj za argument <code>step</code> da kreirate opadajuću sekvencu celih brojeva:</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(40, 0, -10):\n    print(num)\n</code></pre>\n<ul>\n<li>Funkcija <code>list</code> se takođe može koristiti za kreiranje liste celih brojeva koristeći <code>range()</code> kao komponentu. Komponenta <code>list</code> se koristi za konverziju iterabilnog objekta u listu. Evo primera za kreiranje liste celih brojeva, čak i između 2 i 10 (uključujući):</li>\n</ul>\n<pre><code class=\"language-py\">numbers = list(range(2, 11, 2))\nprint(numbers) # [2, 4, 6, 8, 10]\n</code></pre>\n<h2>Funkcija <code>enumerate()</code> i <code>zip()</code> u Python</h2>\n<ul>\n<li><strong><code>enumerate()</code></strong>: Koristi se za iteriranje kroz sekvencu i praćenje indeksa svakog elementa u toj sekvenci. Funkcija <code>enumerate()</code> koja uzima <em>iterable</em> kao argument i vraća objekat <code>enumerate</code> koji uključuje indeks i vrednost svakog elementa u <em>iterable</em>-u.</li>\n</ul>\n<pre><code class=\"language-python\">languages = ['Spanish', 'English', 'Russian', 'Chinese']\n\nfor index, language in enumerate(languages):\n    print(f'Index {index} and language {language}')\n\n# Result\n# Index 0 and language Spanish\n# Index 1 and language English\n# Index 2 and language Russian\n# Index 3 and language Chinese\n\n</code></pre>\n<ul>\n<li>Funkcija <code>enumerate()</code> se takođe može koristiti van opsega <code>for</code>:</li>\n</ul>\n<pre><code class=\"language-python\">languages = ['Spanish', 'English', 'Russian', 'Chinese']\n\nprint(list(enumerate(languages)))\n# [(0, 'Spanish'), (1, 'English'), (2, 'Russian'), (3, 'Chinese')]\n</code></pre>\n<ul>\n<li>\n<p>Funkcija <code>enumerate()</code> prima takođe opcionalni argument <code>start</code> koji određuje početnu vrednost za brojanje. Ako ovaj argument nije dostavljen, brojanje počinje na <code>0</code>.</p>\n</li>\n<li>\n<p><strong><code>zip()</code></strong>: Koristi se za iteriranje preko više iterabilnih objekata istovremeno. Evo primera korišćenja funkcije <code>zip()</code> za iteriranje preko liste <code>developers</code> i <code>ids</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">developers = ['Naomi', 'Dario', 'Jessica', 'Tom']\nids = [1, 2, 3, 4]\n\nfor name, id in zip(developers, ids):\n    print(f'Name: {name}')\n    print(f'ID: {id}')\n\n\n\"\"\"\nResult\n\nName: Naomi\nID: 1\nName: Dario\nID: 2\nName: Jessica\nID: 3\nName: Tom\nID: 4\n\"\"\"\n\n</code></pre>\n<h2>List comprehensions u Python</h2>\n<ul>\n<li><strong>Definicija</strong>: List comprehension vam omogućava da kreirate novu listu u jednoj liniji spajanjem petlje i uslova direktno unutar uglastih zagrada. Ovo čini kod kraćim i često lakšim za čitanje.</li>\n</ul>\n<pre><code class=\"language-py\">even_numbers = [num for num in range(21) if num % 2 == 0]\nprint(even_numbers)\n</code></pre>\n<h2>Iterabilna funkcija</h2>\n<ul>\n<li><strong><code>filter()</code></strong>: Koristi se za filtriranje elemenata iz iterabljive kolekcije po određenim uslovima. Vraća iterator koji sadrži samo elemente koji zadovoljavaju uslov. Evo primera kreiranja nove liste reči koje su duže od četiri slova:</li>\n</ul>\n<pre><code class=\"language-py\">words = ['tree', 'sky', 'mountain', 'river', 'cloud', 'sun']\n\ndef is_long_word(word):\n    return len(word) > 4\n\nlong_words = list(filter(is_long_word, words))\nprint(long_words) # ['mountain', 'river', 'cloud']\n</code></pre>\n<ul>\n<li><strong><code>map()</code></strong>: Koristi se za primenu funkcije na svakom elementu u iterabilnom objektu i vraćanje novog iterabilnog objekta sa rezultatima. Evo primera korišćenja funkcije <code>map()</code> za konverziju liste temperatura iz Celzija u Farenhajta:</li>\n</ul>\n<pre><code class=\"language-py\">celsius = [0, 10, 20, 30, 40]\n\ndef to_fahrenheit(temp):\n    return (temp * 9/5) + 32\n\nfahrenheit = list(map(to_fahrenheit, celsius))\nprint(fahrenheit) # [32.0, 50.0, 68.0, 86.0, 104.0]\n</code></pre>\n<ul>\n<li><strong><code>sum()</code></strong>: Koristi se za dobijanje zbira iz iterabilnog objekta kao što su lista ili tuple. Evo primera korišćenja funkcije <code>sum()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers)\nprint(total) # Result: 50\n</code></pre>\n<ul>\n<li>Možete takođe proslediti opcionalni argument <code>start</code> koji postavlja početnu ukupnu vrednost. Evo ažuriranog primera koji koristi argument <code>start</code> kao pozicionalni argument:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers, 10) # positional argument\nprint(total) # 60\n</code></pre>\n<ul>\n<li>Takođe možete koristiti argument <code>start</code> kao tekstualni argument umesto ovoga:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers, start=10) # keyword argument\nprint(total) # 60\n</code></pre>\n<h2>Lambda funkcija</h2>\n<ul>\n<li><strong>Definicija</strong>: Lambda funkcija u Python je kratak način za kreiranje anonimne funkcije (anonimna funkcija).</li>\n<li>Lambda funkcija se često koristi kao argument za drugu funkciju. Evo primera lambda funkcije:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\n\neven_numbers = list(filter(lambda x: x % 2 == 0, numbers))\nprint(even_numbers)  # [2, 4]\n</code></pre>\n<ul>\n<li>Najbolje prakse za korišćenje lambda funkcije su da je ne prosleđujete kao argument, već da ih održavate jednostavnim i razumljivim, te da ih koristite samo jednom za kratke funkcije.</li>\n</ul>\n<h2>Rečnik</h2>\n<ul>\n<li><strong>Rečnik</strong>: Rečnik je interni struktura podataka koja skladišti kolekcije parova ključ-vrednost. Ključevi moraju biti nemenjivi tipovi podataka. Ovo je opšta sintaksa rečnika za Python:</li>\n</ul>\n<pre><code class=\"language-python\">dictionary = {\n    key1: value1,\n    key2: value2\n}\n</code></pre>\n<ul>\n<li><strong>Komponenta za <code>dict()</code></strong>: Komponenta <code>dict()</code> je alternativa za kreiranje rečnika. Prolazi listu tuple-a kao argument komponenti <code>dict()</code>. Ove tuple imaju ključeve kao prvi element i vrednosti kao drugi element.</li>\n</ul>\n<pre><code class=\"language-python\">pizza = dict([('name', 'Margherita Pizza'), ('price', 8.9), ('calories_per_slice', 250), ('toppings', ['mozzarella', 'basil'])])\n</code></pre>\n<ul>\n<li><strong>Notacija vitičastih zagrada (Bracket Notation)</strong>: Da biste dobili vrednost para ključ-vrednost, možete koristiti sintaksu poznatu kao notacija vitičastih zagrada.</li>\n</ul>\n<pre><code class=\"language-python\">dictionary[key]\n</code></pre>\n<h2>Uobičajeni načini za rečnik</h2>\n<ul>\n<li><strong><code>get()</code></strong>: Putanja za <code>get()</code> dobija vrednost vezanu za ključ. Slično je notaciji zagrada, ali omogućava postavljanje podrazumevane vrednosti, sprečavajući greške ako ključ ne postoji.</li>\n</ul>\n<pre><code class=\"language-python\">dictionary.get(key, default)\n</code></pre>\n<ul>\n<li><strong><code>keys()</code> na <code>values()</code></strong>: Metode za <code>keys()</code> na <code>values()</code> vraćaju objekat pogleda sa svim ključevima i svim vrednostima iz rečnika, respektivno. Objekat pogleda je način za pregledavanje sadržaja u rečniku bez kreiranja zasene kopije podataka.</li>\n</ul>\n<pre><code class=\"language-python\">pizza = {\n    'name': 'Margherita Pizza',\n    'price': 8.9,\n    'calories_per_slice': 250\n}\n\npizza.keys()\n# dict_keys(['name', 'price', 'calories_per_slice'])\n\npizza.values()\n# dict_values(['Margherita Pizza', 8.9, 250])\n</code></pre>\n<ul>\n<li><strong><code>items()</code></strong>: Metoda <code>items()</code> vraća objekat pogleda koji sadrži sve parove ključ-vrednost u rečniku, uključujući i ključeve i vrednosti.</li>\n</ul>\n<pre><code class=\"language-python\">pizza.items()\n# dict_items([('name', 'Margherita Pizza'), ('price', 8.9), ('calories_per_slice', 250)])\n</code></pre>\n<ul>\n<li><strong><code>clear()</code></strong>: Metoda <code>clear()</code> uklanja sve parove ključ-vrednost iz rečnika.</li>\n</ul>\n<pre><code class=\"language-python\">pizza.clear()\n</code></pre>\n<ul>\n<li><strong><code>pop()</code></strong>: Metoda <code>pop()</code> uklanja par ključ-vrednost sa ključem navedenim kao prvi argument i vraća njegovu vrednost. Ako ključ ne postoji, vraća podrazumevanu vrednost kao drugi argument. Ako ključ ne postoji i nije navedena podrazumevana vrednost, će biti izbačena greška <code>KeyError</code>.</li>\n</ul>\n<pre><code class=\"language-python\">pizza.pop('price', 10)\npizza.pop('total_price') # KeyError\n</code></pre>\n<ul>\n<li><strong><code>popitem()</code></strong>: U Python 3.7 i više, metoda <code>popitem()</code> uklanja poslednji postavljen element.</li>\n</ul>\n<pre><code class=\"language-python\">pizza.popitem()\n</code></pre>\n<ul>\n<li><strong><code>update()</code></strong>: Metoda <code>update()</code> spaja parove ključ-vrednost sa parovima ključ-vrednost drugog rečnika. Ako imaju isti ključ, njihove vrednosti se prepisuju. Novi ključevi će biti dodati kao novi parovi ključ-vrednost.</li>\n</ul>\n<pre><code class=\"language-python\">pizza.update({ 'price': 15, 'total_time': 25 })\n</code></pre>\n<h2>Pregled rečnika</h2>\n<ul>\n<li><strong>Iteriranje po vrednostima</strong>: Ako je potrebno iterirati po vrednostima u rječniku, možete napisati <code>for</code> loop i <code>values()</code> da biste dobili sve vrijednosti rječnika.</li>\n</ul>\n<pre><code class=\"language-python\">products = {\n    'Laptop': 990,\n    'Smartphone': 600,\n    'Tablet': 250,\n    'Headphones': 70,\n}\n\nfor price in products.values():\n    print(price)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">990\n600\n250\n70\n</code></pre>\n<ul>\n<li><strong>Iteracija po ključevima</strong>: Ako vam je potrebno da iterirate po ključevima u sözlüku <code>products</code> iznad, možete direktno napisati <code>products.keys()</code> ili <code>products</code>.</li>\n</ul>\n<pre><code class=\"language-python\">for product in products.keys():\n    print(product)\n    \n# Or\n\nfor product in products:\n    print(product)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">Laptop\nSmartphone\nTablet\nHeadphones\n</code></pre>\n<ul>\n<li><strong>Iteriranje po parovima ključ-vrednost</strong>: Ako želite da iterirate ključeve i njihove odgovarajuće vrednosti, možete iterirati po <code>products.items()</code>. Dobijate pojedinačne tuple sa njihovim ključevima i vrednostima.</li>\n</ul>\n<pre><code class=\"language-python\">for product in products.items():\n    print(product)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">('Laptop', 990)\n('Smartphone', 600)\n('Tablet', 250)\n('Headphones', 70)\n</code></pre>\n<p>Da biste sačuvali ključ i vrednost u različitim parametrima ciklusa, morate ih razdvojiti zarezom. Prvi parametar čuva ključ, a drugi vrednost.</p>\n<pre><code class=\"language-python\">for product, price in products.items():\n    print(product, price)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">Laptop 990\nSmartphone 600\nTablet 250\nHeadphones 70\n</code></pre>\n<ul>\n<li><strong><code>enumerate()</code></strong>: Ako želiš da prolaziš kroz rečnik i istovremeno pratiš indeks, možeš koristiti funkciju <code>enumerate()</code>. Ona vraća objekat <code>enumerate</code> koji svakom elementu dodeljuje redni broj kao indeks. Brojanje može početi od bilo kog broja, ali podrazumevano počinje od 0.</li>\n</ul>\n<p>Postavljanje atributa i polja u različite parametre ciklusa je uobičajen način za korišćenje <code>enumerate()</code>. Na primer, za <code>products.items()</code>, možete dobiti ceo par vrednosti ključa zajedno sa indeksom:</p>\n<pre><code class=\"language-python\">for index, product in enumerate(products.items()):\n    print(index, product)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">0 ('Laptop', 990)\n1 ('Smartphone', 600)\n2 ('Tablet', 250)\n3 ('Headphones', 70)\n</code></pre>\n<p>Da biste definisali početnu vrednost brojanja, možete proslediti drugi argument na <code>enumerate()</code>. Na primer, ovde počinjemo da brojimo od 1.</p>\n<pre><code class=\"language-python\">for index, product in enumerate(products.items(), 1):\n    print(index, product)\n</code></pre>\n<p>Rezultati:</p>\n<pre><code class=\"language-md\">1 ('Laptop', 990)\n2 ('Smartphone', 600)\n3 ('Tablet', 250)\n4 ('Headphones', 70)\n</code></pre>\n<h2>Množine</h2>\n<ul>\n<li>\n<p><strong>Setovi</strong>: Setovi su struktura podataka ugrađena u Python koja ne dozvoljava duplikate vrednosti. Setovi su promenljivi i bez redosleda, što znači da njihovi elementi nisu sačuvani po određenom nizu, pa ih ne možete pristupiti pomoću indeksa ili ključeva. Takođe, setovi mogu sadržati samo imutabilne tipove podataka, kao što su brojevi, niz znakova i tuple.</p>\n</li>\n<li>\n<p><strong>Definisati set</strong>: Da biste definisali set, potrebno je da napišete njegove elemente unutar zaokobljenih zagrada i razdvojite ih zarezom.</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3, 4, 5}\n</code></pre>\n<ul>\n<li><strong>Definisati set prazno</strong>: Ako treba da definišete set prazno, morate koristiti funkciju <code>set()</code>. Samo pisanje praznih vitičastih zagrada direktno će kreirati rečnik.</li>\n</ul>\n<pre><code class=\"language-python\">set() # Set\n{}    # Dictionary\n</code></pre>\n<h2>Standardni načini za set</h2>\n<ul>\n<li><strong><code>add()</code></strong>: Možete dodati funkciju na set putem <code>add()</code>, prosleđujući novu funkciju kao argument.</li>\n</ul>\n<pre><code class=\"language-python\">my_set.add(6)\n</code></pre>\n<ul>\n<li><strong><code>remove()</code> na <code>discard()</code></strong>: Kada uklonite element iz set, možete koristiti metodu <code>remove()</code> ili <code>discard()</code>, prosleđujući element koji želite da uklonite kao argument. Metoda <code>remove()</code> će baciti grešku/izuzetak <code>KeyError</code> ako element nije pronađen, dok <code>discard()</code> neće bacati grešku.</li>\n</ul>\n<pre><code class=\"language-python\">my_set.remove(4)\nmy_set.discard(4)\n</code></pre>\n<ul>\n<li><strong><code>clear()</code></strong>: Putanja za <code>clear()</code> uklanja sve elemente iz set.</li>\n</ul>\n<pre><code class=\"language-python\">my_set.clear()\n</code></pre>\n<h2>Operacije za kalkulator set</h2>\n<ul>\n<li><strong><code>issubset()</code> na <code>issuperset()</code></strong>: Putanje za <code>issubset()</code> i <code>issuperset()</code> proveravaju da li set je podskup ili nadskup drugog set, respektivno.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3, 4, 5}\nyour_set = {2, 3, 4, 5}\n\nprint(your_set.issubset(my_set)) # True\nprint(my_set.issuperset(your_set)) # True\n</code></pre>\n<ul>\n<li><strong><code>isdisjoint()</code></strong>: Put <code>isdisjoint()</code> proverava da li dva seta nemaju slične elemente.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3}\nyour_set = {4, 5, 6}\n\nprint(my_set.isdisjoint(your_set)) # True\n</code></pre>\n<ul>\n<li><strong>Operater unije (<code>|</code>)</strong>: Operater unije <code>|</code> vraća set novi sa svim elementima iz oba skupa.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3}\nyour_set = {4, 5, 6}\n\nmy_set | your_set # {1, 2, 3, 4, 5, 6}\n</code></pre>\n<ul>\n<li><strong>Operater preseka (intersection) (<code>&#x26;</code>)</strong>: Operater preseka <code>&#x26;</code> vraća novi set sa elementima koji su prisutni u oba skupa.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3, 4, 5}\nyour_set = {2, 3, 4, 6}\n\nmy_set &#x26; your_set # {2, 3, 4}\n</code></pre>\n<ul>\n<li><strong>Operater razlike (difference) (<code>-</code>)</strong>: Operater razlike <code>-</code> vraća set novi skup sa elementima prvog skupa koji nisu u drugom skupu.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3, 4, 5}\nyour_set = {2, 3, 4, 6}\n\nmy_set - your_set # {1, 5}\n</code></pre>\n<ul>\n<li><strong>Operater simetrične razlike (symmetric difference) (<code>^</code>)</strong>: Operater simetrične razlike <code>^</code> vraća set sa novim elementima koji su u prvom ili drugom skupu, ali ne i oba.</li>\n</ul>\n<pre><code class=\"language-python\">my_set = {1, 2, 3, 4, 5}\nyour_set = {2, 3, 4, 6}\n\nmy_set ^ your_set # {1, 5, 6}\n</code></pre>\n<ul>\n<li><strong>Operater za proveru prisutnosti (<code>in</code>)</strong>: Može se proveriti da li je element u set ili ne, koristeći operater <code>in</code>.</li>\n</ul>\n<pre><code class=\"language-python\">print(5 in my_set)\n</code></pre>\n<h2>Standardna biblioteka za Python</h2>\n<ul>\n<li><strong>Standardna biblioteka za Python</strong>: Biblioteka vam pruža unapred napisan i ponovno upotrebljiv kod, kao što su funkcije, klase i strukture podataka, koje možete koristiti u svojim projektima. Python ima veliku standardnu biblioteku sa ugrađenim modulima koji implementiraju standardna rešenja za mnoge probleme i zadatke. Neki primeri popularnih ugrađenih modula su <code>math</code>, <code>random</code>, <code>re</code> (skraćeno od \"regularnih izraza\"), i <code>datetime</code>.</li>\n</ul>\n<h2>Izjava o import</h2>\n<ul>\n<li>\n<p><strong>Deklaracija import</strong>: Da biste pristupili elementima navedenim u ugrađenim modulima, koristite deklaraciju import. Deklaracije import obično se pišu na vrhu fajla. Deklaracije import rade isto za funkcije, klase, konstante, parametre i sve ostale elemente navedene u modulima.</p>\n</li>\n<li>\n<p><strong>Osnovna izjava za import</strong>: Možete koristiti glavnu reč <code>import</code> praćenu imenom modula:</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">import module_name\n</code></pre>\n<p>Zatim, ako treba da pozovete funkciju iz tog modula, koristićete notaciju tačke, naziv modula praćen nazivom funkcije.</p>\n<pre><code class=\"language-python\">module_name.function_name()\n</code></pre>\n<p>Na primer, napišite sledeće u svoj kod da učitate modul <code>math</code> i pronađete kvadratni koren iz 36:</p>\n<pre><code class=\"language-python\">import math\n\nmath.sqrt(36)\n</code></pre>\n<ul>\n<li><strong>Uvoz modula pod drugačijim imenom</strong>: Ako je potrebno uvesti module pod drugim imenom (poznatom kao \"alias\"), možete koristiti ključnu reč <code>as</code> kao alias na kraju izjave import. Ovo se često koristi za dugačka imena modula ili za izbegavanje kolizija imena.</li>\n</ul>\n<pre><code class=\"language-python\">import module_name as module_alias\n</code></pre>\n<p>Na primer, referenciranje modula <code>math</code> kao <code>m</code> u vašem kodu, možete postaviti alias ovako:</p>\n<pre><code class=\"language-python\">import math as m\n</code></pre>\n<p>Zatim, možete dobiti komponente modula koristeći alias:</p>\n<pre><code class=\"language-python\">m.sqrt(36)\n</code></pre>\n<ul>\n<li><strong>Uključivanje specifičnih elemenata</strong>: Ako vam ne treba sve iz modula, možete uključiti specifične elemente koristeći <code>from</code>. U ovom slučaju, izjava import počinje sa <code>from</code>, praćena nazivom modula, zatim glavnom reči <code>import</code>, i na kraju imenima elemenata koje želite uključiti.</li>\n</ul>\n<pre><code class=\"language-python\">from module_name import name1, name2\n</code></pre>\n<p>Zatim, možete koristiti ova imena bez modulatorskog prefiksa u vašoj skripti za Python. Na primer:</p>\n<pre><code class=\"language-python\">from math import radians, sin, cos\n\nangle_degrees = 40\nangle_radians = radians(angle_degrees)\n\nsine_value = sin(angle_radians)\ncos_value = cos(angle_radians)\n\nprint(sine_value) # 0.6427876096865393\nprint(cos_value)  # 0.766044443118978\n</code></pre>\n<p>Ovo je pomoć, ali može uzrokovati sukobe imena ako već imaš funkcije ili parametre sa istim imenima. Molimo te, imaj ovo na umu kada odlučuješ koji tip deklaracije da koristiš za import.</p>\n<p>Ako treba da date ovim imenima alias, možete to uraditi i korišćenjem glavne reči <code>as</code> praćeno aliasom.</p>\n<pre><code class=\"language-python\">from module_name import name1 as alias1, name2 as alias2\n</code></pre>\n<ul>\n<li><strong>Izjava za import sa zvezdama (from modul import <code>*</code>)</strong>: Zvezda mu govori Python da želiš da sve prebaci u taj modul, ali želiš da to uradi tako da ne moraš da koristiš ime modula kao prefiks.</li>\n</ul>\n<pre><code class=\"language-python\">from module_name import *\n</code></pre>\n<p>Na primer, ako koristite ovo da učitate modul <code>math</code>, moći ćete pozvati bilo koju funkciju navedenu u tom modulu bez prikazivanja naziva modula kao prefiksa.</p>\n<pre><code class=\"language-python\">from math import *\nprint(sqrt(36))  # 6.0\n</code></pre>\n<p>Međutim, ovo se obično ne preporučuje jer može uzrokovati kolizije imena i otežati određivanje odakle ta imena potiču.</p>\n<h2><code>if __name__ == '__main__'</code></h2>\n<ul>\n<li><strong>Parametar za <code>__name__</code></strong>: <code>__name__</code> je specijalni parametar ugrađen u/unutar Python. Kada se datoteka Python izvršava direktno, Python postavlja vrednost ovog parametra na niz karaktera <code>\"__main__\"</code>. Ali ako se datoteka Python uvozi kao modul u drugi skript za Python, vrednost parametra <code>__name__</code> postavlja se na ime tog modula.</li>\n</ul>\n<p>To je razlog da često naiđete na ove uslove u skriptama za Python. One imaju kod koji se želi pokrenuti <strong>samo</strong> ako je skripta za Python izvršena kao glavni program.</p>\n<pre><code class=\"language-python\">if __name__ == '__main__': \n    # Code\n</code></pre>\n<h2>Uobičajene greške u Python</h2>\n<ul>\n<li><strong>SyntaxError</strong>: Greška koju Python vraća kada vaš kod ne prati pravila sintakse. Na primer, kod <code>print(\"Hello there\"</code> će izazvati sintaksnu grešku sa porukom <code>SyntaxError: '(' was never closed</code>, jer nedostaju zatvarajuće zagrade.</li>\n<li><strong>NameError</strong>: Python vraća grešku <code>NameError</code> kada pokušavate da pristupite parametru ili funkciji koju niste definisali. Na primer, ako imate liniju <code>print(username)</code> u vašem kodu bez prethodno definisanog parametra <code>username</code>, dobićete NameError sa porukom <code>NameError: name 'username' is not defined</code>.</li>\n<li><strong>TypeError</strong>: Ovo je greška koju Python vraća kada obavljate operaciju na nekompatibilnim tipovima podataka. Na primer, ako pokušavate da dodate niz znakova na broj, dobićete grešku <code>TypeError: can only concatenate str (not \"int\") to str</code>.</li>\n<li><strong>IndexError</strong>: Dobićete grešku <code>IndexError</code> ako pokušate da pristupite indeksu koji ne postoji u listi ili bilo kojoj drugoj sekvenci, kao što su tuple i niz znakova. Na primer, u nizu znakova <code>Hello world</code>, indeks poslednjeg znaka je <code>10</code>. Ako nastavite da pristupate znakovima na ovaj način, <code>greet = \"hello world\"; print(greet[11])</code>, dobićete grešku sa porukom <code>IndexError: string index out of range</code>.</li>\n<li><strong>AttributeError</strong>: Python vraća ovu grešku kada pokušavate da koristite metod ili atribut koji ne postoji na objektu tog tipa. Na primer, pozivanje <code>.append()</code> na nizu znakova kao što je <code>\"hello\".append(\"!\")</code> vodi do greške sa porukom <code>AttributeError: 'str' object has no attribute 'append'</code>.</li>\n</ul>\n<h2>Efikasne metode za ispravljanje grešaka u Python</h2>\n<ul>\n<li><strong>Korišćenje funkcije <code>print</code></strong>: Unos stanja za <code>print()</code> u različitim delovima vašeg koda tokom debagovanja pomaže vam da vidite vrednosti promenljivih i kako teče kod.</li>\n<li><strong>Korišćenje ugrađenog debugger-a Python (<code>pdb</code>)</strong>: Python pruža modul <code>pdb</code> za debagovanje. To je deo standardne biblioteke Python, pa je uvek dostupan za korišćenje. Sa <code>pdb</code>, možete postaviti <em>breakpoint</em> (tačku za pauziranje) na funkciju <code>set_trace()</code> kako biste započeli korak po korak izvršavanje koda i interaktivno pregledali promenljive.</li>\n<li><strong>Korišćenje alata za debagovanje u IDE-u</strong>: Mnogi razvojni okruženja (IDE) i editori koda kao što su Pycharm i VS Code nude alate za debagovanje sa <em>breakpoints</em>-ovima, korak po korak izvršavanjem, inspekcijom promenljivih i drugim funkcijama za debagovanje.</li>\n</ul>\n<h2>Rešavanje grešaka</h2>\n<ul>\n<li>\n<p><strong><code>try...except</code></strong>: Ovo se koristi za izvršavanje dela koda koje može izazvati grešku. Blok <code>try</code> je mesto gde očekujete da će doći do greške, dok blok <code>except</code> hvata specifičnu grešku i izvršava se ako ta greška nastane. Evo primera:</p>\n<pre><code class=\"language-py\">try:\n  print(22 / 0)\nexcept ZeroDivisionError:\n  print('You can\\'t divide by zero!')\n  # You can't divide by zero!\n</code></pre>\n</li>\n</ul>\n<p>Takođe možete povezati više delova <code>except</code> kako biste obradili više vrsta grešaka:</p>\n<pre><code class=\"language-py\">try:\n  number = int(input('Enter a number: '))\n  print(22 / number)\nexcept ZeroDivisionError:\n  print('You cannot divide by zero!')\n  # You cannot divide by zero! prints when you enter 0\nexcept ValueError:\n  print('Please enter a valid number!')\n  # Please enter a valid number! prints when you don't enter an integer\n</code></pre>\n<ul>\n<li>\n<p><strong><code>else</code> na <code>finally</code></strong>: Ove sekcije dodaju <code>try...except</code>. Ako ne dođe do greške, sekcija <code>else</code> se izvršava. Sekcija <code>finally</code> se izvršava uvek, bez obzira na to da li je došlo do greške ili ne.</p>\n<pre><code class=\"language-py\">try:\n  result = 100 / 4\nexcept ZeroDivisionError:\n  print('You cannot divide by zero!') # This will not run\nelse:\n  print(f'Result is {result}') # Result is 25.0\nfinally:\n  print('Execution complete!') # Execution complete!\n</code></pre>\n</li>\n<li>\n<p><strong>Karakteristika grešaka</strong>: Ovo vam omogućava da dobijete samu grešku radi bolje korekcije i direktnog štampanja poruke o grešci. Da biste dobili karakteristiku grešaka, morate koristiti ključnu reč <code>as</code>. Evo primera:</p>\n<pre><code class=\"language-py\">try:\n    value = int('This will raise an error')\nexcept ValueError as e:\n    print(f'Caught an error: {e}')\n    # Caught an error: invalid literal for int() with base 10: 'This will raise an error'\n</code></pre>\n</li>\n<li>\n<p><strong>Izjava za <code>raise</code></strong>: Ovo vam omogućava da ručno bacite grešku. Možete ga koristiti za bacanje greške kada se ispuni određeni uslov. Evo primera:</p>\n<pre><code class=\"language-py\">def divide(a, b):\n    if b == 0:\n        raise ZeroDivisionError('You cannot divide by zero')\n    return a / b\n```## Indikacija greške\n\n</code></pre>\n</li>\n<li>\n<p>Izjava za <code>raise</code> takođe je važna kada kreirate svoje prilagođene greške, jer ga možete koristiti da bacite izuzetak sa specifičnom porukom. Evo primera toga:</p>\n<pre><code class=\"language-py\">class InvalidCredentialsError(Exception):\n    def __init__(self, message=\"Invalid username or password\"):\n        self.message = message\n        super().__init__(self.message)\n\ndef login(username, password):\n    stored_username = \"admin\"\n    stored_password = \"password123\"\n    \n    if username != stored_username or password != stored_password:\n        raise InvalidCredentialsError()\n    \n    return f\"Welcome, {username}!\"\n</code></pre>\n</li>\n</ul>\n<p>Ovde je kako možete koristiti funkciju <code>login</code> sa greškom <code>InvalidCredentialsError</code>:</p>\n<pre><code class=\"language-py\"># failed login attempt\ntry:\n    message = login(\"user\", \"wrongpassword\")\nexcept InvalidCredentialsError as e:\n    print(f\"Login failed: {e}\")\nelse:\n    print(message)\n\n# successful login attempt\ntry:\n    message = login(\"admin\", \"password123\")\nexcept InvalidCredentialsError as e:\n    # This block is not executed because the login was successful\n    print(f\"Login failed: {e}\")\nelse:\n    # The else block runs if the 'try' block completes without an exception\n    print(message)\n</code></pre>\n<p>Izjava <code>raise</code> se takođe može koristiti zajedno sa glavnom rečju <code>from</code> za povezivanje grešaka, prikazujući vezu između različitih grešaka:</p>\n<pre><code class=\"language-py\">def parse_config(filename):\n  try:\n      with open(filename, 'r') as file:\n          data = file.read()\n          return int(data)\n  except FileNotFoundError:\n      raise ValueError('Configuration file is missing') from None\n  except ValueError as e:\n      raise ValueError('Invalid configuration format') from e\n\nconfig = parse_config('config.txt')\n</code></pre>\n<h2>Klase i predmete za Python</h2>\n<ul>\n<li><strong>Definicija klase</strong>: Klasa je šema za kreiranje objekata. Definiše ponašanje koje će objekat imati kroz svoje atribute i metode. Evo osnovnog primera definicije klase u Python:</li>\n</ul>\n<pre><code class=\"language-python\">class Dog:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def bark(self):\n        print(f'{self.name.upper()} says woof woof!')\n</code></pre>\n<ul>\n<li><strong>Kreiranje objekata</strong>: Objekti su instancije klase. Kreiraju se pozivanjem klase i potrebnih argumenata.</li>\n</ul>\n<pre><code class=\"language-python\">dog1 = Dog('Jack', 3)\ndog2 = Dog('Thatcher', 5)\n\ndog1.bark()  # JACK says woof woof!\ndog2.bark()  # THATCHER says woof woof!\n</code></pre>\n<ul>\n<li><strong>Pozivanje metoda na objektima</strong>: Možete pozvati metode na objektima kako biste izvršili akcije ili dobili informacije.</li>\n</ul>\n<pre><code class=\"language-python\">object_name1.method_name()\nobject_name2.method_name()\n</code></pre>\n<ul>\n<li><strong>Razlika između klase i objekta</strong>: Klasa je ponovno upotrebljiv šablon, dok je objekat specifična instanca te klase koja sadrži stvarne podatke.</li>\n</ul>\n<h2>Karakteristike</h2>\n<ul>\n<li><strong>Svojstva događaja</strong>: Definisana su unutar <code>__init__()</code> koristeći <code>self</code>, i jedinstvene su za svaki objekat.</li>\n<li><strong>Svojstva klase</strong>: Definisana su direktno unutar klase i deljene sa svim događajima.</li>\n</ul>\n<pre><code class=\"language-python\">class Dog:\n    species = 'French Bulldog'  # Class attribute\n\n    def __init__(self, name):\n        self.name = name  # Instance attribute\n\nprint(Dog.species) # French Bulldog\n\njack = Dog('Jack')\nprint(jack.name)     # Jack\nprint(jack.species)  # French Bulldog\n</code></pre>\n<h2>Put</h2>\n<ul>\n<li><strong>Metoda</strong>: Funkcije definisane unutar klase koje rade na svojstvima objekta.</li>\n</ul>\n<pre><code class=\"language-python\">class Car:\n    def __init__(self, color, model):\n        self.color = color\n        self.model = model\n\n    def describe(self):\n        return f'This car is a {self.color} {self.model}'\n\nmy_car_1 = Car('red', 'Tesla Model S')\nprint(my_car_1.describe())  # This car is a red Tesla Model S\n</code></pre>\n<ul>\n<li><strong>Pronalaženje rute</strong>: Pronalazi rute na objektima koristeći notaciju tačke. Evo primera pozivanja putem <code>describe</code> na dva različita vozila:</li>\n</ul>\n<pre><code class=\"language-python\">class Car:\n    def __init__(self, color, model):\n        self.color = color  \n        self.model = model  \n\n    def describe(self):\n        return f'This car is a {self.color} {self.model}'\n\nmy_car_1 = Car('red', 'Tesla Model S')\nmy_car_2 = Car('green', 'Lamborghini Revuelto')\n\nprint(my_car_1.describe()) # Calling method using the dot notation\n\nprint(my_car_2.describe()) # Calling method using the dot notation\n</code></pre>\n<h2>Putevi za dunder (za magiju)</h2>\n<ul>\n<li><strong>Definicija</strong>: Specijalne putanje koje počinju i završavaju sa dve podvučnice (primer, <code>__init__</code>, <code>__len__</code>, <code>__str__</code>, <code>__eq__</code>). Python koristi ove interne za ugrađene operacije.</li>\n</ul>\n<pre><code class=\"language-python\">class Book:\n    def __init__(self, title, pages):\n        self.title = title\n        self.pages = pages\n\n    def __len__(self):\n        return self.pages\n\n    def __str__(self):\n        return f\"'{self.title}' has {self.pages} pages\"\n\n    def __eq__(self, other):\n        return self.pages == other.pages\n\nbook1 = Book('Built Wealth Like a Boss', 420)\nprint(len(book1))        # 420\nprint(str(book1))        # 'Built Wealth Like a Boss' has 420 pages\n</code></pre>\n<ul>\n<li>\n<p><strong>Pozivanje \"dunder\" metoda indirektno</strong>: Ne morate pozivati \"dunder\" metode direktno. Umesto toga, Python ih poziva automatski kada se dogode određene akcije. Ove operacije uključuju:</p>\n</li>\n<li>\n<p><strong>Matematičke operacije kao sabiranje, oduzimanje, množenje, deljenje i druge</strong>. <code>__add__()</code> se koristi za sabiranje, <code>__sub__()</code> za oduzimanje, <code>__mul__()</code> za množenje, i <code>__truediv__()</code> za deljenje.</p>\n<ul>\n<li>\n<p><strong>Operacije sa nizovima karaktera kao konkatenacija, ponavljanje, kreiranje šablona i konverzija u tekst</strong>. <code>__add__()</code> se koristi za konkatenaciju, <code>__mul__()</code> za ponavljanje, <code>__format__()</code> za kreiranje šablona, <code>__str__()</code> i <code>__repr__()</code> za konverziju u tekst, itd.</p>\n</li>\n<li>\n<p><strong>Operacije poređenja kao jednako, manje od, veće od i druge</strong>. <code>__eq__()</code> se koristi za proveru jednakosti, <code>__lt__()</code> za manje od, <code>__gt__()</code> za veće od, itd.</p>\n</li>\n<li>\n<p><strong>Iterativne operacije kao činjenje nečega ponovljivim i pomeranje kroz elemente</strong>. <code>__iter__()</code> se koristi za vraćanje iteracionog elementa, a <code>__next__()</code> za dobijanje sledećeg elementa.</p>\n</li>\n</ul>\n</li>\n</ul>\n<h2>Stvarni primer iz sveta: Automobil za kupovinu</h2>\n<ul>\n<li><strong>Klasa automobila sa metodama ispod</strong>: Omogućava dodavanje, brisanje, ponavljanje i pregledavanje sadržaja pomoću ugrađenih funkcija.</li>\n</ul>\n<pre><code class=\"language-python\">class Cart:\n    def __init__(self):\n        self.items = []\n\n    def add(self, item):\n        self.items.append(item)\n\n    def remove(self, item):\n        if item in self.items:\n            self.items.remove(item)\n        else:\n            print(f'{item} is not in cart')\n\n    def list_items(self):\n        return self.items\n\n    def __len__(self):\n        return len(self.items)\n\n    def __getitem__(self, index):\n        return self.items[index]\n\n    def __contains__(self, item):\n        return item in self.items\n\n    def __iter__(self):\n        return iter(self.items)\n\ncart = Cart()\ncart.add('Laptop')\nprint(len(cart))        # 1\nprint('Laptop' in cart) # True\n</code></pre>\n<h2>Šta je program koji zavisi od objekata?</h2>\n<ul>\n<li><strong>Objektno usmerizovano programiranje</strong>: Paradigma programiranja u kojoj programeri koriste svaki element svog koda kao stvarni objekat iz realnog sveta. Vrlo je poznato kao OOP. Četiri glavna principa koji vam pomažu da efikasno organizujete i upravljate kodom su <strong>enkapsulacija</strong>, <strong>nasleđivanje</strong>, <strong>polimorfizam</strong> i <strong>apstrakcija</strong>.</li>\n<li><strong>Klase</strong>: Šema za kreiranje objekata. Svaki objekat stvoren iz klase ima atribute koji definišu podatke i metode koje određuju ponašanje tog objekta.</li>\n</ul>\n<h2>Šta je instalacija?</h2>\n<ul>\n<li><strong>Enkapsulacija</strong>: Kombinovanje atributa i metoda objekta u jednu jedinicu. Omogućava skrivanje internog stanja objekta iza jednostavnog skupa javnih atributa i metoda koje funkcionišu kao vrata. Iza tih vrata nalaze se privatni atributi i metode koji kontrolišu kako se podaci menjaju i ko može da ih vidi.</li>\n<li><strong>Primer enkapsulacije</strong>: Ako želite da pratite stanje novčanika, dozvoljavate deponovanje i povlačenje novca, ali ne želite da bilo ko direktno pristupi stanju novčanika:</li>\n</ul>\n<pre><code class=\"language-py\">class Wallet:\n   def __init__(self, balance):\n       self.__balance = balance # Private attribute\n\n   def deposit(self, amount):\n       if amount > 0:\n           self.__balance += amount # Add to the balance safely\n\n   def withdraw(self, amount):\n       if 0 &#x3C; amount &#x3C;= self.__balance:\n           self.__balance -= amount # Remove from the balance safely\n\naccount = Wallet(500)\nprint(account.__balance) # AttributeError: 'Wallet' object has no attribute '__balance'\n</code></pre>\n<ul>\n<li><strong>Razlika između postavljanja modifikatora pristupa u jednom i dva reda za svojstvo</strong>: Postavljanje modifikatora pristupa u jednom redu za svojstvo i metodu znači da je namenjeno za interno korišćenje. Ovo predstavlja ugovor, a ne sprečava pristup svojstvu izvana. Postavljanje modifikatora pristupa u dva reda za svojstvo i metodu potpuno onemogućava pristup izvan njihove klase.</li>\n</ul>\n<h2>Šta su primaoci i pošiljaoci?</h2>\n<ul>\n<li><strong>Wapokeaji na wapezaji</strong>: Metode koje vam omogućavaju da kontrolišete kako su svojstva klase dostupna i modifikovana. Dobijate vrednost za wapokeaji, a postavljate vrednost za wapezaji.</li>\n<li><strong>Sifa</strong>: Oni povezuju wapokeaji i wapezaji, omogućavajući pristup podacima. Izvršavaju dodatnu logiku iza scene kada dobijate, postavljate ili brišete vrednost.</li>\n<li><strong>Zašto sifa umesto metode</strong>: Svojstva se koriste umesto metoda radi bolje čitljivosti i čistog koda. Omogućavaju vam da dobijate vrednosti pomoću dot notacije, kao obična svojstva, bez zagrada.</li>\n<li><strong>Kreiranje wapokeaji</strong>: Da kreirate wapokeaji, koristite dekorator <code>@property</code>. Evo wapokeaji koji dobija krug kruga:</li>\n</ul>\n<pre><code class=\"language-py\">class Circle:\n    def __init__(self, radius):\n        self.radius = radius # Calling the setter\n\n    @property\n    def radius(self): # A getter to get the radius\n        return self._radius\n  \n    @property\n    def area(self):  # A getter to calculate area\n        return 3.14 * (self._radius ** 2)\n\nmy_circle = Circle(3)\n\nprint(my_circle.radius) # 3\nprint(my_circle.area) # 28.26\n</code></pre>\n<p>Korišćenje self.radius unutar <code>__init__()</code> osigurava da programeri funkcionišu tokom kreiranja objekta, tako da se nevažeći vrednosti radijusa uhvate odmah.</p>\n<ul>\n<li><strong>Kreiranje komponente</strong>: Da biste kreirali komponentu koja će postaviti krug, morate navesti drugu putanju sa istim imenom i koristiti <code>@&#x3C;property_name>.setter</code> na njoj:</li>\n</ul>\n<pre><code class=\"language-py\">class Circle:\n    def __init__(self, radius):\n        self.radius = radius # Calling the setter\n\n    @property\n    def radius(self):  # A getter to get the radius\n        return self._radius\n\n    @radius.setter\n    def radius(self, value):  # A setter to set the radius\n        if value &#x3C;= 0:\n            raise ValueError('Radius must be positive')\n        self._radius = value\n\nmy_circle = Circle(3)\nprint('Initial radius:', my_circle.radius) # Initial radius: 3\n\nmy_circle.radius = 8\nprint('After modifying the radius:', my_circle.radius) # After modifying the radius: 8\n</code></pre>\n<ul>\n<li><strong>Kako Python obrađuje primaoca i pošiljaoce</strong>: Kada definiš primaoca i pošiljaoce, Python automatski poziva svaki put kada koristiš standardnu sintaksu svojstva kao što je ovo:</li>\n</ul>\n<pre><code class=\"language-py\">my_circle.radius # This will call the getter\nmy_circle.radius = 4 # This will call the setter\n</code></pre>\n<p>Unutar provajdera, nije prikladno navesti ime samog atributa jer će to uzrokovati <code>RecursionError</code>.</p>\n<ul>\n<li><strong>Stripper</strong>: Nakon postavljanja i dohvaćanja vrednosti za pošiljaoca i primaoca, možete kontrolisati kako se briše pomoću <code>deleter</code>. Stripper izvršava specifičnu logiku kada koristite izjavu <code>del</code> na svojstvu. Za kreiranje stripper-a, koristite dekorator <code>@&#x3C;property_name>.deleter</code>.</li>\n</ul>\n<pre><code class=\"language-py\">  # Deleter\n    @radius.deleter\n    def radius(self):\n        print(\"Deleting radius...\")\n        del self._radius\n</code></pre>\n<h2>Šta je nasleđivanje i kako pomaže u ponovnom korišćenju koda?</h2>\n<ul>\n<li><strong>Nasleđivanje</strong>: Proces u kojem podklasa koristi svojstva i metode roditeljske klase. Nasleđivanje pomaže ponovnoj upotrebi koda, pruža jasnu hijerarhiju i omogućava modifikovanje ponašanja bez ponovnog pisanja koda. Kada se implementira nasleđivanje, podklasa preuzima ime roditeljske klase:</li>\n</ul>\n<pre><code class=\"language-py\">class Parent:\n    # Parent attributes and methods\n\nclass Child(Parent):\n    # Child inherits, extends, and/or overrides where necessary\n</code></pre>\n<ul>\n<li><strong>Direktno i višestrano nasleđe</strong>: Kada dete nasleđuje osobine i putanje od jednog roditelja, kao što vidite gore, taj proces se naziva <strong>direktno nasleđe</strong>. Kada dete nasleđuje osobine i putanje od više od jednog roditelja, to je <strong>višestrano nasleđe</strong>. Sintaksa mu je ovakva:</li>\n</ul>\n<pre><code class=\"language-py\">class Parent:\n    # Attributes and methods for Parent\n\nclass Child:\n    # Attributes and methods for Child\n\nclass GrandChild(Parent, Child):\n    # GrandChild inherits from both Parent and Child\n    # GrandChild can combine or override behavior from each\n</code></pre>\n<ul>\n<li><strong>Metoda za <code>super()</code></strong>: Metoda koja vam omogućava da pozovete metodu iz roditeljske klase, kada klasa ima drugačiju implementaciju te metode, ili dodaje tu metodu, bez potrebe za ponovnim pisanjem koda.</li>\n</ul>\n<h2>Šta je polimorfizam i kako pomaže u táiспользовању koda?</h2>\n<ul>\n<li><strong>Polimorfizam</strong>: Princip OOP-a koji omogućava različitim klasama da koriste isti naziv metode, ali svaka klasa ga implementira drugačije kada se pozove. Evo njegove sintakse:</li>\n</ul>\n<pre><code class=\"language-py\">class A:\n   def action(self): ...\n\nclass B:\n   def action(self): ...\n\nclass C:\n   def action(self): ...\n\nClass().method()  # Works for A, B, or C\n</code></pre>\n<ul>\n<li><strong>Polimorfizam zavisno od nasleđa</strong>: Roditelj pruža putanju, a svaka klasa deteta je modifikuje u zavisnosti od njihove upotrebe.</li>\n</ul>\n<h2>Šta je promena imena i kako funkcioniše?</h2>\n<ul>\n<li><strong>Promena imena</strong>: Proces u kojem Python unutar sebe menja ime svojstva koje ima dvolinijski prefiks dodavanjem novog reda i imena klase kao prefiksa, menjajući <code>__attribute</code> u <code>_ClassName__attribute</code>.</li>\n<li><strong>Svrha promene imena</strong>: Glavna svrha je sprečavanje slučajnog nasleđivanja svojstava i metoda prilikom korišćenja nasleđivanja. Evo koda koji ovo olakšava za razumevanje:</li>\n</ul>\n<pre><code class=\"language-py\">class Parent:\n    def __init__(self):\n        self.__data = 'Parent data'\n\nclass Child(Parent):\n    def __init__(self):\n        super().__init__()\n        self.__data = 'Child data'\n\nc = Child()\nprint(c.__dict__) # {'_Parent__data': 'Parent data', '_Child__data': 'Child data'}\n</code></pre>\n<h2>Šta je sažimanje i kako pomaže u strukturiranju složenih sistema?</h2>\n<ul>\n<li><strong>Apstrakcija</strong>: Programski koncept u kojem su kompleksni detalji implementacije objekta ili sistema skriveni, a prikazuju se samo ključne karakteristike. U Python i drugim programskim jezicima, apstrakcija pojednostavljuje složene sisteme povećanjem ponovne upotrebljivosti.</li>\n<li><strong>Primer apstrakcije</strong>: Dobar primer apstrakcije u svakodnevnom životu je automobil koji vam omogućava da koristite samo točkove, pedale i menjač bez znanja kako rade motor ili kočnice.</li>\n<li><strong>Kako Python implementira apstrakciju</strong>: Python vrši apstrakciju putem modula <code>abc</code>. Modul pruža klasu <code>ABC</code> (apstraktna bazna klasa) i dekorator <code>@abstractmethod</code>. Apstraktna bazna klasa (ABC) definiše zajedničke metode i svojstva koje izvedene klase moraju implementirati. Ne može se direktno instancirati.</li>\n<li><strong>Kako je apstraktna metoda definisana</strong>: Apstraktna metoda je definisana sa <code>@abstractmethod</code> i mora biti prepisana u izvedenim klasama, čak i ako ima podrazumevanu implementaciju. Osnovna sintaksa apstrakcije je sledeća:</li>\n</ul>\n<pre><code class=\"language-py\">from abc import ABC, abstractmethod\n\n# Define an abstract base class\nclass AbstractClass(ABC):\n    @abstractmethod\n    def abstract_method(self):\n        pass\n\n# Concrete subclass that implements the abstract method\nclass ConcreteClassOne(AbstractClass):\n    def abstract_method(self):\n        print('Implementation in ConcreteClassOne')\n\n# Another concrete subclass\nclass ConcreteClassTwo(AbstractClass):\n    def abstract_method(self):\n        print('Implementation in ConcreteClassTwo')\n</code></pre>\n<h2>Algoritmi u Big O notaciji</h2>\n<ul>\n<li>\n<p><strong>Algoritmi</strong>: Skup jasnih instrukcija za rešavanje problema ili izvršavanje zadatka. Algoritmi moraju biti kompletni sa konačnim brojem koraka, a svaki korak mora biti tačan i neuključiv.</p>\n</li>\n<li>\n<p><strong>Big O notacija</strong>: Opisuje najgori slučaj performansi, ili stopu rasta, algoritma kada veličina ulaza raste. Razmatra kako rast potrošnje resursa u odnosu na veličinu ulaza, zanemarujući konstante faktore i niže redove članova.</p>\n</li>\n</ul>\n<h3>Normalna kompleksnost vremena</h3>\n<ul>\n<li><strong>O(1) - Konstantno vreme</strong>: Algoritmi zahtevaju isto vreme bez obzira na veličinu ulaznih podataka.</li>\n</ul>\n<pre><code class=\"language-python\">def check_even_or_odd(number):\n    if number % 2 == 0:\n        return 'Even'\n    else:\n        return 'Odd'\n</code></pre>\n<ul>\n<li>\n<p><strong>O(log n) - Logaritamsko vreme</strong>: Vreme raste polako kako raste ulaz. Često se koristi u algoritmima koji ponavljajuće smanjuju veličinu problema za polovinu (kao što je Binarna pretraga).</p>\n</li>\n<li>\n<p><strong>O(n) - Linearno vreme</strong>: Vreme izvršavanja raste proporcionalno veličini ulaza.</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">for grade in grades:\n    print(grade)\n</code></pre>\n<ul>\n<li>\n<p><strong>O(n log n) - Logaritamsko vreme</strong>: Tipična složenost efikasnih algoritama sortiranja kao što su Merge Sort i Quick Sort.</p>\n</li>\n<li>\n<p><strong>O(n²) - Kvadratično vreme</strong>: Vreme izvršavanja raste kvadratno. Često se javlja u ugnježdene petlje.</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">for i in range(n):\n    for j in range(n):\n        print(\"Hello, World!\")\n</code></pre>\n<h3>Kompleksnost prostora</h3>\n<ul>\n<li><strong>O(1) - Konstantni prostor</strong>: Algoritam koristi konstantnu količinu memorije bez obzira na veličinu ulaza.</li>\n<li><strong>O(n) - Linearni prostor</strong>: Potrošnja memorije raste proporcionalno veličini ulaza.</li>\n<li><strong>O(n²) - Kvadratni prostor</strong>: Potrošnja memorije raste kvadratno u odnosu na veličinu ulaza.</li>\n</ul>\n<h2>Metode za rešavanje problema</h2>\n<ul>\n<li>\n<p><strong>Razumevanje problema</strong>: Pročitajte izjavu o problemu nekoliko puta. Identifikujte ulaz, očekivani rezultat i način na koji se ulaz pretvara u rezultat.</p>\n</li>\n<li>\n<p><strong>Pseudokod</strong>: Opis logike algoritma na visokom nivou koja ne zavisi od bilo kog jezika. Koristi uobičajen jezik pomešan sa programskim strukturama kao što su <code>IF</code>, <code>ELSE</code>, <code>FOR</code>, <code>WHILE</code>.</p>\n</li>\n</ul>\n<pre><code class=\"language-md\">GET original_string\nSET reversed_string = \"\"\nFOR EACH character IN original_string:\n  ADD character TO THE BEGINNING OF reversed_string\nDISPLAY reversed_string\n</code></pre>\n<ul>\n<li><strong>Izuzetni slučajevi</strong>: Specifičan, validan unos koji se dešava na granicama onoga što algoritam treba da obrađuje. Uvek razmatrajte i testirajte izuzetne slučajeve.</li>\n</ul>\n<h2>Redovi podataka</h2>\n<ul>\n<li>\n<p><strong>Trajno polje</strong>: Ima veličinu postavljenu tokom inicijalizacije. Komponente se skladište u susednim memorijskim oblastima. Veličina ne može biti promenjena tokom izvršavanja programa.</p>\n</li>\n<li>\n<p><strong>Dinamički nizovi</strong>: Mogu rasti ili smanjiti se automatski tokom izvršavanja programa. On upravlja promenama veličine tako što automatski kopira u veći niz kada je to potrebno.</p>\n</li>\n</ul>\n<h3>Alati za Python (Redovi za izmene)</h3>\n<pre><code class=\"language-python\">numbers = [3, 4, 5, 6]\n\n# Access elements\nnumbers[0]  # 3\n\n# Update elements\nnumbers[2] = 16\n\n# Add elements\nnumbers.append(7)\nnumbers.insert(3, 15)  # Insert at specific index\n\n# Remove elements\nnumbers.pop(2)  # Remove at specific index\nnumbers.pop()   # Remove last element\n</code></pre>\n<h3>Kompleksnost vremena za slojeve promena</h3>\n<ul>\n<li><strong>Pristup</strong>: O(1)</li>\n<li><strong>Ubacivanje na kraj</strong>: O(1) prosečno, O(n) vreme skaliranja veličine</li>\n<li><strong>Ubacivanje u sredinu</strong>: O(n)</li>\n<li><strong>Brisanje</strong>: O(n) za sredinu, O(1) na kraju</li>\n</ul>\n<h2>Ustani</h2>\n<ul>\n<li>\n<p><strong>Stack</strong>: Struktura podataka tipa Last-In, First-Out (LIFO). Elementi se dodaju i brišu samo o vrhu.</p>\n</li>\n<li>\n<p><strong>Operacija pusha</strong>: Dodaje element na vrh steka. Vremenska složenost: O(1).</p>\n</li>\n<li>\n<p><strong>Operacija uklanjanja</strong>: Uklanja element sa vrha steka. Vremenska složenost: O(1).</p>\n</li>\n</ul>\n<pre><code class=\"language-python\"># Using Python list as stack\nstack = []\n\n# Push operations\nstack.append(1)\nstack.append(2)\nstack.append(3)\n\n# Pop operations\ntop_element = stack.pop()  # Returns 3\n</code></pre>\n<h2>Foleni</h2>\n<ul>\n<li>\n<p><strong>Red</strong>: Struktura podataka tipa First-In, First-Out (FIFO). Elementi se dodaju sa zadnje strane i uklanjaju sa prednje strane.</p>\n</li>\n<li>\n<p><strong>Operacija dodavanja u red</strong>: Dodavanje elementa na zadnji kraj reda. Vremenska složenost: O(1).</p>\n</li>\n<li>\n<p><strong>Operacija uklanjanja sa prednje strane koljke</strong>: Uklanjanje elementa sa prednje strane koljke. Vremenska složenost: O(1).</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">from collections import deque\n\n# Using deque for efficient queue operations\nqueue = deque()\n\n# Enqueue operations\nqueue.append(1)\nqueue.append(2)\nqueue.append(3)\n\n# Dequeue operations\nfirst_element = queue.popleft()  # Returns 1\n</code></pre>\n<h2>Povezane liste</h2>\n<ul>\n<li><strong>Povezana lista</strong>: Linearna struktura podataka gde svaki čvor sadrži podatke i referencu ka sledećem čvoru. Čvorovi su povezani kao lanac.</li>\n</ul>\n<h3>Samostalni povezani liste</h3>\n<ul>\n<li><strong>Struktura</strong>: Svaki čvor ima podatke i jednu referencu ka sledećem čvoru.</li>\n<li><strong>Traversiranje</strong>: Može se kretati samo napred, od glave do repa.</li>\n<li><strong>Čvor glave</strong>: Prvi čvor u listi, obično je jedini čvor dostupan direktno.</li>\n<li><strong>Čvor repa</strong>: Poslednji čvor u listi, prikazuje <code>None</code>.</li>\n</ul>\n<h3>Operacije vremenske složenosti</h3>\n<ul>\n<li><strong>Ubacivanje na početak</strong>: O(1)</li>\n<li><strong>Ubacivanje na kraj</strong>: O(n) - mora se proći do kraja</li>\n<li><strong>Ubacivanje u sredinu</strong>: O(n) - mora se proći do pozicije</li>\n<li><strong>Brisanje sa početka</strong>: O(1)</li>\n<li><strong>Brisanje sa kraja</strong>: O(n) - mora se proći da bi se dobio prethodni čvor</li>\n<li><strong>Brisanje u sredini</strong>: O(n) - mora se proći da bi se pronašao čvor</li>\n</ul>\n<h3>Dvostruke liste</h3>\n<ul>\n<li><strong>Struktura</strong>: Svaki čvor ima podatke i dve reference: sledeći čvor i prethodni čvor.</li>\n<li><strong>Traversiranje</strong>: Može se kretati u oba pravca.</li>\n<li><strong>Memorija</strong>: Zahteva više memorije nego samo spojene liste zbog dodatne reference.</li>\n</ul>\n<h2>Mapovi za hash na skupu</h2>\n<h3>Karte i karte za hash</h3>\n<ul>\n<li>\n<p><strong>Mapa (Apstraktni tip podataka)</strong>: Upravlja kolekcijama parova ključ-vrednost. Svaki ključ mora biti jedinstven, ali vrednosti mogu se ponavljati.</p>\n</li>\n<li>\n<p><strong>Hash mapa</strong>: Stvarno implementiranje ADT mape korišćenjem metode hasovanja (hashing). Koristi funkciju hasovanja da kreira vrednosti hasa za ključeve, koje određuju mesto skladištenja u osnovnom nizu.</p>\n</li>\n</ul>\n<h3>Rečnici za Python (Mapa za hash)</h3>\n<pre><code class=\"language-python\"># Creating dictionaries\nmy_dictionary = {\n    \"A\": 1,\n    \"B\": 2, \n    \"C\": 3\n}\n\n# Alternative creation\nmy_dictionary = dict(A=1, B=2, C=3)\n\n# Access and modify\nvalue = my_dictionary[\"A\"]  # 1\nmy_dictionary[\"A\"] = 4      # Update value\ndel my_dictionary[\"A\"]      # Remove key-value pair\n\n# Check membership\n\"C\" in my_dictionary\n\n# Get keys, values, items\nmy_dictionary.keys()\nmy_dictionary.values()\nmy_dictionary.items()\n</code></pre>\n<h3>Vremenska složenost za hash mape</h3>\n<ul>\n<li><strong>Prosečan slučaj</strong>: O(1) za ubacivanje, dohvaćanje, brisanje</li>\n<li><strong>Najgori slučaj</strong>: O(n) kada postoji mnogo kolizija hasha</li>\n</ul>\n<h3>Skup</h3>\n<ul>\n<li>\n<p><strong>Set:</strong> Neuređene kolekcije jedinstvenih elemenata. Nema duplikata, niti se održava specifičan redosled.</p>\n</li>\n<li>\n<p><strong>Samo nepromenljivi elementi</strong>: Setovi mogu sadržati nepromenljive tipove podataka (broj, niz karaktera, tuple) jer vrednosti hešovanja moraju ostati konstantne.</p>\n</li>\n</ul>\n<pre><code class=\"language-python\"># Creating sets\nnumbers = {1, 2, 3, 4}\nempty_set = set()  # Must use set(), not {}\n\n# Add and remove elements\nnumbers.add(5)\nnumbers.remove(4)      # Raises KeyError if not found\nnumbers.discard(4)     # No error if not found\n\n# Set operations\nset_a = {1, 2, 3, 4}\nset_b = {2, 3, 4, 5, 6}\n\n# Union, intersection, difference, symmetric difference\nset_a.union(set_b)                    # or set_a | set_b\nset_a.intersection(set_b)             # or set_a &#x26; set_b\nset_a.difference(set_b)               # or set_a - set_b\nset_a.symmetric_difference(set_b)     # or set_a ^ set_b\n\n# Subset and superset checks\nset_a.issubset(set_b)\nset_a.issuperset(set_b)\nset_a.isdisjoint(set_b)\n\n# Membership testing\n5 in numbers\n</code></pre>\n<h3>Vremenska složenost za skup</h3>\n<ul>\n<li><strong>Prosečan slučaj</strong>: O(1) za dodavanje, brisanje, proveru članstva</li>\n<li><strong>Najgori slučaj</strong>: O(n) usled kolizija haša</li>\n</ul>\n<h2>Kolizije haša</h2>\n<ul>\n<li>\n<p><strong>Kolizija hasha</strong>: Dešava se kada dva različita ključa generišu istu vrednost hasha.</p>\n</li>\n<li>\n<p><strong>Metode rešavanja kolizija</strong>:</p>\n<ul>\n<li><strong>Lančasto povezivanje (Chaining)</strong>: Svaki red prikazuje spojenu listu koja čuva sve elemente sa istom vrednošću hasha.</li>\n<li><strong>Otvorena adresa (Open Addressing)</strong>: Pronađi sledeći dostupan unos koristeći prethodno definisan niz.</li>\n</ul>\n</li>\n</ul>\n<h2>Kada koristiti svaku strukturu podataka</h2>\n<ul>\n<li><strong>Lista</strong>: Kada vam je potreban redosni pristup, definisana struktura i ne znate veličinu unapred</li>\n<li><strong>Stack</strong>: Za LIFO operacije (funkcija pop, evaluacija izraza, povratak nazad)</li>\n<li><strong>Red (Queue)</strong>: Za FIFO operacije (planiranje zadataka, pretraživanje po širini/BFS)</li>\n<li><strong>Povezana lista (Linked List)</strong>: Kada je potrebno često dodavanje/brisanje na početku, nepoznata veličina, nije potreban nasumični pristup</li>\n<li><strong>Hash mape (Hash Maps)</strong>: Za brzi pristup parovima ključ-vrednost, brojanje događaja, keširanje (cache)</li>\n<li><strong>Set (Skup)</strong>: Za proveru jedinstvenosti, operacije skupa za izračunavanje, uklanjanje duplikata</li>\n</ul>\n<h2>Algoritmi za pretraživanje</h2>\n<p>Algoritmi za pretraživanje omogućavaju pronalaženje cilja unutar određene liste stavki.</p>\n<p>U nauci o računarstvu, postoje dva algoritma pretraživanja koja morate znati. To su <strong>linearno pretraživanje</strong> i <strong>binarno pretraživanje</strong>. Važno je razumeti razliku između ova dva algoritma i kada koristiti svaki od njih.</p>\n<h3>Pretraživanje linije</h3>\n<ul>\n<li>Linearno pretraživanje iterira kroz niz elemenata, proveravajući svaki element od početka sve dok se ne pronađe željeni element.</li>\n<li>Ako se željeni element pronađe, vraća se element koji je u nizu.</li>\n<li>Ako željeni element nije pronađen, vraća <code>-1</code>, što znači <strong>nevažeći element</strong> u mnogim programskim jezicima.</li>\n<li>Jer linearno pretraživanje proverava svaki element sve dok ne pronađe željeni, nije efikasno za velike nizove elemenata.</li>\n<li>Vremenska složenost linearnog pretraživanja je <code>O(n)</code> jer vreme potrebno za pretragu kroz niz raste sa redom i veličinom niza.</li>\n<li>Prostorna složenost linearnog pretraživanja je <code>O(1)</code> jer ne zahteva dodatni prostor za pretragu kroz niz.</li>\n</ul>\n<h3>Binarna pretraga</h3>\n<ul>\n<li>Binarna pretraga radi tako što deli listu elemenata na pola, i proverava da li je ciljna vrednost u sredini liste.</li>\n<li>Uslov za binarnu pretragu je da elementi u listi moraju biti u rastućem redosledu.</li>\n<li>Binarna pretraga je najefikasniji algoritam za pretraživanje kroz veliku listu elemenata jer deli listu na pola i ignoriše pol koji ne sadrži cilj.</li>\n<li>Ako se ciljni element pronađe u sredini liste, vraća se indeks tog ciljnog elementa.</li>\n<li>Ako element nije pronađen, algoritam proverava da li je cilj u levom ili desnom delu liste.</li>\n<li>Nastavlja da deli preostale delove liste na pola sve dok se ciljni element ne pronađe.</li>\n<li>Ako ciljni element na kraju nije pronađen u listi, vraća se <code>-1</code>.</li>\n<li>Vremenska složenost binarnog pretraživanja je <code>O(log n)</code> jer vreme potrebno za pretraživanje kroz listu raste logaritamički sa veličinom liste.</li>\n<li>Prostorna složenost binarnog pretraživanja je <code>O(1)</code> jer ne zahteva dodatni prostor za pretraživanje kroz listu.</li>\n</ul>\n<h3>Kako se linearna pretraga razlikuje od binarne pretrage</h3>\n<ul>\n<li>Binarna pretraga je efikasnija za velike liste elemenata u poređenju sa linearnom pretragom.</li>\n<li>Vremenska složenost linearne pretrage je <code>O(n)</code> jer vreme potrebno za pretraživanje kroz listu raste linearno sa veličinom liste.</li>\n<li>Vremenska složenost binarne pretrage je <code>O(log n)</code> jer vreme potrebno za pretraživanje kroz listu raste logaritamski sa veličinom liste.</li>\n</ul>\n<h2>Algoritmi za sortiranje i metod razdvajanja-vladanja</h2>\n<p>U računarskoj nauci, podela i osvajanje je tehnika koja se koristi za razdvajanje problema na manje probleme kako bi bilo lakše da se reše. Rekurzija je metoda koja se često koristi u principu podele i osvajanja, i predstavlja moćnu strategiju koja se koristi za implementaciju mnogih efikasnih algoritama sortiranja kao što su <em>merge sort</em>.</p>\n<h3>Merge sort</h3>\n<ul>\n<li>Merge sort je sortirajući algoritam koji koristi pristup podelama i osvajanju (divide-and-conquer).</li>\n<li>Funkcioniše deljenjem niza rekursivno u sve manje nizove, sve dok svaki podniz ne sadrži samo jedan element.</li>\n<li>Nakon toga, te manji nizove ponovno spaja u sortirano poređenje.</li>\n<li>Vremenska složenost Merge sort-a je <code>O(n log n)</code> jer se niz ponavljajuće deli na pola <code>(log n)</code> i zatim spaja zajedno <code>(O(n))</code>.</li>\n<li>Prostorna složenost Merge sort-a je <code>O(n)</code> jer nije sortirajući algoritam koji se izvršava na mestu (in-place).</li>\n</ul>\n<h2>Sažetak grafa</h2>\n<p>Graf je skup čvorova (nodi) povezanih ivicama (vezaima). Svaki čvor može biti povezan sa mnogo drugih čvorova, stvarajući mrežu. Različiti tipovi grafova su:</p>\n<ul>\n<li>Usmereni: Grafovi imaju smer (od jednog čvora ka drugom), često se prikazuju pravim linijama i strelicama.</li>\n<li>Nesmerni: Grafovi nemaju smer, prikazuju se jednostavnim linijama.</li>\n<li>Čvorovi: Svaki čvor ima labelu ili identifikator.</li>\n<li>Sa ciklusima: Imaju cikluse (putanja koja počinje i završava na istom čvoru).</li>\n<li>Bez ciklusa (DAG): Nemaju cikluse.</li>\n<li>Grafovi sa etiketama/labelama: Svaki graf ima labelu, koja se obično piše blizu odgovarajućeg grafa.</li>\n<li>Sa težinama: Grafovi imaju težinu (vrednost) povezanu s njima, koju je moguće koristiti za izvođenje matematičkih operacija.</li>\n<li>Diskonektovani/Razdvojeni: Imaju dva ili više čvorova koja nisu povezana sa bilo kojim grafom.</li>\n</ul>\n<p>Graf se koristi u različitim aplikacijama kao što su mape, mreže, sistemi preporuka i rešavanje zavisnosti.</p>\n<h2>Kroz graf</h2>\n<p>Ovo uključuje posetu svih čvorova u grafu. Dva glavna algoritma su:</p>\n<ul>\n<li>\n<p><strong>Širokopredno pretraživanje (BFS)</strong></p>\n<ul>\n<li>Koristi red.</li>\n<li>Istražuje sloj po sloj.</li>\n<li>Pronalazi najkraći put u neotežnjenim grafikama.</li>\n</ul>\n</li>\n<li>\n<p><strong>Dubinska pretraga (DFS)</strong></p>\n<ul>\n<li>Koristi stos (ili rekurziju).</li>\n<li>Istražuje celu granu pre povratka (backtrackinga).</li>\n<li>Pogodan je za otkrivanje ciklusa i pronalaženje putanja.</li>\n</ul>\n</li>\n</ul>\n<h2>Reprezentacija grafa</h2>\n<p>Grafik se može predstaviti na dva glavna načina:</p>\n<ul>\n<li>\n<p><strong>Lista susjedstva</strong></p>\n<ul>\n<li>Svaki čvor ima listu svojih susjeda.</li>\n<li>Koristi malo memorije za razrijedke grafove.</li>\n<li>Lako je iterirati kroz susjedima.</li>\n</ul>\n</li>\n<li>\n<p><strong>Matrica susjednosti</strong></p>\n<ul>\n<li>Dvodimenzionalna matrica gdje redovi i kolone predstavljaju čvorove.</li>\n<li>Koristi mnogo prostora za velike grafove.</li>\n<li>Brzo provjeriti da li postoji veza između dva čvora.</li>\n</ul>\n</li>\n</ul>\n<h2>Drveće</h2>\n<p>Drvo je specijalna vrsta grafa koja nema cikluse i povezana je. Glavne karakteristike su:</p>\n<ul>\n<li>Nema ciklusa ili je cikličan (putanja gde su početni i krajnji čvor isti).</li>\n<li>Mora biti povezan (svaki čvor može biti dostignut iz bilo kog drugog čvora).</li>\n</ul>\n<h3>Uobičajene vrste drveća</h3>\n<p>Uobičajene vrste drveća su:</p>\n<ul>\n<li>\n<p>Binarna stabla</p>\n<ul>\n<li>Svaki čvor ima najmanje dva potomka, levi i desni potomak.</li>\n</ul>\n</li>\n<li>\n<p>Binarna stabla za pretraživanje (BST)</p>\n<ul>\n<li>Binarno stablo gde je svaki levi sin manji od svog roditelja, a svaki desni sin veći od svog roditelja.</li>\n</ul>\n</li>\n</ul>\n<h2>Miti o riječima (pokušaji)</h2>\n<p>Takođe se nazivaju stablo prefiksa i koriste se za skladištenje skupova sekvenci slova, gde svaki čvor predstavlja slovo.</p>\n<p>Prefiks se deli samo jednom, i efikasno radi za zadatke kao što su dovršavanje teksta i provera pravopisa.</p>\n<p>Operacije pretrage i umetanja imaju vremensku složenost od O(L), gde je L dužina niza znakova.</p>\n<h2>Funkcionalnosti visokog prioriteta</h2>\n<p>Red sa prioritetom je tip apstraktnih podataka gde svaki element ima prioritet.</p>\n<p>Red na staku uzima samo redosled unosa, dok prioritetni red koristi prioritet elemenata.</p>\n<p>Standardni redovi prate FIFO (Prvo uđe, prvo izađe), a stike prate LIFO (Poslednje uđe, prvo izađe). Međutim, u redovima sa prioritetom, elementi sa visokim prioritetom obrađuju se prvi bez obzira na redosede unosa.</p>\n<h2>Snop</h2>\n<p>To je struktura podataka specifičnog stabla sa posebnim svojstvom koje se zove klasterovo svojstvo.</p>\n<p>Svojstvo grafa određuje odnos između roditeljskog čvora i dječijeg čvora. Postoji dve vrste grafova:</p>\n<ul>\n<li>\n<p>Max-Heap</p>\n<ul>\n<li>Vrednost svakog roditeljskog čvora je veća ili jednaka vrednostima njegovih dece.</li>\n<li>Najveći element se nalazi na korenu.</li>\n</ul>\n</li>\n<li>\n<p>Minimalni heap (Min-Heap)</p>\n<ul>\n<li>Vrednost svakog roditeljskog čvora je manja ili jednaka vrednosti njegovih potomaka.</li>\n<li>Najmanji element se nalazi na korenu.</li>\n</ul>\n</li>\n</ul>\n<h3>Primer modula za <code>heapq</code> od Python</h3>\n<pre><code class=\"language-py\">import heapq\n\n# Create empty heap\nmy_heap = []\n\n# Insert elements\nheapq.heappush(my_heap, 9)\nheapq.heappush(my_heap, 3)\nheapq.heappush(my_heap, 5)\nprint(my_heap) # [3, 9, 5]\n\n# Remove smallest element\nprint(heapq.heappop(my_heap))  # 3\nprint(my_heap) # [5, 9]\n\n# Push + Pop in one step\nprint(heapq.heappushpop(my_heap, 7)) # 5\nprint(my_heap) # [7, 9]\n\n# Transform list into heap\nnums = [5, 7, 3, 1]\nheapq.heapify(nums)\n</code></pre>\n<h3>Korišćenje prioriteta</h3>\n<pre><code class=\"language-py\">my_heap = []\nheapq.heappush(my_heap, (3, \"A\"))\nheapq.heappush(my_heap, (2, \"B\"))\nheapq.heappush(my_heap, (1, \"C\"))\n\n# Removes lowest number = highest priority\nprint(heapq.heappop(my_heap))  # (1, \"C\")\n</code></pre>\n<h2>Uvod u program promena</h2>\n<ul>\n<li><strong>Definicija</strong>: Dinamičko programiranje je algoritamska tehnika koja rešava složene probleme tako što ih razlaže na manje podprobleme i skladišti rezultate kako bi se izbegli ponovni proračuni.</li>\n<li><strong>Preklapajući podproblemi</strong>: Preklapajući podproblemi su manji problemi koji se često javljaju prilikom rešavanja većeg problema. Umesto da se stalno ponovo računa, skladištimo njihova rešenja.</li>\n<li><strong>Optimalna struktura problema</strong>: Optimalno rešenje celog problema poseduje optimalna rešenja za manje podprobleme. To znači da možemo izgraditi optimalno rešenje kombinovanjem optimalnih rešenih manjih delova.</li>\n</ul>\n<h2>Rešenje za program promena</h2>\n<ul>\n<li><strong>Memoization (Top-down pristup)</strong>: Memoizacija čuva rezultate skupih poziva funkcija i vraća sačuvane rezultate kada se ponovo pojavi isti ulaz.</li>\n</ul>\n<pre><code class=\"language-py\">def climb_stairs_memo(n, memo={}):\n    \"\"\"Dynamic programming with memoization\"\"\"\n    # Check if we've already calculated this value\n    if n in memo:\n        return memo[n]  # Return cached result - O(1) lookup!\n    \n    # Base cases\n    if n &#x3C;= 2:\n        return n\n    \n    # Calculate once and store in memo for future use\n    memo[n] = climb_stairs_memo(n-1, memo) + climb_stairs_memo(n-2, memo)\n    return memo[n]\n</code></pre>\n<ul>\n<li><strong>Tabulacija (Metoda od dna ka vrhu)</strong>: Tabulacija izgrađuje rešenja od najnižeg nivoa, popunjavajući tabelu rešenjima za manje probleme.</li>\n</ul>\n<pre><code class=\"language-py\">def climb_stairs_tabulation(n):\n    \"\"\"Dynamic programming with tabulation\"\"\"\n    if n &#x3C;= 2:\n        return n\n    \n    # Create array to store results for all steps from 0 to n\n    dp = [0] * (n + 1)\n    dp[1] = 1  # 1 way to reach step 1\n    dp[2] = 2  # 2 ways to reach step 2\n    \n    # Build up the solution iteratively\n    for i in range(3, n + 1):\n        # Ways to reach step i = ways to reach (i-1) + ways to reach (i-2)\n        dp[i] = dp[i-1] + dp[i-2]\n    \n    return dp[n]\n</code></pre>\n<h2>Praktične primene programa koji koriste dinamičko programiranje</h2>\n<ul>\n<li><strong>Optimizacija putanje podataka</strong>: GPS sistemi koriste algoritme dinamičkog programiranja za pronalaženje najkraće rute između lokacija.</li>\n<li><strong>Obrada teksta</strong>: Funkcije provere pravopisa i automatskog dovršavanja teksta često se oslanjaju na dinamičko programiranje radi izračunavanja rastojanja editovanja između reči.</li>\n<li><strong>Finansijsko modelovanje</strong>: Investicione strategije i optimizacija portfolija često koriste metode dinamičkog programiranja.</li>\n<li><strong>Alokacija resursa</strong>: Problem plešaka (knapsack problem) i njegove varijante vide se u planiranju rasporeda, budžetiranje i upravljanje resursima.</li>\n</ul>\n<h2>Kada koristiti dinamičko programiranje</h2>\n<p>Trebalo bi razmotriti korišćenje dinamičkog programiranja u sledećim slučajevima:</p>\n<ul>\n<li>Problem se može podeliti na manje, ponavljajuće probleme.</li>\n<li>Problem pokazuje uzorak malih delova.</li>\n<li>Standardno rešenje koje se ponavlja bi zahtevalo ponavljajuće proračune.</li>\n<li>Moraš poboljšati u smislu vremenske složenosti na štetu prostorne složenosti.</li>\n</ul>\n</section>",
  "translationPending": false,
  "block": "review-python",
  "blockLabel": "review",
  "blockLayout": "link",
  "hasEditableBoundaries": false,
  "order": 79,
  "instructions": "",
  "questions": [],
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 0,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "review-python",
  "certification": "python-v9"
}