---
id: 66ed9002f45ce3ece4053eb6
title: Pokušao sam da napravim lažni CSS stil za klasu
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je lažna diploma?

#### --distractors--

To je pravilo za CSS koje kreira dodatni sadržaj unutar sekcije kada je potrebno.

---

Svojstvo koje menja veličinu ili raspored elementa na stranici.

---

Oni su selektori koji se koriste za dodavanje efekata kretanja elementu tokom interakcije.

#### --answer--

To je ključna reč dodata selektoru koja postavlja stil za element u zavisnosti od njegovog stanja.

### --question--

#### --text--

Koja je vrsta klase koja se koristi kada se uređaj za prikaz postavi na element?

#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--

Koja je veštačka funkcija koja vam omogućava stilizovanje prvog slova paragrafa?

#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--

Koji veštački klasa menja stil elementa kada se klikne?

#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--

Koja pseudo-klasa se koristi za postavljanje stila elementu kada je spreman da primi unos korisnika, ako se tekstualno polje aktivira klikom ili tabulatorom?

#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--

Koje pravilo za CSS među ovim dodaje tekst `Note:` pre svakog elementa paragrafa koji ima klasu `note`?

#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--

Koju pseudoklasu koristiti za polje za unos kada je izabrano ili fokusirano?

#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--

Koja je tačna sintaksa za CSS da se izabere poslednji element liste?

#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--

Koji je lažni obrazac koji cilja na polja za unos koja nemaju potrebe da se popune?

#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--

Šta radi lažni kurs `:disabled`?

#### --distractors--

Postavljate stil za odabrana polja unosa.

---

Postavlja stil za istaknute elemente.

---

Odaberite elemente koji ne odgovaraju određenom selektoru.

#### --answer--

Postavlja stil na elemente koji nisu dostupni putem interakcije korisnika.

### --question--

#### --text--

Koja je lažna klasa koja se koristi kada deo forme zadovolji kriterijume validacije?

#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--

Zar među ovim nije lažan lokalni kurs?

#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--

Koji od ovih selektuje treći element iz liste?

#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--

Koje će komponente imati `color` od `blue` za sljedeće CSS?

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--

Šta radi lažna klasa `:not()`?

#### --distractors--

Postavi stilove na sve elemente.

---

Izaberi sva deca roditelja.

---

Izaberite elemente koji odgovaraju određenom filteru.

#### --answer--

Odaberite elemente koji ne odgovaraju određenom selektoru.

### --question--

#### --text--

Ovo pravilo za CSS šta radi?

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--

Izaberite prvi element iz `p` u dokumentu.

---

Izaberite sve elemente iz `p` u dokumentu.

---

Izaberite prvo dete svakog elementa `p`.

#### --answer--

Odaberite prvi element od `p` unutar roditeljskog kontejnera.

### --question--

#### --text--

Šta radi lažna škola za `:last-of-type`?

#### --distractors--

Biraš prvo dete određenog tipa unutar svog roditelja.

---

On bira srednjeg deteta određenog tipa unutar svoje porodice/roditelja.

---

On bira svako dete određene vrste unutar svog roditelja.

#### --answer--

Izaberi poslednje dete određene vrste unutar svojih roditelja.

### --question--

#### --text--

Koji je veštački sloj korišćen za odabir drugog elementa u listi?

#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--

Koji je među ovim lažni kurs sa aktivnostima?

#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--

Koja od ovih je lažna lekcija sa aktivnostima?

#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--

Koja pseudo-klasa se koristi za ciljanje omogućenih elemenata forme?

#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--

Koju lažnu/veštačku klasu koristite koja vam omogućava da izaberete elemente brojeći od kraja?

#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--

Da li vam bilo šta od ovoga omogućava da izaberete funkcije koje imaju posebne karakteristike za decu?

#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--

Koji od ovih bira elemente koji nemaju sadržaj ili bilo koja potomka?

#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--

Kome je namenjena ova anketa o CSS?

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--

Svi ulazni elementi.

---

Svi uniznosni elementi sa vrednostima u dozvoljenom opsegu.

---

Sva polja za unos koja prolaze validaciju.

#### --answer--

Sva polja unosa koja nisu prošla validaciju.

### --question--

#### --text--

Koja je pseudo-klasa koja bira polja za unos čija vrednost je automatski popunjena od strane pregledača?

#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--

Koji je veštački modul koji bira karakteristiku ako on ili njegovo potomstvo ima pažnje?

#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--

Koja je lažna klasa koja predstavlja linkove koji ukazuju na isti dokument?

#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--

Koji od ovih postavlja stil za element `p` kada je cilj komada URL?

#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--

Koja je dummy klasa korišćena kada je element cilj komada od URL?

#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--

Veštačka klasa za `:only-child` šta bira?

#### --distractors--

Izaberi roditelja koji ima samo jedno dete.

---

Izabere sva deca unutar roditelja.

---

Izaberite roditelja koji ima samo jednu vrstu dece.

#### --answer--

Izaberite element koji nema brata niti sestre unutar svog roditelja.

### --question--

#### --text--

Koji umjetni razred bira karakteristiku ako je jedinstven takvog roda unutar svog roditelja?

#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--

Koje pravilo od CSS će koristiti `color` iz `yellow` za drugi element/stavku `p` u sledećem HTML?

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--

Koji pseudo-element vam omogućava da izaberete markere stavki liste za stilizovanje?

#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--

Koja je pseudo-klasa koja vam omogućava da ciljate element najvišeg nivoa u dokumentu, obično element `html`?

#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--

Kakva je veštačka klasa za CSS koja ima nultu sposobnost, osiguravajući da neće kolidirati sa drugim specifičnim pravilima stila?

#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--

Koji od ovih je veštačka klasa za strukturu stabla?

#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--

Koje pravilo CSS će postaviti `background-color` za sledeći element da bude `red` ako je njegova vrednost izvan definisanog opsega?

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--

Koje generičke komponente koriste atribut `content` za ubacivanje sadržaja nakon komponente?

#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--

Koji veštački model generiše obrasce za osobinu ako je njena vrednost unutar definisanog opsega?

#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`