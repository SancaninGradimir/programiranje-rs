---
id: 66f1ae758b77cfc3e4da6151
title: Pokušaj sa TypeScript-om
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

TypeScript Šta je to?

#### --distractors--

Sistem koji se koristi za sistemske programe.

---

Alat za upravljanje bazom podataka.

---

Programski jezik koji se koristi za dekorisanje mreža.

#### --answer--

Najnovija verzija JavaScript koja dodaje stabilnu klasifikaciju.

### --question--

#### --text--

Zašto se TypeScript bi trebalo koristiti u projektu?

#### --distractors--

Uklanja potrebu za testiranjem u programu/aplikaciji.

---

Brže je od svih programskih jezika.

---

Olakšava dekorisanje mrežama.

#### --answer--

Poboljšava upravljanje dodavanjem specifikacije tipa za JavaScript.

### --question--

#### --text--

Koji jezik TypeScript se prevodi u?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Šta bi bio rezultat ovog komada koda ispod?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Koja je glavna svrha interfejsa u TypeScript?

#### --distractors--

Izvršiti kod od TypeScript do JavaScript.

---

Kreirati novi tip podataka.

---

Kreiranje nove stvari za JavaScript.

#### --answer--

Definisati strukturu objekta.

### --question--

#### --text--

Koji među sledećim nije osnovni tip u TypeScript?

#### --distractors--

string

---

boolean

---

broj

#### --answer--

array

### --question--

#### --text--

Koji od sledećeg definiše tip literala tačno u TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Koja je glavna reč u reči `type` unutar TypeScript?

#### --distractors--

Objavi kriterijume.

---

Kreiranje klasa.

---

Implementiranje interfejsa.

#### --answer--

Specificirati specifičan tip.

### --question--

#### --text--

Koji od sledećih definiše tip konekcije/poveznice?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Koja je sposobnost među onima `interface` koje su jedinstvene i nemoguće za dostizanje korišćenjem `type` u TypeScript?

#### --distractors--

Opisati oblike stvari.

---

Kreiranje tipova konekcija.

---

Definisanje tipova koji uključuju samo specifične vrednosti.

#### --answer--

Objavljivanje sprovođenja časova.

### --question--

#### --text--

Šta je glavna svrha korišćenja generika u TypeScript?

#### --distractors--

Implementacija nasleđa među razredima.

---

Definisati strukturu objekta.

---

Kreirati novi tip podataka.

#### --answer--

Pisanje ponovno upotrebljivog koda koji može raditi sa više tipova.

### --question--

#### --text--

Koji od sljedećih je tačan primjer generičke funkcije u TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

Šta znači *type narrowing* u TypeScript?

#### --distractors--

Smanite veličinu prevedenog JavaScript.

---

Smanjiti broj tipova u konekciji.

---

Uklanjanje nepotrebnih tipova iz projekta TypeScript.

#### --answer--

Pisanje koda koji sužava tip veze na specifičniji tip.

### --question--

#### --text--

Koji fajlovi se obično koriste za konfigurisanje opcija kompajlera za TypeScript?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

Šta znači izbor `rootDir` u fajlu `tsconfig.json`?

#### --distractors--

Verzija TypeScript koja se koristi tokom prevoda.

---

Okruženje za izvršavanje (primer, Node.js ili pretraživač) za kod TypeScript.

---

Posebni modulski sistem koji se koristi za import i export.

#### --answer--

Kabrasha koji čuva vaše izvorne fajlove.

### --question--

#### --text--

Koji primer koristi `instanceof` da smanji vrstu i dobije siguran pristup atributu `value`?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Šta znači staviti `"strict": true` u `tsconfig.json` unutar TypeScript?

#### --distractors--

Omogućava vam da izvršite stabilne provere kako biste pronašli greške pre pokretanja vašeg koda.

---

Podstičem te da koristiš ES6 sintaksu svaki put.

---

Preporučuje se da prevedete kod pre izvršavanja.

#### --answer--

Omogućava set opcija za reviziju tipa kao što je zahtevati obradu `null` ili `undefined` radi onemogućavanja `any` koja nije jasna.

### --question--

#### --text--

Svrha tipa `never` u TypeScript je šta?

#### --distractors--

Predstavlja null ili undefined.

---

Da predstavlja bilo koju moguću vrednost.

---

Predstavlja klasa koja može biti bilo šta osim null.

#### --answer--

Predstavlja određeni tip vrednosti koje ne mogu da se dogode.

### --question--

#### --text--

Koji je poziv funkcije za `getRandomValue` koji će biti uspešno preveden u TypeScript?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Kako možete popraviti grešku kompajlera za TypeScript `'email' is possibly 'null'` u sledećem kodu koristeći sekvencijalno povezivanje?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`