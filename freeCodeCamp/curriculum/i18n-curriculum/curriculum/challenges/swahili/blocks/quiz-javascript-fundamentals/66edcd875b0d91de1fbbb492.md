---
id: 66edcd875b0d91de1fbbb492
title: Pokušaj sa osnovama JavaScript-a
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji od ovih će stvoriti nešto za String?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Možete li konvertovati nešto što je String u običan niz znakova?

#### --distractors--

Za komponentu `String()`.

---

Putem `.stringify()`.

---

Zatvori nešto `String` sa dvostrukim navodnicima.

#### --answer--

Kroz `.toString()`.

### --question--

#### --text--

Zašto možete koristiti metode kao `.length`, `.repeat()`, `.concat()`, i `.slice()` u sekvencu izvornih slova u JavaScript?

#### --distractors--

Ove putanje postoje inherentno u sekvenci prirodnih slova.

---

Niz izvornih slova se čuva kao interni objekti.

---

JavaScript konvertuje sekvencu znakova u niz podataka.

#### --answer--

JavaScript privremeno zaključava izvorni niz slova u objektu String.

### --question--

#### --text--

Šta će sledeći kod prikazati na konzoli?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Koja od sledećih vraća vrednost za `Number` iz sekvence karaktera `"123"`?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

`Number(true)` na `Number(false)` šta vraćaju, respektivno?

#### --distractors--

`undefined` na `undefined`

---

`NaN` na `NaN`

---

`true` i `false`

#### --answer--

`1` na `0`

### --question--

#### --text--

`Number(undefined)` na `Number(null)` šta će vratiti, respektivno?

#### --distractors--

`NaN` na `NaN`

---

`0` na `0`

---

`undefined` na `null`

#### --answer--

`NaN` na `0`

### --question--

#### --text--

Koji od sljedećih je najbolji naziv za booleanski atribut/polje koje provjerava da li korisnik ima dozvolu da nešto uradi na vašem web sajtu?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Koji od sljedećih je najbolji naziv za funkciju koju korisnik izvršava prilikom slanja forme?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

Šta je red podataka sa prazninom?

#### --distractors--

Red podataka gde jedan ili više indikatora ima vrednost `null`.

---

Podatni red gde jedan ili više indikatora ima vrednost od `undefined`.

---

Redak podataka gde jedan ili više indikatora imaju null vrednost.

#### --answer--

Red podataka gde jedan ili više indikatora nisu dodeljene vrednosti.

### --question--

#### --text--

Koji od sledećih primera `arr` neće biti red podataka sa propustom?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Šta je glavni cilj lintera?

#### --distractors--

Automatski formatiranje koda.

---

Poboljšati performanse koda.

---

Napravite dokumentaciju koda.

#### --answer--

Identifikovati greške i probleme koji mogu nastati u kodu.

### --question--

#### --text--

Koja je korist korišćenja formatera u kodu?

#### --distractors--

Omogućava programerima da efikasnije prikupljaju kod.

---

Konvertuje kod u specifičan programski jezik.

---

Identifikujte logičke greške.

#### --answer--

Osigurajte da se stil koda održava doslednim.

### --question--

#### --text--

U JavaScript, ko ili šta obično ima odgovornost za alokiranje memorije parametrima?

#### --distractors--

Razvojnici

---

Operativni sistem.

---

Usługa

#### --answer--

Ostali detalji vremena izvršenja.

### --question--

#### --text--

Šta će se pojaviti na konzoli za sledeći kod?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

Hoisting u JavaScript šta je?

#### --distractors--

Uvoditi greške na samom početku koda.

---

Proces kreiranja novih parametara.

---

Automatsko prebacivanje deklaracije za `var` na sekciju.

#### --answer--

Proces prebacivanja deklaracija parametara unutar svog opsega.

### --question--

#### --text--

Zašto je korišćenje glavne reči od `var` smatrano neprikladnim u savremenom JavaScript?

#### --distractors--

Sporo koliko `let` na `const`.

---

Uzrokuje sintaktičke greške.

---

Njegove vrste/tipove nije moguće videti.

#### --answer--

Nalaze se van njihovog dela.

### --question--

#### --text--

Šta je svrha modula u JavaScript?

#### --distractors--

Poboljšati performanse koda.

---

Učiniti kod teško razumljivim.

---

Kreiranje novih programskih jezika.

#### --answer--

Strukturirati kod u ponovo upotrebljive module/jedinice.

### --question--

#### --text--

Kako dodajete skriptu `app.js` kao module u fajl HTML?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Koji od sledećih primera prenosi funkcionalnost `calculateSum` iz modula `utils.js` i dovršava prenos i upotrebu u fajlu `app.js`?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
