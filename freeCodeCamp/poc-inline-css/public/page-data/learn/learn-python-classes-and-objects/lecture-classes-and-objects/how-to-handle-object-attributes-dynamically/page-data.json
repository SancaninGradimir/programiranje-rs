{"componentChunkName":"component---src-templates-challenges-generic-show-tsx","path":"/learn/learn-python-classes-and-objects/lecture-classes-and-objects/how-to-handle-object-attributes-dynamically","result":{"data":{"challengeNode":{"challenge":{"assignments":[],"bilibiliIds":null,"block":"lecture-classes-and-objects","challengeType":19,"description":"<section id=\"description\">\n<p>U početnoj obuci, naučili ste o svojstvima elemenata kao atributima koje poseduje element. To znači da čuvaju podatke koji definišu stanje ili karakteristiku tog elementa.</p>\n<p>Na primer, običan automobil ima marku i model. Marka i model mogu biti svojstva elemenata za klasu <code>Car</code>:</p>\n<pre><code class=\"language-python\">class Car: \n    def __init__(self, brand, model): \n        self.brand = brand \n        self.model = model \n\nmy_car = Car('Lamborghini', 'Gallardo') \nprint(my_car.brand) # Lamborghini \nprint(my_car.model) # Gallardo \n</code></pre>\n<p>Ali ponekad ne znate koji vam atributi trebaju dok vaš program počne da se izvršava. Zamislite da pišete skriptu koja prima imena atributa od korisnika ili iz fajla za konfiguraciju. To nisu atributi koje možete fiksirati (hardkodovati) u kodu na samom početku.</p>\n<p>Ovo je mesto gde ulazi u pitanja rukovanja svojstvima dinamički. Na ovaj način, možete pristupiti, menjati, pregledavati ili čak brisati svojstva koristeći njihova imena kao parametre, a ne kao fiksna imena u vašem kodu. Ovo vašem programu daje sposobnost prilagođavanja kako bi reagovao na različite podatke ili unos korisnika u realnom vremenu.</p>\n<p>Python daje vam četiri ugrađena funkcije za rad sa svojstvima elemenata putem promena. One su <code>getattr()</code>, <code>setattr()</code>, <code>hasattr()</code>, i <code>delattr()</code>.</p>\n<p>Omogućava vam da dobijete pristup, kreirate, pregledate i brišete atribute koristeći imena parametara. Hajde da istražimo svaki od njih kako funkcioniše.</p>\n<p><code>getattr()</code> omogućava čitanje svojstva iz elementa kada ne znate njegovo ime do vremena izvršavanja. Ako svojstvo ne postoji, vraća <code>AttributeError</code>, osim ako ne pružite podrazumevanu vrednost.</p>\n<p>Da bi je koristio, prosleđuješ objekat, ime osobine i podrazumevanu vrednost po potrebi:</p>\n<pre><code class=\"language-python\">getattr(object, attribute_name, default_value) \n</code></pre>\n<p>Evo primera:</p>\n<pre><code class=\"language-python\">class Person: \n    def __init__(self, name, age): \n        self.name = name \n        self.age = age \n\nperson = Person('John Doe', 30) \n \nprint(getattr(person, 'name')) # John Doe \nprint(getattr(person, 'age')) # 30 \nprint(getattr(person, 'city', 'Milano')) # Milano\n</code></pre>\n<p>U gornjem primeru, <code>Milano</code> je zadana vrednost jer <code>city</code> nije u klasi <code>Person</code>.</p>\n<p>Kao što smo ranije rekli, stvarna snaga <code>getattr()</code> se pojavljuje kada naziv atributa dolazi iz parametra, kao što je iz unosa korisnika ili nekog fajla.</p>\n<p>U tom slučaju, ne možete koristiti standardnu sintaksu od <code>object.attribute_name</code> jer ime atributa nije stabilno.</p>\n<pre><code class=\"language-python\">class Person: \n    def __init__(self, name, age): \n        self.name = name \n        self.age = age \n\nperson = Person('John Doe', 30)\n\nattr_name = input('Enter the attribute you want to see: ')\nprint(getattr(person, attr_name, 'Attribute not found'))\n</code></pre>\n<p>U ovom slučaju, ako korisnik unese <code>name</code>, videće <code>John Doe</code>, a ako unese <code>age</code>, videće <code>30</code>. Ako unese nešto što ne postoji u klasi kao što je <code>email</code>, videće <code>Attribute not found</code>.</p>\n<p>Ovo je zapravo prednost obrade atributa na dinamičan način. Omogućava vašem kodu da reaguje na unos i podatke koje nikada ranije nije video.</p>\n<p>Štaviše, možda želite da pregledate sve svojstva koja ima element, a ne samo ona koja već znate. Ugrađena funkcija za <code>dir()</code> vam omogućava da to uradite. Vraća listu svih imena svojstava na elementu. Evo kako ga koristiti:</p>\n<pre><code class=\"language-python\">class Person: \n    def __init__(self, name, age): \n        self.name = name \n        self.age = age \n\nperson = Person('John Doe', 30)\n\n# Loop through all attributes of the person object with dir() function\nfor attr in dir(person):\n    # Ignore dunder methods like __init__ or __str__ and regular methods\n    if not attr.startswith('__') and not callable(getattr(person, attr)): \n        value = getattr(person, attr)\n        print(f'{attr}: {value}')\n\n# Output\n# age: 30\n# name: John Doe\n</code></pre>\n<p>U gornjem kola, <code>callable()</code> je ugrađena funkcija koja vraća <code>True</code> ako prosleđeni element može biti nazvan funkcijom ili metodom, i <code>False</code> inače. Kada se pregleda <code>not callable(getattr(person, attr))</code>, kola preskače metodu i ispisuje samo svojstva podataka kao što su <code>name</code> i <code>age</code>.</p>\n<p>Funkcija za <code>setattr()</code> omogućava vam da kreirate novu osobinu ili ažurirate postojeću na način promena. Sintaksa je sledeća:</p>\n<pre><code class=\"language-python\">setattr(object, attribute_name, value) \n</code></pre>\n<p>Ovde je primer koji postavlja konfiguracione atribute na osnovu podataka iz konfiguracionog fajla ili varijable okruženja:</p>\n<pre><code class=\"language-python\">class Configuration:\n    pass\n\n# Data loaded at runtime (like from a config or env file)\nsettings_data = {\n    'server_url': 'https://api.example.com',\n    'timeout_sec': 30,\n    'max_retries': 5\n}\n\nconfig_obj = Configuration()\n\n# Dynamically set attributes using dictionary keys and values\nfor attr_name, attr_value in settings_data.items():\n    setattr(config_obj, attr_name, attr_value)\n\nprint(config_obj.server_url) # https://api.example.com\nprint(config_obj.timeout_sec) # 30\n</code></pre>\n<p>Takođe postoji <code>hasattr()</code>. Pre nego što nešto uradite sa nekom funkcijom ili je obrišete, dobra praksa je proveriti da li postoji. To je <code>hasattr()</code> što vam omogućava da to uradite. Proverava da li funkcija postoji i vraća <code>True</code> ili <code>False</code> u zavisnosti od rezultata.</p>\n<p>Ovo je osnovna sintaksa:</p>\n<pre><code class=\"language-python\">hasattr(object, attribute_name)  \n</code></pre>\n<p>Ovde je primer koji proverava promenu prisutnosti atributa u strukturi klase <code>Product</code>:</p>\n<pre><code class=\"language-python\">class Product:\n    def __init__(self, name, price):\n        self.name = name\n        self.price = price\n\nproduct_a = Product('T-Shirt', 25)\n\nrequired_attributes = ['name', 'price', 'inventory_id']\n\nfor attr in required_attributes:\n    if not hasattr(product_a, attr):\n        print(f\"ERROR: Product is missing the required attribute: '{attr}'\")\n    else:\n        # Access the attributes dynamically once their existence is confirmed\n        print(f'{attr}: {getattr(product_a, attr)}')\n\n# Output:\n# name: T-Shirt\n# price: 25\n# ERROR: Product is missing the required attribute: 'inventory_id'\n</code></pre>\n<p>Greška je nastala zato što <code>inventory_id</code> ne postoji u klasi <code>Product</code> i njegovoj instanci.</p>\n<p>Konačno, <code>delattr()</code> vam omogućava da obrišete svojstva putem promena:</p>\n<pre><code class=\"language-python\">delattr(object, attribute_name) \n</code></pre>\n<p>Na primer, zamislite da je karakteristika završila obradu, pa odlučite da očistite bilo koje osetljive ili privremene atribute koji mogu postojati pre snimanja konačne verzije. Nakon toga, možete koristiti <code>dir()</code> oko preostalih atributa:</p>\n<pre><code class=\"language-python\">class UserSession:\n    def __init__(self, user_id, token):\n        self.user_id = user_id\n        self.auth_token = token # sensitive\n        self.temp_counter = 0 # temporary\n\nsession = UserSession(101, 'a1b2c3d4e5')\n\n# List of attributes to remove dynamically before \"saving\" the session\nattributes_to_clean = ['auth_token', 'temp_counter']\n\n# Dynamically remove specified attributes\nfor attr in attributes_to_clean:\n    if hasattr(session, attr):\n        delattr(session, attr)\n        print(f'Removed attribute: {attr}')\n\nprint('\\nFinal attributes remaining:')\n\n# Loop through the remaining attributes with dir()\nfor attr in dir(session):\n    # Ignore dunder methods like __init__ or __str__ and regular methods\n    if not attr.startswith('__') and not callable(getattr(session, attr)):\n        print(f' - {attr}: {getattr(session, attr)}')\n\n# Output:\n# Removed attribute: auth_token\n# Removed attribute: temp_counter\n\n# Final attributes remaining:\n#  - user_id: 101\n</code></pre>\n<p>Tako možeš da obrađuješ atribute putem promena!</p>\n</section>","nodules":null,"explanation":null,"helpCategory":"Python","instructions":"","fields":{"slug":"/learn/learn-python-classes-and-objects/lecture-classes-and-objects/how-to-handle-object-attributes-dynamically"},"questions":[{"text":"<p>Šta vam <code>getattr()</code> funkcija u Python omogućava da do?</p>","answers":[{"answer":"<p>Čitaj svojstva iz elementa kada mu ime nije poznato do vremena izvršavanja.</p>","feedback":null,"audioId":null},{"answer":"<p>Obriši atribut sa elementa.</p>","feedback":"<p>Zamislite koja je funkcija koja vam pomaže <em>da dobijete</em> značajke putem transformacije.</p>","audioId":null},{"answer":"<p>Postavi novu osobinu na element.</p>","feedback":"<p>Razmislite o kojem alatu vam pomaže da <em>dobijete</em> funkcije putem promena.</p>","audioId":null},{"answer":"<p>Proveri da li atribut postoji u polju.</p>","feedback":"<p>Razmislite o funkciji koja vam pomaže da <em>dobijete</em> karakteristike putem transformacije.</p>","audioId":null}],"solution":1},{"text":"<p>Zašto biste želeli da obradite svojstva elementa putem promena u Python?</p>","answers":[{"answer":"<p>Promeniti svojstvo u direktnu listu.</p>","feedback":"<p>Razmislite o odgovaranju na unos korisnika ili eksterne podatke putem transformacija.</p>","audioId":null},{"answer":"<p>Da se svojstva čitaju samo tokom izvršavanja.</p>","feedback":"<p>Razmislite o odgovaranju na unos korisnika ili eksterne podatke putem transformacija.</p>","audioId":null},{"answer":"<p>Smanite broj kreiranih klasnih atributa.</p>","feedback":"<p>Razmislite o odgovaranju na korisnički unos ili spoljašnje podatke putem transformacije.</p>","audioId":null},{"answer":"<p>Raditi sa funkcionalnostima čije imena nisu poznata do vremena implementacije.</p>","feedback":null,"audioId":null}],"solution":4},{"text":"<p>Koja je tačna sintaksa za proveru da li element ima određena svojstva u Python?</p>","answers":[{"answer":"<p><code>checkattr(object, attribute_name)</code></p>","feedback":"<p>Zamislite kakvu funkciju čiji naziv znači \"ima osobine.\"</p>","audioId":null},{"answer":"<p><code>hasattr(object, attribute_name)</code></p>","feedback":null,"audioId":null},{"answer":"<p><code>setattr(object, attribute_name)</code></p>","feedback":"<p>Zamislite koju funkciju čije ime znači „ima atribute“.</p>","audioId":null},{"answer":"<p><code>getattr(object, attribute_name)</code></p>","feedback":"<p>Zamisli koja je funkcija čije ime znači \"ima osobine.\"</p>","audioId":null}],"solution":2}],"scene":null,"superBlock":"learn-python-classes-and-objects","tests":[],"title":"Kako rukovati svojstvima elementa putem promena?","transcript":null,"translationPending":false,"videoId":null,"videoLocaleIds":null}}},"pageContext":{"challengeMeta":{"blockHashSlug":"/learn/learn-python-classes-and-objects/#lecture-classes-and-objects","dashedName":"how-to-handle-object-attributes-dynamically","certification":"learn-python-classes-and-objects","disableLoopProtectTests":false,"disableLoopProtectPreview":false,"superBlock":"learn-python-classes-and-objects","chapter":null,"module":null,"block":"lecture-classes-and-objects","isFirstStep":false,"template":null,"required":[],"isLastChallengeInBlock":true,"nextChallengePath":"/learn/learn-python-classes-and-objects/workshop-musical-instrument-inventory/step-1","prevChallengePath":"/learn/learn-python-classes-and-objects/lecture-classes-and-objects/what-are-special-methods-and-what-are-they-used-for","id":"6908d38f7cccc31602743340","saveSubmissionToDB":null},"projectPreview":{"challengeData":{"challengeType":19,"challengeFiles":[]}},"id":"/learn/learn-python-classes-and-objects/lecture-classes-and-objects/how-to-handle-object-attributes-dynamically"}},"staticQueryHashes":["3858520367"],"slicesMap":{}}