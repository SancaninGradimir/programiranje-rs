---
id: 66edd07682767adff3a6231e
title: Kratak eksperiment upravljanja DOM-om i događajima klika za JavaScript
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta znači model dokumenta (Document Object Model)?

#### --distractors--

Programski jezik koji se koristi za interakciju sa stranicom HTML.

---

Skup elemenata koji sadrže samo decimalne brojeve.

---

Skup klasa za JavaScript koji predstavlja komponente od HTML.

#### --answer--

Programski interfejs koji omogućava interakciju sa dokumentima za HTML.

### --question--

#### --text--

API Zašto staje?

#### --distractors--

Jednostavnost Napremenog Softvera

---

Dodavanje aplikativnog interfejsa

---

Drugi interfejs programa

#### --answer--

Interfejs programa

### --question--

#### --text--

Da li je moguće koristiti JavaScript da se dobiju sve tagove za ankere od HTML koje nemaju hipertekstualne veze?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

Šta radi atribut `textContent`?

#### --distractors--

Briše tekstualni sadržaj iz svih komponenti HTML.

---

Briše tekstualni sadržaj iz vašeg fajla JavaScript.

---

Dodaje tekst svim redovima podataka i stavkama u vaš fajl JavaScript.

#### --answer--

Obnavlja standardni tekstualni sadržaj elementa.

### --question--

#### --text--

Svojstvo `innerHTML`, šta radi?

#### --distractors--

To je karakteristika `Element` koja se koristi za dobijanje svih tekstualnih sadržaja za element/funkciju HTML.

---

To je karakteristika `Element` koja se koristi za dodavanje detalja unutar vašeg HTML.

---

Funkcija `Element` koja se koristi za izazivanje događaja u vašem HTML.

#### --answer--

To je funkcija `Element` koja se koristi za postavljanje ili ispravljanje interpunkcijskih znakova za HTML.

### --question--

#### --text--

Koji od sljedećih će kreirati validnog rukovaoca događaja?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

Nijedan od ovih nije validan način za `document`?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

Koja od ovih će postaviti svojstvo `class` na `my-class`?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

Šta je `Event`?

#### --distractors--

Postoji nešto specifično što se koristi za uklanjanje događaja iz vašeg projekta JavaScript.

---

Koristi se za dodavanje događaja direktno u vaše projekte JavaScript.

---

To je nešto specifično koje se koristi prilikom rada sa nizovima podataka.

#### --answer--

To je opterećenje koje izaziva događaj kada korisnik interaguje sa vašim veb-sajtom na neki način.

### --question--

#### --text--

Koji je metod korišćen za uklanjanje rukovatelja događaja koji su prethodno dodati na karakteristiku koristeći metodu `addEventListener()`?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

Šta je inline obrađivač događaja?

#### --distractors--

Posebne karakteristike na elementu HTML koje se koriste za brisanje svih događaja.

---

Specifični atributi na HTML elementu koji se koriste samo za pokretanje slanja forme u vašem kodu iz JavaScript-a.

---

Specifične karakteristike na elementu HTML koje se koriste za brisanje događaja.

#### --answer--

Specifične osobine na elementu HTML koje se koriste za izvršavanje koda JavaScript kada dođe događaj.

### --question--

#### --text--

Koje su dve glavne komponente/stvari od API za mrežu?

#### --distractors--

API za Java na API za treće strane.

---

API za vinariju na API za Rust.

---

API za sistem na API za treće strane.

#### --answer--

API za vinarije na API za treće strane.

### --question--

#### --text--

Koji događaj se pokreće kada su svi elementi stranice HTML parsirani od strane pregledača?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

Koje svojstvo od ovih predstavlja unutrašnji stil elementa?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

Koja je ispravna način za korišćenje rukovaoca događaja unutar linije?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

Koji od ovih puteva se koristi za kašnjenje akcije na određeno vreme?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

Koji od ovih se može koristiti za kreiranje i kontrolisanje animacijskih putanja direktno unutar JavaScript?

#### --distractors--

API omogućavanje dijagrama kretanja

---

API za kontrolisanje crteža pokreta

---

API za kreiranje crteža pokreta

#### --answer--

API mrežnih dijagrama

### --question--

#### --text--

API za crtežno platno (Canvas API) šta radi?

#### --distractors--

Koristi se za animiranje crteža u vašoj datoteci CSS.

---

Koristi se samo za uklanjanje crteža iz vaše datoteke JavaScript.

---

Koristi se za uklanjanje crteža iz vašeg fajla HTML.

#### --answer--

Koristi se za upravljanje crtežima putem vašeg fajla JavaScript.

### --question--

#### --text--

Šta znači delegiranje događaja (event delegation)?

#### --distractors--

Proces slušanja događaja kada je događaj obrisan.

---

Proces slušanja događaja kada dođe klik događaj.

---

Proces praćenja događaja kada nastupi promenjajni događaj.

#### --answer--

Proces slušanja događaja koji su dosegli roditelja.

### --question--

#### --text--

Da li je bilo koji od ovih načina način koji možete koristiti kada vodite razgovor sa modalom?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`