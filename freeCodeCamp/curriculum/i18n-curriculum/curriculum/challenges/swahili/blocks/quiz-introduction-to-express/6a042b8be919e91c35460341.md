---
id: 6a042b8be919e91c35460341
title: Uvod u kratak test Express-a
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Kakav je odnos između API i usluge mreže?

#### --distractors--

Kila API je mrežna usluga, ali nije svaka mrežna usluga API.

---

API na usluge mrežne komunikacije je isti koncept.

---

Usluge mreže rade samo sa REST, dok API podržavaju mnoge protokole.

#### --answer--

Neka svaka mrežna usluga bude API, ali neku API nije mrežna usluga.

### --question--

#### --text--

Koji je tip HTTP koji klijent koristi da serveru kaže kakav željeni format odgovora?

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

Šta znači "bez stanja" u arhitekturi REST?

#### --distractors--

Čuva podatke sesije klijenta između aplikacija.

---

Klijent ne čuva nijedno stanje samostalno između aplikacija.

---

REST uh ograniči sve aplikacije da budu samo za čitanje.

#### --answer--

Server skladišti istoriju prethodnih zahteva.

### --question--

#### --text--

Koja putanja za HTTP od REST se koristi za uklanjanje resursa?

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

Koja je glavna prednost mikroservisa u poređenju sa monolitom?

#### --distractors--

Popravljanje grešaka lakše sa manjim mrežnim pozivom između servisa.

---

Jednostavan zajednički kod koji doprinose svi timovi.

---

Jednostavno upravljanje infrastrukturom za sve usluge.

#### --answer--

Brzi razvoj, bolja izolacija grešaka i mogućnost korišćenja različitih jezika za svaku uslugu.

### --question--

#### --text--

Šta vraća `express()`?

#### --distractors--

Nova usluga HTTP.

---

Navigačka funkcija.

---

Funkcionalnost centralnog programa.

#### --answer--

Novi primer programa za Express.

### --question--

#### --text--

Šta je Middleware u Express?

#### --distractors--

Interna putanja koja se koristi za uspostavljanje servera Express.

---

Specifična vrsta putanje za HTTP koja se koristi u API za REST.

---

Alat izgrađen unutra za serviranje statičkih fajlova iz direktorijuma.

#### --answer--

Kod koji radi između primanja zahteva i slanja odgovora.

### --question--

#### --text--

`app.listen(port, callback)` hufanya nini katika Express?

#### --distractors--

Definiše putanju podataka za dolazne zahtjeve.

---

Pakuje biblioteku Express.

---

Napravite novi modul za centralnu aplikaciju.

#### --answer--

Pokrenite server i počnite da slušate dolazne zahteve za HTTP.

### --question--

#### --text--

Šta je graničnik podataka u Express?

#### --distractors--

Put za URL koji je postavljen kao `/home`.

---

Sekvenca pretraga argumenata dodaje se na URL.

---

Put za serviranje statičkih fajlova.

#### --answer--

Instanca u URL koja ima prefiks od `:` koji drži promenljivu vrednost.

### --question--

#### --text--

Kako se dohvaća promenljiva vrednost koja je zadržana iz URL kao `/post/42` u putanji podataka Express definisanoj kao `/post/:postId`?

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

`res.json()` hufanya nini katika Express?

#### --distractors--

Pošaljite odgovor u običnom tekstu.

---

Uputite klijenta na različit URL.

---

Ovaj prikazuje pogled na mašinu šablona i šalje HTML koji proističe.

#### --answer--

Šalje odgovor za JSON i automatski menja nešto vezano za JavaScript.

### --question--

#### --text--

`res.redirect(url)` hufanya nini katika Express?

#### --distractors--

Unosite broj stanja za HTTP.

---

Pošalji odgovor za JSON.

---

Prikazuje pogled koristeći engine šablona.

#### --answer--

Uputite klijenta za različito URL.

### --question--

#### --text--

Svrha `app.route()` je šta u Express?

#### --distractors--

Kreiraj novi primer aplikacije Express.

---

Služiti statičke fajlove iz definisanog direktorijuma.

---

Objašnjavanje različitih načina za URL.

#### --answer--

Povezivanje handler-a za više ruta/putanja HTTP u jednu liniju u jednom segmentu.

### --question--

#### --text--

`express.Router()` šta to pravi?

#### --distractors--

Novi primer programa za Express koji modifikuje `app` rukovanjem svu server logiku.

---

Handler funkcija centralnog softvera posvećena hvatanju i obradi grešaka na nivou data veze.

---

Ugrađeni statički server fajlova koji renderuje fajlove putem putanja URL direktno.

#### --answer--

Komponenta za rutiranje, kao mala aplikacija Express, koja postavlja staze podataka i unosi ih u glavnu aplikaciju.

### --question--

#### --text--

Kako postaviti ruta planer u glavni program Express?

#### --distractors--

Za pozivanje `router.listen()`.

---

Za pozivanje `router.start()`.

---

Da pozove `app.get()` direktno unutar putfindera.

#### --answer--

Za pozivanje `app.use(path, router)`.

### --question--

#### --text--

Koji Middleware, izgrađen unutar Express, se koristi za serviranje statičkih fajlova?

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

Šta se dešava kada korisnik traži statičku datoteku koja ne postoji u konfigurisanom direktorijumu `express.static()`?

#### --distractors--

Express vraća grešku na serveru i ne uspeva.

---

Express usmerava korisnika na početnu stranicu.

---

Express kreira praznu datoteku u specificiranoj putanji.

#### --answer--

Express direktno odgovara sa 404.

### --question--

#### --text--

Koja od sljedećeg je istinito o SOAP u poređenju sa REST?

#### --distractors--

SOAP koristi JSON i lakši je od REST, pa je bolji za moderne aplikacije.

---

SOAP je vrsta usluge na mreži koja se najviše koristi danas, zauzima mesto REST u mnogim sistemima.

---

SOAP podržava različite formate odgovora, uključujući običan tekst, JSON, i binarne podatke.

#### --answer--

SOAP koristi XML, što je samo uobičajeno u korporativnim sistemima koji zahtevaju stroge standarde i bezbednost.

### --question--

#### --text--

Šta je rukovodilac (ili vođica) putanje podataka u Express?

#### --distractors--

Konfiguracioni element koji mapira sve dostupne putanje podataka i njihove putanje za HTTP.

---

Funkcija glavnog programa koja obrađuje i služi statičke fajlove direktno iz jedne foldere.

---

Sloj baze podataka koji obrađuje i upravlja problemima baze podataka vezanim za putanju podataka.

#### --answer--

Funkcija povratnog poziva koja se izvršava kada zahtev odgovara putanji podataka, i dobija pristup `req` i `res`.

### --question--

#### --text--

Koja je korist od korišćenja `express.Router()` umesto definisanja svih puteva direktno u `app.js`?

#### --distractors--

Omogućava serveru da obrađuje mnogo zahteva za HTTP istovremeno.

---

Generiše i registruje ekstenziju API za REST automatski na osnovu imena fajlova putanje podataka.

---

Omogućava serveru da se pokrene i radi bez potrebe za specifikovanjem broja porta.

#### --answer--

Razdvaja probleme i čini glavni fajl čistim premeštavanjem povezanih metoda u sopstvene module.