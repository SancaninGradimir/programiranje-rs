---
id: 694470b9a65b278f981ec62e
title: Korak 5
challengeType: 1
dashedName: step-5
---

# --description--

Konačno, pokušajte sa svojom funkcijom `countup` pozivanjem sa argumentom `5` unutar deklaracije `console.log()`.

Rezultat bi trebalo da bude `[1, 2, 3, 4, 5]`.

# --hints--

Morate pozvati funkciju `countup` i listu `5` unutar deklaracije `console.log()`

```js
assert.deepStrictEqual(countup(5), [1, 2, 3, 4, 5]);
assert.match(code, /console\.log\s*\(\s*countup\s*\(\s*5\s*\)\s*\)/)
```

# --seed--

## --seed-contents--

```js
function countup(number) {
  let countArray = [];
  if (number < 1) {
    return [];
  } else {
    countArray = countup(number - 1);
    countArray.push(number);
    return countArray;
  }
}
--fcc-editable-region--

--fcc-editable-region--
```

# --solutions--

```js
function countup(number) {
  let countArray = [];
  if (number < 1) {
    return [];
  } else {
    countArray = countup(number - 1);
    countArray.push(number);
    return countArray;
  }
}
console.log(countup(5));
```
