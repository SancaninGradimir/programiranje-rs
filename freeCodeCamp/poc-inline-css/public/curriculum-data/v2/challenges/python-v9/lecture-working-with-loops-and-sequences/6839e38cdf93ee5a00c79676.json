{
  "id": "6839e38cdf93ee5a00c79676",
  "title": "Koje su najčešće korišćene metode za liste?",
  "challengeType": 19,
  "dashedName": "what-are-some-common-methods-used-for-lists",
  "solutions": [],
  "questions": [
    {
      "text": "<p>Koji primer iz sledećih će tačno uneti broj 2,5 u polje 2 u listi brojeva?</p>",
      "answers": [
        {
          "answer": "<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insertInto(2.5, 2)\n</code></pre>",
          "feedback": "<p>Zapamtite da prvi model predstavlja mesto gde želite da unesete, a drugi model predstavlja element koji želite da unesete.</p>",
          "audioId": null
        },
        {
          "answer": "<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insert(2, 2.5)\n</code></pre>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insertInto(2, 2.5)\n</code></pre>",
          "feedback": "<p>Zapamtite da prvi model predstavlja mesto gde želite da unesete, a drugi model predstavlja element koji želite da unesete.</p>",
          "audioId": null
        },
        {
          "answer": "<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insert(2.5, 2)\n</code></pre>",
          "feedback": "<p>Zapamti da prvi argument predstavlja mesto na koje želiš da umetneš element, a drugi predstavlja element koji želiš da umetneš.</p>",
          "audioId": null
        }
      ],
      "solution": 2
    },
    {
      "text": "<p>Koji način iz ovih će premestiti listu elemenata na svoje mesto?</p>",
      "answers": [
        {
          "answer": "<p><code>reverse()</code></p>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<p><code>reversing()</code></p>",
          "feedback": "<p>Proveri poslednji deo obuke za odgovor.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>reversedList()</code></p>",
          "feedback": "<p>Pregledajte poslednji deo lekcije za odgovor.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>reversingList()</code></p>",
          "feedback": "<p>Pregledajte poslednji deo lekcije za odgovor.</p>",
          "audioId": null
        }
      ],
      "solution": 1
    },
    {
      "text": "<p>Da li je bilo šta od ovoga putanja/metoda za listu?</p>",
      "answers": [
        {
          "answer": "<p><code>pop()</code></p>",
          "feedback": "<p>Ovaj od ovih načina se koristi u JavaScript. Nije Python.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>clear()</code></p>",
          "feedback": "<p>Ovo se koristi u JavaScript. Nije Python.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>splice()</code></p>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<p><code>append()</code></p>",
          "feedback": "<p>Ovo od ovih metoda se koristi u JavaScript. Nije Python.</p>",
          "audioId": null
        }
      ],
      "solution": 3
    }
  ],
  "assignments": [],
  "tests": [],
  "description": "<section id=\"description\">\n<p>U prethodoj lekciji naučio si o tipu podataka lista i kako da pristupiš elementima liste i isečeš listu. U ovoj lekciji nastavićeš da učiš o listama i nekim uobičajenim metodama koje se koriste sa njima, kao što su <code>append()</code>, <code>pop()</code> i <code>sort()</code>.</p>\n<p>Prva metoda o kojoj ćemo govoriti je <code>append()</code>. Ona se koristi za dodavanje elementa na kraj liste. Evo primera korišćenja metode <code>append()</code> za dodavanje broja <code>6</code> u listu brojeva:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<p>Ako želiš da dodaš jednu listu na kraj druge, možeš takođe da koristiš metodu <code>append()</code> na sledeći način:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.append(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, [6, 8, 10]]\n</code></pre>\n<p>Obrati pažnju da je cela lista <code>even_numbers</code> ugnježdena unutar liste <code>numbers</code>.</p>\n<p>Ali ako želiš da dodaš sve lične brojeve iz liste <code>even_numbers</code> na kraju liste <code>numbers</code>, onda možeš koristiti metodu <code>extend()</code>.</p>\n<p>Putanja za <code>extend()</code> je ista kao putanja za <code>append()</code>, ali sa <code>extend()</code> možete dodati mnogo elemenata iz jedne liste u drugu. Evo primera dodavanja brojeva <code>6</code>, <code>8</code> i <code>10</code> iz jedne liste na kraju liste za <code>numbers</code>:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.extend(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, 6, 8, 10]\n</code></pre>\n<p>Kao što vidiš, ugnježdena lista je uklonjena i sada je to samo lista brojeva.</p>\n<p>Ako želiš da umetneš element na određeno mesto u listi, možeš da koristiš metodu <code>insert()</code>. Ova metoda prihvata dva argumenta: indeks na koji želiš da umetneš novi element i element koji želiš da umetneš.</p>\n<p>Evo primera korišćenja metode <code>insert()</code>:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insert(2, 2.5)\n\nprint(numbers) # [1, 2, 2.5, 3, 4, 5]\n</code></pre>\n<p>Sledeći kod će umetnuti broj <code>2.5</code> na indeks <code>2</code> u listi <code>numbers</code>.</p>\n<p>Ako želiš da ukloniš element iz liste, možeš da koristiš metodu <code>remove()</code>. Metoda <code>remove()</code> kao argument prima vrednost elementa koji želiš da ukloniš:</p>\n<pre><code class=\"language-py\">numbers = [10, 20, 30, 40, 50, 50]\nnumbers.remove(50)\n\nprint(numbers) # [10, 20, 30, 40, 50]\n</code></pre>\n<p>Važno je znati da ovaj metod će ukloniti samo prvo pojavljivanje elementa. Ne sve:</p>\n<pre><code class=\"language-py\">numbers = [10, 20, 30, 40, 50, 50, 50]\nnumbers.remove(50)\n\nprint(numbers) # [10, 20, 30, 40, 50, 50]\n</code></pre>\n<p>Da biste uklonili element koji se nalazi u određenom polju u listi, možete koristiti metodu <code>pop()</code>, kao što sledi:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop(1) # The number 2 is returned\n</code></pre>\n<p>Ako ne navedete element putem <code>pop</code>, vraća se poslednji element.</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop() # The number 5 is returned\n</code></pre>\n<p>Ako želite da uklonite ceo spisak, onda možete koristiti metodu <code>clear()</code> kao što sledi:</p>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.clear()\n\nprint(numbers) # []\n</code></pre>\n<p>Sledeći metod koji ćemo razgovarati je metoda <code>sort()</code>. Ovaj metod se koristi za raspoređivanje elemenata na njihovo mesto. Evo primera raspoređivanja liste brojeva po redosuku na njihovo mesto:</p>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nnumbers.sort()\n\nprint(numbers) # [1, 2, 19, 35, 41, 67]\n</code></pre>\n<p>Za razliku od metode <code>sort()</code>, postoji funkcija <code>sorted()</code> koja radi za sve što se može iterirati i vraća novu sortiranu listu umesto da modifikuje originalnu listu. Primer:</p>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nsorted_numbers = sorted(numbers)\n\nprint(numbers) # [19, 2, 35, 1, 67, 41]\nprint(sorted_numbers) # [1, 2, 19, 35, 41, 67]\n</code></pre>\n<p>Kao podsjetnik, nešto što se može ponoviti je posebna vrsta stvari koje možete ponoviti, omogućavajući vam da dobijete svaki element direktno. Naučićete više o tome kako rade kola u Python u sledećoj lekciji.</p>\n<p>Metoda <code>sort()</code> i funkcija <code>sorted()</code> prihvataju opcione parametre <code>key</code> i <code>reverse</code>. Saznaćete više o ovim opcionalnim parametrima u sledećoj lekciji kada počnete da učite o tuplovima. Takođe ćete saznati više o drugim ugrađenim funkcijama kao što je <code>sorted()</code> u narednim lekcijama.</p>\n<p>Sledeća metoda koju ćemo razmotriti je metoda <code>reverse()</code>. Ova metoda će promeniti spisak komponenti na mestu, kao što sledi:</p>\n<pre><code class=\"language-py\">numbers = [6, 5, 4, 3, 2, 1]\nnumbers.reverse()\n\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<p>Poslednja metoda koju ćemo pomenuti je <code>index</code>. Ovo se koristi za dobijanje prvog primera gde je funkcija dostupna u listi. Evo primera korišćenja metode <code>index</code> za pretraživanje jezika <code>'Java'</code> u listi <code>programming_languages</code>:</p>\n<pre><code class=\"language-py\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\nprogramming_languages.index('Java') # 1\n</code></pre>\n<p>Ako element ne može biti pronađen, onda Python vraća <code>ValueError</code>:</p>\n<pre><code class=\"language-py\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\nprogramming_languages.index('JavaScript')\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nValueError: 'JavaScript' is not in list\n\"\"\"\n</code></pre>\n<p>Postoji nekoliko drugih lista ruta od Python, ali ova početna lista ruta je dobro mesto za početak.</p>\n</section>",
  "translationPending": false,
  "block": "lecture-working-with-loops-and-sequences",
  "blockLabel": "lecture",
  "blockLayout": "challenge-list",
  "hasEditableBoundaries": false,
  "order": 19,
  "instructions": "",
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 1,
  "isLastChallengeInBlock": false,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-loops-and-sequences",
  "certification": "python-v9"
}