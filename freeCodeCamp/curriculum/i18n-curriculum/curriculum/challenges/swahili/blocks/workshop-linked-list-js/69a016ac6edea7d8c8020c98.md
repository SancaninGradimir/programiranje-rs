---
id: 69a016ac6edea7d8c8020c98
title: Korak 9
challengeType: 1
dashedName: step-9
---

# --description--

Sada, kreiraj petlju `while` sa `current.next !== null` kao uslovom. To znači da bi petlja trebalo da radi sve dok trenutni čvor ima drugi čvor posle sebe.

# --hints--

You should create a `while` loop with the condition `current.next !== null`.

```js
assert.match(__helpers.removeJSComments(add.toString()), /while\s+\(current\.next\s*!==\s*null\)\s*\{\s*/);
```

# --seed--

## --seed-contents--

```js
function initList() {
    return {
        head: null,
        length: 0
    };
}

function isEmpty(list) {
  return list.length === 0;
}

function add(list, element) {
    const node = {
        element: element,
        next: null
    };

    if (isEmpty(list)) {
        list.head = node;
    } else {
        let current = list.head;
        --fcc-editable-region--

        --fcc-editable-region--
    }

    list.length++;
}

const myList = initList();
console.log(isEmpty(myList))
add(myList, 42);
console.log(myList)
console.log(isEmpty(myList))
```
