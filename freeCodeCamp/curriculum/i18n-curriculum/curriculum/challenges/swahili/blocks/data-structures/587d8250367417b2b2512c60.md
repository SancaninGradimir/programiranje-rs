---
id: 587d8250367417b2b2512c60
title: Kreiraj klasu za redove
challengeType: 1
forumTopicId: 301631
dashedName: create-a-queue-class
---

# --description--
Kao i staci, redovi su kolekcija elemenata. Ali za razliku od staka, redovi prate princip FIFO (First In First Out – Prvo ušlo, prvo izađe). Elementi koji se dodaju u red ubacuju se na zadnji deo reda, a uklanjanju je dozvoljeno samo element koji se nalazi na prednjem kraju reda.

Možemo koristiti strukturu podataka da predstavimo redove, ali kao i stike, želimo smanjiti nivo kontrole koji možemo imati na vrhu naših redova.

Dve glavne metode klase reda su `enqueue` i `dequeue`. Metoda `enqueue` ubacuje element na zadnji kraj reda, dok metoda `dequeue` uklanja i vraća element koji se nalazi na prednjem kraju reda. Ostale važne metode su `front`, `size` i `isEmpty`.

# --instructions--
Napišite metodu za `enqueue` koja ubacuje element na zadnji kraj reda, metodu za `dequeue` koja uklanja i vraća prednji element, metodu za `front` koja nam omogućava da vidimo prednji element, metodu za `size` koja prikazuje veličinu, i metodu za `isEmpty` koja proverava da li je red prazan.

# --hints--
Vaša klasa za `Queue` treba da ima metodu `enqueue`.

```js
assert(
  (function () {
    var test = new Queue();
    return typeof test.enqueue === 'function';
  })()
);
```

Darasa lako la `Queue` bi trebalo da ima metodu za `dequeue`.

```js
assert(
  (function () {
    var test = new Queue();
    return typeof test.dequeue === 'function';
  })()
);
```

Vaša klasa za `Queue` treba da ima metodu `front`.

```js
assert(
  (function () {
    var test = new Queue();
    return typeof test.front === 'function';
  })()
);
```

Vaša klasa za `Queue` treba da ima metodu `size`.

```js
assert(
  (function () {
    var test = new Queue();
    return typeof test.size === 'function';
  })()
);
```

Vaša klasa za `Queue` bi trebalo da ima metodu `isEmpty`.

```js
assert(
  (function () {
    var test = new Queue();
    return typeof test.isEmpty === 'function';
  })()
);
```

Putanja `dequeue` treba da ukloni i vrati prednji element reda

```js
assert(
  (function () {
    var test = new Queue();
    test.enqueue('Smith');
    test.enqueue('John');
    return test.dequeue() === 'Smith';
  })()
);
```

Putanja `front` treba da vrati vrednost prednjeg elementa redova

```js
assert(
  (function () {
    var test = new Queue();
    test.enqueue('Smith');
    test.enqueue('John');
    return test.front() === 'Smith';
  })()
);
```

Putanja `size` bi trebalo da vrati dužinu reda

```js
assert(
  (function () {
    var test = new Queue();
    test.enqueue('Smith');
    return test.size() === 1;
  })()
);
```

Putanja `isEmpty` mora vratiti `false` ako postoje elementi u folderu

```js
assert(
  (function () {
    var test = new Queue();
    test.enqueue('Smith');
    return !test.isEmpty();
  })()
);
```

# --seed--

## --seed-contents--

```js
function Queue() {
  var collection = [];
  this.print = function() {
    console.log(collection);
  };
  // Only change code below this line

  // Only change code above this line
}
```

# --solutions--

```js
function Queue () { 
    var collection = [];
    this.print = function() {
        console.log(collection);
    };
    // Only change code below this line
    this.enqueue = function(item) {
        collection.push(item);
    }

    this.dequeue = function() {
        return collection.shift();
    }

    this.front = function() {
        return collection[0];
    }

    this.size = function(){
        return collection.length;
    }

    this.isEmpty = function() {
        return collection.length === 0 ? true : false;
    }
    // Only change code above this line
}
```
