---
id: 66f1a417757b6ca4eecd89d6
title: Pokušaj fajla stanja sa hookovima za React
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji je ispravan način za dodavanje događaja klika na dugme u React?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

React Synthetic Event šta je?

#### --distractors--

Tip JSX elementa za događaj `inputs` i `submit` samo.

---

Tip JSX elementa za događaj `submit` samo.

---

Način menjanja faze širenja događaja kako bi bio prijateljskiji za korisnika.

#### --answer--

Paket prirodnih događaja kao događaja `click` i `submit`.

### --question--

#### --text--

Koji od sljedećih je prefiks koji se obično koristi za nazive metoda rukovaoca događaja?

#### --distractors--

isHandling

---

hasHandled

---

obrada

#### --answer--

handle

### --question--

#### --text--

Koje su tri glavna koraka u renderovanju (rendering) za React?

#### --distractors--

Handshake, pakovanje na hidrataciju.

---

Ekstrakcija podataka, interpretacija i implementacija.

---

Dodavanje, ažuriranje i brisanje.

#### --answer--

Stimulisati, prikazati i odobriti.

### --question--

#### --text--

Koji od sljedećih je ispravan način za rad sa hook-om `useState`?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Koja od sledećih karakteristika nije ispravna za rukovanje događajima React?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Koja je ispravna metoda iz sledećih za ažuriranje stanja reda podataka?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

Šta je pogrešno u ovom funkciji?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Ažuriranje varijable `useState` nije prihvaćeno unutar definisane funkcije.

---

`previousState` Treba ga obuhvatiti uglastim zagradama.

---

Funkcija strelice ne sme se koristiti unutar funkcije za postavljanje stanja.

#### --answer--

`previousState` mora se sačuvati pre `name` ili osigurati da druge atribute nisu promenjene.

### --question--

#### --text--

Koja od sledećih metoda je ispravan način za ažuriranje stanja kako bi se uklonili elementi iz redova podataka?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

Zašto se svojstvo `ref` obično koristi u React?

#### --distractors--

Koristi se za ispitivanje problema performansi u delovima React-a.

---

Koristi se za proveru problema sa pristupačljivošću u delovima React-a.

---

Koristi se za uklanjanje čvorova DOM.

#### --answer--

Koristi se za dobijanje pristupa čvora DOM.

### --question--

#### --text--

Ili između sledećih puteva je tačan način za dobijanje trenutne vrednosti `ref`?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Koliko će se ova poruka ispisati u konzoli?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Tri puta.

---

Dva puta.

---

Nema razloga jer je niz zavisnosti prazan.

#### --answer--

Samo jednom prilikom prvog prikaza.

### --question--

#### --text--

Hooks specifični za React, za šta se obično koriste?

#### --distractors--

Koriste se za izvršavanje složenijih zadataka programa.

---

Retko se koriste, preporučuju se unutrašnji hookovi.

---

Zamenjuje putanje životnog ciklusa iz prethodnih verzija React.

#### --answer--

Pomažu u ponovnom korišćenju logike sa stanjem na raznim mestima.

### --question--

#### --text--

Koliko će se ova poruka ispisati na konzolu?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Biće beleženo svaki put kada se formular pošalje ili kada dođe događaj klika.

---

Biće napisano dva puta.

---

Biće napisano samo jednom.

#### --answer--

Ovaj deo će biti prikazan ponovo svaki put.

### --question--

#### --text--

Koja od sledećih je ispravan način za sprečavanje osvežavanja pretraživača za događaj `onSubmit`?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Koji od sledećih hookova se obično koristi za dohvaćanje podataka?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Koje od sledećih svojstava nije validno na nativnom Event objektu?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Koji od koraka pri prikazivanju se dešava kada React detektuje da se nešto promenilo i korisnički interfejs možda zahteva ažuriranje?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Koji od ovih koraka se tiče kada React uzima promene pripremljene iz virtuelnog DOM i postavlja ih u stvarni DOM?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Koja je namena `dependencies` u `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

Opcijalni list za `dependencies` nema nikakvog efekta u vezi sa `useEffect`.

---

Opciono polje `dependencies` uzrokuje da efekat nastavi da radi neograničeno.

---

Opcionalni argument za `dependencies` se koristi za sprečavanje izvršavanja efekta.

#### --answer--

Opcionalni list za `dependencies` kontroliše kada se efekat pokreće.