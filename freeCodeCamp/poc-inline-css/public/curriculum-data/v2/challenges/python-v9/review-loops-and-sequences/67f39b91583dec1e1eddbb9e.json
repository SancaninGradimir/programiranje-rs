{
  "id": "67f39b91583dec1e1eddbb9e",
  "title": "Serijska inspekcija i ciklusi",
  "challengeType": 31,
  "dashedName": "review-loops-and-sequences",
  "solutions": [],
  "assignments": [
    "<p>Proverite podatke i pojmove za cikluse i sekvence.</p>"
  ],
  "tests": [],
  "description": "<section id=\"description\">\n<h2>Alati za Python</h2>\n<ul>\n<li><strong>Uvod</strong>: U Python, tip podataka liste je organizovan red elemenata koji mogu biti niz slova, brojeva ili čak drugih lista. Lista je dinamična i ima indeksiranje koje počinje od nule.</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\n</code></pre>\n<ul>\n<li><strong>Pristup elementima u listi</strong>: Da biste dobili element iz liste <code>cities</code>, možete se osvrnuti na njegov indeks u nizu:</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\ncities[0] # Los Angeles\n</code></pre>\n<ul>\n<li><strong>Dohvatanje elemenata korišćenjem negativnog selektora</strong>: Da biste dobili poslednji element bilo koje liste, možete koristiti <code>-1</code> kao identifikator selektora:</li>\n</ul>\n<pre><code class=\"language-python\">cities = ['Los Angeles', 'London', 'Tokyo']\ncities[-1] # Tokyo\n</code></pre>\n<ul>\n<li>\n<p>Negativni obrazac se koristi za dobijanje elemenata počevši od kraja liste, a ne od početka, za obrazac <code>0</code>.</p>\n</li>\n<li>\n<p><strong>Kreiranje liste korišćenjem komponente <code>list()</code></strong>: Liste se mogu kreirati takođe i koristeći komponentu <code>list()</code>. Komponenta <code>list()</code> koristi se za konverziju ponovljivog elementa u listu:</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">developer = 'Jessica'\n\nprint(list(developer)) \n# Result: ['J', 'e', 's', 's', 'i', 'c', 'a']\n</code></pre>\n<ul>\n<li><strong>Dobijanje dužine liste</strong>: Možete koristiti funkciju <code>len()</code> da dobijete dužinu liste:</li>\n</ul>\n<pre><code class=\"language-python\">numbers = [1, 2, 3, 4, 5]\nlen(numbers) # 5\n</code></pre>\n<ul>\n<li><strong>Izmena liste</strong>: Lista je dinamička, što znači da možete izmeniti bilo koji element unutar liste samo ako dostavite validan ključni broj. Da biste izmenili listu za određeni ključ, možete postaviti novu vrednost za taj ključ:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\nprogramming_languages[0] = 'JavaScript'\nprint(programming_languages) # ['JavaScript', 'Java', 'C++', 'Rust']\n</code></pre>\n<ul>\n<li><strong>Greška modela izvan opsega</strong>: Ako unesete parametar (pozitivan ili negativan) koji je van granica liste, dobićete <code>IndexError</code>:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\nprogramming_languages[10] = 'JavaScript'\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nIndexError: list assignment index out of range\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Uklanjanje stavki iz liste</strong>: Stavke se mogu ukloniti sa liste korišćenjem ključne reči <code>del</code>:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Jane Doe', 23, 'Python Developer']\ndel developer[1]\nprint(developer) # ['Jane Doe', 'Python Developer']\n</code></pre>\n<ul>\n<li><strong>Provera da li je element u listi</strong>: Glavna funkcija <code>in</code> može se koristiti za proveru da li je element u listi:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ['Python', 'Java', 'C++', 'Rust']\n\n'Rust' in programming_languages # True\n'JavaScript' in programming_languages # False\n</code></pre>\n<ul>\n<li><strong>Ugnježđivanje lista unutar drugog lista</strong>: Liste se mogu organizovati/porediti unutar drugog lista:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\n</code></pre>\n<ul>\n<li>Da biste dobili interno sortirani spisak, moraćete da ga dobijete koristeći <code>2</code> jer liste imaju indeks koji počinje od nule.</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\ndeveloper[2] # ['Python', 'Rust', 'C++']\n</code></pre>\n<ul>\n<li>Da biste dobili drugi jezik iz liste koja je unutra, moraćete ga dobiti koristeći šablon <code>1</code>:</li>\n</ul>\n<pre><code class=\"language-python\">developer = ['Alice', 25, ['Python', 'Rust', 'C++']]\ndeveloper[2][1] # Rust\n</code></pre>\n<ul>\n<li><strong>Izvlačenje vrednosti iz liste:</strong> Izvlačenje vrednosti iz liste je metoda koja se koristi za postavljanje vrednosti iz liste za nove parametre. Evo primera izvlačenja liste za <code>developer</code> za nove parametre nazvane <code>name</code>, <code>age</code> i <code>job</code>, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ['Alice', 34, 'Rust Developer']\nname, age, job = developer\n</code></pre>\n<ul>\n<li>\n<p>Ako broj parametara na levi strani operatera dodeljivanja ne odgovara ukupnom broju elemenata u spisku, dobićete <code>ValueError</code>.</p>\n</li>\n<li>\n<p><strong>Sakupljanje preostalih stavki sa liste</strong>: Da biste sakupljali bilo koje preostale stavke sa liste, možete koristiti zvezdastu operaciju (<code>*</code>) kao što sledi:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">developer = ['Alice', 34, 'Rust Developer']\nname, *rest = developer\n</code></pre>\n<ul>\n<li><strong>Delimična obrada liste</strong>: Procesiranje je koncepcija dobijanja dela liste korišćenjem operatora za procesiranje <code>:</code>. Da biste obradili listu počevši od elementa <code>1</code> i završili pre elementa <code>3</code>, možete koristiti sledeću sintaksu:</li>\n</ul>\n<pre><code class=\"language-python\">desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']\ndesserts[1:3] # ['Cookies', 'Ice Cream']\n</code></pre>\n<ul>\n<li><strong>Inkrementalni korak</strong>: Takođe je moguće specificirati inkrementalni korak koji određuje koliko se vrednost povećava. Evo primera ako želite da izbacite samo listu pozitivnih celih brojeva koristeći obradu:</li>\n</ul>\n<pre><code class=\"language-python\">numbers = [1, 2, 3, 4, 5, 6]\nnumbers[1::2] # [2, 4, 6]\n</code></pre>\n<h2>Načini za listu</h2>\n<ul>\n<li><strong>append()</strong>: Koristi se za dodavanje elementa na kraj liste. Evo primera korišćenja metode <code>append()</code> za dodavanje broja <code>6</code> u listu <code>numbers</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<ul>\n<li><strong>Dodavanje druge liste</strong>: Metoda <code>append()</code> se takođe može koristiti za dodavanje jedne liste na kraj druge:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.append(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, [6, 8, 10]]\n</code></pre>\n<ul>\n<li><strong>extend()</strong>: Koristi se za dodavanje više stavki na kraj liste. Evo primera dodavanja brojeva <code>6</code>, <code>8</code>, i <code>10</code> na kraj liste <code>numbers</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\neven_numbers = [6, 8, 10]\n\nnumbers.extend(even_numbers)\nprint(numbers) # [1, 2, 3, 4, 5, 6, 8, 10]\n</code></pre>\n<ul>\n<li><strong>insert()</strong>: Koristi se za umetanje elementa na određenoj poziciji unutar liste pomoću specifičnog primera/sintakse. Evo primera korišćenja metode <code>insert()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.insert(2, 2.5)\n\nprint(numbers) # [1, 2, 2.5, 3, 4, 5]\n</code></pre>\n<ul>\n<li><strong>remove():</strong> Koristi se za uklanjanje elementa iz liste. Metoda <code>remove()</code> će ukloniti samo prvo pojavljivanje elementa unutar liste:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5, 5, 5]\nnumbers.remove(5)\n\nprint(numbers) # [1, 2, 3, 4, 5, 5]\n\n</code></pre>\n<ul>\n<li><strong>pop()</strong>: Koristi se za uklanjanje određenog elementa iz niza i vraćanje njegove vrednosti:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop(1) # The number 2 is returned\n</code></pre>\n<ul>\n<li>Ako ne navedete element na način <code>pop</code>, onda se briše poslednji element.</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.pop() # The number 5 is returned\n</code></pre>\n<ul>\n<li><strong>clear()</strong>: Koristi se za uklanjanje svih elemenata sa liste:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\nnumbers.clear()\n\nprint(numbers) # []\n</code></pre>\n<ul>\n<li><strong>sort()</strong>: Metoda <code>sort()</code> se koristi za sortiranje elemenata na mestu. Evo primera sortiranja nasumičnog niza <code>numbers</code> na mestu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nnumbers.sort()\n\nprint(numbers) # [1, 2, 19, 35, 41, 67]\n</code></pre>\n<ul>\n<li><strong>sorted()</strong>: Unutrašnja funkcija koja vraća novu sortiranu listu umesto da modifikuje originalnu listu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [19, 2, 35, 1, 67, 41]\nsorted_numbers = sorted(numbers)\n\nprint(sorted_numbers) # [1, 2, 19, 35, 41, 67]\nprint(numbers) # [19, 2, 35, 1, 67, 41]\n</code></pre>\n<ul>\n<li><strong>reverse()</strong>: Koristi se za menjanje redosleda elemenata u nizu:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [6, 5, 4, 3, 2, 1]\nnumbers.reverse()\n\nprint(numbers) # [1, 2, 3, 4, 5, 6]\n</code></pre>\n<ul>\n<li><strong>index()</strong>: Koristi se za dobijanje indeksa prvog elementa koji se može pronaći u nizu:</li>\n</ul>\n<pre><code class=\"language-python\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\nprogramming_languages.index('Java') # 1\n</code></pre>\n<ul>\n<li>Ako element ne može biti dostupan putem <code>index()</code>, onda će rezultat biti <code>ValueError</code>.</li>\n</ul>\n<h2>Tuple u Python</h2>\n<ul>\n<li><strong>Opis</strong>: Tuple je tip podataka Python koji se koristi za kreiranje usrednjenog niza vrednosti. Tuple mogu sadržati mešavinu različitih tipova podataka:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\n</code></pre>\n<ul>\n<li>Tuple je nepromenljiv (immutable), jer se elementi unutar tuple-a ne mogu promeniti nakon kreiranja. Ako pokušate da modifikujete element tuple-a, dobićete <code>TypeError</code>:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Python', 'Java', 'C++', 'Rust')\nprogramming_languages[0] = 'JavaScript'\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nTypeError: \"tuple\" object does not support item assignment\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Dobijanje elemenata iz tupaola</strong>: Da biste dobili element iz tupaola, koristite notaciju zagrada i indeks:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\ndeveloper[1] # 34\n</code></pre>\n<ul>\n<li>Negativni indeks se može koristiti za dobijanje elemenata počevši od kraja tupela:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (1, 2, 3, 4, 5)\nnumbers[-2] # 4\n</code></pre>\n<ul>\n<li>Ako pokušate da unesete indeks broj koji prelazi ili je jednak dužini tupola, dobićete <code>IndexError</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (1, 2, 3, 4, 5)\nnumbers[7]\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nIndexError: tuple index out of range\n\"\"\"\n</code></pre>\n<ul>\n<li>Tuple se takođe može kreirati koristeći komponentu <code>tuple()</code>. Unutar komponente, možete uneti ponovljive elemente kao niz karaktera, listu i čak drugi tuple.</li>\n</ul>\n<pre><code class=\"language-python\">developer = 'Jessica'\n\nprint(tuple(developer)) \n# Result: ('J', 'e', 's', 's', 'i', 'c', 'a')\n</code></pre>\n<ul>\n<li><strong>Provera elemenata u tupelu</strong>: Da biste proverili da li je element u tupelu, možete koristiti ključnu reč <code>in</code> kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Python', 'Java', 'C++', 'Rust')\n\n'Rust' in programming_languages # True\n'JavaScript' in programming_languages # False\n</code></pre>\n<ul>\n<li><strong>Izvlačenje elemenata iz tupola</strong>: Elementi se mogu izvlačiti iz tupola na sledeći način:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\nname, age, job = developer\n</code></pre>\n<ul>\n<li>Ako trebate sakupiti preostale elemente iz tupaola, možete koristiti zvezdasti operator (<code>*</code>) kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Alice', 34, 'Rust Developer')\nname, *rest = developer\n</code></pre>\n<ul>\n<li><strong>Procesiranje tupa</strong>: Procesiranje se može koristiti za izvlačenje dela tupa. Na primer, stavke <code>pie</code> i <code>cookies</code> mogu se procesirati u različite tuple:</li>\n</ul>\n<pre><code class=\"language-py\">desserts = ('cake', 'pie', 'cookies', 'ice cream')\ndesserts[1:3] # ('pie', 'cookies')\n</code></pre>\n<ul>\n<li><strong>Uklanjanje elemenata iz tuple-a</strong>: Uklanjanje elementa iz tuple-a će izazvati <code>TypeError</code> jer su tuple-i nepromenljivi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = ('Jane Doe', 23, 'Python Developer')\ndel developer[1]\n\n\"\"\"\nTraceback (most recent call last):\n  File \"&#x3C;stdin>\", line 1, in &#x3C;module>\nTypeError: \"tuple\" object doesn't support item deletion\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Kada koristiti tuple umesto liste?</strong>: Ako vam je potreban promenljiv skup stavki gde možete dodavati, uklanjati i menjati elemente, koristite listu. Ako znate da radite sa fiksnom i nepromenljivom kolekcijom podataka, koristite tuple.</li>\n</ul>\n<h2>Uobičajene metode za tuple</h2>\n<ul>\n<li><strong><code>count()</code></strong>: Koristi se za određivanje koliko puta je element prisutan u tuplu. Na primer, možete proveriti koliko puta se jezik <code>'Rust'</code> pojavljuje u tuplu:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.count('Rust') # 2\n</code></pre>\n<ul>\n<li>Ako element pomenut u putanji <code>count()</code> potpuno nije prisutan u tuple, onda vrednost return će biti <code>0</code>:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.count('JavaScript') # 0\n</code></pre>\n<ul>\n<li>\n<p>Ako nisu navedeni argumenti putem <code>count()</code>, onda Python vraća <code>TypeError</code>.</p>\n</li>\n<li>\n<p><strong>index()</strong>: Koristi se za pronalaženje indeksa gde se određeni element nalazi u tuplu. Evo primera korišćenja metode <code>index()</code> za dobijanje indeksa jezika <code>'Java'</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')\nprogramming_languages.index('Java') # 1\n</code></pre>\n<ul>\n<li>\n<p>Ako navedeni element ne može biti pronađen, onda će Python vratiti <code>ValueError</code>.</p>\n</li>\n<li>\n<p>Takođe možete uneti početni obrazac pomoću <code>index()</code> da specificirate gde ćete početi pretraživati element u tuplu:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\nprogramming_languages.index('Python', 3) # 5\n</code></pre>\n<ul>\n<li>Takođe možete umetnuti poslednji primer koristeći <code>index()</code> da biste specificirali gde ćete prestati pretraživati element u tupelu:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python', 'JavaScript', 'Python')\nprogramming_languages.index('Python', 2, 5) # 2\n</code></pre>\n<ul>\n<li><strong><code>sorted()</code></strong>: Koristi se za aranžiranje elemenata u bilo kom ponovljivom objektu i vraćanje nove, organizovane liste. Evo primera kreiranja nove numerisane liste korišćenjem funkcije <code>sorted()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = (13, 2, 78, 3, 45, 67, 18, 7)\nsorted(numbers) # [2, 3, 7, 13, 18, 45, 67, 78]\n</code></pre>\n<ul>\n<li><strong>Измена ponašanja sortiranja</strong>: Možete prilagoditi ponašanje sortiranja za ponovljivi element koristeći opcionalne argumente <code>reverse</code> i <code>key</code>. Evo primera korišćenja argumenta <code>key</code> za sortiranje elemenata u tupel prema dužini:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\nsorted(programming_languages, key=len)\n\n# Result\n# ['C++', 'Rust', 'Java', 'Rust', 'Python', 'Python']\n</code></pre>\n<ul>\n<li>Možete kreirati novu listu vrednosti u obrnutom redosledu, koristeći argument <code>reverse</code>, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')\n\nprint(sorted(programming_languages, reverse=True))\n\n# Result\n# ['Rust', 'Rust', 'Python', 'Python', 'Java', 'C++']\n</code></pre>\n<h2>Krugovi u Python</h2>\n<ul>\n<li>\n<p><strong>Opis</strong>: Petlje se koriste za ponavljanje dela koda određeni broj puta.</p>\n</li>\n<li>\n<p><strong>Petlja za <code>for</code></strong>: Koristi se za iteriranje preko sekvence (poput liste, tupa ili niza karaktera) i izvršavanje bloka koda za svaki element u toj sekvenci. Evo primera korišćenja petlje <code>for</code> za iteriranje preko liste i štampanje svakog elementa na konzolu:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">programming_languages = ['Rust', 'Java', 'Python', 'C++']\n\nfor language in programming_languages:\n    print(language)\n\n\"\"\"\nResult \n\nRust\nJava\nPython\nC++\n\"\"\"\n</code></pre>\n<ul>\n<li>Ovo je primer korišćenja petlje <code>for</code> za ponavljanje sekvence slova <code>code</code> i štampanje svakog slova:</li>\n</ul>\n<pre><code class=\"language-py\">for char in 'code':\n    print(char)\n\n\"\"\"\nResult \n\nc\no\nd\ne\n\"\"\"\n</code></pre>\n<ul>\n<li>Krugovi <code>for</code> mogu se ugraditi unutar. Evo primera korišćenja kruga <code>for</code> koji je ugrađen:</li>\n</ul>\n<pre><code class=\"language-py\">categories = ['Fruit', 'Vegetable']\nfoods = ['Apple', 'Carrot', 'Banana']\n\nfor category in categories:\n    for food in foods:\n        print(category, food)\n\n\"\"\"\nResult\n\nFruit Apple\nFruit Carrot\nFruit Banana\nVegetable Apple\nVegetable Carrot\nVegetable Banana\n\"\"\"\n\n</code></pre>\n<ul>\n<li><strong>Petlja za <code>while</code></strong>: Ponovi deo koda dok uslov nije <code>False</code>. Evo primera korišćenja petlje <code>while</code> za igru na sreću:</li>\n</ul>\n<pre><code class=\"language-py\">secret_number = 3\nguess = 0\n\nwhile guess != secret_number:\n    guess = int(input('Guess the number (1-5): '))\n    if guess != secret_number:\n        print('Wrong! Try again.')\n\nprint('You got it!')\n\n\"\"\"\nResult\n\nGuess the number (1-5): 2\nWrong! Try again.\nGuess the number (1-5): 1\nWrong! Try again.\nGuess the number (1-5): 3\nYou got it!\n\"\"\"\n\n</code></pre>\n<ul>\n<li>\n<p><strong>Kifungu cha <code>break</code> na <code>continue</code></strong>: Koristi se u krugovima za promenu izvršenja kruga.</p>\n</li>\n<li>\n<p>Klauzula <code>break</code> se koristi jednom po ciklusu kada se ispuni određeni uslov. Evo primera korišćenja klauzule <code>break</code> za listu <code>developer_names</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">developer_names = ['Jess', 'Naomi', 'Tom']\n\nfor developer in developer_names:\n    if developer == 'Naomi':\n        break\n    print(developer)\n</code></pre>\n<ul>\n<li>Deo <code>continue</code> se koristi za preskakanje te iteracije i nastavak sa sledećom iteracijom ciklusa. Evo primera korišćenja deoa <code>continue</code> umesto deoa <code>break</code>:</li>\n</ul>\n<pre><code class=\"language-py\">developer_names = ['Jess', 'Naomi', 'Tom']\n\nfor developer in developer_names:\n    if developer == 'Naomi':\n        continue\n    print(developer)\n</code></pre>\n<ul>\n<li>Ciklusi <code>for</code> i <code>while</code> mogu biti povezani sa odjeljkom <code>else</code>, koji se izvršava samo kada ciklus ne završi sa odjeljkom <code>break</code>:</li>\n</ul>\n<pre><code class=\"language-python\">words = ['sky', 'apple', 'rhythm', 'fly', 'orange']\n\nfor word in words:\n    for letter in word:\n        if letter.lower() in 'aeiou':\n            print(f\"'{word}' contains the vowel '{letter}'\")\n            break\n    else:\n        print(f\"'{word}' has no vowels\")\n</code></pre>\n<h2>Ranges i njihova primena u krugovima</h2>\n<ul>\n<li><strong>Funkcija <code>range()</code></strong>: Koristi se za generisanje sekvence celobrojnih brojeva.</li>\n</ul>\n<pre><code class=\"language-py\">range(start, stop, step)\n</code></pre>\n<ul>\n<li>Važan argument za <code>stop</code> je kompletan (neinkluzivan) broj koji predstavlja kraj generisane sekvence brojeva. Evo primera korišćenja funkcije <code>range()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(3):\n    print(num)\n</code></pre>\n<ul>\n<li>Ako argument <code>start</code> nije dostavljen, podrazumevano će biti <code>0</code>. Za podrazumevanu sekvencu celih brojeva, inkrement će biti <code>1</code>. Možete koristiti opcioni argument <code>step</code> da promenite vrednost podrazumevanog inkrementa. Evo primera generisanja sekvence pozitivnih celih brojeva od 2 do 10 (uključujući 10).</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(2, 11, 2):\n    print(num)\n</code></pre>\n<ul>\n<li>\n<p>Ako ne pružite nijedan argument za funkciju <code>range()</code>, onda ćete dobiti <code>TypeError</code>.</p>\n</li>\n<li>\n<p>Funkcija <code>range()</code> prihvata samo cele brojeve kao argumente, a ne decimalne brojeve. Korišćenjem decimalnih brojeva će doći do <code>TypeError</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-py\">ERROR!\nTraceback (most recent call last):\n  File \"&#x3C;main.py>\", line 1, in &#x3C;module>\nTypeError: 'float' object cannot be interpreted as an integer\n</code></pre>\n<ul>\n<li>Možete koristiti negativan ceo broj za argument <code>step</code> da biste generisali niz celih brojeva u opadajućem redosledu:</li>\n</ul>\n<pre><code class=\"language-py\">for num in range(40, 0, -10):\n    print(num)\n</code></pre>\n<ul>\n<li>Funkcija <code>range()</code> takođe se može koristiti za kreiranje liste celobrojnih brojeva korišćenjem zajedno sa komponentom <code>list</code>. Komponenta <code>list</code> koristi se za konverziju iterabilnog objekta u listu. Evo primera generisanja liste pozitivnih celobrojnih brojeva između 2 i 10, uključujući:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = list(range(2, 11, 2))\nprint(numbers) # [2, 4, 6, 8, 10]\n</code></pre>\n<h2>Funkcija <code>enumerate()</code> i <code>zip()</code> u Python</h2>\n<ul>\n<li><strong><code>enumerate()</code></strong>: Koristi se za iteraciju kroz niz i praćenje indeksa svakog elementa u tom nizu. Funkcija <code>enumerate()</code>, koja uzima iterabilni objekat kao argument i vraća objekat <code>enumerate</code> koji uključuje indeks i vrednost svakog elementa u iterabilnom objektu.</li>\n</ul>\n<pre><code class=\"language-python\">languages = ['Spanish', 'English', 'Russian', 'Chinese']\n\nfor index, language in enumerate(languages):\n    print(f'Index {index} and language {language}')\n\n# Result\n# Index 0 and language Spanish\n# Index 1 and language English\n# Index 2 and language Russian\n# Index 3 and language Chinese\n\n</code></pre>\n<ul>\n<li>Funkcija <code>enumerate()</code> takođe se može koristiti izvan opsega <code>for</code>:</li>\n</ul>\n<pre><code class=\"language-python\">languages = ['Spanish', 'English', 'Russian', 'Chinese']\n\nprint(list(enumerate(languages)))\n# [(0, 'Spanish'), (1, 'English'), (2, 'Russian'), (3, 'Chinese')]\n</code></pre>\n<ul>\n<li>\n<p>Funkcija <code>enumerate()</code> takođe prima opcionu argumentu <code>start</code> koja određuje početnu vrednost za brojanje. Ako ovaj argument nije dostavljen, onda će brojanje početi od <code>0</code>.</p>\n</li>\n<li>\n<p><strong><code>zip()</code></strong> : Koristi se za ponavljanje preko više stavki koje mogu biti ponovljene paralelno. Evo primera korišćenja funkcije <code>zip()</code> za ponavljanje preko <code>developers</code> i <code>ids</code>:</p>\n</li>\n</ul>\n<pre><code class=\"language-python\">developers = ['Naomi', 'Dario', 'Jessica', 'Tom']\nids = [1, 2, 3, 4]\n\nfor name, id in zip(developers, ids):\n    print(f'Name: {name}')\n    print(f'ID: {id}')\n\n\n\"\"\"\nResult\n\nName: Naomi\nID: 1\nName: Dario\nID: 2\nName: Jessica\nID: 3\nName: Tom\nID: 4\n\"\"\"\n\n</code></pre>\n<h2>Razumevanje liste u Python</h2>\n<ul>\n<li><strong>Opis</strong>: List komprehensija vam omogućava da kreirate novi list u jednoj liniji kombinujući petlju i uslov direktno unutar uglastih zagrada. To čini kod kraćim i često lakšim za čitanje.</li>\n</ul>\n<pre><code class=\"language-py\">even_numbers = [num for num in range(21) if num % 2 == 0]\nprint(even_numbers)\n</code></pre>\n<h2>Načini za ponavljajuće stavke</h2>\n<ul>\n<li><strong><code>filter()</code></strong>: Koristi se za filtriranje elemenata iz iterabilnog objekta prema određenom uslovu. Vraća generator sa elementima koji zadovoljavaju taj uslov. Evo primera kreiranja nove liste reči koje imaju više od četiri slova:</li>\n</ul>\n<pre><code class=\"language-py\">words = ['tree', 'sky', 'mountain', 'river', 'cloud', 'sun']\n\ndef is_long_word(word):\n    return len(word) > 4\n\nlong_words = list(filter(is_long_word, words))\nprint(long_words) # ['mountain', 'river', 'cloud']\n</code></pre>\n<ul>\n<li><strong><code>map()</code></strong>: Koristi se za korišćenje funkcije za svaki element u iterativnom objektu i vraćanje novog objekta sa rezultatima. Evo primera korišćenja funkcije <code>map()</code> za konverziju liste temperatura iz Celzija u Farenhajta:</li>\n</ul>\n<pre><code class=\"language-py\">celsius = [0, 10, 20, 30, 40]\n\ndef to_fahrenheit(temp):\n    return (temp * 9/5) + 32\n\nfahrenheit = list(map(to_fahrenheit, celsius))\nprint(fahrenheit) # [32.0, 50.0, 68.0, 86.0, 104.0]\n</code></pre>\n<ul>\n<li><strong><code>sum()</code></strong>: Koristi se za dobijanje zbira iz iterabilnog objekta kao što su lista ili tupel. Evo primera korišćenja funkcije <code>sum()</code>:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers)\nprint(total) # Result: 50\n</code></pre>\n<ul>\n<li>Takođe možete uključiti opcionalni argument <code>start</code> koji postavlja početnu vrednost za ukupno. Evo ažuriranog primera korišćenja argumenta <code>start</code> kao pozicionog parametra:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers, 10) # positional argument\nprint(total) # 60\n</code></pre>\n<ul>\n<li>Takođe možete izabrati da koristite argument <code>start</code> kao glavni argument reči, kako sledi:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [5, 10, 15, 20]\ntotal = sum(numbers, start=10) # keyword argument\nprint(total) # 60\n</code></pre>\n<h2>Lambda funkcija</h2>\n<ul>\n<li><strong>Opis</strong>: Lambda funkcija u Python je kratak način za kreiranje anonimne funkcije (anonimna funkcija).</li>\n<li>Lambda funkcija se često koristi kao argument za drugu funkciju. Evo primera lambda funkcije:</li>\n</ul>\n<pre><code class=\"language-py\">numbers = [1, 2, 3, 4, 5]\n\neven_numbers = list(filter(lambda x: x % 2 == 0, numbers))\nprint(even_numbers)  # [2, 4]\n</code></pre>\n<ul>\n<li>Najbolji načini korišćenja lambda funkcija su izostavljanje njene vrednosti u parametru, čime se čini jednostavnim i razumljivim, i korišćenje za kratke, jednokratne funkcije.</li>\n</ul>\n</section>",
  "translationPending": false,
  "block": "review-loops-and-sequences",
  "blockLabel": "review",
  "blockLayout": "link",
  "hasEditableBoundaries": false,
  "order": 22,
  "instructions": "",
  "questions": [],
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 0,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-loops-and-sequences",
  "certification": "python-v9"
}