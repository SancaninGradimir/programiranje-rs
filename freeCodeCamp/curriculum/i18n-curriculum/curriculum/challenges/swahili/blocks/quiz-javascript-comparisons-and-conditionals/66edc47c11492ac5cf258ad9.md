---
id: 66edc47c11492ac5cf258ad9
title: Pokušao sam da upišem poređenje sa uslovima JavaScript-a
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

Da bi prošao kratki test, moraš tačno odgovoriti na najmanje 9 od 10 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Šta je rezultat matematičkog izraza `undefined > 0`?

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

Koja logička operacija `||` predstavlja?

#### --distractors--

XOR

---

I

---

NE

#### --answer--

OR

### --question--

#### --text--

Koje rezultate dobijamo iz sledećeg koda?

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

Greška se pojavljuje.

#### --answer--

`true`

### --question--

#### --text--

Šta je pozitivna i lažna vrednost?

#### --distractors--

Vrednost koja je `true` i `false` istovremeno.

---

Vrednost koja se menja/promenjava u zavisnosti od konteksta.

---

Vrednost koja radi drugačije od vrednosti `true` ili `false`.

#### --answer--

Vrednost koja se smatra `true` ili `false` kada se nalazi u kontekstu vrednosti tačno ili netačno.

### --question--

#### --text--

Šta se ispisuje na konzolu iz sledećeg koda?

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

Ništa se ne štampa na konzoli.

---

Greška se pojavljuje.

#### --answer--

`True!`

### --question--

#### --text--

Koja je razlika između `undefined` i `null`?

#### --distractors--

`null` na `undefined` ukazuju na nevažeću i nedostupnu memorijsku oblast.

---

`null` je tajna vrednost postavljena za parametre i ne može se promeniti, dok `undefined` nije eksplicitna vrednost postavljena za parametre.

---

`null` je svojstvo čitavog sveta, `undefined` nije.

#### --answer--

Nulte vrednosti su `undefined`, dok `null` predstavlja namernu odsutnost vrednosti objekta.

### --question--

#### --text--

Šta se dešava kada ne uključite `break` prilikom izvršavanja deklaracije `switch`?

#### --distractors--

Izjava o `switch` će se pojaviti iznenada.

---

Izjava o `switch` uzrokuje grešku nakon prethodne izjave.

---

Ujak od `switch` će otići nakon prvog meča.

#### --answer--

Kod nastavlja da procenjuje izjavu `case` koja sledi, čak i nakon pronalaženja podudarnosti.

### --question--

#### --text--

Šta se ispisuje na konzolu iz sledećeg koda?

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

Greška se pojavljuje.

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5`, Zatim se pojavljuje greška.

### --question--

#### --text--

Koji će rezultati proizaći iz sledećeg koda JavaScript?

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

Šta se ispisuje na konzolu sa sledećim kodom?

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

Greška se pojavljuje.

---

Ništa nije objavljeno.

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`