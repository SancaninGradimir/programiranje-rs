{
  "id": "694f8510e8a0a3d89e146a01",
  "title": "Korak 24",
  "challengeType": 1,
  "dashedName": "step-24",
  "challengeFiles": [
    {
      "id": "",
      "editableRegionBoundaries": [
        87,
        89
      ],
      "history": [
        "script.js"
      ],
      "name": "script",
      "ext": "js",
      "path": "script.js",
      "fileKey": "scriptjs",
      "contents": "const squad = [];\n\nconst firstAstronaut = {\n  id: 1,\n  name: \"Andy\",\n  role: \"Commander\",\n  isEVAEligible: true,\n  priority: 3\n};\n\nfunction addCrewMember(crew, astronaut) {\n  for (let i = 0; i < crew.length; i++) {\n    if (crew[i].id === astronaut.id) {\n      console.log(\"Duplicate ID: \" + astronaut.id);\n      return;\n    }\n  }\n  crew.push(astronaut);\n}\n\naddCrewMember(squad, firstAstronaut);\n\nconst remainingCrew = [\n  { id: 2, name: \"Bart\", role: \"Pilot\", isEVAEligible: false, priority: 8 },\n  { id: 3, name: \"Caroline\", role: \"Engineer\", isEVAEligible: true, priority: 4 },\n  { id: 4, name: \"Diego\", role: \"Scientist\", isEVAEligible: false, priority: 1 },\n  { id: 5, name: \"Elise\", role: \"Medic\", isEVAEligible: true, priority: 7 },\n  { id: 6, name: \"Felix\", role: \"Navigator\", isEVAEligible: true, priority: 6 },\n  { id: 7, name: \"Gertrude\", role: \"Communications\", isEVAEligible: false, priority: 4 },\n  { id: 8, name: \"Hank\", role: \"Mechanic\", isEVAEligible: true, priority: 2 },\n  { id: 9, name: \"Irene\", role: \"Specialist\", isEVAEligible: true, priority: 5 },\n  { id: 10, name: \"Joan\", role: \"Technician\", isEVAEligible: false, priority: 1 },\n];\n\nfor (let i = 0; i < remainingCrew.length; i++) {\n  addCrewMember(squad, remainingCrew[i]);\n}\n\nfunction swapCrewMembers(crew, fromIndex, toIndex) {\n  if (\n    fromIndex < 0 || \n    toIndex < 0 ||\n    fromIndex >= crew.length ||\n    toIndex >= crew.length\n  ) {\n    console.log(\"Invalid crew indices\");\n    return;\n  }\n\n  const updatedCrew = crew.slice();\n  updatedCrew[fromIndex] = updatedCrew.splice(toIndex, 1, updatedCrew[fromIndex])[0];\n\n  return updatedCrew; \n}\n\nconst updatedSquad = swapCrewMembers(squad, 2, 5);\n\nfunction sortByPriorityDescending(crew) {\n  for (let i = 0; i < crew.length - 1; i++) {\n    for (let j = 0; j < crew.length - 1 - i; j++) {\n      if (crew[j].priority < crew[j + 1].priority) {\n        const temp = crew[j];\n        crew[j] = crew[j + 1];\n        crew[j + 1] = temp;\n      }\n    }\n  }\n}\n\nfunction getEVAReadyCrew(crew) {\n  const eligible = [];\n  for (const astronaut of crew) {\n    if (astronaut.isEVAEligible) eligible.push(astronaut);\n  }\n  sortByPriorityDescending(eligible); \n\n  return eligible;\n}\n\nconst EVAReadySquad = getEVAReadyCrew(updatedSquad);\n\nfunction chunkCrew(crew, size) {\n  if (size < 1) {\n    console.log(\"Chunk size must be >= 1\");\n    return;\n  }\n  \n\n}",
      "error": null,
      "seed": "const squad = [];\n\nconst firstAstronaut = {\n  id: 1,\n  name: \"Andy\",\n  role: \"Commander\",\n  isEVAEligible: true,\n  priority: 3\n};\n\nfunction addCrewMember(crew, astronaut) {\n  for (let i = 0; i < crew.length; i++) {\n    if (crew[i].id === astronaut.id) {\n      console.log(\"Duplicate ID: \" + astronaut.id);\n      return;\n    }\n  }\n  crew.push(astronaut);\n}\n\naddCrewMember(squad, firstAstronaut);\n\nconst remainingCrew = [\n  { id: 2, name: \"Bart\", role: \"Pilot\", isEVAEligible: false, priority: 8 },\n  { id: 3, name: \"Caroline\", role: \"Engineer\", isEVAEligible: true, priority: 4 },\n  { id: 4, name: \"Diego\", role: \"Scientist\", isEVAEligible: false, priority: 1 },\n  { id: 5, name: \"Elise\", role: \"Medic\", isEVAEligible: true, priority: 7 },\n  { id: 6, name: \"Felix\", role: \"Navigator\", isEVAEligible: true, priority: 6 },\n  { id: 7, name: \"Gertrude\", role: \"Communications\", isEVAEligible: false, priority: 4 },\n  { id: 8, name: \"Hank\", role: \"Mechanic\", isEVAEligible: true, priority: 2 },\n  { id: 9, name: \"Irene\", role: \"Specialist\", isEVAEligible: true, priority: 5 },\n  { id: 10, name: \"Joan\", role: \"Technician\", isEVAEligible: false, priority: 1 },\n];\n\nfor (let i = 0; i < remainingCrew.length; i++) {\n  addCrewMember(squad, remainingCrew[i]);\n}\n\nfunction swapCrewMembers(crew, fromIndex, toIndex) {\n  if (\n    fromIndex < 0 || \n    toIndex < 0 ||\n    fromIndex >= crew.length ||\n    toIndex >= crew.length\n  ) {\n    console.log(\"Invalid crew indices\");\n    return;\n  }\n\n  const updatedCrew = crew.slice();\n  updatedCrew[fromIndex] = updatedCrew.splice(toIndex, 1, updatedCrew[fromIndex])[0];\n\n  return updatedCrew; \n}\n\nconst updatedSquad = swapCrewMembers(squad, 2, 5);\n\nfunction sortByPriorityDescending(crew) {\n  for (let i = 0; i < crew.length - 1; i++) {\n    for (let j = 0; j < crew.length - 1 - i; j++) {\n      if (crew[j].priority < crew[j + 1].priority) {\n        const temp = crew[j];\n        crew[j] = crew[j + 1];\n        crew[j + 1] = temp;\n      }\n    }\n  }\n}\n\nfunction getEVAReadyCrew(crew) {\n  const eligible = [];\n  for (const astronaut of crew) {\n    if (astronaut.isEVAEligible) eligible.push(astronaut);\n  }\n  sortByPriorityDescending(eligible); \n\n  return eligible;\n}\n\nconst EVAReadySquad = getEVAReadyCrew(updatedSquad);\n\nfunction chunkCrew(crew, size) {\n  if (size < 1) {\n    console.log(\"Chunk size must be >= 1\");\n    return;\n  }\n  \n\n}"
    }
  ],
  "solutions": [],
  "assignments": [],
  "tests": [
    {
      "text": "<p>Treba da kreiraš prazan red podataka nazvan <code>chunks</code>.</p>",
      "testString": "const funcStr = __helpers.removeJSComments(chunkCrew.toString());\nassert.match(\n  funcStr,\n  /(var|let|const)\\s+chunks/,\n  \"You must have at least one space between the declaration keyword (let/const) and 'chunks'\"\n);\nconst cleaned = __helpers.removeWhiteSpace(__helpers.removeJSComments(chunkCrew.toString()));\nassert.match(cleaned, /(?:^|[^a-zA-Z])(var|let|const)chunks=\\[\\]/);"
    },
    {
      "text": "<p>Treba da koristiš kola <code>for</code> za kreiranje delova.</p>",
      "testString": "const cleaned = __helpers.removeWhiteSpace(__helpers.removeJSComments(chunkCrew.toString()));\nassert.match(cleaned, /for\\s*\\(/);"
    },
    {
      "text": "<p>Unutar petlje <code>for</code>, treba da pozoveš <code>push()</code> u niz podataka <code>chunks</code> da unese svaki odrezani deo (koji mora biti kreiran pozivom <code>slice()</code> u niz podataka <code>crew</code>).</p>",
      "testString": "const cleaned = __helpers.removeWhiteSpace(__helpers.removeJSComments(chunkCrew.toString()));\nassert.match(cleaned, /chunks\\.push\\(crew\\.slice\\(/);"
    },
    {
      "text": "<p>Nakon ciklusa, treba da vratite red podataka <code>chunks</code>.</p>",
      "testString": "const cleaned = __helpers.removeWhiteSpace(__helpers.removeJSComments(chunkCrew.toString()));\nassert.match(cleaned, /returnchunks;?}$/);"
    },
    {
      "text": "<p>Vaša funkcija za <code>chunkCrew</code> treba da vrati segmente dužine <code>size</code> iz niza ulaznih podataka <code>crew</code> bez modifikacije.</p>",
      "testString": "const sample = [1, 2, 3, 4, 5];\nconst copyBefore = [...sample];\nconst chunks = chunkCrew(sample, 2);\nassert.isArray(chunks);\nassert.deepEqual(chunks, [[1, 2], [3, 4], [5]]);\nassert.deepEqual(sample, copyBefore, 'chunkCrew should not mutate the original crew');"
    }
  ],
  "description": "<section id=\"description\">\n<p>Da biste kreirali komade veličine <code>n</code> iz <code>array</code> bez promene, možete spojiti kolo <code>for</code> sa putanjom <code>slice()</code> kao što sledi:</p>\n<pre><code class=\"language-js\">const result = [];\nfor (let i = 0; i &#x3C; array.length; i += n) {\n  result.push(array.slice(i, i + n));\n}\n</code></pre>\n<p>Primer iznad:</p>\n<ul>\n<li>\n<p>kreira prazan red podataka nazvan <code>result</code></p>\n</li>\n<li>\n<p>prolazi kroz red originalnih podataka <code>array</code> u koracima veličine <code>n</code></p>\n</li>\n<li>\n<p>kreira delove niza (<code>array</code>) veličine <code>n</code> i dodaje ih u <code>result</code> koristeći <code>slice()</code></p>\n</li>\n</ul>\n<p>Da biste kompletirali funkciju <code>chunkCrew</code>, potrebno je:</p>\n<ul>\n<li>\n<p>kreiraj red podataka pod nazivom <code>chunks</code></p>\n</li>\n<li>\n<p>okruži <code>crew</code> za korake veličine <code>size</code>, kreirajući i ubacujući komade iz <code>crew</code> unutar <code>chunks</code> koristeći <code>slice()</code></p>\n</li>\n<li>\n<p>Obnovi red podataka <code>chunks</code></p>\n</li>\n</ul>\n</section>",
  "translationPending": false,
  "block": "workshop-space-mission-roster",
  "blockLabel": "workshop",
  "blockLayout": "challenge-grid",
  "hasEditableBoundaries": true,
  "order": 68,
  "instructions": "",
  "questions": [],
  "superBlock": "javascript-v9",
  "superOrder": 1,
  "challengeOrder": 23,
  "isLastChallengeInBlock": false,
  "required": [],
  "helpCategory": "JavaScript",
  "usesMultifileEditor": true,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "javascript",
  "module": "javascript-loops",
  "certification": "javascript-v9"
}