---
id: 692487767b6b728454f53f29
title: Pokušao sam da napravim modul za Node.js osnovne module
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji način `fs` dodaje sadržaj u postojeću datoteku bez brisanja onoga što je već tu?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

Šta se dešava kada koristite više vremenskih linija iz modula `fs`?

#### --distractors--

Obrišite postojeće fajlove direktno.

---

Radi brže od metoda koje nisu istovremene u svakoj situaciji.

---

Vraća obećanja (Promises) podrazumevano.

#### --answer--

Zaustavlja program dok operacija ne bude završena.

### --question--

#### --text--

Parametar za `utf8`, šta radi u `fs.readFile()`?

#### --distractors--

Sprečava brisanje grešaka.

---

Promeni ekstenziju fajla.

---

Komprimuje velike fajlove/datoteke.

#### --answer--

Govori Node da vrati sekvencu pročitanih karaktera umesto privremenog buffera.

### --question--

#### --text--

Koji je metod `Buffer` koji kreira novi privremeni keš ispunjen nulama?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

`Buffer.toString()` Šta radiš?

#### --distractors--

Konvertuje privremeni keš u binarni kod.

---

Izbrišite sadržaj privremenog keša.

---

Prikazuje trajanje budžeta za privremeno čuvanje.

#### --answer--

Konvertuje privremeno keširani sadržaj u čitljiv niz karaktera.

### --question--

#### --text--

Koja funkcija za `crypto` zahteva privatni ključ za kreiranje potpisa?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Zašto bi trebalo da izbegavaš direktno pisanje svojih kriptoalgoritama?

#### --distractors--

To je protiv zakona u programima za Node.js.

---

Node.js ne uključuje se u šifrovanje.

---

JavaScript ne može da izračuna heš.

#### --answer--

Može biti opasno bez dobro testiranih biblioteka.

### --question--

#### --text--

Koja je metoda od `crypto` predložena za kreiranje sigurnosnih slučajnih tokena umesto `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Koji je način kojim modul operativnog sistema vraća arhitekturu procesora (CPU)?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

`os.userInfo()` Šta vraćaš?

#### --distractors--

Korisničko ime.

---

Samo IP adresa uređaja.

---

Identifikacija ulogovanog korisnika.

#### --answer--

Izveštaj o korisniku trenutnog sistema.

### --question--

#### --text--

Koji je način za `path` spajanje dela i završavanje dodatnih rezova?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Koji način iz `path` stvara kompletnu putanju na osnovu trenutnog radnog direktorijuma?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

Šta predstavlja međunarodni standard za `__dirname`?

#### --distractors--

Ime korisnika koji pokreće Node.

---

Nastavak trenutnog fajla.

---

Putanja direktorijuma počinje sa `./`.

#### --answer--

Puni put do direktorijuma koji čuva trenutnu datoteku.

### --question--

#### --text--

Kakva je karakteristika procesa koja ima varijable okruženja kao `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

Šta radi događaj `process.on("exit")`?

#### --distractors--

Ponovno pokreni Node program.

---

Pošalji signal `exit` za detetke procese.

---

Izbriši zapis ciklusa događaja.

#### --answer--

Izvršiti kod pre nego što proces Node završi.

### --question--

#### --text--

Koja vrsta protoka može da čita i piše podatke, ali ne modifikuje?

#### --distractors--

Protok koji se čita

---

Tok promena

---

Napisani tok

#### --answer--

Dupleksni protok

### --question--

#### --text--

Šta je potrebno za efikasno čitanje i pisanje velikih fajlova?

#### --distractors--

Koristi samo `Buffer.alloc()`

---

Uvoz modula za `crypto`

---

Sačuvajte fajl prvo u memoriju

#### --answer--

Koristeći tokove za obradu podataka u delovima

### --question--

#### --text--

Koji način povezuje čitljivi i upisivani tok?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Koji događaj strema pokazuje da nema više podataka za zapisivanje?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

Šta se piše ako štampate deo iz toka koji se čita bez `toString()`?

#### --distractors--

Sadržaj teksta je jasan

---

ID procesa

---

Ime operativnog sistema

#### --answer--

Stavka privremenog keša sa binarnim podacima