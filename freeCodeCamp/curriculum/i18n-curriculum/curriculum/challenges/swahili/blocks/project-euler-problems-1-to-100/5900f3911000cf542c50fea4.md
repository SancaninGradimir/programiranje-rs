---
id: 5900f3911000cf542c50fea4
title: 'Vežba 37: osnovni brojevi koje je moguće izrezati'
challengeType: 1
forumTopicId: 302031
dashedName: problem-37-truncatable-primes
---

# --description--

Broj 3797 ima zanimljivo svojstvo. Iako je sam po sebi prost broj, moguće je uklanjati jednu po jednu cifru sleva nadesno i svaki dobijeni broj ostaje prost: 3797, 797, 97 i 7. Isto važi i kada se cifre uklanjaju zdesna nalevo: 3797, 379, 37 i 3.

Pronađite ceo skup osnovnih brojeva `n` samo (8 ≤ `n` ≤ 11) koji se mogu čitati od leve ka desnoj i takođe od desne ka levoj.

NAPOMENA: 2, 3, 5 i 7 smatraju se premi brojevima koji mogu biti faktorizovani.

# --hints--

`truncatablePrimes(8)` treba da vrati broj.

```js
assert(typeof truncatablePrimes(8) === 'number');
```

`truncatablePrimes(8)` inapaswa kurudisha 1986.

```js
assert(truncatablePrimes(8) == 1986);
```

`truncatablePrimes(9)` inapaswa kurudisha 5123.

```js
assert(truncatablePrimes(9) == 5123);
```

`truncatablePrimes(10)` trebalo bi da vrati 8920.

```js
assert(truncatablePrimes(10) == 8920);
```

`truncatablePrimes(11)` treba da vrati 748317.

```js
assert(truncatablePrimes(11) == 748317);
```

# --seed--

## --seed-contents--

```js
function truncatablePrimes(n) {

  return n;
}

truncatablePrimes(11);
```

# --solutions--

```js
// solution required
```
