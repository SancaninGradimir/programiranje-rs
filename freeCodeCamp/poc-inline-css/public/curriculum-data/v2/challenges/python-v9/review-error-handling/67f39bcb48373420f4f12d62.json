{
  "id": "67f39bcb48373420f4f12d62",
  "title": "Provera obrade grešaka",
  "challengeType": 31,
  "dashedName": "review-error-handling",
  "solutions": [],
  "assignments": [
    "<p>Proverite teme i koncepte za rešavanje grešaka.</p>"
  ],
  "tests": [],
  "description": "<section id=\"description\">\n<h2>Uobičajene greške u Python</h2>\n<ul>\n<li><strong>SyntaxError</strong>: Greška koju Python izbacuje kada vaš kod ne prati njegove sintaksne pravila. Na primer, kod <code>print(\"Hello there\"</code> će izazvati sintaksnu grešku sa porukom <code>SyntaxError: '(' was never closed</code>, jer kod nedostaje zatvarajuća zagrada.</li>\n<li><strong>NameError</strong>: Python izbacuje <code>NameError</code> kada pokušavate da pristupite parametru ili funkciji koje niste definisali. Na primer, ako imate liniju <code>print(username)</code> u vašem kodu bez prethodno definisanog parametra <code>username</code>, dobićete NameError sa porukom <code>NameError: name 'username' is not defined</code>.</li>\n<li><strong>TypeError</strong>: Ovo je greška koju Python baca kada izvršavate operacije na dve ili više nekompatibilnih tipova podataka. Na primer, ako pokušate da dodate niz karaktera na broj, dobićete grešku <code>TypeError: can only concatenate str (not \"int\") to str</code>.</li>\n<li><strong>IndexError</strong>: Dobići ćete <code>IndexError</code> ako pokušate da pristupite indeksu koji ne postoji u listi ili bilo kojoj drugoj sekvenci kao što su tupupla i niz karaktera. Na primer, u nizu karaktera <code>Hello world</code>, indeks poslednjeg karaktera je <code>11</code>. Ako nastavite da pristupate karakterima na ovaj način, <code>greet = \"hello world\"; print(greet[12])</code>, dobićete grešku sa porukom <code>IndexError: string index out of range</code>.</li>\n<li><strong>AttributeError</strong>: Python izbacuje ovu grešku kada pokušavate da koristite metodu ili atribut koji ne postoji u objektu tog tipa. Na primer, pozivanje <code>.append()</code> na niz karaktera kao što je <code>\"hello\".append(\"!\")</code> će rezultovati greškom sa porukom <code>AttributeError: 'str' object has no attribute 'append'</code>.</li>\n</ul>\n<h2>Efikasne metode za ispravljanje grešaka u Python</h2>\n<ul>\n<li><strong>Korišćenjem funkcije <code>print</code></strong>: Unos informacija za <code>print()</code> na različitim mestima u vašem kodu tokom debagovanja pomaže vam da vidite vrednosti varijabli i kako teče vaš kod.</li>\n<li><strong>Korišćenjem ugrađenog Debuggera Python (<code>pdb</code>)</strong>: Python pruža modul <code>pdb</code> za debagovanje. To je deo standardne biblioteke Python, pa je uvek dostupan za upotrebu. Za <code>pdb</code>, možete postaviti <em>breakpoint</em> na funkciju <code>set_trace()</code> kako biste mogli da prolazite kroz kod korak po korak i inspekujete varijable interaktivno.</li>\n<li><strong>Vežbanje korišćenja alata za debagovanje u IDE-u</strong>: Integrisana razvojna okruženja (IDE) i uređivači koda kao što su Pycharm i VS Code pružaju alate za debagovanje sa <em>breakpoints</em>-ovima, izvršavanjem korak po korak, inspekcijom varijabli i drugim funkcijama za debagovanje.</li>\n</ul>\n<h2>Rešavanje greške</h2>\n<ul>\n<li>\n<p><strong><code>try...except</code></strong>: Ovo se koristi za izvršavanje dela koda koje može izazvati grešku. Deo koda <code>try</code> je deo gde očekujete da će greška nastupiti, dok deo <code>except</code> hvata specifičnu grešku i izvršava se ako ta greška nastupi. Evo primera:</p>\n<pre><code class=\"language-py\">try:\n  print(22 / 0)\nexcept ZeroDivisionError:\n  print('You can\\'t divide by zero!')\n  # You can't divide by zero!\n</code></pre>\n</li>\n</ul>\n<p>Takođe možete povezati više delova <code>except</code> radi rukovanja većim brojem vrsta grešaka:</p>\n<pre><code class=\"language-py\">try:\n  number = int(input('Enter a number: '))\n  print(22 / number)\nexcept ZeroDivisionError:\n  print('You cannot divide by zero!')\n  # You cannot divide by zero! prints when you enter 0\nexcept ValueError:\n  print('Please enter a valid number!')\n  # Please enter a valid number! prints when you enter a string  \n</code></pre>\n<ul>\n<li>\n<p><strong><code>else</code> na <code>finally</code></strong>: Ovi delovi dodaju <code>try...except</code>. Ako ne dođe do greške, deo <code>else</code> radi. Deo <code>finally</code> funkcioniše uvek bez obzira na to da li su se greške desile ili ne.</p>\n<pre><code class=\"language-py\">try:\n  result = 100 / 4\nexcept ZeroDivisionError:\n  print('You cannot divide by zero!') # This will not run\nelse:\n  print(f'Result is {result}') # Result is 25.0\nfinally:\n  print('Execution complete!') # Execution complete!\n</code></pre>\n</li>\n<li>\n<p><strong>Objekat izuzetka (Exception Object)</strong>: Ovo vam omogućava da uhvatite sam izuzetak radi efikasnijeg rukovanja greškama i direktnog ispisivanja poruke o izuzetku. Da biste dobili objekat izuzetka, morate koristiti ključnu reč <code>as</code>. Evo primera:</p>\n<pre><code class=\"language-py\">try:\n    value = int('This will raise an error')\nexcept ValueError as e:\n    print(f'Caught an error: {e}')\n    # Caught an error: invalid literal for int() with base 10: 'This will raise an error'\n</code></pre>\n</li>\n<li>\n<p><strong>Izjava za <code>raise</code></strong>: Ovo vam omogućava da ručno bacite grešku. Možete ga koristiti za bacanje greške kada je određeni uslov ispunjen. Evo primera:</p>\n<pre><code class=\"language-py\">def divide(a, b):\n    if b == 0:\n        raise ZeroDivisionError('You cannot divide by zero')\n    return a / b\n```## Indikacija greške\n\n</code></pre>\n</li>\n</ul>\n<p>Izjava <code>raise</code> je takođe važna kada kreirate svoje prilagođene greške, jer ga možete koristiti za bacanje greške sa specifičnom porukom. Evo primera:</p>\n<pre><code class=\"language-py\">class InvalidCredentialsError(Exception):\n    def __init__(self, message=\"Invalid username or password\"):\n        self.message = message\n        super().__init__(self.message)\n\ndef login(username, password):\n    stored_username = \"admin\"\n    stored_password = \"password123\"\n    \n    if username != stored_username or password != stored_password:\n        raise InvalidCredentialsError()\n    \n    return f\"Welcome, {username}!\"\n</code></pre>\n<p>Evo kako možete koristiti funkciju <code>login</code> zajedno sa greškom <code>InvalidCredentialsError</code>:</p>\n<pre><code class=\"language-py\"># failed login attempt\ntry:\n    message = login(\"user\", \"wrongpassword\")\nexcept InvalidCredentialsError as e:\n    print(f\"Login failed: {e}\")\nelse:\n    print(message)\n\n# successful login attempt\ntry:\n    message = login(\"admin\", \"password123\")\nexcept InvalidCredentialsError as e:\n    # This block is not executed because the login was successful\n    print(f\"Login failed: {e}\")\nelse:\n    # The else block runs if the 'try' block completes without an exception\n    print(message)\n</code></pre>\n<p>Izraz <code>raise</code> takođe se može koristiti sa glavnim terminom <code>from</code> za povezivanje grešaka, pokazujući odnos između različitih grešaka:</p>\n<pre><code class=\"language-py\">def parse_config(filename):\n  try:\n      with open(filename, 'r') as file:\n          data = file.read()\n          return int(data)\n  except FileNotFoundError:\n      raise ValueError('Configuration file is missing') from None\n  except ValueError as e:\n      raise ValueError('Invalid configuration format') from e\n\nconfig = parse_config('config.txt')\n</code></pre>\n</section>",
  "translationPending": false,
  "block": "review-error-handling",
  "blockLabel": "review",
  "blockLayout": "link",
  "hasEditableBoundaries": false,
  "order": 32,
  "instructions": "",
  "questions": [],
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 0,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-error-handling",
  "certification": "python-v9"
}