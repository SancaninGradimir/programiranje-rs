{
  "id": "5a24c314108439a4d4036156",
  "title": "Koristite centralnu aplikaciju za obradu više zadataka istovremeno",
  "challengeType": 6,
  "forumTopicId": 301451,
  "dashedName": "use-middleware-to-handle-asynchronous-actions",
  "challengeFiles": [
    {
      "id": "",
      "editableRegionBoundaries": [],
      "history": [
        "script.js"
      ],
      "name": "script",
      "ext": "js",
      "path": "script.js",
      "fileKey": "scriptjs",
      "contents": "const REQUESTING_DATA = 'REQUESTING_DATA'\nconst RECEIVED_DATA = 'RECEIVED_DATA'\n\nconst requestingData = () => { return {type: REQUESTING_DATA} }\nconst receivedData = (data) => { return {type: RECEIVED_DATA, users: data.users} }\n\nconst handleAsync = () => {\n  return function(dispatch) {\n    // Dispatch request action here\n\n    setTimeout(function() {\n      let data = {\n        users: ['Jeff', 'William', 'Alice']\n      }\n      // Dispatch received data action here\n\n    }, 2500);\n  }\n};\n\nconst defaultState = {\n  fetching: false,\n  users: []\n};\n\nconst asyncDataReducer = (state = defaultState, action) => {\n  switch(action.type) {\n    case REQUESTING_DATA:\n      return {\n        fetching: true,\n        users: []\n      }\n    case RECEIVED_DATA:\n      return {\n        fetching: false,\n        users: action.users\n      }\n    default:\n      return state;\n  }\n};\n\nconst store = Redux.createStore(\n  asyncDataReducer,\n  Redux.applyMiddleware(ReduxThunk.default)\n);",
      "error": null,
      "seed": "const REQUESTING_DATA = 'REQUESTING_DATA'\nconst RECEIVED_DATA = 'RECEIVED_DATA'\n\nconst requestingData = () => { return {type: REQUESTING_DATA} }\nconst receivedData = (data) => { return {type: RECEIVED_DATA, users: data.users} }\n\nconst handleAsync = () => {\n  return function(dispatch) {\n    // Dispatch request action here\n\n    setTimeout(function() {\n      let data = {\n        users: ['Jeff', 'William', 'Alice']\n      }\n      // Dispatch received data action here\n\n    }, 2500);\n  }\n};\n\nconst defaultState = {\n  fetching: false,\n  users: []\n};\n\nconst asyncDataReducer = (state = defaultState, action) => {\n  switch(action.type) {\n    case REQUESTING_DATA:\n      return {\n        fetching: true,\n        users: []\n      }\n    case RECEIVED_DATA:\n      return {\n        fetching: false,\n        users: action.users\n      }\n    default:\n      return state;\n  }\n};\n\nconst store = Redux.createStore(\n  asyncDataReducer,\n  Redux.applyMiddleware(ReduxThunk.default)\n);"
    }
  ],
  "solutions": [
    [
      {
        "id": "",
        "history": [
          "script.js"
        ],
        "name": "script",
        "ext": "js",
        "path": "script.js",
        "fileKey": "scriptjs",
        "contents": "const REQUESTING_DATA = 'REQUESTING_DATA'\nconst RECEIVED_DATA = 'RECEIVED_DATA'\n\nconst requestingData = () => { return {type: REQUESTING_DATA} }\nconst receivedData = (data) => { return {type: RECEIVED_DATA, users: data.users} }\n\nconst handleAsync = () => {\n  return function(dispatch) {\n    dispatch(requestingData());\n    setTimeout(function() {\n      let data = {\n        users: ['Jeff', 'William', 'Alice']\n      }\n      dispatch(receivedData(data));\n    }, 2500);\n  }\n};\n\nconst defaultState = {\n  fetching: false,\n  users: []\n};\n\nconst asyncDataReducer = (state = defaultState, action) => {\n  switch(action.type) {\n    case REQUESTING_DATA:\n      return {\n        fetching: true,\n        users: []\n      }\n    case RECEIVED_DATA:\n      return {\n        fetching: false,\n        users: action.users\n      }\n    default:\n      return state;\n  }\n};\n\nconst store = Redux.createStore(\n  asyncDataReducer,\n  Redux.applyMiddleware(ReduxThunk.default)\n);",
        "error": null,
        "seed": "const REQUESTING_DATA = 'REQUESTING_DATA'\nconst RECEIVED_DATA = 'RECEIVED_DATA'\n\nconst requestingData = () => { return {type: REQUESTING_DATA} }\nconst receivedData = (data) => { return {type: RECEIVED_DATA, users: data.users} }\n\nconst handleAsync = () => {\n  return function(dispatch) {\n    dispatch(requestingData());\n    setTimeout(function() {\n      let data = {\n        users: ['Jeff', 'William', 'Alice']\n      }\n      dispatch(receivedData(data));\n    }, 2500);\n  }\n};\n\nconst defaultState = {\n  fetching: false,\n  users: []\n};\n\nconst asyncDataReducer = (state = defaultState, action) => {\n  switch(action.type) {\n    case REQUESTING_DATA:\n      return {\n        fetching: true,\n        users: []\n      }\n    case RECEIVED_DATA:\n      return {\n        fetching: false,\n        users: action.users\n      }\n    default:\n      return state;\n  }\n};\n\nconst store = Redux.createStore(\n  asyncDataReducer,\n  Redux.applyMiddleware(ReduxThunk.default)\n);"
      }
    ]
  ],
  "assignments": [],
  "tests": [
    {
      "text": "<p>Kreator akcije <code>requestingData</code> treba da vrati nešto istog tipa kao vrednost <code>REQUESTING_DATA</code>.</p>",
      "testString": "assert(requestingData().type === REQUESTING_DATA);"
    },
    {
      "text": "<p>Kreator akcije <code>receivedData</code> treba da vrati objekat istog tipa kao vrednost od <code>RECEIVED_DATA</code>.</p>",
      "testString": "assert(receivedData('data').type === RECEIVED_DATA);"
    },
    {
      "text": "<p><code>asyncDataReducer</code> Trebalo bi da bude funkcija.</p>",
      "testString": "assert(typeof asyncDataReducer === 'function');"
    },
    {
      "text": "<p>Radnja slanja <code>requestingData</code> treba da ažurira svojstvo fetchovanja u skladištu <code>state</code> na <code>true</code>.</p>",
      "testString": "assert(\n  (function () {\n    const initialState = store.getState();\n    store.dispatch(requestingData());\n    const reqState = store.getState();\n    return initialState.fetching === false && reqState.fetching === true;\n  })()\n);"
    },
    {
      "text": "<p>Slanje <code>handleAsync</code> mora prvo poslati akciju zahteva podataka, a zatim poslati akciju sa primljenim podacima nakon kašnjenja.</p>",
      "testString": "assert(\n  (function () {\n    const noWhiteSpace = __helpers.removeWhiteSpace(handleAsync.toString());\n    return (\n      noWhiteSpace.includes('dispatch(requestingData())') === true &&\n      noWhiteSpace.includes('dispatch(receivedData(data))') === true\n    );\n  })()\n);"
    }
  ],
  "description": "<section id=\"description\">\n<p>Do sada su ove izazove izbegli diskutovati asinhrone akcije, ali je to neizbežan deo razvoja mreže. Ponekad ćete morati da pozovete asinhrone akcije u vašoj Redux aplikaciji, pa kako rešavate ovakve zahteve? Redux pruža <em>middleware</em> dizajniran posebno za ove svrhe, nazvan Redux Thunk. Evo kratkog objašnjenja kako koristiti ovo sa Redux-om.</p>\n<p>Da biste uključili Redux Thunk middleware, ga šaljete kao argument za <code>Redux.applyMiddleware()</code>. Ova izjava se zatim pruža kao drugi opcioni parametar funkciji <code>createStore()</code>. Pogledajte kod ispod u editoru da biste ovo videli. Zatim, da biste kreirali asinhroni akciju, umotavate funkciju u kreator akcija koji uzima <code>dispatch</code> kao argument. Unutar ove funkcije, možete poslati akcije i izvršiti asinhrone zahteve.</p>\n<p>U ovom primeru, zahtev koji nije trenutni je kreiran pozivom <code>setTimeout()</code>. Uobičajeno je poslati akciju pre pokretanja bilo kakvog ne-trenutnog ponašanja kako bi stanje vaše aplikacije znalo da su podaci potrebni (ovo stanje može prikazivati ikonu učitavanja, na primer). Zatim, kada povratite podatke, koristite drugu akciju koja nosi podatke kao opterećenje (payload) zajedno sa informacijom da je akcija završena.</p>\n<p>Zapamtite da prosljeđujete <code>dispatch</code> kao parametar ovom specifičnom kreatoru akcije. Ovo ćete koristiti za slanje svojih akcija, prosleđujući akciju direktno na dispatch, a middleware obrađuje sve ostalo.</p>\n</section>",
  "instructions": "<section id=\"instructions\">\n<p>Napišite oba dispača u kreator akcije <code>handleAsync()</code>. Pošaljite <code>requestingData()</code> pre <code>setTimeout()</code> (kreirani poziv za API). Zatim, nakon prijema podataka (dummy), pošaljite akciju <code>receivedData()</code>, prosleđujući ove podatke. Sada znate kako da rukujete asinhronim akcijama u Reduxu. Sve ostalo i dalje radi kao pre.</p>\n</section>",
  "translationPending": false,
  "block": "redux",
  "blockLayout": "legacy-challenge-list",
  "hasEditableBoundaries": false,
  "order": 4,
  "questions": [],
  "superBlock": "front-end-development-libraries",
  "superOrder": 17,
  "challengeOrder": 11,
  "isLastChallengeInBlock": false,
  "required": [
    {
      "src": "https://cdnjs.cloudflare.com/ajax/libs/redux/3.7.2/redux.min.js"
    },
    {
      "src": "https://cdnjs.cloudflare.com/ajax/libs/redux-thunk/2.2.0/redux-thunk.min.js"
    }
  ],
  "helpCategory": "JavaScript",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "certification": "front-end-development-libraries"
}