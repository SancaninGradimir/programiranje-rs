{
  "id": "67c9e932c6c4234532d46394",
  "title": "Testiranje React formulara, pristup podacima i rutiranje",
  "challengeType": 31,
  "dashedName": "review-react-forms-data-fetching-and-routing",
  "solutions": [],
  "assignments": [
    "<p>Proveriti rutiranje za React, upravljanje stanjem, forme i dostupnost podataka.</p>"
  ],
  "tests": [],
  "description": "<section id=\"description\">\n<h2>Rad sa formularima u React</h2>\n<ul>\n<li><strong>Kontrolisani polja za unos</strong>: Ovo je mesto gde čuvate vrednost polja za unos u <em>state</em> i ažurirate ga putem događaja <code>onChange</code>. Ovo vam daje potpunu kontrolu nad podacima forme i omogućava trenutnu validaciju i prikaz uslova.</li>\n</ul>\n<pre><code class=\"language-jsx\">import { useState } from \"react\";\n\nfunction App() {\n  const [name, setName] = useState(\"\");\n\n  const handleChange = (e) => {\n    setName(e.target.value);\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log(name);\n  };\n\n  return (\n    &#x3C;>\n      &#x3C;form onSubmit={handleSubmit}>\n        &#x3C;label htmlFor=\"name\">Your name&#x3C;/label> &#x3C;br />\n        &#x3C;input value={name} id=\"name\" onChange={handleChange} type=\"text\" />\n        &#x3C;button type=\"submit\">Submit&#x3C;/button>\n      &#x3C;/form>\n    &#x3C;/>\n  );\n}\n\nexport default App;\n</code></pre>\n<ul>\n<li><strong>Nekontrolisane ulazne komponente</strong>: Umesto rukovanja ulaznim delovima putem veze <code>useState</code>, nekontrolisane komponente u HTML čuvaju svoja interna stanja uz pomoć DOM. Pošto DOM kontroliše vrednosti unosa, potrebno je dohvatiti vrednosti ulaznih delova za <code>ref</code>.</li>\n</ul>\n<pre><code class=\"language-jsx\">import { useRef } from \"react\";\n\nfunction App() {\n const nameRef = useRef();\n\n const handleSubmit = (e) => {\n   e.preventDefault();\n   console.log(nameRef.current.value);\n };\n\n return (\n   &#x3C;form onSubmit={handleSubmit}>\n     &#x3C;label htmlFor=\"name\">Your&#x3C;/label>{\" \"}\n     &#x3C;input type=\"text\" ref={nameRef} id=\"name\" />\n     &#x3C;button type=\"submit\">Submit&#x3C;/button>\n   &#x3C;/form>\n );\n}\n\nexport default App;\n</code></pre>\n<h2>Rad sa vezom za <code>useActionState</code></h2>\n<ul>\n<li><strong>Akcije na serveru</strong>: Ovo su funkcije koje se izvršavaju na serveru kako bi omogućile obradu formulara direktno na serveru bez potrebe za krajnjom tačkom (endpoint) API. Evo primera iz Next.js aplikacije:</li>\n</ul>\n<pre><code class=\"language-js\">\"use server\";\n\nasync function submitForm(formData) {\n const name = formData.get(\"name\");\n return { message: `Hello, ${name}!` };\n}\n</code></pre>\n<p>Komanda za <code>\"user server\"</code> prikazuje funkciju kao akciju servera.</p>\n<ul>\n<li><strong>Kiungo cha <code>useActionState</code></strong>: Ova veza ažurira stanje na osnovu rezultata slanja forme. Ovde je osnovna sintaksa za komponentu <code>useActionState</code>:</li>\n</ul>\n<pre><code class=\"language-js\">const [state, action, isPending] = useActionState(actionFunction, initialState, permalink);\n</code></pre>\n<ul>\n<li><code>state</code> je trenutno stanje koje akcija vraća.</li>\n<li><code>action</code> je funkcija koja pokreće server akciju.</li>\n<li><code>isPending</code> je vrednost tačno ili netačno koja pokazuje da li akcija teče.</li>\n<li>Parametar <code>actionFunction</code> je sama server akcija.</li>\n<li><code>initialState</code> je parametar koji pokazuje početno stanje pre nego što akcija počne.</li>\n<li><code>permalink</code> je opciona niz znakova koja pokazuje URL jedinstveni identifikator stranice koju obrazac menja.</li>\n</ul>\n<h2>Dostupnost podataka u React</h2>\n<ul>\n<li><strong>Opcije za dohvaćanje podataka</strong>: Postoji mnogo različitih načina za dohvaćanje podataka u React. Možete koristiti naivni (native) Fetch API, ili alate trećih strana kao što su Axios ili SWR.</li>\n<li><strong>Često korišćene promene stanja prilikom dohvaćanja podataka</strong>: Bez obzira na metod koji koristite za dohvaćanje podataka u React, postoje delovi stanja koje ćete morati pratiti. Prvo su sami podaci. Drugo je praćenje da li su podaci i dalje dostupni. Treće je stanje koje će držati sve greške koje mogu nastupiti tokom procesa dohvaćanja podataka.</li>\n</ul>\n<pre><code class=\"language-js\">const [data, setData] = useState(null);\nconst [loading, setLoading] = useState(true);\nconst [error, setError] = useState(null);\n</code></pre>\n<p>Pošto je dostupnost podataka sporedni efekat, bolje je koristiti <code>Fetch API</code> unutar veze <code>useEffect</code>.</p>\n<pre><code class=\"language-js\">useEffect(() => {\n  const fetchData = async () => {\n    try {\n      const res = await fetch(\"https://jsonplaceholder.typicode.com/posts\");\n      \n      if (!res.ok) {\n        throw new Error(\"Network response was not ok\");\n      }\n\n      const data = await res.json();\n      setData(data);\n    } catch (err) {\n      setError(err);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  fetchData();\n}, []);\n</code></pre>\n<p>Zatim možete prikazati poruku učitavanja ako nije potpuna dostupnost podataka, poruku greške ako je došlo do greške prilikom dohvaćanja podataka, ili rezultate.</p>\n<pre><code class=\"language-jsx\">if (loading) {\n  return &#x3C;p>Loading...&#x3C;/p>;\n}\n\nif (error) {\n  return &#x3C;p>{error.message}&#x3C;/p>;\n}\n\nreturn (\n  &#x3C;ul>\n    {data.map((post) => (\n      &#x3C;li key={post.id}>{post.title}&#x3C;/li>\n    ))}\n  &#x3C;/ul>\n);\n</code></pre>\n<p>Ako želite koristiti Axios, morate ga instalirati i uvesti:</p>\n<pre><code class=\"language-bash\">npm i axios\n</code></pre>\n<pre><code class=\"language-js\">import axios from \"axios\";\n</code></pre>\n<p>Zatim možete dobiti podatke koristeći <code>axios.get</code>:</p>\n<pre><code class=\"language-jsx\">const [data, setData] = useState(null);\nconst [loading, setLoading] = useState(true);\nconst [error, setError] = useState(null);\n\nuseEffect(() => {\n  const fetchData = async () => {\n    try {\n      const res = await axios.get(\n        \"https://jsonplaceholder.typicode.com/users\"\n      );\n      setData(res.data);\n    } catch (err) {\n      setError(err);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  fetchData();\n}, []);\n</code></pre>\n<p>Da biste dobili podatke koristeći link <code>useSWR</code>, prvo morate da ga instalirate i dovedete.</p>\n<pre><code class=\"language-bash\">npm i swr\n</code></pre>\n<pre><code class=\"language-js\">import useSWR from \"swr\";\n</code></pre>\n<p>Evo kako možete koristiti link da biste dobili podatke:</p>\n<pre><code class=\"language-jsx\">import useSWR from \"swr\";\n\nconst fetcher = (url) => fetch(url).then((res) => res.json());\n\nconst FetchTodos = () => {\n const { data, error } = useSWR(\n   \"https://jsonplaceholder.typicode.com/todos\",\n   fetcher\n );\n\n if (!data) {\n   return &#x3C;h2>Loading...&#x3C;/h2>;\n }\n if (error) {\n   return &#x3C;h2>Error: {error.message}&#x3C;/h2>;\n }\n\n return (\n   &#x3C;>\n     &#x3C;h2>Todos&#x3C;/h2>\n     &#x3C;div>\n       {data.map((todo) => (\n         &#x3C;h3 key={todo.id}>{todo.title}&#x3C;/h3>\n       ))}\n     &#x3C;/div>\n   &#x3C;/>\n );\n};\n\nexport default FetchTodos;\n</code></pre>\n<h2>Rad sa vezom <code>useOptimistic</code></h2>\n<ul>\n<li><strong>Povez za <code>useOptimistic</code></strong>: Ovaj mehanizam se koristi za održavanje interfejsa korisnika (UI) aktivnim dok čeka da akcija async bude završena u pozadini. Pomaže u upravljanju \"optimističnim ažuriranjima\" unutar UI-a, tehnika koja pruža trenutne ažuracije interfejsu na osnovu očekivanog rezultata akcije, kao što je čekanje odgovora sa servera.</li>\n</ul>\n<p>Evo osnovne sintakse:</p>\n<pre><code class=\"language-js\">const [optimisticState, addOptimistic] = useOptimistic(actualState, updateFunction);\n</code></pre>\n<ul>\n<li><code>optimisticState</code> je privremeno stanje koje poboljšava trenutno za bolji korisnički doživljaj.</li>\n<li><code>addOptimistic</code> je funkcija koja izvršava optimističko ažuriranje pre stvarnog menjanja stanja.</li>\n<li><code>actualState</code> je stvarna vrednost stanja koja proizlazi iz rezultata akcije, kao što je dobijanje podataka sa servera.</li>\n<li><code>updateFunction</code> je funkcija koja određuje kako bi optimistično stanje trebalo da se ažurira kada se pozove.</li>\n</ul>\n<p>Ovo je primer korišćenja veze za <code>useOptimistic</code> u sekciji <code>TaskList</code>:</p>\n<pre><code class=\"language-jsx\">\"use client\";\n\nimport { useOptimistic } from \"react\";\n\nexport default function TaskList({ tasks, addTask }) {\n  const [optimisticTasks, addOptimisticTask] = useOptimistic(\n    tasks,\n    (state, newTask) => [...state, { text: newTask, pending: true }]\n  );\n\n  async function handleSubmit(e) {\n    e.preventDefault();\n    const formData = new FormData(e.target);\n\n    addOptimisticTask(formData.get(\"task\"));\n\n    addTask(formData);\n    e.target.reset();\n  }\n\n  return &#x3C;>{/* UI */}&#x3C;/>;\n}\n</code></pre>\n<ul>\n<li><strong><code>startTransition</code></strong>: Ovo se koristi za prikaz dela interfejsa (UI) i prikaz ažuriranja stanja kao nekritične promene. To omogućava da se UI promeni tokom kompleksnih ažuriranja. Evo osnovne sintakse:</li>\n</ul>\n<pre><code class=\"language-js\">startTransition(action);\n</code></pre>\n<p><code>action</code> izvršava ažuriranje stanja ili inicijalizuje logiku vezanu za nehitne promene. Ovo osigurava da hitna ažuriranja korisničkog interfejsa (kao što je kucanje ili klikanje) ne budu blokirana.</p>\n<h2>Rad sa vezom za <code>useMemo</code></h2>\n<ul>\n<li><strong>Memorizacija</strong>: Ovo je tehnika optimizacije gde su rezultati poziva skupih funkcija sačuvani (zapamćeni) na osnovu specifičnih argumenata. Kada se isti argumenti ponovo pozovu, vraćaju se sačuvani rezultati umesto da se funkcija ponovo izračunava.</li>\n<li><strong>Poveznik <code>useMemo</code></strong>: Ovaj veznik se koristi za skladištenje izračunatih vrednosti. Evo primera čuvanja rezultata sortiranja velikog niza podataka. <code>expensiveSortFunction</code> će se izvršiti samo kada <code>largeArray</code> promeni:</li>\n</ul>\n<pre><code class=\"language-js\">const memoizedSortedArray = useMemo(\n  () => expensiveSortFunction(largeArray),\n  [largeArray]\n);\n</code></pre>\n<h2>Rad sa komponentom <code>useCallback</code></h2>\n<ul>\n<li><strong>Poveznik za <code>useCallback</code></strong>: Ovo se koristi za skladištenje referenci funkcija.</li>\n</ul>\n<pre><code class=\"language-js\">const handleClick = useCallback(() => {\n  // code goes here\n}, [dependency]);\n</code></pre>\n<ul>\n<li><strong><code>React.memo</code></strong>: Ovo se koristi za čuvanje sekcije ili sprečavanje da se ponovo prikaže bez razloga kada njegove parametre nisu promenili.</li>\n</ul>\n<pre><code class=\"language-jsx\">const MemoizedComponent = React.memo(({ prop }) => {\n return (\n   &#x3C;>\n     {/* Presentation */}\n   &#x3C;/>\n )\n});\n</code></pre>\n<h2>Alat za upravljanje zavisnostima</h2>\n<ul>\n<li><strong>Definicija zavisnosti</strong>: U programu, zavisnost je kada jedan deo ili modul u programu zavisi od drugog kako bi pravilno funkcionisao. Zavisnost je uobičajena u programima jer omogućava programerima da koriste funkcije ili alate koje su prethodno napravili drugi. Dve glavne zavisnosti potrebne za projekat React biće paketi <code>react</code> i <code>react-dom</code>:</li>\n</ul>\n<pre><code class=\"language-json\">\"dependencies\": {\n  \"react\": \"^18.3.1\",\n  \"react-dom\": \"^18.3.1\"\n}\n</code></pre>\n<ul>\n<li><strong>Definicija menadžera paketa</strong>: Da biste upravljali zavisnostima softvera u projektu, potrebno je da koristite menadžer paketa. Menadžer paketa je alat koji se koristi za instalaciju, ažuriranje i uklanjanje zavisnosti. Mnogi popularni programski jezici kao JavaScript, Python, Ruby i Java, koriste menadžere paketa. Popularni menadžeri paketa za JavaScript su npm, Yarn i pnpm.</li>\n<li><strong>Datoteka <code>package.json</code></strong>: Ovo je ključna konfiguraciona datoteka u projektima koja sadrži metadata o vašem projektu, uključujući ime, verziju i zavisnosti. Takođe opisuje skripte, informacije o licenci i druge postavke koje pomažu u upravljanju projektom i njegovim zavisnostima.</li>\n<li><strong>Datoteka <code>package-lock.json</code></strong>: Ova datoteka će zaključati tačnu verziju svih paketa korišćenih u vašem projektu. Kada ažurirate paket, nova verzija će biti ažurirana i u ovoj fajl za zaključavanje.</li>\n<li><strong>Direktorijum <code>node_modules</code></strong>: Ovaj direktorijum sadrži tačan kod zavisnosti naveden u vašoj datoteci <code>package.json</code>, uključujući direktne zavisnosti vašeg projekta i zavisnosti tih zavisnosti.</li>\n<li><strong>Zavisnosti za razvoj</strong>: Ovo su paketi koji se koriste samo za razvoj, a ne za produkciju. Primer je biblioteka za testiranje kao što je Jest. Instaliraćete Jest kao zavisnost za razvoj jer vam je potreban za interno testiranje vaše aplikacije, ali nije potreban za pokretanje aplikacije u produkciji.</li>\n</ul>\n<pre><code class=\"language-json\">\"devDependencies\": {\n  \"@eslint/js\": \"^9.17.0\",\n  \"@types/react\": \"^18.3.18\",\n  \"@types/react-dom\": \"^18.3.5\",\n  \"@vitejs/plugin-react\": \"^4.3.4\",\n  \"eslint\": \"^9.17.0\",\n  \"eslint-plugin-react\": \"^7.37.2\",\n  \"eslint-plugin-react-hooks\": \"^5.0.0\",\n  \"eslint-plugin-react-refresh\": \"^0.4.16\",\n  \"globals\": \"^15.14.0\",\n  \"vite\": \"^6.0.5\"\n}\n</code></pre>\n<h2>Planer puta za React</h2>\n<ul>\n<li><strong>Uvod</strong>: React Router je biblioteka treće strane koja omogućava dodavanje rutiranja u React aplikacije. Da bi počeo, potrebno je da instaliraš React Router u postojeći React projekat na sledeći način:</li>\n</ul>\n<pre><code class=\"language-bash\">npm i react-router\n</code></pre>\n<p>Zatim unutar fajla <code>main.jsx</code> ili <code>index.jsx</code>, biće vam potrebno da postavite strukturu putanje kao što sledi:</p>\n<pre><code class=\"language-jsx\">import { StrictMode } from \"react\";\nimport { createRoot } from \"react-dom/client\";\nimport { BrowserRouter, Routes, Route } from \"react-router\";\nimport App from \"./App.jsx\";\n\nimport \"./index.css\";\n\ncreateRoot(document.getElementById(\"root\")).render(\n  &#x3C;StrictMode>\n    &#x3C;BrowserRouter>\n      &#x3C;Routes>\n        &#x3C;Route path=\"/\" element={&#x3C;App />} />\n      &#x3C;/Routes>\n    &#x3C;/BrowserRouter>\n  &#x3C;/StrictMode>\n);\n</code></pre>\n<p><code>path</code> i <code>element</code> služe za povezivanje URL sa UI sekcijom. U ovom slučaju, postavljamo putanju za početnu stranicu koja se odnosi na sekciju <code>App</code>.</p>\n<ul>\n<li><strong>Mnogi komentari i konfiguracija putanja</strong>: Uobičajeno je u velikim aplikacijama da imaju mnogo komentara i putanja organizovane na sledeći način:</li>\n</ul>\n<pre><code class=\"language-jsx\">&#x3C;Routes>\n  &#x3C;Route index element={&#x3C;Home />} />\n  &#x3C;Route path=\"about\" element={&#x3C;About />} />\n\n  &#x3C;Route path=\"products\">\n    &#x3C;Route index element={&#x3C;ProductsHome />} />\n    &#x3C;Route path=\":category\" element={&#x3C;Category />} />\n    &#x3C;Route path=\":category/:productId\" element={&#x3C;ProductDetail />} />\n    &#x3C;Route path=\"trending\" element={&#x3C;Trending />} />\n  &#x3C;/Route>\n&#x3C;/Routes>\n</code></pre>\n<p>Parametri <code>index</code> u ovim primerima namijenjeni su da predstavljaju osnovni put za određeni dio puta. Stoga, deo <code>Home</code> će biti prikazan u korijenskom putu <code>/</code> kada je deo <code>ProductsHome</code> prikazan u putu <code>/products</code>.</p>\n<ul>\n<li><strong>Grupisanje unutar putanje</strong>: Možete grupisati putanju unutar druge putanje, što uzrokuje dodavanje putanje deteta na putanju roditelja.</li>\n</ul>\n<pre><code class=\"language-jsx\">&#x3C;Route path=\"products\">\n  &#x3C;Route path=\"trending\" element={&#x3C;Trending />} />\n&#x3C;/Route>\n</code></pre>\n<p>U gornjem primeru, putanja za popularne proizvode će biti products/trending.</p>\n<ul>\n<li><strong>Promenljivi delovi</strong>: Promenljivi deo je tamo gde bilo koji deo putanje URL se menja.</li>\n</ul>\n<pre><code class=\"language-jsx\">&#x3C;Route path=\":category\" element={&#x3C;Category />} />\n</code></pre>\n<p>U ovom primeru imamo promenljivi deo nazvan <code>category</code>. Kada korisnik klikne URL kao products/brass-instruments, onda se sadržaj menja na sekciju <code>Category</code> i možete dobiti relevante podatke putem tog promenljivog dela.</p>\n<ul>\n<li><strong>Poveznica za <code>useParams</code></strong>: Ova poveznica se koristi za dobijanje promenljivih parametara iz putanje URL.</li>\n</ul>\n<pre><code class=\"language-jsx\">import { useParams } from \"react-router\";\n\nexport default function Category() {\n  let params = useParams();\n  {/* Accessing the category param: params.category */}\n  {/* rest of code goes here */}\n}\n</code></pre>\n<h2>Sistemi za React</h2>\n<ul>\n<li><strong>Uvod</strong>: Sistemi React pružaju funkcije kao što su rutiranje, optimizacija slika, pristup podacima, verifikacija identiteta i još mnogo toga. To znači da možda nećete morati da postavljate zasebne frontend i backend aplikacije za neke slučajeve upotrebe. Primeri sistema React su Next.js i Remix.</li>\n<li><strong>Rutiranje u Next.js</strong>: Ovaj okvir za rutiranje uključuje podršku za dinamičke rute, paralelne rute, rukovače ruta (route handlers), preusmeravanja, internacionalizaciju i više.</li>\n</ul>\n<p>Ovo je primer kako kreirati prilagođeni rukovatelja zahteva:</p>\n<pre><code class=\"language-js\">export async function GET() {\n  const res = await fetch(\"https://example-api.com\");\n  const data = await res.json();\n\n  return Response.json({ data });\n}\n</code></pre>\n<ul>\n<li><strong>Optimizacija slika u Next.js-u</strong>: Sekcija <code>Image</code> dodaje izvornu funkcionalnost HTML od <code>img</code>, omogućavajući brzo učitavanje stranica i optimizaciju veličine. To znači da će slike biti učitane tek kada uđu u vidokrug (viewport), a sekcija <code>Image</code> direktno serviratiće slike odgovarajuće veličine za svaki uređaj.</li>\n</ul>\n<pre><code class=\"language-jsx\">import Image from \"next/image\";\n\nexport default function Page() {\n  return (\n    &#x3C;Image src=\"link-to-image-goes-here\" alt=\"descriptive-title-goes-here\" />\n  );\n}\n</code></pre>\n<h2>Prosleđivanje svojstava detijskim komponentama (Prop drilling)</h2>\n<ul>\n<li><strong>Definicija</strong>: Prenos parametara je proces prenošenja parametara iz roditeljske regije ka duboko lokalisovanim regionima potomstva, čak i kada određeni regioni potomstva ne zahtevaju te parametre.</li>\n</ul>\n<h2>Upravljanje stanjem</h2>\n<ul>\n<li><strong>API konteksta</strong>: Kontekst se odnosi na situaciju kada rodenski deo generiše informacije za dete delove bez potrebe za direktnim prosleđivanjem kroz parametre. <code>createContext</code> koristi se za kreiranje objekta konteksta koji predstavlja kontekst koji će drugi delovi čitati. <code>Provider</code> koristi se za prosleđivanje vrednosti konteksta detetovim delovima.</li>\n</ul>\n<pre><code class=\"language-jsx\">import { useState, createContext } from \"react\";\n\nconst CounterContext = createContext();\n\nconst CounterProvider = ({ children }) => {\n  const [count, setCount] = useState(0);\n\n  return (\n    &#x3C;CounterContext.Provider value={{ count, setCount }}>\n      {children}\n    &#x3C;/CounterContext.Provider>\n  );\n};\n\nexport { CounterContext, CounterProvider };\n</code></pre>\n<ul>\n<li>\n<p><strong>Redux</strong>: Redux upravlja stanjem (state) pružajući centralno skladište i strogu kontrolu nad ažuriranjima stanja. Koristi predvidljiv obrazac akcija (actions), reducersa i middleware-a. Akcije su opterećenje podataka koje šalju podatke iz vaše aplikacije u Redux skladište, što se često dešava usled interakcije korisnika. Reducersi su funkcije koje definišu kako bi stanje trebalo da se promeni u skladu sa tim akcijama, osiguravajući da se stanje ažurira nepromenljivo (immutably). Middleware, s druge strane, služi kao most između slanja akcija i reducer-a, omogućavajući vam da dodate funkcionalnost Redux-u (npr., logovanje, rukovanje akcijama async) bez menjanja osnovnog toka.</p>\n</li>\n<li>\n<p><strong>Zustand</strong>: Ovo rešenje za upravljanje stanjem je odlično za male do srednje aplikacije. Radi korišćenjem hook-a <code>useStore</code> za direktno dobijanje stanja (state) u komponentama i stranicama. Ovo vam omogućava da menjate i dohvatate podatke bez potrebe za akcijama, reducerima ili provider-om.</p>\n</li>\n</ul>\n<h2>Ispravljanje grešaka u sekcijama React korišćenjem alata za programere za React</h2>\n<ul>\n<li><strong>Alati za razvojne komponente React</strong>: Ovo je dodatak za pretraživač koji možete koristiti u Chrome-u, Firefox-u i Edge-u za inspekciju delova React i identifikovanje problema sa performansama. Za Safari, potrebno je instalirati npm paket <code>react-devtools</code>. Nakon instalacije DevTools-a React i otvaranja aplikacije React u pretraživaču, otvorite alate za razvojne komponente pretraživača da biste dobili dve dodatne kartice namenjene debagovanju React – Komponenti i Profil.</li>\n<li><strong>Kartica komponenti</strong>: Ova kartica prikazuje svaku komponentu u obliku drveta. Ovde možete uraditi sledeće:\n<ul>\n<li>pregledati komponentno drvo aplikacije</li>\n<li>inspekcionisati i menjati parametre, stanje i vrednosti konteksta u realnom vremenu</li>\n<li>videti izvorni kod svake odabrane komponente</li>\n<li>zapisivati podatke o komponentama u konzolu</li>\n<li>inspekcionisati komponente DOM komponente</li>\n</ul>\n</li>\n<li><strong>Kartica profila</strong>: Ova kartica vam pomaže da analizirate performanse komponenti. Možete snimiti performanse komponente kako biste identifikovali neželjeno ponovno renderovanje, videli vreme renderovanja i zatim optimizovati sporije komponente.</li>\n</ul>\n<h2>Sekcije za servere za React</h2>\n<ul>\n<li><strong>Objašnjenje</strong>: React Server Components su React komponente koje se prikazuju isključivo na serveru i klijentu šalju samo konačni HTML. To znači da mogu direktno pristupati serverskim resursima i značajno smanjiti količinu JavaScript koda koji se šalje pregledaču.</li>\n</ul>\n</section>",
  "translationPending": false,
  "block": "review-react-forms-data-fetching-and-routing",
  "blockLabel": "review",
  "blockLayout": "link",
  "hasEditableBoundaries": false,
  "order": 23,
  "instructions": "",
  "questions": [],
  "superBlock": "front-end-development-libraries-v9",
  "superOrder": 2,
  "challengeOrder": 0,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "JavaScript",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "front-end-development-libraries",
  "module": "react-state-hooks-and-routing",
  "certification": "front-end-development-libraries-v9"
}