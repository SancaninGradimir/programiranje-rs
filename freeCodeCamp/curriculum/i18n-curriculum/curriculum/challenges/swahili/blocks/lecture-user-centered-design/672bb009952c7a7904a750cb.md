---
id: 672bb009952c7a7904a750cb
title: Koja je najbolja metoda za dizajniranje neograničeno/bez kraja?
challengeType: 19
dashedName: what-are-best-practices-for-designing-infinite-scrolls
---

# --interactive--

**NAPOMENA**: Neki primeri interakcije mogu koristiti CSS i JavaScript koje još niste naučili. Ne brinite zbog razumevanja celog koda. Svrha primera je da vam pokaže pregled ovih dizajnerskih koncepata kako biste bolje razumeli kako stvari funkcionišu. Da biste videli pregled, moraćete da omogućite interaktivni urednik.

Kuvinjari bila mwisho je dizajn obrazac koji učitava više sadržaja dok korisnik skroluje dole na stranici. Često se koristi na sajtovima društvenih mreža kao što je Twitter. Na primer, ako ste prijavljeni i želite da vidite više tvita, možete skrolovati nadole i više tvitova će biti učitano. Ovo je primer neograničenog skrolovanja (infinite scrolling).

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}
```

```js
// Simulate loading more content as the user scrolls
window.addEventListener('scroll', () => {
  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

function loadMorePosts() {
  const container = document.querySelector('.infinite-scroll');
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

Beskonačno skrolovanje se takođe koristi umesto paginacije sadržaja na stranicama. Paginacija je dizajnerski stil koji deli sadržaj na različite stranice. Ovo se često koristi kada postoji mnogo sadržaja za prikazivanje. Primer paginacije je kada pretražujete nešto na Google-u i vidite rezultate pretrage na više stranica. Paginacijom, morate kliknuti na dugme da biste prešli na sledeću stranicu. Beskonačnim skrolovanjem, samo nastavite da skrolujete nadole i dodatni sadržaj će biti učitan.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="pagination">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will be on the next pages -->
</div>
<div class="pagination-controls">
  <button class="prev" disabled>Previous</button>
  <button class="next">Next</button>
</div>
<script src="index.js"></script>
```

```css
.pagination {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.pagination-controls {
  margin-top: 12px;
}

.pagination-controls button {
  padding: 8px 16px;
  margin-right: 8px;
}
```

```js
let currentPage = 1;
const postsPerPage = 3;
const totalPosts = 9; 
const totalPages = Math.ceil(totalPosts / postsPerPage);
const container = document.querySelector('.pagination');
const prevButton = document.querySelector('.prev');
const nextButton = document.querySelector('.next'); 

function renderPosts() {
  container.innerHTML = '';
  const start = (currentPage - 1) * postsPerPage;
  const end = start + postsPerPage;
  for (let i = start; i < end && i < totalPosts; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${i + 1}`;
    container.appendChild(post);
  }
  prevButton.disabled = currentPage === 1;
  nextButton.disabled = currentPage === totalPages;
}

prevButton.addEventListener('click', () => {
  if (currentPage > 1) {
    currentPage--;
    renderPosts();
  }
});

nextButton.addEventListener('click', () => {
  if (currentPage < totalPages) {
    currentPage++;
    renderPosts();
  }
});

renderPosts();
```

:::

Kada uvodiš beskonačno skrolovanje u svoj dizajn, postoji nekoliko najboljih praksi koje treba imati na umu. Prva je da obezbediš dugme „Učitaj još“, koje učitava sledeći skup rezultata kada korisnik klikne na njega. To je dobar način da korisniku daš kontrolu nad prikazom dodatnog sadržaja kada to želi.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will load when the user clicks "Load More" -->
</div>
<button class="load-more">Load More</button>
<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.load-more {
  margin-top: 12px;
  padding: 8px 16px;
}
```

```js
const loadMoreButton = document.querySelector('.load-more');
const container = document.querySelector('.infinite-scroll');   
loadMoreButton.addEventListener('click', () => {
  loadMorePosts();
});

function loadMorePosts() {
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

Još jedna preporuka je da dodaš dugme „Nazad“. Ono omogućava korisniku da se vrati na prethodnu stranicu bez ponovnog skrolovanja od samog vrha. To poboljšava korisničko iskustvo i daje korisniku veću kontrolu nad načinom pregledanja sadržaja.

Ponekad ćeš videti dizajne sa dugmetom „Nazad na vrh“, koje vraća korisnike na početak stranice sa rezultatima. Takođe je važno prikazati indikator učitavanja. Korisnici treba jasno da vide da se učitava dodatni sadržaj; u suprotnom mogu pomisliti da stranica ne radi.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<div class="loading-indicator" style="display: none;">Loading...</div>

<button id="back-to-top" class="back-to-top">↑ Back to Top</button>

<script src="index.js"></script>

```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.loading-indicator {
  margin-top: 12px;
  font-weight: bold;
}

.back-to-top {
  position: fixed;
  bottom: 30px;
  right: 30px;
  display: none;
  padding: 10px 15px;
  background-color: #007BFF;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
}

.back-to-top:hover {
  background-color: #0056b3;
}
```

```js
const container = document.querySelector('.infinite-scroll');
const loadingIndicator = document.querySelector('.loading-indicator');
const backToTopBtn = document.getElementById('back-to-top');

window.addEventListener('scroll', () => {
  if (window.scrollY > 400) {
    backToTopBtn.style.display = 'block';
  } else {
    backToTopBtn.style.display = 'none';
  }

  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

backToTopBtn.addEventListener('click', () => {
  window.scrollTo({ top: 0, behavior: 'smooth' });
});

function loadMorePosts() {
  loadingIndicator.style.display = 'block';
  setTimeout(() => {
    for (let i = 0; i < 3; i++) {
      const post = document.createElement('div');
      post.className = 'post';
      post.textContent = `Post ${container.children.length + 1}`;
      container.appendChild(post);
    }
    loadingIndicator.style.display = 'none';
  }, 1000);
}
``` 

:::

Na kraju, postaraj se da podnožje stranice uvek bude dostupno korisniku. Ako podnožje sadrži važne informacije, korisnik treba da može da mu pristupi u svakom trenutku.

Ukratko, beskonačno skrolovanje je dobar način za prikaz sadržaja na tvom veb sajtu. Međutim, prilikom njegovog dizajniranja treba slediti najbolje prakse kako bi korisničko iskustvo bilo što bolje.

# --questions--

## --text--

Šta je beskonačno skrolovanje?

## --answers--

Obrazac dizajna koji zamenjuje klizač za pomeranje stranice.

### --feedback--

Kagua mwanzo wa funzo ambapo dhana hii ilianzishwa.

---

Stil dizajniranja koji menja veličinu stranice.

### --feedback--

Pregledajte početak lekcije gde je ovaj koncept uveden.

---

Stil dizajna za menjanje boje pozadine stranice.

### --feedback--

Pregledajte početak lekcije gde je ovaj koncept uveden.

---

Dizajnerski obrazac koji učitava više sadržaja kada korisnik skrološe dole na stranici

## --video-solution--

4

## --text--

Koja je funkcija dugmeta "učitaj više" u beskonačnom skrolovanju?

## --answers--

Osveži stranicu.

### --feedback--

Ime otkriva svoju ulogu.

---

Dajemo korisniku kontrolu da vidi više sadržaja kada mu se sviđa.

---

Omogućava korisnicima da se vrate na prethodno učitani sadržaj.

### --feedback--

Ime otkriva svoju ulogu.

---

Ograničava količinu podataka koja se može preuzeti.

### --feedback--

Ime otkriva svoju ulogu.

## --video-solution--

2

## --text--

Zašto je važno keširati dostupan sadržaj prilikom korišćenja beskonačnog skrolovanja?

## --answers--

Pomaže da boje budu vidljivije na stranici.

### --feedback--

Razmisli kojim informacijama korisnik treba da ima pristup.

---

Pomaže u pojednostavljivanju učitavanja stranica.

### --feedback--

Razmislite o tome na koje informacije korisnik treba da ima pristup.

---

Nije važno staviti ga u direktorijum gde je dostupan.

### --feedback--

Razmislite o tome koje informacije korisnik bi trebalo da ima pristup.

---

Da bi korisnik mogao da pristupi važnim linkovima i informacijama iz zajednice u bilo koje vreme.

## --video-solution--

4