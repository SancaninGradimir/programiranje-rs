---
id: 68376949899fcbad3c9cac23
title: Pokušao je da napiše JavaScript kod za pristup
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Da bi prošao kratki test, moraš tačno odgovoriti na najmanje 9 od 10 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koje svojstvo elementa se koristi za svrhe pristupačnosti da bi pokazalo da li je kontrola dodata ili skrivena?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Koja dva atributa elementa mogu se koristiti zajedno sa `aria-expanded` za uspostavljanje programskog odnosa između kontrolujućeg i kontrolisanog elementa?

#### --distractors--

`aria-labelledBy` na `aria-owns`

---

`aria-controls` na `aria-label`

---

`aria-live` na `aria-owns`

#### --answer--

`aria-controls` na `aria-owns`

### --question--

#### --text--

Koji od sledećih atributa kreira živo područje na vašoj stranici, koje se može koristiti za obaveštavanje korisnika čitača ekrana o promenama sadržaja koje nastupaju u tom živom području?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Koje karakteristike `div` će biti onemogućene samo za osobe koje koriste asistivne tehnologije, kao što su čitači ekrana?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Koje je važeće korišćenje atributa elementa za `aria-expanded`?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Koji atribut elementa se koristi za kreiranje programskog odnosa između kontrolnog i ciljnog elementa na stranici?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Koje od sledećih NIJE uobičajena upotreba direktnih područja?

#### --distractors--

Poruka koja se prikazuje nakon izvršene akcije (kao što je poruka o grešci ili potvrda).

---

Sadržaj koji se redovno ažurira (kao ticker, marquee ili brojač koji se smanjuje).

---

Opšti status izveštaj (kao ažuriranje o procesu).

#### --answer--

Poruka koja se prikazuje pre nego što se izvrši akcija (kao prozor za potvrdu).

### --question--

#### --text--

Koji primer od sledećih je ispravan način da se pokaže koji je tab trenutno izabran?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Nije među sljedećim vrijednost validnog atributa elementa `aria-live`?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Koji od vrednosti atributa elementa `aria-live` znači da je ažuriranje veoma važno i ima najviši prioritet?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`