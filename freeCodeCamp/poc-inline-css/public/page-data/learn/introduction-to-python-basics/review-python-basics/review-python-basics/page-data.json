{"componentChunkName":"component---src-templates-challenges-generic-show-tsx","path":"/learn/introduction-to-python-basics/review-python-basics/review-python-basics","result":{"data":{"challengeNode":{"challenge":{"assignments":["<p>Proveri temu i koncepte za osnove Python.</p>"],"bilibiliIds":null,"block":"review-python-basics","challengeType":31,"description":"<section id=\"description\">\n<h2>Python Šta je to?</h2>\n<ul>\n<li><strong>Uvod</strong>: Python je programski jezik opšte namene, poznat po svojoj jednostavnosti i lakoj primeni. Python se koristi u mnogim oblastima, kao što su data nauka i mašinsko učenje, web razvoj, pisanje skripti i direktno funkcionisanje zadataka, ugrađeni sistemi i IoT, i još mnogo toga.</li>\n<li><strong>Primene</strong>: Python se koristi u data nauci, mašinskom učenju, web razvoju, sajber bezbednosti, direktnom funkcionisanju zadataka i mikroračunarima kao što su Raspberry Pi i matične ploče kompatibilne sa MicroPython.</li>\n</ul>\n<h2>Varijable</h2>\n<ul>\n<li><strong>Deklarisanje parametara</strong>: Da biste deklarisali parametar, počinjete sa imenom parametra praćenim operatorom dodeljivanja (<code>=</code>) a zatim vrednost. Ovo može biti broj, niz karaktera, tačna ili netačna vrednost, itd. Evo nekoliko primera:</li>\n</ul>\n<pre><code class=\"language-py\">name = 'John Doe'\nage = 25\n</code></pre>\n<ul>\n<li>\n<p><strong>Konvencije imenovanja parametara</strong>: Ovde su konvencije imenovanja koje treba da koristite za parametre:</p>\n</li>\n<li>\n<p>Imena varijabli mogu počinjati samo sa slovom ili donjom crtom (_), a ne brojem.</p>\n<ul>\n<li>Imena varijabli mogu sadržati samo slova i brojeve (a-z, A-Z, 0-9) i donje crte (_).</li>\n<li>Imena varijabli su osetljiva na veličinu slova — <code>age</code>, <code>Age</code>, i <code>AGE</code> se sve smatraju različitim.</li>\n<li>Imena varijabli ne smeju biti rezervne ključne reči za Python kao što su <code>if</code>, <code>class</code>, ili <code>def</code>.</li>\n<li>Imena varijabli sa više reči razdvajaju se donjim crtama. Primer <code>snake_case</code>.</li>\n</ul>\n</li>\n</ul>\n<h2>Komentari</h2>\n<ul>\n<li><strong>Jedelinijski komentari</strong>: Ove vrste komentara treba da se koriste za kratke opise koje želite ostaviti u svom kodu.</li>\n</ul>\n<pre><code class=\"language-py\"># This is a single line comment\n</code></pre>\n<ul>\n<li><strong>Višelinijski niz znakova</strong>: Ove vrste niza znakova mogu se koristiti za ostavljanje velikih opisa ili dodavanje komentara u delovima koda.</li>\n</ul>\n<pre><code class=\"language-py\">\"\"\"\nThis is a multi-line string.\nHere is some code commented out.\n\nname = 'John Doe'\nage = 25\n\"\"\"\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>print()</code></strong>: Da biste ispisali podatke na konzolu, možete koristiti funkciju <code>print()</code> kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">print('Hello world!') # Hello world!\n</code></pre>\n<h2>Tipovi standardnih podataka u Python</h2>\n<ul>\n<li><strong>Uvod</strong>: Python je jezik čije se tipovi menjaju tokom izvršavanja kao JavaScript, što znači da nema potrebe eksplicitno deklarisati tipove parametara. Jezik zna tip parametra na osnovu onoga što unesete u taj parametar.</li>\n<li><strong>Celi broj</strong>: Broj bez decimalnih mesta:</li>\n</ul>\n<pre><code class=\"language-py\">my_integer_var = 10\nprint('Integer:', my_integer_var) # Integer: 10\n</code></pre>\n<ul>\n<li><strong>Decimalni broj</strong>: Broj sa decimalnim delovima:</li>\n</ul>\n<pre><code class=\"language-py\">my_float_var = 4.50\nprint('Float:', my_float_var) # Float: 4.5\n</code></pre>\n<ul>\n<li><strong>Niz slova</strong>: Niz slova koji je postavljen unutar navodnika:</li>\n</ul>\n<pre><code class=\"language-py\">my_string_var = 'hello'\nprint('String:', my_string_var) # String: hello\n</code></pre>\n<ul>\n<li><strong>Vrednost Tačno ili Netačno</strong>: Vrednost koja predstavlja <code>True</code> ili <code>False</code>:</li>\n</ul>\n<pre><code class=\"language-py\">my_boolean_var = True\nprint('Boolean:', my_boolean_var) # Boolean: True\n</code></pre>\n<ul>\n<li><strong>Skup</strong>: Neporedjena kolekcija jedinstvenih elemenata:</li>\n</ul>\n<pre><code class=\"language-py\">my_set_var = {7, 5, 8}\nprint('Set:', my_set_var) # Set: {7, 5, 8}\n</code></pre>\n<ul>\n<li><strong>Diktionar</strong>: Kolekcija parova ključ-vrednost, koji su zatvoreni unutar uglastih zagrada:</li>\n</ul>\n<pre><code class=\"language-py\">my_dictionary_var = {\"name\": \"Alice\", \"age\": 25}\nprint('Dictionary:', my_dictionary_var) # Dictionary: {'name': 'Alice', 'age': 25}\n</code></pre>\n<ul>\n<li><strong>Tuple</strong>: Kolekcija koja je strukturirana i nepromenljiva, zatvorena unutar običnih zagradi:</li>\n</ul>\n<pre><code class=\"language-py\">my_tuple_var = (7, 5, 8)\nprint('Tuple:', my_tuple_var) # Tuple: (7, 5, 8)\n</code></pre>\n<ul>\n<li><strong>Niz brojeva</strong>: Niz brojeva, često korišćen u petljama:</li>\n</ul>\n<pre><code class=\"language-py\">my_range_var = range(5)\nprint(my_range_var) # range(0, 5)\n</code></pre>\n<ul>\n<li><strong>Lista</strong>: Struktuirana kolekcija komponenti koja podržava različite vrste podataka:</li>\n</ul>\n<pre><code class=\"language-py\">my_list = [22, 'Hello world', 3.14, True]\nprint(my_list) # [22, 'Hello world', 3.14, True]\n</code></pre>\n<ul>\n<li><strong>None</strong>: Specifična vrednost koja predstavlja odsustvo vrednosti:</li>\n</ul>\n<pre><code class=\"language-py\">my_none_var = None\nprint('None:', my_none_var) # None: None\n</code></pre>\n<h2>Vrste nepromenjivog i promenljivog</h2>\n<ul>\n<li><strong>Nepromenljivi tipovi</strong>: Ovi tipovi se ne mogu promeniti nakon deklarisanja, iako možete preusmeriti njihove parametre na nešto novo, što se naziva ponovno kastovanje (recasting). Oni uključuju cele brojeve, decimalne brojeve, kompleksne brojeve, logičke vrednosti (tačno ili netačno), niz znakova (string), tuple, sekvence i <code>None</code>.</li>\n<li><strong>Promenljivi tipovi</strong>: Ovi tipovi se mogu promeniti nakon deklarisanja. Možete dodati, ukloniti ili modifikovati njihove elemente. Oni uključuju kolekivne tipove kao što su liste, setovi i dikcionari.</li>\n<li><strong>Funkcija <code>type()</code></strong>: Da biste videli tip parametra, možete koristiti funkciju <code>type()</code> na sledeći način:</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'Hello there!'\nage = 21\n\nprint(type(greeting)) # &#x3C;class 'str'>\nprint(type(age)) # &#x3C;class 'int'>\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>isinstance()</code></strong>: Koristi se za proveru da li parametar odgovara određenom tipu podataka:</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'Hello world'\nname = 'John Doe'\n\nprint(isinstance(greeting, str)) # True\nprint(isinstance(name, int)) # False\n</code></pre>\n<h2>Rad sa nizovima znakova</h2>\n<ul>\n<li><strong>Definicija</strong>: Kao što ste zapamtili iz JavaScript, niz slova je nepromenljiv, što znači da ga ne možete promeniti nakon kreiranja. U Python, možete koristiti pojedinačne ili dvostruke navodne znakove. Preporučuje se da odaberete jedno pravilo i sledite ga:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\ncity = \"Los Angeles\"\n</code></pre>\n<ul>\n<li><strong>Dobijanje karakter(a) iz sekvence karaktera</strong>: Možete dobiti karakter iz sekvence karaktera koristeći notaciju uglastih zagrada, kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">my_str = 'Hello world'\n\nprint(my_str[0])  # H\nprint(my_str[6])  # w\n\nprint(my_str[-1])  # d\nprint(my_str[-2]) # l\n</code></pre>\n<ul>\n<li><strong>Izbegavanje sekvence karaktera</strong>: Možete koristiti kosu crtu (<code>\\</code>) ako vaša sekvenca karaktera sadrži navodnike kao što je sledeće:</li>\n</ul>\n<pre><code class=\"language-py\">msg = 'It\\'s a sunny day'\nquote = \"She said, \\\"Hello!\\\"\"\n</code></pre>\n<ul>\n<li><strong>Konkatenacija niza karaktera</strong>: Da biste spojili niz znakova, možete koristiti operator <code>+</code> kao što sledi:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\nprint('My name is ' + developer + '.') # My name is Jessica.\n</code></pre>\n<p>Drug način za spajanje niza znakova je korišćenje operatora <code>+=</code>. Ovo se koristi za konkatenaciju i segmentaciju u jednom koraku, kao što sledi:</p>\n<pre><code class=\"language-py\">greeting = 'My name is '\ndeveloper = 'Jessica.'\n\ngreeting += developer\nprint(greeting) # My name is Jessica.\n</code></pre>\n<ul>\n<li><strong><code>f-strings</code></strong>: Ovo je skraćenica za uređenu sekvencu karaktera. Omogućava vam da izvršite unos vrednosti i takođe uspostavite sa kratkom i čitljivom sintaksom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\ngreeting = f'My name is {developer}.'\nprint(greeting) # My name is Jessica.\n</code></pre>\n<ul>\n<li><strong>Извлачење подпореже низа символа</strong>: Ovo je mesto gde možete izvući delove sekvence karaktera. Ovde je osnovna sintaksa:</li>\n</ul>\n<pre><code class=\"language-py\">str[start:stop:step]\n</code></pre>\n<p>Broj za početak pokazuje tačku od koje treba započeti ekstrakciju. Broj za kraj je mesto gde bi deo trebalo da se završi. Ovaj broj nije uključen. Broj koraka ukazuje interval za povećanje ekstrakcije. Evo primera:</p>\n<pre><code class=\"language-py\">message = 'Python is fun!'\n\nprint(message[0:6])  # Python\nprint(message[7:])  # is fun!\nprint(message[::2])  # Pto sfn\n</code></pre>\n<ul>\n<li><strong>Dobijanje dužine niza znakova</strong>: Funkcija <code>len()</code> se koristi za vraćanje broja znakova u nizu:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(len(developer)) # 7\n</code></pre>\n<h2>Rad s operacijom od <code>in</code></h2>\n<ul>\n<li><strong>Opereta ya <code>in</code></strong>: Ovo vraća vrednost tačno ili netačno koja određuje da li znakovi ili slova postoje u nizu karaktera, ili ne:</li>\n</ul>\n<pre><code class=\"language-py\">my_str = 'Hello world'\n\nprint('Hello' in my_str)  # True\nprint('hey' in my_str)    # False\nprint('hi' in my_str)    # False\nprint('e' in my_str)  # True\nprint('f' in my_str)  # False\n</code></pre>\n<h2>Uobičajene metode sekvenci karaktera</h2>\n<ul>\n<li><strong><code>str.upper()</code></strong>: Ovo vraća novu sekvencu znakova sa svim slovima konvertovanim u VELIKA SLOVA:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(developer.upper()) # JESSICA\n</code></pre>\n<ul>\n<li><strong><code>str.lower()</code></strong>: Ovo vraća novu sekvencu znakova sa svim slovima konvertovanim u mala slova:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Jessica'\n\nprint(developer.lower()) # jessica\n</code></pre>\n<ul>\n<li><strong><code>str.strip()</code></strong>: Ovo vraća kopiju niza znakova koji uklanja određene znakove sa početka i kraja (ako nije naveden argument, uklanja vodeće i zakažne razmake).</li>\n</ul>\n<pre><code class=\"language-py\">greeting = '  hello world  '\n\ntrimmed_my_str = greeting.strip()\nprint(trimmed_my_str)  # 'hello world'\n</code></pre>\n<ul>\n<li><strong><code>replace()</code></strong>: Ovo vraća novu sekvencu karaktera gde su svi događaji stare sekvence zamenjeni novom sekvencom.</li>\n</ul>\n<pre><code class=\"language-py\">greeting = 'hello world'\n\nreplaced_my_str = greeting.replace('hello', 'hi')\nprint(replaced_my_str)  # 'hi world'\n</code></pre>\n<ul>\n<li><strong><code>split()</code></strong>: Ovo se koristi za razdvajanje niza znakova u listu koristeći definisani separator. Separator je niz znakova koji određuje mesto na kojem treba da se podeli.</li>\n</ul>\n<pre><code class=\"language-py\">dashed_name = 'example-dashed-name'\n\nsplit_words = dashed_name.split('-')\nprint(split_words)  # ['example', 'dashed', 'name']\n</code></pre>\n<ul>\n<li><strong><code>join()</code></strong>: Ovo se koristi za spajanje elemenata iterabilne kolekcije u niz znakova pomoću funkcije. Iterabilna kolekcija je kolekcija elemenata koja se može iterirati, kao što su lista, niz znakova ili tuple.</li>\n</ul>\n<pre><code class=\"language-py\">example_list = ['example', 'dashed', 'name']\n\njoined_str = ' '.join(example_list)\nprint(joined_str)  # example dashed name\n</code></pre>\n<ul>\n<li><strong><code>str.startswith(prefix)</code></strong>: Ovo vraća vrednost tačno/netačno koja ukazuje da li niz znakova počinje sa definisanim prefiksom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.startswith('N')\nprint(result)  # True\n</code></pre>\n<ul>\n<li><strong><code>str.endswith(suffix)</code></strong>: Ovo vraća booleanski vrednost koja ukazuje da li niz znakova završava sa navedenim sufiksom:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.endswith('N')\nprint(result)  # False\n</code></pre>\n<ul>\n<li><strong><code>str.find()</code></strong>: Ovo vraća šablon prvog događaja male sekvence. Ako nije dostupan, <code>-1</code> se vraća:</li>\n</ul>\n<pre><code class=\"language-py\">developer = 'Naomi'\n\nresult = developer.find('N')\nprint(result)  # 0\n\ncity = 'Los Angeles'\nprint(city.find('New')) # -1\n</code></pre>\n<ul>\n<li><strong><code>str.count(substring)</code></strong>: Ovo broji koliko puta manja sekvenca se pojavljuje u nizu karaktera:</li>\n</ul>\n<pre><code class=\"language-py\">city = 'Los Angeles'\nprint(city.count('e')) # 2\n</code></pre>\n<ul>\n<li><strong><code>str.capitalize()</code></strong>: Ovo vraća novu sekvencu slova sa velikim početnim slovom i ostalim malim:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.capitalize()) # Chocolate cake\n</code></pre>\n<ul>\n<li><strong><code>str.isupper()</code></strong>: Ovo vraća <code>True</code> ako su svi znakovi u nizu velika slova i <code>False</code> inače:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.isupper()) # False\n</code></pre>\n<ul>\n<li><strong><code>str.islower()</code></strong>: Ovo vraća <code>True</code> ako su sva slova u nizu mala i <code>False</code> inače:</li>\n</ul>\n<pre><code class=\"language-py\">dessert = 'chocolate cake'\nprint(dessert.islower()) # True\n</code></pre>\n<ul>\n<li><strong><code>str.title()</code></strong>: Ovo vraća novu sekvencu slova tako da je prvo slovo svake reči veliko:</li>\n</ul>\n<pre><code class=\"language-py\">city = 'los angeles'\nprint(city.title()) # Los Angeles\n</code></pre>\n<ul>\n<li><strong><code>str.maketrans()</code></strong>: Ova metoda se koristi za kreiranje tabele odgovaranja slova po slovo radi prevođenja. Često se koristi zajedno sa metodom <code>translate()</code> koja koristi tu tabelu u sekvenci znakova i vraća prevedene rezultate.</li>\n</ul>\n<pre><code class=\"language-py\">trans_table = str.maketrans('abc', '123')\nprint(trans_table) # {97: 49, 98: 50, 99: 51}\n\nresult = 'abcabc'.translate(trans_table)\nprint(result)  # 123123\n</code></pre>\n<h2>Operacije koje se koriste sa celobrojnim i decimalnim brojevima</h2>\n<ul>\n<li><strong>Osnovne matematičke operacije</strong>: U Python, možete izvršiti osnovne matematičke operacije sa celim brojevima i decimalnim brojevima, uključujući sabiranje, oduzimanje, množenje i deljenje:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\nfloat_1 = 5.4\nfloat_2 = 12.0\n\n# Addition\n\nprint('Integer Addition:', int_1 + int_2) # Integer Addition: 68\nprint('Float Addition:', float_1 + float_2) # Float Addition: 17.4\n\n# Subtraction\n\nprint('Int Subtraction:', int_1 - int_2) # Int Subtraction: 44\nprint('Float Subtraction:',  float_2 - float_1) # Float Subtraction: 6.6\n\n# Multiplication\n\nprint('Int Multiplication:', int_1 * int_2) # Int Multiplication: 672\nprint('Float Multiplication:', float_2 * float_1) # Float Multiplication: 64.80000000000001\n\n# Division\n\nprint('Division:', int_1 / int_2) # Division: 4.666666666666667\nprint('Float Division:', float_2 / float_1) # Float Division: 2.222222222222222\n</code></pre>\n<p>Kada sabirate decimalni broj i ceo broj, rezultat će biti konvertovan u decimalni broj, kao što sledi:</p>\n<pre><code class=\"language-py\">int_1 = 56\nfloat_1 = 5.4\n\nprint(int_1 + float_1) # 61.4\n</code></pre>\n<ul>\n<li><strong>Modulo Operator (<code>%</code>)</strong>: Ovo vraća ostatak kada se broj deli sa drugim brojem:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\n\nprint(int_1 % int_2) # 8\n</code></pre>\n<ul>\n<li><strong>Celo brojno deljenje (<code>//</code>)</strong>: Ova funkcija se koristi za deljenje dva broja i za zaokruživanje rezultata na najbliži ceo broj:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 56\nint_2 = 12\n\nprint(int_1 // int_2) # 4\n</code></pre>\n<ul>\n<li><strong>Operacija Snage (<code>**</code>)</strong>: Ova operacija se koristi za povećanje broja drugom snagom:</li>\n</ul>\n<pre><code class=\"language-py\">int_1 = 4\nint_2 = 2\n\nprint(int_1 ** int_2) # 16\n</code></pre>\n<ul>\n<li><strong>Funkcija za <code>float()</code></strong>: Možete koristiti ovu funkciju da biste konvertovali ceo broj u decimalni broj.</li>\n</ul>\n<pre><code class=\"language-py\">num = 4\n\nprint(float(num)) # 4.0\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>int()</code></strong>: Možete koristiti ovu funkciju za konverziju decimalnog broja u ceo broj.</li>\n</ul>\n<pre><code class=\"language-py\">num = 4.0\n\nprint(int(num)) # 4\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>round()</code></strong>: Koristi se za zaokruživanje broja na najbliži ceo broj:</li>\n</ul>\n<pre><code class=\"language-py\">num_1 = 3.4\nnum_2 = 7.7\n\nprint(round(num_1)) # 3\nprint(round(num_2)) # 8\n</code></pre>\n<ul>\n<li><strong>Funkcija <code>abs()</code></strong>: Koristi se za vraćanje pune vrednosti broja:</li>\n</ul>\n<pre><code class=\"language-py\">num = -13\n\nprint(abs(num)) # 13\n</code></pre>\n<ul>\n<li><strong>Funkcija za <code>pow()</code></strong>: Ovo se koristi za dohvaćanje broja drugom metodom:</li>\n</ul>\n<pre><code class=\"language-py\">result = pow(2, 3) \nprint(result)  # 8\n</code></pre>\n<h2>Prošireni zadaci</h2>\n<ul>\n<li><strong>Definicija</strong>: Poboljšano dodeljivanje vrednosti kombinuje binarnu operaciju i dodeljivanje u jednom koraku. Uzima varijablu, primenjuje operaciju na drugu vrednost i čuva rezultat nazad u istu varijablu.</li>\n</ul>\n<pre><code class=\"language-py\"># Addition assignment \nmy_var = 10\nmy_var += 5\n\nprint(my_var) # 15\n\n# Subtraction assignment\ncount = 14\ncount -= 3\n\nprint(count) # 11\n\n# Multiplication assignment \nproduct = 65\nproduct *= 7\n\nprint(product) # 455\n\n# Division assignment \nprice = 100\nprice /= 4\n\nprint(price) # 25.0\n\n# Floor Division assignment \ntotal_pages = 23\ntotal_pages //= 5\n\nprint(total_pages) # 4\n\n# Modulo assignment \nbits = 35\nbits %= 2\n\nprint(bits) # 1\n\n# Exponentiation assignment \npower = 2\npower **= 3\n\nprint(power) # 8\n</code></pre>\n<p>Postoje takođe i druge operacije sa poboljšanom alokacijom, poput onih za bitwise operacije. Obuhvataju <code>&#x26;=</code>, <code>^=</code>, <code>>>=</code>, i <code>&#x3C;&#x3C;=</code>.</p>\n<h2>Rad sa zaposlenima</h2>\n<ul>\n<li><strong>Definicija</strong>: Funkcije su ponovno upotrebljivi delovi koda koji primaju ulaz (argument) i vraćaju rezultate. Da biste pozvali funkciju, morate navesti naziv funkcije praćen standardnim zagradama:</li>\n</ul>\n<pre><code class=\"language-py\"># Defining a function\n\ndef get_sum(num_1, num_2):\n    return num_1 + num_2\n\nresult = get_sum(3, 4) # function call\nprint(result) # 7\n</code></pre>\n<p>Ako funkcija ne vraća eksplicitne vrednosti, onda je vrednost default za return jednaka <code>None</code>:</p>\n<pre><code class=\"language-py\">def greet():\n    print('hello') \n\nresult = greet() # hello\nprint(result) # None\n</code></pre>\n<p>Takođe možete da pružite vrednosti za default za parametre kao što sledi:</p>\n<pre><code class=\"language-py\">def get_sum(num_1, num_2=2):\n    return num_1 + num_2\n\nresult = get_sum(3) \nprint(result) # 5\n</code></pre>\n<p>Ako pozovete funkciju bez ispravnog broja argumenata, dobićete <code>TypeError</code>:</p>\n<pre><code class=\"language-py\">def calculate_sum(a, b):\n    print(a + b)\n\ncalculate_sum()\n\n# TypeError: calculate_sum() missing 2 required positional arguments: 'a' and 'b'\n</code></pre>\n<h2>Standardne ugrađene funkcije</h2>\n<ul>\n<li><strong>Funkcija za <code>input()</code></strong>: Koristi se za traženje unosa od korisnika:</li>\n</ul>\n<pre><code class=\"language-py\">name = input('What is your name?') # User types 'Kolade' and presses Enter  \nprint('Hello', name) # Hello Kolade\n</code></pre>\n<ul>\n<li><strong>Kitendakazi cha <code>int()</code></strong>: Koristi se za konverziju brojeva, vrednosti istina ili laž, ili alfanumeričkih nizova u kompletan broj:</li>\n</ul>\n<pre><code class=\"language-py\">print(int(3.14)) # 3\nprint(int('42')) # 42\nprint(int(True)) # 1\nprint(int(False)) # 0 \n</code></pre>\n<h2>Opseg u Python</h2>\n<ul>\n<li><strong>Unutrašnji opseg</strong>: Ovo je slučaj kada se promenljiva deklarše unutar funkcije ili klase i može se pristupiti samo unutar te funkcije ili klase.</li>\n</ul>\n<pre><code class=\"language-py\">def my_func():\n    num = 10\n    print(num)\n</code></pre>\n<ul>\n<li><strong>Opseg:</strong> Ovo je situacija kada funkcija definisana unutar druge funkcije može pristupiti parametrima spoljne/nadređene funkcije.</li>\n</ul>\n<pre><code class=\"language-py\">def outer_func():\n    msg = 'Hello there!'\n    \n    def inner_func():\n        print(msg)\n    inner_func()\n\nprint(outer_func()) # Hello there!\n</code></pre>\n<ul>\n<li><strong>Globalni opseg</strong>: Ovo se odnosi na parametre deklarisane van funkcija ili klasa koje su dostupne iz bilo kog dela programa.</li>\n</ul>\n<pre><code class=\"language-py\">tax = 0.70 \n\ndef get_total(subtotal):\n    total = subtotal + (subtotal * tax)\n    return total\n\nprint(get_total(100))  # 170.0\n</code></pre>\n<ul>\n<li><strong>Unutrašnji Opseg</strong>: Imena sačuvana u Python za prethodno deklarisane funkcije, module, ključne reči i objekte.</li>\n</ul>\n<pre><code class=\"language-py\">print(str(45)) # '45'\nprint(type(3.14)) # &#x3C;class 'float'>\nprint(isinstance(3, str)) # False\n</code></pre>\n<h2>Operatori poređenja</h2>\n<ul>\n<li><strong>Sawa (<code>==</code>)</strong>: Ovo proverava da li su dve vrednosti jednake:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 == 4) # False\n</code></pre>\n<ul>\n<li><strong>Sio sawa (<code>!=</code>)</strong>: Ovo proverava da li su dve vrednosti različite:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 != 4) # True\n</code></pre>\n<ul>\n<li><strong>Strogo veće od (<code>></code>)</strong>: Ovo proverava da li je jedna vrednost veća od druge:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 > 4) # False\n</code></pre>\n<ul>\n<li><strong>Najmanje po intenzitetu (<code>&#x3C;</code>)</strong>: Ovo proverava da li je jedna vrednost manja od druge:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 &#x3C; 4) # True\n</code></pre>\n<ul>\n<li><strong>Veće ili jednako (<code>>=</code>)</strong>: Ovo proverava da li je jedna vrednost veća ili jednaka drugoj:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 >= 4) # False\n</code></pre>\n<ul>\n<li><strong>Manje ili jednako (<code>&#x3C;=</code>)</strong>: Ovo proverava da li je jedna vrednost manje ili jednaka drugoj:</li>\n</ul>\n<pre><code class=\"language-py\">print(3 &#x3C;= 4) # True\n</code></pre>\n<h2>Rad sa izjavama za <code>if</code>, <code>elif</code> i <code>else</code></h2>\n<ul>\n<li><strong>Matamko ya <code>if</code></strong>: Ovo su uslovi koji se koriste za određivanje da li je nešto tačno ili netačno. Ako je uslov potvrđen da <code>True</code>, onda će se taj deo koda izvršiti.</li>\n</ul>\n<pre><code class=\"language-py\">age = 18\n\nif age >= 18:\n    print('You are an adult') # You are an adult\n</code></pre>\n<ul>\n<li><strong>Član <code>elif</code></strong>: Ovo su uslovi koji prate izjavu od <code>if</code>. Član <code>elif</code> je primenjiv samo ako su svi prethodni uslovi potvrđeni kao <code>False</code> i njegov uslov je potvrđen kao <code>True</code>.</li>\n</ul>\n<pre><code class=\"language-py\">age = 16\n\nif age >= 18:\n    print('You are an adult')\nelif age >= 13:\n    print('You are a teenager')  # You are a teenager\n</code></pre>\n<ul>\n<li><strong>Klauzula za <code>else</code></strong>: Ovo će biti izvršeno ako ne postoji nijedan drugi uslov koji je potvrđen kao <code>True</code>.</li>\n</ul>\n<pre><code class=\"language-py\">age = 12\n\nif age >= 18:\n    print('You are an adult')\nelif age >= 13:\n    print('You are a teenager')\nelse:\n    print('You are a child')  # You are a child\n</code></pre>\n<p>Takođe možete koristiti izjave za <code>if</code> kako je postavljeno u nastavku:</p>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nif is_citizen:\n    if age >= 18:\n        print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<h2>Vrednosti Istine i Laži</h2>\n<ul>\n<li><strong>Definicija</strong>: U Python, svaka vrednost ima prirodnu vrednost istinito ili netačno, ili ugrađeno osećanje ako treba da se tretira kao <code>True</code> ili <code>False</code> u kontekstu logike. Većina vrednosti se smatra istinitom, tj. potvrđuje da je <code>True</code> u kontekstu logike. Ostale su netačne, smatra se da su <code>False</code>. Evo primera lažnih vrednosti:</li>\n</ul>\n<pre><code class=\"language-md\">None\nFalse\nInteger 0\nFloat 0.0\nEmpty strings ''\n</code></pre>\n<p>Druge vrednosti, kao što su brojevi različiti od nule i ne-prazan niz slova, su validne.</p>\n<h2>Rad sa funkcijom <code>bool()</code></h2>\n<ul>\n<li><strong>Opis</strong>: Ako želite da proverite da li je vrednost istinita ili lažna, možete koristiti funkciju unutar <code>bool()</code>. Ona konvertuje vrednost u jednaku istinitoj ili lažnoj i vraća <code>True</code> za istinite vrednosti i <code>False</code> za lažne vrednosti. Ovde su nekoliko primera:</li>\n</ul>\n<pre><code class=\"language-py\">print(bool(False)) # False\nprint(bool(0))  # False\nprint(bool('')) # False\n\nprint(bool(True)) # True\nprint(bool(1)) # True\nprint(bool('Hello')) # True\n</code></pre>\n<h2>Opereta za Vrednost Istinitog ili Nistinitog i Sekcionisanje puta</h2>\n<ul>\n<li><strong>Definicija</strong>: Ovo su specijalni operatori koji vam omogućavaju da kombinujete više matematičkih izraza kako biste kreirali složenu logiku odlučivanja u vašem kodu. Postoje tri operatera sa vrednošću tačno ili netačno u Python: <code>and</code>, <code>or</code> i <code>not</code>.</li>\n<li><strong>Operater <code>and</code></strong>: Ovaj operator uzima dva operanda i vraća prvi operand ako je netačan, inače vraća drugi operand. Oba operanda moraju biti tačna da bi se izraz generisao kao tačan.</li>\n</ul>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nprint(is_citizen and age) # 25\n</code></pre>\n<p>Možete takođe koristiti operaciju <code>and</code> na sledećim uslovima:</p>\n<pre><code class=\"language-py\">is_citizen = True\nage = 25\n\nif is_citizen and age >= 18:\n    print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<ul>\n<li><strong>Opereta ya <code>or</code></strong>: Ovaj operator vraća prvi operand ako je tačan, inače vraća drugi operand. Izraz <code>or</code> je tačan ako je bar jedan operand tačan. Evo primera:</li>\n</ul>\n<pre><code class=\"language-py\">age = 19\nis_employed = False\n\nprint(age or is_employed) # 19\n</code></pre>\n<p>Kao što je slučaj sa operetom <code>and</code>, možete koristiti operetu <code>or</code> na uslovima kao što su sledeći:</p>\n<pre><code class=\"language-py\">age = 19\nis_student = True\n\nif age &#x3C; 18 or is_student:\n    print('You are eligible for a student discount') # You are eligible for a student discount\nelse:\n    print('You are not eligible for a student discount')\n</code></pre>\n<ul>\n<li><strong>Prekid puta</strong>: Operatori za <code>and</code> i <code>or</code> poznati su kao operateri prekida puta. Prekid puta znači da Python proverava vrednosti od leve ka desnoj i zaustavlja se čim detektuje konačan rezultat.</li>\n<li><strong>Operater <code>not</code></strong>: Ovaj operater uzima jedan operand i konvertuje njegovu istinitu ili neistinitu vrednost. Konvertuje istinite vrednosti u <code>False</code>, a netačne (false) vrednosti u <code>True</code>. Za razliku od prethodnih operatera koje smo gledali, <code>not</code> uvek vraća <code>True</code> ili <code>False</code>. Evo primera:</li>\n</ul>\n<pre><code class=\"language-py\">print(not '') # True, because empty string is falsy\nprint(not 'Hello') # False, because non-empty string is truthy\nprint(not 0) # True, because 0 is falsy\nprint(not 1) # False, because 1 is truthy\nprint(not False) # True, because False is falsy\nprint(not True) # False, because True is truthy\n</code></pre>\n<p>Ovde je primer operete <code>not</code> u uslovu:</p>\n<pre><code class=\"language-py\">is_admin = False\n\nif not is_admin:\n    print('Access denied for non-administrators.') # Access denied for non-administrators.\nelse:\n    print('Welcome, Administrator!')\n</code></pre>\n</section>","nodules":null,"explanation":null,"helpCategory":"Python","instructions":"","fields":{"slug":"/learn/introduction-to-python-basics/review-python-basics/review-python-basics"},"questions":[],"scene":null,"superBlock":"introduction-to-python-basics","tests":[],"title":"Python Osnove Pregled","transcript":null,"translationPending":false,"videoId":null,"videoLocaleIds":null}}},"pageContext":{"challengeMeta":{"blockHashSlug":"/learn/introduction-to-python-basics/#review-python-basics","dashedName":"review-python-basics","certification":"introduction-to-python-basics","disableLoopProtectTests":false,"disableLoopProtectPreview":false,"superBlock":"introduction-to-python-basics","chapter":null,"module":null,"block":"review-python-basics","isFirstStep":true,"template":null,"required":[],"isLastChallengeInBlock":true,"nextChallengePath":"/learn/introduction-to-python-basics/quiz-python-basics/quiz-python-basics","prevChallengePath":"/learn/introduction-to-python-basics/lab-rpg-character/build-an-rpg-character","id":"67f39b40deaec81a3e40e0c5","saveSubmissionToDB":null},"projectPreview":{"challengeData":{"challengeType":31,"challengeFiles":[]}},"id":"/learn/introduction-to-python-basics/review-python-basics/review-python-basics"}},"staticQueryHashes":["3858520367"],"slicesMap":{}}