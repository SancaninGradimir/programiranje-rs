---
id: 67f41268a129e63f8e071736
title: "Pokušao je da napravi niz petlja (ili: seriju petlji)"
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koja je tačna sintaksa za listu?

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

Šta će biti ispisano na konzolu?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

Šta je značenje iterativnog objekta (iterable)?

#### --distractors--

To je posebna vrsta niza karaktera koji može da sadrži mnogo karaktera.

---

Ovo je specijalni tip broja koji se koristi za predstavljanje veoma velikih vrednosti.

---

To je poseban tip podataka koji se koristi posebno u specifičnim klasama i funkcijama.

#### --answer--

Radi se o posebnom tipu nečega što se može ponoviti komad po komad.

### --question--

#### --text--

Šta se ispisuje na konzoli?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

Koja od sledećih se koristi za dobijanje broja elemenata u listi?

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

Šta se dešava ako pokušate da prosledite model (pozitivan ili negativan model) koji je van granica liste?

#### --distractors--

Primaćeš `SyntaxError`.

---

Dobićete `RangeError`.

---

Dobićete `TypeError`.

#### --answer--

Dobićete `IndexError`.

### --question--

#### --text--

Šta će biti ispisano na konzolu?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

Koja od sledećih metoda se koristi za dodavanje elementa na kraj liste?

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

Šta će biti ispisano na konzolu?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

Koji od sljedećih načina se koristi za dodavanje elementa u specifičan model liste?

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

Šta će biti ispisano na konzolu?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

Koji od sledećih metoda se koristi za brisanje cele liste?

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Tuple šta je?

#### --distractors--

To je vrsta podataka koja se menja, sa neuređenom sekvencijom vrednosti.

---

To je poseban tip podataka koji se koristi za predstavljanje velikih brojeva.

---

To je metoda koja se koristi za dodavanje elemenata u listu.

#### --answer--

Nepromenljiva i uređena sekvenca vrednosti.

### --question--

#### --text--

Koja od sledećih metoda je ispravna za izvlačenje elemenata iz tupa?

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

Daje `RangeError`.

---

Nemaće nikakvih rezultata.

---

Kreiraće beskonačan ciklus.

#### --answer--

Daje `TypeError`.

### --question--

#### --text--

Koji će biti rezultat sledećeg koda?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

Koji od sledećih funkcija se koristi za kreiranje nove liste sortirane vrednosti?

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

Koji je od sljedećih ispravan način korištenja kola za `for`?

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

Koji od sledećih funkcija se koristi za kreiranje sekvence celih brojeva?

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

Koja od sledećih funkcija se koristi za iteraciju preko elemenata koji mogu biti obrađeni više puta istovremeno?

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`