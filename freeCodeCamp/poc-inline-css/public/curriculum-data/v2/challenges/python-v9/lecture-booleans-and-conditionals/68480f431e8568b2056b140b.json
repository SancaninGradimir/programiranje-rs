{
  "id": "68480f431e8568b2056b140b",
  "title": "Šta su realne i imaginarne vrednosti, i kako radi operacija za realnu ili imaginarnu vrednost u procesu sečenja kruga?",
  "challengeType": 19,
  "dashedName": "what-are-truthy-and-falsy-values-and-how-do-boolean-operators-and-short-circuiting-work",
  "solutions": [],
  "questions": [
    {
      "text": "<p>Šta će sledeći kod prikazati?</p>\n<pre><code class=\"language-python\">age = 20\nhas_ticket = True\n\nif age >= 18 and has_ticket:\n    print(\"You can watch the movie.\")\nelse:\n    print(\"You can't watch the movie.\")\n</code></pre>",
      "answers": [
        {
          "answer": "<p><code>You can watch the movie.</code></p>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<p><code>SyntaxError</code></p>",
          "feedback": "<p>Setiti se šta se dešava kada su svi uslovi <code>True</code>.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>TypeError</code></p>",
          "feedback": "<p>Zapamti šta se dešava kada su svi uslovi <code>True</code>.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>You can't watch the movie.</code></p>",
          "feedback": "<p>Zapamtite šta se dešava kada su svi uslovi <code>True</code>.</p>",
          "audioId": null
        }
      ],
      "solution": 1
    },
    {
      "text": "<p>Koja od sledećih stavki NIJE smatrana lažnom vrednošću u Python?</p>",
      "answers": [
        {
          "answer": "<p><code>\"\"</code></p>",
          "feedback": "<p>Zapamtite da se niz znakova koji nije prazan smatra stvarnom vrednošću.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>0.0</code></p>",
          "feedback": "<p>Zapamtite da se niz znakova koji nije prazan tretira kao stvarna vrednost.</p>",
          "audioId": null
        },
        {
          "answer": "<p><code>\"False\"</code></p>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<p><code>0</code></p>",
          "feedback": "<p>Zapamti da niz neprvih slova se tretira kao stvarna vrednost.</p>",
          "audioId": null
        }
      ],
      "solution": 3
    },
    {
      "text": "<p>Šta se dešava kada Python procenjuje vrednost <code>x or y</code>?</p>",
      "answers": [
        {
          "answer": "<p>Vrati <code>x</code> i ako <code>x</code> na <code>y</code>, sve se smatraju da su <code>True</code></p>",
          "feedback": "<p>Razmotri <em>\"prekid kruga\"</em> i kada Python neće biti potrebno proveravati drugu vrednost.</p>",
          "audioId": null
        },
        {
          "answer": "<p>Vrati <code>x</code> ako se smatra da je <code>True</code>, i preskače na proveru <code>y</code></p>",
          "feedback": null,
          "audioId": null
        },
        {
          "answer": "<p>Daima inathamini <code>x</code> na <code>y</code> bila kujali thamani zao</p>",
          "feedback": "<p>Razmislite o \"prekidanju kruga\" i kada Python ne mora da proverava drugu vrednost.</p>",
          "audioId": null
        },
        {
          "answer": "<p>Vrati <code>False</code> ako je <code>x</code> ili <code>y</code> na <code>False</code></p>",
          "feedback": "<p>Razmislite o \"prekidu ciklusa\" i kada Python neće biti potrebno proveravati drugu vrednost.</p>",
          "audioId": null
        }
      ],
      "solution": 2
    }
  ],
  "assignments": [],
  "tests": [],
  "description": "<section id=\"description\">\n<p>U prethodnoj lekciji, naučili ste kako da koristite operatore poređenja i uslovne izjave za kontrolisanje toka vaših programa.</p>\n<p>Iako su vrlo snažne, često naiđete na situaciju u kojoj je potrebno uporediti mnogo vrednosti istovremeno. Ovo može dovesti do ugnježdjenih uslovnih rečenica, na primer:\n<strong>Napomena:</strong> Prvih 13 koraka mora biti završeno unutar datoteke <code>index.html</code>.</p>\n<pre><code class=\"language-python\">is_citizen = True\nage = 25\n\nif is_citizen:\n    if age >= 18:\n        print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<p>Primer iznad će prvo proveriti da li je <code>is_citizen</code> jednako <code>True</code>. Ako je tačno, onda će ići na ugnježdenu rečenicu <code>if</code> i proveriti da li je <code>age</code> veće ili jednako od <code>18</code>. Pošto je <code>age</code> veće ili jednako od <code>18</code>, poruka koja će biti ispisana na terminalu biće <code>You are eligible to vote</code>. Ako bi <code>is_citizen</code> bilo <code>False</code>, onda bi poruka ispisana na terminalu bila <code>You are not eligible to vote</code>.</p>\n<p>Ako radite sa složenijim uslovnim rečenicama, onda možete koristiti operatore Python <code>and</code>, <code>or</code>, i <code>not</code>.</p>\n<p>Ali pre nego što uđemo u te opere/performanse, hajde da istražimo koje su stvarne vrednosti i koje nisu.</p>\n<p>U Python, svaka vrednost ima inherentnu istinitu ili lažnu vrednost, ili ugrađeno stanje da li treba tretirati kao <code>True</code> ili <code>False</code> u logičkom kontekstu. Mnoge vrednosti se smatraju <strong>istinitim vrednostima</strong>, to jest, smatraju se kao <code>True</code> u logičkom kontekstu. Ostale su <strong>lažne vrednosti</strong>, što znači da se smatraju kao <code>False</code>.</p>\n<p>Evo nekih lažnih vrednosti:</p>\n<ul>\n<li><code>None</code></li>\n<li><code>False</code></li>\n<li>Puni broj <code>0</code></li>\n<li>Sekvencijalni broj <code>0.0</code></li>\n<li>Niz samo slova <code>\"\"</code></li>\n</ul>\n<p>Druge vrednosti, kao što su brojevi koji nisu nula i ne-prazan niz slova, su stvarni vrednosti.</p>\n<p>Ako želite da proverite da li je vrednost tačna ili netačna, možete koristiti ugrađenu funkciju <code>bool()</code>. Ona izvršava eksplicitnu konverziju vrednosti u tačno ili netačno i vraća <code>True</code> za tačne vrednosti i <code>False</code> za netačne. Evo nekoliko primera:</p>\n<pre><code class=\"language-py\">print(bool(False)) # False\nprint(bool(0))  # False\nprint(bool('')) # False\n\nprint(bool(True)) # True\nprint(bool(1)) # True\nprint(bool('Hello')) # True\n</code></pre>\n<p>Sada kada razumete realne i imaginarne vrednosti, možemo pogledati operatore realnih ili imaginarnih vrednosti, koji su takođe poznati kao logički operateri. Ovo su specijalni operateri koji vam omogućavaju da kombinujete više matematičkih izraza kako biste stvorili složeniju logiku za donošenje odluka u vašem kodu.</p>\n<p>Postoji tri opere za vrednost tačno ili netačno u Python: <code>and</code>, <code>or</code>, na <code>not</code>.</p>\n<p>Prvo hajde da pregledamo operetu <code>and</code>.</p>\n<p>Opereta <code>and</code> uzima dva parametra i vraća prvi parametar ako je vrednost lažna, inače vraća drugi parametar. Svi parametri moraju biti istinite vrednosti da bi izraz dao istinitu vrednost.</p>\n<p>Evo primera:</p>\n<pre><code class=\"language-python\">is_citizen = True\nage = 25\n\nprint(is_citizen and age) # 25\n</code></pre>\n<p>U gornjem primeru, broj 25 se ispisuje na terminal jer će operator <code>and</code> proceniti drugi parametar ako je prvi parametar <code>True</code>. Operator <code>and</code> poznat je kao operator za prekid kruga. Prekida krug znači da <code>Python</code> proverava vrednosti od leve ka desnoj i zaustavlja se čim detektuje konačan rezultat.</p>\n<p>Često ćete koristiti <code>and</code> unutar rečenica od <code>if</code> da biste proverili da li su zadovoljeni mnogi uslovi. Evo kako možete poboljšati prethodni primer koristeći operator <code>and</code> umesto strukturiranih rečenica iz <code>if</code>:</p>\n<pre><code class=\"language-python\">is_citizen = True\nage = 25\n\nif is_citizen and age >= 18:\n    print('You are eligible to vote') # You are eligible to vote\nelse:\n    print('You are not eligible to vote')\n</code></pre>\n<p>U gornjem primeru, <code>is_citizen</code> je <code>True</code>, i na <code>age >= 18</code> procenjeno je da je <code>True</code>. Pošto su oba parametra operacije <code>and</code> tačne vrednosti, uslov <code>is_citizen and age >= 18</code> je procenjen da je <code>True</code>, i poziv <code>print</code> u sekciji <code>if</code> se izvršava.</p>\n<p>Sada hajde da izučimo operator <code>or</code>. Ovaj operator vraća prvi argument ako je istinitostna vrednost, inače vraća drugi argument. Izraz <code>or</code> vraća istinitostnu vrednost ako je barem jedan argument istinit. Operator <code>or</code> je poznat i kao operator kratkog kruga. Evo primera:</p>\n<pre><code class=\"language-python\">age = 19\nis_employed = False\n\nprint(age or is_employed) # 19\n</code></pre>\n<p>Sledeći kod će ispisati broj 19 jer je prvi parametar <code>age</code> vrednost istina.</p>\n<p>Ako trebate proveriti da li je jedan ili više izraza <code>True</code>, možete koristiti operator <code>or</code> u uslovu kao što je ovaj:</p>\n<pre><code class=\"language-python\">age = 19\nis_student = True\n\nif age &#x3C; 18 or is_student:\n    print('You are eligible for a student discount') # You are eligible for a student discount\nelse:\n    print('You are not eligible for a student discount')\n</code></pre>\n<p>U ovom slučaju, <code>age &#x3C; 18</code> je <code>False</code>, ali <code>is_student</code> je <code>True</code>. Pošto je bar jedan uslov tačan, celokupna izjava od <code>or</code> se procenjuje kao <code>True</code>, i u sekciji <code>if</code> se štampa poruka o smanjenju.</p>\n<p>Poslednja operacija koju ćemo istražiti je operacija <code>not</code>, koja uzima jedan parametar i menja njegovu istinitu ili lažnu vrednost. Menja istinite vrednosti u <code>False</code> i lažne vrednosti u <code>True</code>. Za razliku od operacija koje smo videli ranije, <code>not</code> uvek vraća <code>True</code> ili <code>False</code>.</p>\n<p>Evo nekoliko primera:</p>\n<pre><code class=\"language-python\">print(not '') # True, because empty string is falsy\nprint(not 'Hello') # False, because non-empty string is truthy\nprint(not 0) # True, because 0 is falsy\nprint(not 1) # False, because 1 is truthy\nprint(not False) # True, because False is falsy\nprint(not True) # False, because True is truthy\n</code></pre>\n<p>Uobičajeno je koristiti operator <code>not</code> u uslovima da se proveri da li nešto nije <code>True</code> ili <code>False</code>, kao što je:</p>\n<pre><code class=\"language-python\">is_admin = False\n\nif not is_admin:\n    print('Access denied for non-administrators.') # Access denied for non-administrators.\nelse:\n    print('Welcome, Administrator!')\n</code></pre>\n<p>Pošto je <code>is_admin</code> <code>False</code>, onda <code>not is_admin</code> kaže da nije <code>False</code> koja je <code>True</code>. Stoga poruka <code>Access denied for non-administrators.</code> će biti odštampana.</p>\n<p>Sada kada razumete istinite i nistinite vrednosti, operatore za <code>and</code>, <code>or</code>, na <code>not</code>, i kako radi proces prekida kruga, možete napisati uslovnu logiku koja je fleksibilnija i lakša za čitanje.</p>\n</section>",
  "translationPending": false,
  "block": "lecture-booleans-and-conditionals",
  "blockLabel": "lecture",
  "blockLayout": "challenge-list",
  "hasEditableBoundaries": false,
  "order": 7,
  "instructions": "",
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 1,
  "isLastChallengeInBlock": true,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": false,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-basics",
  "certification": "python-v9"
}