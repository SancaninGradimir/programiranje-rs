---
id: 68879d6de6398893be8c4b7b
title: Korak 25
challengeType: 20
dashedName: step-25
---

# --description--

Sada dodajte telo e-pošte kako biste dovršili glavni sadržaj. Na putanji `display_full_email`, dodajte drugi podatak za štampanje u formatu `Body: body` (gde je `body` sadržaj e-pošte) da biste prikazali sadržaj e-pošte.

# --hints--

Trebalo bi da umetnete telo e-mail poruke koristeći `f'Body: {self.body}'`.

```js
({
    test: () => {
        assert(runPython(`_Node(_code).find_class("Email").find_function("display_full_email").has_call("print(f'Body: {self.body}')")`));
    }
})
```

# --seed--

## --seed-contents--

```py
class Email:
    def __init__(self, sender, receiver, subject, body):
        self.sender = sender
        self.receiver = receiver
        self.subject = subject
        self.body = body
        self.read = False

    def mark_as_read(self):
        self.read = True

--fcc-editable-region--
    def display_full_email(self):
        self.mark_as_read()
        print('\n--- Email ---')
        print(f'From: {self.sender.name}')
        print(f'To: {self.receiver.name}')
        print(f'Subject: {self.subject}')

--fcc-editable-region--

class User:
    def __init__(self, name):
        self.name = name
        self.inbox = Inbox()

    def send_email(self, receiver, subject, body):
        email = Email(sender=self, receiver=receiver, subject=subject, body=body)
        receiver.inbox.receive_email(email)

class Inbox:
    def __init__(self):
        self.emails = []

    def receive_email(self, email):
        self.emails.append(email)

```
