---
id: 5900f38c1000cf542c50fe9f
title: 'Vežba 32: Proizvod sa svim ciframa'
challengeType: 1
forumTopicId: 301976
dashedName: problem-32-pandigital-products
---

# --description--

Kaže se da broj sa ciframa `n` je pandigitalan ako koristi sve cifre od 1 do `n` samo jednom; na primer, broj sa ciframa 5, 15234, je pandigitalan od 1 do 5.

Proizvod 7254 je jedinstven, jer identifikator, 39 × 186 = 7254, koji uključuje proizvođača, distributera i proizvod, sadrži sve cifre od 1 do 9.

Pronađi sve proizvode čiji identifikator proizvođača/dobavljača/proizvoda može biti napisan sa svim ciframa od 1 do `n`.

**Napomena:** Neki proizvodi mogu biti dostupni na više načina, pa obavezno osigurajte da ih uključite samo jednom u svoj ukupni iznos.

# --hints--

`pandigitalProducts(4)` Trebalo bi da vrati broj.

```js
assert(typeof pandigitalProducts(4) === 'number');
```

`pandigitalProducts(4)` trebalo bi da vrati `12`.

```js
assert.strictEqual(pandigitalProducts(4), 12);
```

`pandigitalProducts(6)` treba vratiti `162`.

```js
assert.strictEqual(pandigitalProducts(6), 162);
```

`pandigitalProducts(7)` trebalo bi da vrati `0`.

```js
assert.strictEqual(pandigitalProducts(7), 0);
```

`pandigitalProducts(8)` bi trebalo da vrati `13458`.

```js
assert.strictEqual(pandigitalProducts(8), 13458);
```

`pandigitalProducts(9)` trebalo bi da vrati `45228`.

```js
assert.strictEqual(pandigitalProducts(9), 45228);
```

# --seed--

## --seed-contents--

```js
function pandigitalProducts(n) {

  return true;
}

pandigitalProducts(4);
```

# --solutions--

```js
// Check for pandigital number
function isPandigital(digitStr) {
  return digitStr
    .split('')
    .sort()
    .every((c, i) => c == i + 1);
}

// Concatenate 3 numbers
function concatenate3Nums(a, b, c) {
  return a.toString() + b.toString() + c.toString();
}

// Find sum of all pandigital products
function pandigitalProducts(n) {
  const products = [];
  let sum = 0;
  const max = Number(Array(n)
    .fill(0)
    .map((_, i) => (n - i).toString())
    .join(''));
  const outerLimit = Math.sqrt(max);

  for (let factor1 = 2; factor1 < outerLimit; factor1++) {
    const innerLimit = max / factor1;

    for (let factor2 = factor1; factor2 < innerLimit; factor2++) {
      const product = factor1 * factor2;
      const concatenated = concatenate3Nums(factor1, factor2, product);

      if (concatenated.length > n) {
        break;
      } else if (concatenated.length == n &&
        isPandigital(concatenated) &&
        !products.includes(product)
      ) {
        products.push(product);
        sum += product;
      }
    }
  }
  return sum;
}
```
