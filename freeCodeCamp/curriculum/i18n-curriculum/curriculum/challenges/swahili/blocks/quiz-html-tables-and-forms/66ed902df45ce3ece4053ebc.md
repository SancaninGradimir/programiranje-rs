---
id: 66ed902df45ce3ece4053ebc
title: Kratak test tabela i formulara za HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koji atribut među sledećim se koristi za određivanje URL gde bi podaci forme trebalo poslati?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Koje su dve komponente koje se koriste za identifikovanje redova i zaglavlja u tabeli?

#### --distractors--

Element `r` se koristi za red, a element `thead` se koristi za zaglavlje.

---

Element sa `row` se koristi za red i element sa `th` se koristi za zaglavlje.

---

Komponenta za `tr` koristi se za red, a komponenta za `head` koristi se za zaglavlje.

#### --answer--

Element za `tr` se koristi za red, a element za `th` se koristi za zaglavlje.

### --question--

#### --text--

Funkcija `td`, za šta se koristi?

#### --distractors--

Koristi se za definisanje tipa podataka tabele.

---

Koristi se za spajanje dve redne.

---

Koristi se za spajanje dve redne u tabeli.

#### --answer--

Koristi se za definisanje ćelije tabele.

### --question--

#### --text--

Šta je karakteristika `colspan`?

#### --distractors--

Određuje visinu reda u tabeli.

---

Ukloniti prekid reda između teksta u redu.

---

Koristi se za definisanje broja redova.

#### --answer--

Definisać broj redova koje ćelije tabele treba da obuhvate.

### --question--

#### --text--

Uloga atributa `placeholder` je li lepljiva?

#### --distractors--

Služi za slanje podataka na server kada se formular pošalje.

---

Koristi se za povezivanje elementa `label` sa elementom `input`.

---

Indikuje se da je polje za unos samo za čitanje (read-only) za korisnika.

#### --answer--

Koristi se za prikazivanje saveta u polju za unos.

### --question--

#### --text--

Koja je svrha funkcije `thead`?

#### --distractors--

Koristi se za spajanje sadržaja tela u tabelu HTML.

---

Koristi se za spajanje informacija iz tabele HTML.

---

Koristi se za povezivanje web sadržaja u tabelu HTML.

#### --answer--

Koristi se za povezivanje sadržaja zaglavlja u tabeli HTML.

### --question--

#### --text--

Koji od sledećih primera pokazuje jasan odnos/asocijaciju između etikete i polja za unos?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

Validator HTML za šta se koristi?

#### --distractors--

Validator je alat koji organizuje vaš kod od HTML automatski.

---

Validator je alat koji koristi stilove na vašem HTML.

---

Validator je alat koji omogućava da vaš kod HTML radi brže.

#### --answer--

Validator je alat koji proverava sinaksu koda HTML kako bi osigurao da je validan.

### --question--

#### --text--

Koji element se koristi za definisanje ćelije u tabeli?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Koji od sledećih alata omogućava pregled i modifikovanje strukture web stranice HTML?

#### --distractors--

Uklanjanje DOM.

---

Ispit za DOM.

---

Certifikat za DOM.

#### --answer--

Istraživač DOM.

### --question--

#### --text--

Koji je ispravan način povezivanja povezanih polja za unos?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Koja od sledećih se koristi za označavanje da je deo unosa samo za čitanje?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Koji atribut se koristi za određivanje da polje za unos mora biti popunjeno pre slanja forme?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Za šta se koriste Devtools?

#### --distractors--

Ovi alati sugrađeni direktno u pregledaču i koriste se za otkrivanje bilo kakvih problema sa lintingom u vašem kodu.

---

Ovi alati su direktno napravljeni u pretraživaču i koriste se za direktno formatiranje vašeg koda.

---

Ovi alati su direktno izgrađeni u pretraživaču i koriste se da pomognu u osiguravanju testiranja od 100 procenata za vaš kod.

#### --answer--

Ovi alati su ugrađeni direktno u pretraživač i koriste se za pomoć pri pronalaženju grešaka, analiziranju i istraživanju veb stranica.

### --question--

#### --text--

Koji je ispravan način za onemogućavanje dela unosa?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Koja vrednost među sledećim je važeća za svojstvo od `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Koja je pravilna upotreba svojstva za `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Koje od sledećih svojstava se koristi za određivanje minimalne i maksimalne dužine karaktera koje je potrebno u polju za unos?

#### --distractors--

`minlen` na `maxlen`

---

`min` na `max`

---

`minimumLen` i `maximumLen`

#### --answer--

`minlength` i `maxlength`

### --question--

#### --text--

Koji primer dugmeta među sljedećim ne koristi ispravnu vrijednost atributa za `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Koji atribut iz navedenih se koristi za određivanje vrednosti dugmeta?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Funkcija atributa `action` unutar etikete za otvaranje `form` je li?

#### --distractors--

Koristi se za ukazivanje da je deo ulaza samo za čitanje.

---

Koristi se za definisanje broja znakova koji moraju biti vidljivi kada korisnik unosi tekst u polje za unos.

---

Koristi se za prikaz uputstva korisniku o tome šta treba da unese u polje za unos.

#### --answer--

Koristi se za određivanje URL gde podaci forme treba poslati.

### --question--

#### --text--

Koji atribut između sljedećih se koristi za određivanje metode koja se koristi prilikom slanja podataka forme? HTTP

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Koja je uobičajena metoda korišćenja prilikom rada sa formularima?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Nije nijedna od sljedećih vrijednost za polje `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Koji je pravi način za kreiranje dugmeta u formularu?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Šta je direktna forma saradnje?

#### --distractors--

Ovo je mesto gde polja za unos mogu biti povezana sa etiketom koristeći svojstva `action` i `for` na elementu `label`.

---

Ovo je mesto gde polja za unos mogu biti povezana sa labelom koristeći atribute `required` i `for` na elementu `label`.

---

Ovo je mesto gde polja za unos mogu biti povezana sa etiketom koristeći atribut `for` na elementu `label`.

#### --answer--

Ovo je mesto gde polja za unos su rotirana unutar elementa `label`.

### --question--

#### --text--

Koja je funkcija elementa `fieldset`?

#### --distractors--

Koristi se za povezivanje sadržaja zaglavlja u tabeli HTML.

---

Koristi se za kreiranje oznake za polje unosa.

---

Služi za dodavanje opisa slike koji objašnjava grupu ulaznih polja.

#### --answer--

Koristi se za povezivanje međusobno povezanih ulaznih delova.

### --question--

#### --text--

Koja od sledećih se koristi za kreiranje padajućeg menija u formularu?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Koji element među sljedećih se koristi za dodavanje zaglavlja za tabelu HTML?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Šta je funkcija elementa `legend`?

#### --distractors--

Koristi se za spajanje sadržaja tela u tabelu HTML.

---

Koristi se za dodavanje opisa slike koji objašnjava ćelije tabele.

---

Koristi se za povezivanje povezanih ulaznih delova.

#### --answer--

Koristi se za dodavanje opisa slika kako bi se opisala grupa polja za unos.

### --question--

#### --text--

Koji od sledećih elemenata se koristi za povezivanje sadržaja naslova u tabelu HTML?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Koja je ispravna metoda za dodavanje redova i ćelija podataka u tabelu?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Šta je istraživač za DOM?

#### --distractors--

Alat koji se koristi za uklanjanje svih elemenata iz DOM.

---

Skup alata ugrađen direktno u pretraživač koji vam pomaže da pronađete greške, analizirate i inspekujete web stranice.

---

Alat koji proverava sintaksu koda za HTML i osigurava da je validan.

#### --answer--

Alat koji vam omogućava da inspekujete i modifikujete strukturu HTML veb stranice.

### --question--

#### --text--

Koji alat od sledećih proverava sintaksu koda HTML kako bi osigurao da je validan?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML važeći

#### --answer--

HTML validator

### --question--

#### --text--

Koji je tačan način da se utvrdi broj redova za ćeliju podataka u tabeli?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Kakav je status za polje unosa?

#### --distractors--

Ovo stanje je za polje za unos kada korisnik ukloni fokus.

---

Ovo stanje je za polje za unos kada je postavljeno kao samo za čitanje od strane korisnika.

---

Ovaj status se odnosi na deo unosa kada ga je korisnik onemogućio.

#### --answer--

Ovo stanje je za polje za unos kada ga korisnik izabere.

### --question--

#### --text--

Koji je ispravan način za kreiranje radio dugmeta?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Koja je funkcija svojstva `for`?

#### --distractors--

Koristi se za prikaz saveta korisniku.

---

Koristi se za određivanje vrednosti unosa.

---

Koristi se za kreiranje oznake za polje unosa.

#### --answer--

Koristi se za određivanje koji deo ulazne oznake je u pitanju.

### --question--

#### --text--

Koji od sledećih atributa se koristi za određivanje broja karaktera koje korisnik može uneti u polje za unos?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Koji atribut od sledećih se koristi da prikaže naslov (placeholder) korisniku kako bi pokazao šta treba da unese u polje za unos?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`