---
id: 672bb015cfc889794359c4e0
title: Ni mbinu bora gani za kubuni mazungumzo ya modal?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**NAPOMENA**: Neki primeri interakcije mogu koristiti CSS i JavaScript koje još niste naučili. Ne brinite zbog razumevanja celog koda. Svrha primera je da vam pokaže pregled ovih dizajnerskih koncepata kako biste bolje razumeli kako stvari funkcionišu. Da biste videli pregled, moraćete da omogućite editor interakcije.

Šta je modalni prozor? To je vrsta iskačućeg prozora koji se prikazuje preko sadržaja veb stranice. HTML ima element `dialog` koji možeš koristiti za kreiranje modalnih prozora.

Sadržaj iza modala je obično tamniji/smanjen po osvetljenosti. Ovo pomaže korisniku da se fokusira na oblast sa kojom želi da radi – u ovom slučaju, modala.

Uvek je dobra ideja omogućiti korisniku da klikne izvan modalnog prozora kako bi ga zatvorio.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Često ćeš na modalnim prozorima videti veoma istaknuta dugmad. Ona se nazivaju CTA (Call to Action – poziv na akciju). Treba da budu lako uočljiva jer prekidanje korisničkog toka modalnim prozorom ima za cilj da podstakne korisnika na određenu radnju.

Modali takođe bi trebalo da imaju dugme za zatvaranje. Iako možda želite da korisnik klikne na vaše CTA-ove, važno je dati im opciju da napuste modal i nastave sa onim što su ranije radili.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Naravno, postoje i pitanja pristupačnosti kada su modalni prozori u pitanju, kao što je pravilno upravljanje fokusom. Međutim, ako ove opšte smernice koristiš kao osnovu, imaćeš čvrstu polaznu tačku za dalji razvoj.

# --questions--

## --text--

Lengo la kupunguza mwangaza wa yaliyomo nyuma ya mazungumzo ya modal ni nini?

## --answers--

Kuhifadhi betri kwenye vifaa vya mkononi.

### --feedback--

Lekcija navodi konkretan razlog za ovaj vizuelni efekat.

---

Da pomogne korisnicima da vizuelno identifikuju oblast/područje na kojem moraju da rade.

---

Naterati sajt da se učita brže.

### --feedback--

Funzo pominje specifičan razlog za ovaj vidljivi efekat.

---

Sprečiti korisnike da vide osetljive informacije.

### --feedback--

Principle navodi specifičan uzrok ovog vizuelnog efekta.

## --video-solution--

2

## --text--

Zašto je CTA prisutan u kontekstu dizajna modala?

## --answers--

Kliknite za aktivaciju.

### --feedback--

Tutorijal daje pun naziv za ovu skraćenicu kada govori o vrlo vidljivim dugmićima na modalima.

---

Dovršite radnju.

### --feedback--

Učenje daje pun naziv za ovu skraćenicu kada govori o dugmićima koja su veoma vidljiva na modalima.

---

Poziv na akciju.

---

Zatvorite aplikaciju.

### --feedback--

Tutorijal daje pun naziv za ovu skraćenicu kada govori o vrlo vidljivim dugmićima na modalima.

## --video-solution--

3

## --text--

Zašto je važno uključiti dugme za zatvaranje u modalni dijalog?

## --answers--

Zakonski je potrebno u nekim zemljama.

### --feedback--

Funzo objašnjava važnost omogućavanja korisnicima da napuste modal.

---

Sprečiti da modal izazove pad pretraživača.

### --feedback--

Učenje objašnjava važnost omogućavanja korisnicima iz modala.

---

Omogućiti korisnicima opciju da izađu iz modala i nastave sa aktivnostima koje su prethodno obavljali.

---

Da se modal učini profesionalnijim.

### --feedback--

Lekcija objašnjava zašto je važno omogućiti korisnicima da izađu iz modalnog prozora.

## --video-solution--

3