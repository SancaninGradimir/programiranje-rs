---
id: 5900f3a61000cf542c50feb9
title: 'Vežba 58: osnovni brojevi za krug'
challengeType: 1
forumTopicId: 302169
dashedName: problem-58-spiral-primes
---

# --description--

Počevši od 1 i praćenjem okvira u suprotnom smeru kazaljke na satu, kao što je prikazano, formiran je kvadratni okvir sa dužinom stranice od 7.

<div style='text-align: center;'>
  <strong><span style='color: red;'>37</span></strong> 36 35 34 33 32 <strong><span style='color: red;'>31</span></strong><br>
  38 <strong><span style='color: red;'>17</span></strong> 16 15 14 <strong><span style='color: red;'>13</span></strong> 30<br>
  39 18 <0xC2><0xA0><strong><span style='color: red;'>5</span></strong> <0xC2><0xA0>4 <0xC2><0xA0><strong><span style='color: red;'>3</span></strong> 12 29<br>
  40 19 <0xC2><0xA0>6 <0xC2><0xA0>1 <0xC2><0xA0>2 11 28<br>
  41 20 <0xC2><0xA0><strong><span style='color: red;'>7</span></strong> <0xC2><0xA0>8 <0xC2><0xA0>9 10 27<br>
  42 21 22 23 24 25 26<br>
  <strong><span style='color: red;'>43</span></strong> 44 45 46 47 48 49<br>
</div>

Fascinirajuće je primetiti da kvadrati bez dvostraničnih brojeva su na donjem desnom dijagonali, ali najzanimljivije je što osam od trnaest brojeva koji se nalaze na obe dijagonale su premijni brojevi; to jest, odnos 8/13 ≈ 62%.

Ako se oko ovog kruga gore doda potpuno novi sloj, stvoriće se kvadratni prsten sa dužinom stranice od 9. Ako ovaj proces nastavi, koja je dužina stranice kvadratnog prstena gde procenat premijnih brojeva na obe dijagonale padne ispod `percent` prvi put?

# --hints--

`spiralPrimes(50)` Trebalo bi da vrati broj.

```js
assert(typeof spiralPrimes(50) === 'number');
```

`spiralPrimes(50)` treba da vrati `11`.

```js
assert.strictEqual(spiralPrimes(50), 11);
```

`spiralPrimes(15)` trebalo bi da vrati `981`.

```js
assert.strictEqual(spiralPrimes(15), 981);
```

`spiralPrimes(10)` trebalo bi da vrati `26241`.

```js
assert.strictEqual(spiralPrimes(10), 26241);
```

# --seed--

## --seed-contents--

```js
function spiralPrimes(percent) {

  return true;
}

spiralPrimes(50);
```

# --solutions--

```js
function spiralPrimes(percent) {
  function isPrime(n) {
    if (n <= 3) {
      return n > 1;
    } else if (n % 2 === 0 || n % 3 === 0) {
      return false;
    }

    for (let i = 5; i * i <= n; i += 6) {
      if (n % i === 0 || n % (i + 2) === 0) {
        return false;
      }
    }
    return true;
  }

  let totalCount = 1;
  let primesCount = 0;
  let curNumber = 1;
  let curSideLength = 1;
  let ratio = 1;
  const wantedRatio = percent / 100;

  while (ratio >= wantedRatio) {
    curSideLength += 2;
    for (let i = 0; i < 4; i++) {
      curNumber += curSideLength - 1;
      totalCount++;
      if (i !== 3 && isPrime(curNumber)) {
        primesCount++;
      }
    }
    ratio = primesCount / totalCount;
  }
  return curSideLength;
}
```
