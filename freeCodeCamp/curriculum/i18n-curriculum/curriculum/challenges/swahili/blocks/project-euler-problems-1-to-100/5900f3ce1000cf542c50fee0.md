---
id: 5900f3ce1000cf542c50fee0
title: 'Zadatak 97: Veliki prosti broj koji nije Mersenov'
challengeType: 1
forumTopicId: 302214
dashedName: problem-97-large-non-mersenne-prime
---

# --description--

Prvi poznati premijski broj koji prelazi milion cifara otkriven je 1999. godine, i to je Mersenov premijski broj tipa $2^{6972593} − 1$; ima 2.098.960 cifara tačno. Kasnije su otkrili druge Mersenove premijske brojeve, tipa $2^p − 1$, sa više cifara.

Međutim, u godini 2004. otkriven je veliki ne-Mersenov premični broj sa 2.357.207 cifara: $28433 × 2^{7830457} + 1$.

Pronađi poslednjih deset cifara tog ne-Mersenovog broja u strukturi $multiplier × 2^{power} + 1$.

# --hints--

`largeNonMersennePrime(19, 6833086)` Trebalo bi da vrati sekvencu slova.

```js
assert(typeof largeNonMersennePrime(19, 6833086) === 'string');
```

`largeNonMersennePrime(19, 6833086)` treba da vrati sekvencu karaktera `3637590017`.

```js
assert.strictEqual(largeNonMersennePrime(19, 6833086), '3637590017');
```

`largeNonMersennePrime(27, 7046834)` treba da vrati niz znakova `0130771969`.

```js
assert.strictEqual(largeNonMersennePrime(27, 7046834), '0130771969');
```

`largeNonMersennePrime(6679881, 6679881)` treba da vrati sekvencu slova `4455386113`.

```js
assert.strictEqual(largeNonMersennePrime(6679881, 6679881), '4455386113');
```

`largeNonMersennePrime(28433, 7830457)` treba da vrati niz znakova `8739992577`.

```js
assert.strictEqual(largeNonMersennePrime(28433, 7830457), '8739992577');
```

# --seed--

## --seed-contents--

```js
function largeNonMersennePrime(multiplier, power) {

  return true;
}

largeNonMersennePrime(19, 6833086);
```

# --solutions--

```js
function largeNonMersennePrime(multiplier, power) {
  function modStepsResults(number, other, mod, startValue, step) {
    let result = startValue;
    for (let i = 0; i < other; i++) {
      result = step(number, result) % mod;
    }
    return result;
  }

  const numOfDigits = 10;
  const mod = 10 ** numOfDigits;
  const digitsAfterPower = modStepsResults(2, power, mod, 1, (a, b) => a * b);
  const digitsAfterMultiply = modStepsResults(
    digitsAfterPower,
    multiplier,
    mod,
    0,
    (a, b) => a + b
  );
  const lastDigits = (digitsAfterMultiply + 1) % mod;

  return lastDigits.toString().padStart(10, '0');
}
```
