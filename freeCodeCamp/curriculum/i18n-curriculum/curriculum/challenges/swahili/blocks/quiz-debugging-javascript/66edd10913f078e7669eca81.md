---
id: 66edd10913f078e7669eca81
title: Pokušao sam da popravim greške u JavaScriptu
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

Da biste položili kratki test, morate tačno odgovoriti na najmanje 18 od 20 pitanja koja su navedena ispod.

# --quizzes--

## --quiz--

### --question--

#### --text--

Koja vrsta greške se javlja kada se parametar koristi pre nego što je deklarisan?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

Koji je to izjava koja se može koristiti da namerno zaustavi izvršavanje vašeg koda radi popravke grešaka? JavaScript

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

Koji je način u konzoli koji se može koristiti za prikaz podataka kao tabele?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Kako možete pregledati mrežne zahteve u alatima za razvoj Chrome-a?

#### --distractors--

Koristeći karticu "Network Links".

---

Koristeći karticu "Console".

---

Koristeći karticu "Sources".

#### --answer--

Koristeći karticu "Network".

### --question--

#### --text--

Koja je ispravna sintaksa za korišćenje `try...catch` u JavaScript?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

Koji od sledećih primera je za `SyntaxError`?

#### --distractors--

Koristi parametar pre objavljivanja.

---

Koristiti parametar dva puta u jednom opsegu.

---

Korišćenje parametra koji je van opsega.

#### --answer--

Nedostaje zarez između elemenata niza podataka.

### --question--

#### --text--

Šta su to tačke prekida (breakpoints) prilikom debagovanja grešaka u JavaScript?

#### --distractors--

Automatsko popravljanje kodnih grešaka.

---

Sprečiti izvršavanje koda.

---

Snimanje poruka na konzoli.

#### --answer--

Prekinuti izvršavanje na određeno mesto.

### --question--

#### --text--

Šta se dešava kada se izgovori/izvrši `throw`?

#### --distractors--

Program se završava odmah, ignorisanjem preostalog koda.

---

Izjava za throw piše grešku na konzoli, ali ne sprečava izvršavanje.

---

Zaustavlja izvršavanje koda do zaustavljanja na tačkama prekida.

#### --answer--

Element greške se odbacuje, i izvršava se komponenta `catch` (ako postoji).

### --question--

#### --text--

Koji metod biste koristili za proveru parametara ili matematičkih izraza prilikom otklanjanja grešaka u alatima za programere?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

Koja vrsta greške će se desiti kada se izvrši sledeći kod?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Greška prepunog steka (stack overflow)

#### --answer--

`ReferenceError`

### --question--

#### --text--

Koji alat se često koristi za praćenje koda linija po liniju tokom otklanjanja grešaka?

#### --distractors--

`console.log()`

---

`alert()`

---

Izjava o `throw`.

#### --answer--

Znak za zaustavljanje u alatima za programere.

### --question--

#### --text--

Koja god vrsta greške se dešava kada pokušavate da dobijete atribut `undefined`?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

Šta element `finally` radi u deklaraciji `try...catch`?

#### --distractors--

Izvršava se samo ako postoji greška.

---

Izvršavanje prolazi ako `catch` je izazvano.

---

Završava izvršavanje funkcije odmah.

#### --answer--

Izvršava se uvek, bez obzira na greške.

### --question--

#### --text--

Koja će biti rezultata izvršavanja sledećeg koda?

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

Kod će ispustiti `SyntaxError`.

---

Kod će ispisati red praznih podataka na konzolu.

---

Kod će proizvesti grešku nedostataka memorije.

#### --answer--

Kod će dati `RangeError`.

### --question--

#### --text--

Koja od navedenih tvrdnji je tačna u vezi sa izjavom `debugger` unutar JavaScript?

#### --distractors--

Izjava za `debugger` je uvek ignorišana od strane pretraživača.

---

Izjava za `debugger` šalje izveštaj o grešci serveru.

---

Izjava o `debugger` uvek osvežava stranicu i briše konzolu.

#### --answer--

Izjava za `debugger` se ignoriše kada alati za programere nisu otvoreni.

### --question--

#### --text--

Šta je glavni cilj „profilovanja“ u JavaScript?

#### --distractors--

Napisati jedinice testove za funkcije JavaScript i kreirati profil izvršavanja koda.

---

Zaustaviti izvršavanje koda i osloboditi resurse koji su zauzeti, kao što su memorija i procesori (CPU).

---

Promeniti kod JavaScript u mašinski kod za brzo izvršavanje.

#### --answer--

Identifikovanje uskih grla performansi beleženjem korišćenja procesora (CPU), poziva funkcija i vremena izvršenja.

### --question--

#### --text--

"Watcher", šta je to u kontekstu ispravljanja grešaka?

#### --distractors--

Alat za zaustavljanje izvršavanja koda.

---

Funkcija koja je napravljena za snimanje grešaka.

---

Način za JavaScript za rukovanje greškama.

#### --answer--

Alat za praćenje vrednosti parametara.

### --question--

#### --text--

Trebalo bi da koristiš `console.error()` umesto `console.log()` liniju?

#### --distractors--

Prikazivanje podataka u tabeli.

---

Snimajte poruku ovde prilikom popravke grešaka.

---

Snimanje informacija o pretraživaču.

#### --answer--

Snimanje poruke o grešci.

### --question--

#### --text--

Šta Kijenzi za `Error()` radi u JavaScript?

#### --distractors--

Zabeležite poruke o grešci na konzoli.

---

Pokrenite kod u sandoksu.

---

Završiti program.

#### --answer--

Kreiranje novog, otbačivog objekta greške.

### --question--

#### --text--

Svrha putanje za `console.dir()` u JavaScript je šta?

#### --distractors--

Kreiranje zasebnih direktorijuma za log fajlove za svaki tip greške.

---

Generisati vizuelnu reprezentaciju izlaza konzole i sačuvati je u PDF fajl.

---

Zaustavljanje izvršavanja programa i prikazivanje poruke o grešci koja je povezana sa korišćenjem memorije i procesora (CPU).

#### --answer--

Pružiti listu sekvenci, sa interakcijom svojstava specifičnog objekta JavaScript.