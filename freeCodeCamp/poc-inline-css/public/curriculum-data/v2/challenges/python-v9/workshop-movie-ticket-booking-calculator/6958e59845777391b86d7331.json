{
  "id": "6958e59845777391b86d7331",
  "title": "Step 18",
  "challengeType": 20,
  "dashedName": "step-18",
  "challengeFiles": [
    {
      "id": "",
      "editableRegionBoundaries": [
        35,
        37
      ],
      "history": [
        "main.py"
      ],
      "name": "main",
      "ext": "py",
      "path": "main.py",
      "fileKey": "mainpy",
      "contents": "base_price = 15\nage = 21\nseat_type = 'Gold'\nshow_time = 'Evening'\n\nif age > 17:\n    print('User is eligible to book a ticket')\n\nif age >= 21:\n    print('User is eligible for Evening shows')\nelse:\n    print('User is not eligible for Evening shows')\n\nis_member = False\nis_weekend = False\n\ndiscount = 0\nif is_member and age >= 21:\n    discount = 3\n    print('User qualifies for membership discount')\nelse:\n    print('User does not qualify for membership discount')\nprint('Discount:', discount)\n\nextra_charges = 0\nif is_weekend or show_time == 'Evening':\n    extra_charges = 2\n    print('Extra charges will be applied')\nelse:\n    print('No extra charges will be applied')\nprint('Extra charges:', extra_charges)\n\nif age >= 21 or age >= 18 and (show_time != 'Evening' or is_member):\n    print('Ticket booking condition satisfied')\n\n    \nelse:\n    print('Ticket booking failed due to restrictions')",
      "error": null,
      "seed": "base_price = 15\nage = 21\nseat_type = 'Gold'\nshow_time = 'Evening'\n\nif age > 17:\n    print('User is eligible to book a ticket')\n\nif age >= 21:\n    print('User is eligible for Evening shows')\nelse:\n    print('User is not eligible for Evening shows')\n\nis_member = False\nis_weekend = False\n\ndiscount = 0\nif is_member and age >= 21:\n    discount = 3\n    print('User qualifies for membership discount')\nelse:\n    print('User does not qualify for membership discount')\nprint('Discount:', discount)\n\nextra_charges = 0\nif is_weekend or show_time == 'Evening':\n    extra_charges = 2\n    print('Extra charges will be applied')\nelse:\n    print('No extra charges will be applied')\nprint('Extra charges:', extra_charges)\n\nif age >= 21 or age >= 18 and (show_time != 'Evening' or is_member):\n    print('Ticket booking condition satisfied')\n\n    \nelse:\n    print('Ticket booking failed due to restrictions')"
    }
  ],
  "solutions": [],
  "assignments": [],
  "tests": [
    {
      "text": "<p>You should declare a variable named <code>service_charges</code> inside your last <code>if</code> statement body.</p>",
      "testString": "({ test: () => (runPython(`\nassert _Node(_code).find_ifs()[4].find_bodies()[0].has_variable(\"service_charges\")\n`)) })"
    },
    {
      "text": "<p>You should assign the integer value <code>0</code> to <code>service_charges</code>.</p>",
      "testString": "({ test: () => (runPython(`\n_var = _Node(_code).find_ifs()[4].find_bodies()[0].find_variable(\"service_charges\") \nassert _var.is_equivalent(\"service_charges = 0\")\n`)) })"
    },
    {
      "text": "<p>You should have an <code>if</code> statement inside your last <code>if</code> statement body.</p>",
      "testString": "({ test: () => (runPython(`assert _Node(_code).find_ifs()[4].find_ifs()[0]`)) })"
    },
    {
      "text": "<p>The new <code>if</code> statement condition should be <code>seat_type == 'Premium'</code>.</p>",
      "testString": "({ test: () => (runPython(`\n_cond = _Node(_code).find_ifs()[4].find_ifs()[0].find_conditions()[0] \nassert _cond.is_equivalent(\"seat_type == 'Premium'\")\n`)) })"
    },
    {
      "text": "<p>You should assign the integer value <code>5</code> to <code>service_charges</code> variable inside your new <code>if</code> statement.</p>",
      "testString": "({ test: () => (runPython(`\n_var = _Node(_code).find_ifs()[4].find_ifs()[0].find_bodies()[0].find_variable(\"service_charges\") \nassert _var.is_equivalent(\"service_charges = 5\")\n`)) })"
    }
  ],
  "description": "<section id=\"description\">\n<p>In Python, an <code>if</code> statement can also be placed inside the body of another <code>if</code> statement. This is called a nested <code>if</code> statement.</p>\n<p>A nested <code>if</code> statement allows you to check an additional condition only after the first condition has already been satisfied. The inner <code>if</code> statement will run only if the outer <code>if</code> condition is true.</p>\n<pre><code class=\"language-py\">if condition1:\n    # Code to execute if condition1 is True\n    if condition2:\n        # Code to execute if both conditions are True\n</code></pre>\n<p>Now you will calculate service charges based on the type of seat the user has selected.</p>\n<p>Inside the body of the last <code>if</code> statement, below the <code>print('Ticket booking condition satisfied')</code> line, create a variable named <code>service_charges</code> and set it to <code>0</code>. Make sure to indent your code by four spaces to keep it inside the outer <code>if</code> statement body.</p>\n<p>Then, create a nested <code>if</code> statement to check if <code>seat_type</code> is equal to <code>Premium</code>. Inside the body of the nested <code>if</code> statement, update the <code>service_charges</code> value to <code>5</code>.</p>\n</section>",
  "translationPending": false,
  "block": "workshop-movie-ticket-booking-calculator",
  "blockLabel": "workshop",
  "blockLayout": "challenge-grid",
  "hasEditableBoundaries": true,
  "order": 8,
  "instructions": "",
  "questions": [],
  "superBlock": "python-v9",
  "superOrder": 3,
  "challengeOrder": 17,
  "isLastChallengeInBlock": false,
  "required": [],
  "helpCategory": "Python",
  "usesMultifileEditor": true,
  "disableLoopProtectTests": false,
  "disableLoopProtectPreview": false,
  "chapter": "python",
  "module": "python-basics",
  "certification": "python-v9"
}