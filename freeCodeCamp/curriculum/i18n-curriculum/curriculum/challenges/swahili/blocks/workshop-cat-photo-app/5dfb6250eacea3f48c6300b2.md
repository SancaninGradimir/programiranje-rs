---
id: 5dfb6250eacea3f48c6300b2
title: Korak 24
challengeType: 0
dashedName: step-24
---

# --description--

Nakon neuređene liste, dodaj novu sliku sa vrednošću `src` atributa postavljenom na:

`https://cdn.freecodecamp.org/curriculum/cat-photo-app/lasagna.jpg`

I sa vrednošću `alt` atributa postavljenom na:

`A slice of lasagna on a plate.`

# --hints--

Imaj `img` element odmah nakon završne oznake `</ul>`.

```js
assert.equal(document.querySelectorAll('section')[1]?.querySelector('ul').nextElementSibling?.nodeName, 'IMG');
```

Nova slika nema atribut polja `alt`. Proverite da postoji razmak nakon imena oznake otvaranja i/ili da postoji razmak pre svih imena atributa polja.

```js
assert.isTrue(document.querySelectorAll('section')[1]?.querySelector('ul').nextElementSibling?.hasAttribute('alt'));
```

Nova slika treba da ima vrednost atributa `alt` `A slice of lasagna on a plate.`. Uverite se da je vrednost atributa `alt` okružena navodnicima.

```js
assert.match(
  document.querySelectorAll('section')[1]?.querySelector('ul').nextElementSibling?.getAttribute('alt')?.replace(/\s+/g, ' '), /^A slice of lasagna on a plate\.?$/i 
);
```

Nova slika nema atribut elementa `src`. Proverite da postoji razmak nakon naziva otvarajuće oznake i/ili da postoji razmak pre svih imena atributa elemenata.

```js
assert.isTrue(document.querySelectorAll('section')[1]?.querySelector('ul').nextElementSibling?.hasAttribute('src'));
```

Nova slika treba da ima vrednost atributa `src` `https://cdn.freecodecamp.org/curriculum/cat-photo-app/lasagna.jpg`. Uverite se da je vrednost atributa `src` okružena navodnicima.

```js
assert.equal(
  document.querySelectorAll('section')[1]?.querySelector('ul').nextElementSibling?.getAttribute('src'),
    'https://cdn.freecodecamp.org/curriculum/cat-photo-app/lasagna.jpg'
);
```

Iako si postavio `src` atribut nove slike na ispravan URL, uvek se preporučuje da vrednosti atributa okružiš navodnicima.

```js
assert.notMatch(code, /\<img\s+.+\s+src\s*=\s*https:\/\/cdn\.freecodecamp\.org\/curriculum\/cat-photo-app\/lasagna\.jpg/);
```

# --seed--

## --seed-contents--

```html
<html>
  <body>
    <main>
      <h1>CatPhotoApp</h1>
      <section>
        <h2>Cat Photos</h2>
        <p>Everyone loves <a href="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg">cute cats</a> online!</p>
        <p>See more <a target="_blank" href="https://freecatphotoapp.com">cat photos</a> in our gallery.</p>
        <a href="https://freecatphotoapp.com"><img src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg" alt="A cute orange cat lying on its back."></a>
      </section>
      <section>
        <h2>Cat Lists</h2>
        <h3>Things cats love:</h3>
        <ul>
          <li>catnip</li>
          <li>laser pointers</li>
          <li>lasagna</li>
        </ul>
--fcc-editable-region--
        
--fcc-editable-region--
      </section>
    </main>
  </body>
</html>
```
