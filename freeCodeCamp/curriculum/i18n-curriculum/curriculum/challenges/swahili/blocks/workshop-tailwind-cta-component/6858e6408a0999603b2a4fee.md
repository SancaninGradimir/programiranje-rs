---
id: 6858e6408a0999603b2a4fee
title: Korak 14
challengeType: 0
dashedName: step-14
---

# --description--

Za `h1`, dodajte klase elemenata `font-bold` i `text-4xl`. Ovo će učiniti tekst podebljanim, većim i takođe će povećati razmak između redova.

Takođe, dodaj klasu elemenata `my-4` da dodaš gornju i donju ivicu za `h1` sa 16 piksela, ili `1rem`.

# --hints--

Tvoja komponenta za `h1` treba da ima servisnu klasu `font-bold`.

```js
const h1El = document.querySelector("h1");
assert.isTrue(h1El.classList.contains("font-bold"));
```

Komponenta za `h1` treba da ima klasu usluge `text-4xl`.

```js
const h1El = document.querySelector("h1");
assert.isTrue(h1El.classList.contains("text-4xl"));
```

Vaš element za `h1` mora imati servisnu klasu `my-4`.

```js
const h1El = document.querySelector("h1");
assert.isTrue(h1El.classList.contains("my-4"));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CTA component</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-indigo-600 text-white mt-8 p-4 md:w-1/2 mx-auto flex flex-col lg:flex-row justify-around items-center rounded-md">
      <div>
        <span class="uppercase">Soundflow</span>
         --fcc-editable-region--
        <h1>Discover New Music</h1>
         --fcc-editable-region--
        <p>Stream your favorite tracks and discover new artists.</p>
      </div>
       <div>
        <a href="#">Learn more</a>
        <a href="#">Start listening</a>
      </div>
    </div>
  </body>
</html>
```
